{
  "name": "@pipe/tempo-real",
  "version": "0.1.0",
  "private": true,
  "description": "Cliente do canal de tempo real: uma ligação, com reconexão, backoff e ping",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json",
    "lint": "eslint src tests",
    "test": "vitest run"
  },
  "dependencies": {
    "@pipe/contracts": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
