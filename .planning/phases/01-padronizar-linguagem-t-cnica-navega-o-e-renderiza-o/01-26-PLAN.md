---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 26
type: execute
wave: 21
depends_on: ["01-25"]
files_modified:
  - apps/crm/src/**
  - apps/crm/tests/**
  - apps/crm/semente/**
  - apps/crm/Dockerfile
  - apps/crm/Dockerfile.dockerignore
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/crm.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-3f-*
autonomous: true
requirements: [STD-09, STD-07, STD-10]

must_haves:
  truths:
    - "apps/crm identifiers, files, non-route folders, route folders (URLs), search param names and test titles follow the approved map (STD-09)"
    - "The two public-route regexes (middleware.ts and estrutura-crm.tsx) use identical new segments"
    - "No rendering change: the set of files with 'use client' and the Server Component structure are identical to before (D-33, CRM-01 not anticipated)"
    - "next build passes; gate passes"
  artifacts:
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-3f-use-client.txt"
      provides: "before/after 'use client' file list comparison"
  key_links:
    - from: "apps/crm/src/middleware.ts public-route regex"
      to: "apps/crm/src/app/** renamed route folders"
      via: "same segments"
      pattern: "RegExp|/\\^"
---

<objective>
Slice 3f: apply the approved map to `apps/crm` within STD-09 limits — rename identifiers and routes only; keep Next App Router and Server Components exactly as they are.

Purpose: CRM follows the convention without deciding its destiny (CRM-01, Phase 4); nothing done here may be throwaway or architectural.
Output: renamed CRM, proof of unchanged rendering boundaries, gate report.

Model routing (D-25): Haiku-eligible tool application; Sonnet verifies the regex pair and the rendering-invariance proof.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-13-PLAN.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Slice rules: plan 01-13 context. Scope crm; kinds symbol, ts-prop, ts-local, file, dir, front-route (route folders), query-param, storage-key, test-title. Invite folder and cookie were done in plans 01-18/01-19.

Facts: 72 TS files, 21 `page.tsx` route folders under `apps/crm/src/app/**`; route folder rename = URL rename; public-route regex `/^\/(entrar|convite)/` at `apps/crm/src/middleware.ts:15` (invite segment already new) and duplicated at `apps/crm/src/componentes/estrutura-crm.tsx:69`; pages read `searchParams` props (Server Components), not `useSearchParams`; extensionless specifiers; `seed:crm` script runs `semente/semente-crm.ts` (script key renames in slice 4).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record rendering baseline; rename CRM identifiers, files, folders</name>
  <files>apps/crm/src/**, apps/crm/tests/**, apps/crm/semente/**, apps/crm/Dockerfile, apps/crm/Dockerfile.dockerignore, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/crm.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-3f-use-client.txt</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/crm.csv
    - apps/crm/src/middleware.ts
    - apps/crm/next.config.* and apps/crm/tsconfig.json
  </read_first>
  <action>
    1. Before: list files containing a top-level `'use client'` directive (`rg -l "^['\"]use client['\"]" apps/crm/src`), and list `apps/crm/src/app/**/{page,layout,route,loading,error,not-found}.tsx` files; save both lists to the scratch dir.
    2. `rename-symbols.ts --scopes crm --kinds symbol,ts-prop,ts-local` (dry-run, apply); `move-files.ts --scopes crm --kinds file,dir` (dry-run, apply) — route folders are dir rows; Next special filenames (`page.tsx`, `layout.tsx`, `route.ts`, `middleware.ts`, `loading.tsx`, `error.tsx`, `not-found.tsx`) must never be renamed (check-map should have KEEP; if a row targets one, stop and escalate).
    3. After: recompute both lists; map old paths through applied rows; the `'use client'` set and the special-file set must be identical. Write `STD/reports/slice-3f-use-client.txt` with before, after(mapped) and `IDENTICAL` or the diff.
    4. `pnpm typecheck` exit 0. Commit `refactor(01-26): rename crm identifiers, files and route folders (STD-09)`.
  </action>
  <verify>
    <automated>pnpm typecheck && grep -c "IDENTICAL" .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-3f-use-client.txt</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` exits 0
    - slice-3f-use-client.txt contains `IDENTICAL` for both lists
    - no Next special filename was renamed (`git diff --name-status std-slice-2-end -- apps/crm | rg "R.*(page|layout|route|middleware)\.tsx?$"` shows only folder moves where basename is unchanged)
  </acceptance_criteria>
  <done>CRM code renamed; rendering boundaries unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: CRM routes, regex pair, params, test titles; build + gate</name>
  <files>apps/crm/src/**, apps/crm/tests/**, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-3f-*</files>
  <read_first>
    - apps/crm/src/middleware.ts and the renamed estrutura-crm component
    - apps/crm/tests/*.test.ts
  </read_first>
  <action>
    `rewrite-literals.ts --scopes crm --kinds front-route,query-param,storage-key,test-title,literal-value` (dry-run, apply). Update both public-route regexes to the same alternation of new segments (sign-in + invite); assert they are string-identical (extract the regex literals and compare). Check `redirect()`/`<Link href>` targets and the middleware destination param match renamed routes (`rg` each old segment in apps/crm/src returns nothing). `pnpm --filter @pipe/crm build` exit 0; `bash tools/std/gate.sh slice-3f` all PASS. Commit `refactor(01-26): crm routes and params; gate slice-3f green`.
  </action>
  <verify>
    <automated>bash tools/std/gate.sh slice-3f</automated>
  </verify>
  <acceptance_criteria>
    - the regex literal in middleware.ts equals the one in the renamed estrutura-crm component (recorded in SUMMARY)
    - `pnpm --filter @pipe/crm build` exits 0
    - `bash tools/std/gate.sh slice-3f` exits 0
  </acceptance_criteria>
  <done>CRM follows the convention without architectural change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated user → CRM pages | middleware public-route check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-26-01 | Elevation of privilege | public-route regex drift exposing protected pages | mitigate | both regexes updated from the same rows and compared string-identical |
| T-01-26-02 | Tampering | accidental client/server boundary change | mitigate | 'use client' and special-file sets proven identical |
</threat_model>

<verification>
Build and gate green; rendering invariance proven.
</verification>

<success_criteria>
STD-09 satisfied.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-26-SUMMARY.md` when done
</output>
