---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 06
type: execute
wave: 3
depends_on: ["01-02", "01-04"]
files_modified:
  - tools/std/codex-run.sh
  - tools/std/codex-prompt.ts
  - tools/std/schemas/map-rows.schema.json
  - tools/std/schemas/comment-triage.schema.json
  - tools/std/schemas/glossary-terms.schema.json
  - tools/std/schemas/nav-classification.schema.json
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/TEMPLATE-map.md
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/TEMPLATE-persisted.md
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/TEMPLATE-comments.md
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/TEMPLATE-glossary.md
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/TEMPLATE-nav.md
  - tools/std/merge-proposals.ts
  - tools/std/check-map.ts
  - tools/std/apply-comments.ts
  - tools/std/map-tools.test.ts
autonomous: true
requirements: [STD-01, STD-02, STD-06, STD-10]

must_haves:
  truths:
    - "Every Codex call goes through one wrapper that forces -m gpt-5.6-sol, model_reasoning_effort=medium and -s read-only (D-23, D-37) and runs Codex in a disposable git worktree of HEAD with every .env*/key/certificate file removed, never in the real working tree (W2)"
    - "The wrapper's tamper guard compares only the disposable worktree's tracked files, so parallel executors and Codex's own -o output never trip it, and on a mismatch it stops and reports without any automatic clean/restore (W1)"
    - "check-map uses the same buildExtraLexicon as the scanner, so correct English names like useReading/getConversation never error"
    - "check-map enforces that every rename-with-db-key wire-key row has the same new name as the matching contracts/front property rows (W4)"
    - "Codex proposals merge into the map/comments CSVs without ever overwriting approved rows"
    - "check-map rejects PT tokens in new names, wrong casing, missing D-05 suffixes, collisions, mismatched endpoint segments, persisted rows, and glossary inconsistencies"
    - "Comment triage decisions are applied mechanically from reviewed CSV rows"
  artifacts:
    - path: "tools/std/codex-run.sh"
      provides: "the only allowed way to call Codex in this phase"
      contains: "gpt-5.6-sol"
    - path: "tools/std/check-map.ts"
      provides: "map validation, sampling, approval"
    - path: "tools/std/merge-proposals.ts"
      provides: "Codex JSON -> CSV merge; persisted rows -> persisted.csv"
    - path: "tools/std/apply-comments.ts"
      provides: "apply translate/remove/update comment actions"
  key_links:
    - from: "tools/std/codex-run.sh"
      to: "codex exec"
      via: "fixed flags + disposable worktree + tracked-only status guard"
      pattern: "-s read-only"
    - from: "tools/std/check-map.ts"
      to: "tools/std/pt-detect.ts buildExtraLexicon"
      via: "rule 3 lexicon"
      pattern: "buildExtraLexicon"
    - from: "tools/std/check-map.ts"
      to: "tools/std/pt-detect.ts"
      via: "PT token check on new names"
      pattern: "isPtToken"
---

<objective>
Build the delegation and validation tooling for the semantic work: Codex wrapper, prompt chunker, output schemas, prompt templates, proposal merger, map checker, comment applier.

Purpose: D-23/D-37 route semantic work to two Codex accounts that propose but never edit. D-03 gates need a mechanical proof that the map is internally consistent before the owner sees it. D-16 comment triage must be applied from reviewed decisions, not retyped.
Output: tools and templates listed in files_modified.

Model routing: Claude (Sonnet) writes the tools and templates. Codex is not called in this plan except a one-row smoke call per account.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-CONTEXT.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-RESEARCH.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-04-SUMMARY.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Tool conventions as in plan 01-02; map contract as in plan 01-04 (`tools/std/lib/map.ts`). Comments CSV columns (from plan 01-05): `id,scope,file,start_line,end_line,kind,pt_score,text,action,new_text,sensitivity,evidence,reviewed_by,status`.

Codex facts (RESEARCH, verified locally, CLI 0.156.1): flags `-m`, `-c key=value`, `-s read-only|workspace-write|danger-full-access`, `-C <dir>`, `--ephemeral`, `--color never`, `--output-schema <file>`, `-o <file>`, prompt from stdin with `-`. Account 1 `CODEX_HOME=$HOME/.codex` defaults to `danger-full-access` + `approval_policy=never`; account 2 `CODEX_HOME=$HOME/.codex-conta2` defaults to `workspace-write`; default models differ. D-37: always `-m gpt-5.6-sol -c model_reasoning_effort="medium" -s read-only`. OpenAI structured output schemas must have `additionalProperties: false` and list every property in `required` (use `""` for "no value").
</context>

<tasks>

<task type="auto">
  <name>Task 1: Codex wrapper, prompt chunker, schemas and prompt templates</name>
  <files>tools/std/codex-run.sh, tools/std/codex-prompt.ts, tools/std/schemas/map-rows.schema.json, tools/std/schemas/comment-triage.schema.json, tools/std/schemas/glossary-terms.schema.json, tools/std/schemas/nav-classification.schema.json, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/TEMPLATE-map.md, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/TEMPLATE-persisted.md, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/TEMPLATE-comments.md, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/TEMPLATE-glossary.md, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/TEMPLATE-nav.md</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-CONTEXT.md (D-01, D-02, D-05, D-08..D-11, D-16, D-17, D-26..D-34, D-38, D-40)
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-RESEARCH.md ("Delegating to Codex")
    - tools/std/lib/map.ts
  </read_first>
  <action>
    `tools/std/codex-run.sh <account 1|2> <schema.json> <prompt.md> <out.json>`: `set -euo pipefail`; account 1 -> `CODEX_HOME="$HOME/.codex"`, account 2 -> `CODEX_HOME="$HOME/.codex-conta2"`, anything else exits 2; resolve `<schema>`, `<prompt>` and `<out>` to absolute paths in the real repo. Isolation (W2, preferred fix): `git worktree prune`, then `WT=$(mktemp -d)/codex-wt` and `git worktree add --detach "$WT" HEAD` (a worktree of HEAD contains no untracked `.env`); then delete from `$WT` every tracked file matching `.env*`, `**/.env*`, `*.pem`, `*.key`, `*.pfx`, `*.p12`, `*.crt` (list with `git -C "$WT" ls-files` + a glob filter; the deletions are part of the recorded baseline). Guard baseline (W1): `BEFORE=$(git -C "$WT" status --porcelain --untracked-files=no)` — tracked files of the disposable worktree only; the real working tree, other executors and the `-o` output (written outside `$WT`, into `STD/out/`) are never part of the comparison. Run `codex exec -m gpt-5.6-sol -c model_reasoning_effort="medium" -s read-only -C "$WT" --ephemeral --color never --output-schema "$2" -o "$4" - < "$3"`; `AFTER=$(git -C "$WT" status --porcelain --untracked-files=no)`; if `BEFORE != AFTER` print `CODEX CHANGED THE WORKTREE: $WT` plus the diff of the two status lists, keep `$WT` for inspection and exit 3 — NO automatic `git restore`, `git clean` or checkout anywhere (the caller stops and reports to the owner); on success `git worktree remove --force "$WT"`. Because the worktree is created from HEAD, callers must commit every STD input a prompt tells Codex to read (GLOSSARY.md, CONVENTIONS-EN.md, nav-contract.md, map CSVs) before calling; the wrapper prints and logs the HEAD sha used. Append `timestamp,account,head,prompt,out,exit` to `STD/reports/codex-log-<account>.csv` (one log file per account so plans running in parallel on different accounts never write the same file). No other flags are configurable (hard-coded model, effort, sandbox).
    `tools/std/codex-prompt.ts` CLI: `node tools/std/codex-prompt.ts --template <TEMPLATE-*.md> --rows <csv or dir> [--filter scope=api,kind=symbol|ts-local] [--status candidate] [--chunk 300] --out-dir <STD/prompts/<job>>` writes `<job>-NNN.md` files = template text + a fenced CSV block of the chunk rows, with `declared_at`/`file` paths resolved to current paths via `resolvePath` (so Codex reads the right file after earlier moves). Prints the chunk count.
    Schemas (strict: `additionalProperties:false`, all properties required):
    - `map-rows.schema.json`: `{rows:[{id, new, persisted: no|yes|unknown, category: ""|A|B|C, decision_ref, notes}]}`
    - `comment-triage.schema.json`: `{items:[{id, action: translate|remove|update|keep-original, new_text, sensitivity: security|architecture|integration|meta-blip|other, evidence, notes}]}`
    - `glossary-terms.schema.json`: `{terms:[{term_pt, term_en, blip_source, ambiguity: none|AMBIGUOUS, candidates, occurrences, notes}]}`
    - `nav-classification.schema.json`: `{screens:[{app, route, state_item, current_location: path|query|state|router-state|storage|none, proposed: path|query|react-state|router-state|storage|NEEDS VALIDATION, deep_link: yes|no|NEEDS VALIDATION, survives_f5: yes|no|NEEDS VALIDATION, blip_evidence, decision_ref, notes}]}`
    Prompt templates (English, each starts with: "You are proposing, not editing. Do not modify any file. Output only JSON matching the schema. Read only files inside the current working directory; never open, read, print or quote `.env*` files, private keys, certificates (`*.pem`, `*.key`, `*.pfx`, `*.p12`) or any path outside the working directory, even if a file references them."):
    - `TEMPLATE-map.md`: fill `new` for each row using `STD/GLOSSARY.md` (approved terms are mandatory) and `STD/CONVENTIONS-EN.md`; D-05 suffixes (`Controlador*`->`*Controller`, `Servico*`->`*Service`, `Guarda*`->`*Guard`, `Erro*`->`*Error`, `Filtro*`->`*Filter`, `Modulo*`->`*Module`); D-01 Blip vocabulary first; keep identical casing style per kind; endpoint rows keep segment count and param positions, kebab-case lowercase segments; mark `persisted=yes` for anything stored in DB/Redis/browser/external registration per D-09/D-11/D-40 and do NOT propose a name for it (`new=KEEP`); use `KEEP` for already-English rows; front-route rows marked in `STD/nav-contract.md` as leaving the URL get `STATE` or `REMOVE` with decision_ref; never rename env vars (D-06/D-36); API-key scope strings like `conversas:ler` are persisted (D-40).
    - `TEMPLATE-persisted.md`: for rows with persisted=unknown, open the declaring and consuming files and decide `persisted` yes/no with `notes` = where it is stored (table.column, Redis key, cookie, localStorage, external registration) and `decision_ref` (D-09, D-10, D-11, D-38, D-40); D-38: `pipe_sessao` cookie and Prometheus metric names are NOT persisted (they rename); D-10: queue names, job payload keys, ws events and non-stored error codes are NOT persisted; codes written to `erro_codigo`/`ultimo_erro` columns ARE persisted; rows whose notes contain `jsonb:<table>.<column>` (types traced to a jsonb column by plan 01-05) default to `persisted=yes` because their property names/literal members are the stored keys/values of existing documents (stored flows, in-flight executions, outbox replay, audit, CRM) — answer `no` only when you cite the exact evidence that this property is never serialized into that column (e.g. it is stripped by a named mapping function before the insert) in `notes`.
    - `TEMPLATE-comments.md`: D-16 three categories (translate needed+current / remove redundant / update-or-remove outdated only with evidence of real behavior), D-17 (why/security/architecture/Meta/Blip/integration comments need exact technical meaning; literal quotes, official names, payloads, captured Blip/Meta text stay in original language -> `keep-original` with evidence), never word-for-word, never change meaning of historical decisions; set `sensitivity`.
    - `TEMPLATE-glossary.md`: from token frequency rows, propose `term_en` per PT domain term; D-01 Blip English term when Blip names the same concept (attendance, queue, ticket, router, builder, bulk-ticket, activeMessage); D-02 "atendimento" must be marked AMBIGUOUS with candidates (module / human session / channel conversation) and evidence from `packages/db/src/schema` and real uses.
    - `TEMPLATE-nav.md`: classify each route/state item per STD-12 and D-26..D-34 using evidence files listed in CONTEXT canonical_refs (`referencias-blip/...` lines); no evidence -> NEEDS VALIDATION; never infer SSR from stable URLs (D-33).
    Smoke-test the wrapper once per account with a 1-row prompt against `map-rows.schema.json` writing to the scratch dir, and confirm the tree is unchanged.
  </action>
  <verify>
    <automated>grep -c "gpt-5.6-sol" tools/std/codex-run.sh && grep -c "\-s read-only" tools/std/codex-run.sh && for f in tools/std/schemas/*.json; do node -e "JSON.parse(require('fs').readFileSync('$f','utf8'))" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'codex exec -m gpt-5.6-sol -c model_reasoning_effort="medium" -s read-only' tools/std/codex-run.sh` returns 1 line
    - `grep -c "danger-full-access\|workspace-write" tools/std/codex-run.sh` prints 0
    - `grep -c "worktree add --detach" tools/std/codex-run.sh` >= 1 and `grep -c "untracked-files=no" tools/std/codex-run.sh` >= 2
    - `grep -v '^\s*#' tools/std/codex-run.sh | grep -c "git restore\|git clean\|git checkout"` prints 0 (no automatic restore)
    - `grep -l "never open, read, print or quote" STD/prompts/TEMPLATE-*.md | wc -l` prints 5
    - smoke run: the wrapper's worktree contains no `.env*` file while Codex runs (the smoke prompt asks Codex to list files named .env* in its working directory; the JSON answer is empty) and the worktree is removed afterwards (`git worktree list` shows only the main tree)
    - all 4 schema files parse as JSON and every object in them has `"additionalProperties": false`
    - 5 TEMPLATE-*.md files exist; `grep -l "Do not modify any file" STD/prompts/TEMPLATE-*.md | wc -l` prints 5
    - `STD/reports/codex-log-1.csv` and `STD/reports/codex-log-2.csv` each contain 1 smoke line with exit 0
    - `git status --porcelain` after the smoke calls shows only the files of this plan
  </acceptance_criteria>
  <done>Codex can be called safely and reproducibly on both accounts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: merge-proposals, check-map, apply-comments with tests</name>
  <files>tools/std/merge-proposals.ts, tools/std/check-map.ts, tools/std/apply-comments.ts, tools/std/map-tools.test.ts</files>
  <read_first>
    - tools/std/lib/map.ts, tools/std/lib/csv.ts, tools/std/pt-detect.ts
    - tools/std/schemas/map-rows.schema.json, tools/std/schemas/comment-triage.schema.json
  </read_first>
  <behavior>
    - merge: a Codex row for an `approved` row is ignored and reported; a row with persisted=yes is removed from the map CSV and appended to STD/persisted.csv
    - check-map: new=`ControladorAnexos`-style PT token -> error; kind=symbol old `ControladorAnexos` new `Attachments` -> error (missing Controller suffix, D-05); two rows in the same file mapping to the same new -> error; endpoint `/v1/a/:id` -> `/v1/b/c/:id` -> error (segment count); `REMOVE` without decision_ref in D-14/D-27/D-28/D-29/D-30 -> error; glossary term `conversa->conversation` and new `ChatList` for old `ListaConversas` -> warning unless notes contains `glossary-exception:`
    - check-map rule 3: old `useLeitura` new `useReading` and old `getConversa` new `getConversation` produce no error (lexicon from buildExtraLexicon excludes use/get); new `useLeitura` still errors
    - check-map rule 10 (W4): wire-key row old `contatoId` new `contactId` in packages-db plus a ts-prop row old `contatoId` new `contactID` in packages-contracts -> error naming both ids; same new -> no error; notes `wire-exception:<reason>` on the ts-prop row silences it
    - check-map casing: literal-value new keeps the old style (kebab/snake/lower); data-attr new is `data-` + kebab-case (value part kebab-case)
    - check-map --approve flips proposed->approved only when there are 0 errors
    - check-map --sample 0.1 --seed 1 returns the same row ids on every run
    - apply-comments: translate replaces comment text keeping comment delimiters and indentation; remove deletes the comment lines (and a trailing blank line left alone); rows not `reviewed_by` sonnet when sensitivity != other are refused
  </behavior>
  <action>
    `tools/std/merge-proposals.ts` CLI: `node tools/std/merge-proposals.ts --in "<glob of Codex out json>" --target <STD/map | STD/comments> --owner codex1|codex2|sonnet [--persisted-only]`. `--persisted-only` updates only `persisted`, `decision_ref`, `notes` and keeps `status=candidate` (used by plan 01-08). Map mode: for each JSON row, update `new`, `persisted`, `category`, `decision_ref`, `notes`, set `owner` and `status=proposed`, never touch rows with status approved|applied|verified; rows ending with persisted=yes are removed from the map CSV and moved: with `--persisted-only` into `STD/persisted.csv`; otherwise into `STD/persisted-candidates-<owner>.csv` (per-owner file so parallel map plans never write the same file; plan 01-12 reconciles candidates into persisted.csv) (persisted.csv columns `id,scope,kind,old,declared_at,where_persisted,decision,decision_ref,notes`; `decision` empty until classified; `where_persisted` from notes). Comments mode: fill `action,new_text,sensitivity,evidence`, set `status=proposed`.
    `tools/std/check-map.ts` CLI: `node tools/std/check-map.ts --map STD/map [--scopes a,b|all] [--require-status proposed|approved] [--glossary STD/GLOSSARY.md] [--sample <fraction> --seed <n> --out <csv>] [--approve]`. Checks: (1) status >= required for every row (rules 2-8 apply only to rows whose status is proposed or later; candidate rows are only checked for rule 1 and rule 9); (2) `new` non-empty; `new != old` unless `KEEP`; special values need decision_ref (REMOVE only with D-14/D-27/D-28/D-29/D-30, STATE only with D-29/D-30/D-31/D-34); (3) no PT token in `new` (`isPtToken` with `buildExtraLexicon(all map rows)` from pt-detect.ts — the same lexicon the scanner uses; never raw old tokens), (4) casing: class/interface/type/enum/component names PascalCase; functions/vars/props camelCase (UPPER_SNAKE allowed when old was UPPER_SNAKE); file/dir/package/app/front-route/endpoint segments kebab-case lowercase (dynamic `:param` segments camelCase); css-class kebab-case; css-var `--` + kebab-case; data-attr `data-` + kebab-case (and kebab-case value after `=`); literal-value same style as old (lowercase/kebab/snake/UPPER); queue/job-name/metric/cookie snake/kebab matching the old style; (5) D-05 suffix rule for old prefixes Controlador/Servico/Guarda/Erro/Filtro/Modulo; (6) collisions: duplicate `new` for same kind within the same declared file, duplicate target path for file/dir rows, duplicate endpoint/front-route new patterns; (7) endpoint/front-route segment count and param positions equal; (8) glossary consistency from the approved table in GLOSSARY.md (warning; `glossary-exception:<reason>` in notes silences it); (9) no row with persisted=yes; (10) wire consistency (W4): for every `wire-key` row with new not in KEEP/REMOVE (these come from wire-contracts.csv `rename-with-db-key`), every `ts-prop` or `wire-key` row in scopes packages-contracts, api, desk-vite, gestao-vite, crm, packages-tempo-real with the same `old` must have the same `new` unless its notes contain `wire-exception:<reason>` — error lists both row ids (only evaluated when both rows are in the selected scopes; plan 01-12 runs `--scopes all`). Output grouped errors/warnings; exit 1 on any error. `--sample` writes a deterministic sample (seeded PRNG, stratified by scope) for Sonnet review. `--approve` sets proposed->approved for the selected scopes only if zero errors.
    `tools/std/apply-comments.ts` CLI: `node tools/std/apply-comments.ts --comments STD/comments --scopes a,b [--dry-run]`: for rows with status `reviewed` (set by Sonnet review) apply `translate`/`update` (replace text, preserve `//`, `/* */`, `/** */` framing, CSS block comments and hash-comment framing for sh/yml/Dockerfile, JSDoc tags and indentation), `remove` (delete the comment lines), `keep-original` (no change; append an exceptions.csv C row with the exact snippet and `D-17` ref); refuse any row with sensitivity in security|architecture|integration|meta-blip whose `reviewed_by` is not `sonnet`; locate comments by resolved current path + original text (line numbers are hints only); mark status `applied`; print applied/refused/not-found counts and exit 1 if not-found > 0.
    Tests in `tools/std/map-tools.test.ts` for all behavior cases (temp dirs).
    Commit: `feat(01-06): codex wrapper, schemas, templates, map checker, comment applier`.
  </action>
  <verify>
    <automated>node --test tools/std/map-tools.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tools/std/map-tools.test.ts` exits 0 with every behavior case as its own test
    - `node tools/std/check-map.ts --help` lists `--approve`, `--sample`, `--require-status`, `--glossary`
    - `grep -c "reviewed_by" tools/std/apply-comments.ts` >= 1
  </acceptance_criteria>
  <done>Map and comment tooling tested and committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Codex agent → local repo | an external agent with a permissive default sandbox runs against the working tree |
| LLM proposal → map | model output becomes rename input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-01 | Tampering | Codex account 1 default `danger-full-access` | mitigate | wrapper hard-codes `-s read-only`; Codex runs in a disposable worktree; tracked-only status guard on that worktree exits 3 and stops (no auto-restore, so no other executor's work can be deleted) |
| T-01-06-02 | Tampering | LLM proposes renaming persisted names | mitigate | schema forces `persisted` field; merge moves persisted=yes to persisted.csv; check-map errors on any persisted row in the map |
| T-01-06-03 | Repudiation | security comment mistranslation | mitigate | apply-comments refuses security/architecture/integration/meta-blip rows not reviewed by Sonnet (D-24) |
| T-01-06-04 | Information disclosure | Codex reading secrets and sending them to OpenAI | mitigate | Codex's working dir is a worktree of HEAD (untracked `.env` absent) with tracked `.env*`/key/certificate files deleted; every template forbids reading `.env*`, keys, certificates or paths outside the working dir; codex-prompt embeds only CSV rows |
</threat_model>

<verification>
Tests pass; wrapper smoke-called on both accounts without changing the tree.
</verification>

<success_criteria>
Semantic work can be delegated read-only and its output validated mechanically before any owner gate.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-06-SUMMARY.md` when done
</output>
