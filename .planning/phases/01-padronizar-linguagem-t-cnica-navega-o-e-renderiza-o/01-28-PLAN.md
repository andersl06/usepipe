---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 28
type: execute
wave: 26
depends_on: ["01-27"]
files_modified:
  - packages/**
  - apps/*/package.json
  - apps/*/src/**
  - apps/*/tests/**
  - apps/*/Dockerfile
  - apps/*/Dockerfile.dockerignore
  - apps/*/vite.config.ts
  - apps/*/vitest.config.ts
  - package.json
  - pnpm-lock.yaml
  - infra/construir-imagens.sh
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/infra.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-4a-*
autonomous: true
requirements: [STD-07, STD-10]

must_haves:
  truths:
    - "Every approved package row (e.g. @pipe/autenticacao, @pipe/armazenamento, @pipe/tempo-real) is renamed one package at a time: directory, package.json name, dependents' workspace deps, import specifiers, aliases, Dockerfile COPY/--filter lines, build script (D-04, D-19)"
    - "pnpm-lock.yaml is regenerated and committed so Dockerfiles with --frozen-lockfile still install"
    - "Images that contain each renamed package build with docker after each package rename"
    - "Gate passes"
  artifacts:
    - path: "pnpm-lock.yaml"
      provides: "lockfile with renamed importers"
  key_links:
    - from: "apps/*/Dockerfile COPY packages/<pkg>/package.json lines"
      to: "renamed packages/<pkg> directories"
      via: "explicit manifest list per Dockerfile"
      pattern: "COPY packages/"
---

<objective>
Slice 4a (D-19 step 4): rename PT-named workspace packages, one at a time, with every build/deploy reference, validating a real docker build after each.

Purpose: Package renames touch non-TS files (Dockerfiles list manifests explicitly, turbo/pnpm filters, lockfile); the Desk build has already broken on the VPS because of a path, so each rename is validated with an image build (Pitfall 6).
Output: renamed packages, regenerated lockfile, gate report.

Model routing (D-25): Haiku-eligible for approved rows; Sonnet handles Dockerfile edits and build failures.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-13-PLAN.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Slice rules: plan 01-13 context. Rows: kind `package` in infra.csv (status approved).
Facts: package dependents — `@pipe/autenticacao` <- api (+ its own dep on db); `@pipe/armazenamento` <- api; `@pipe/tempo-real` <- (none in apps today; depends on contracts). References outside the lockfile (RESEARCH): autenticacao 110 files/294 hits, armazenamento 32/44, tempo-real 21/34. Each Dockerfile lists `COPY packages/<x>/package.json` explicitly (gestao-vite's list omits armazenamento/tempo-real) and uses `--filter "@pipe/x..."`; `infra/construir-imagens.sh` builds images; BuildKit honors `apps/<app>/Dockerfile.dockerignore`. Docker build in Git Bash: prefix `MSYS_NO_PATHCONV=1`; `SEM_PULL=1` uses cached base images.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename each approved package, one at a time, with docker build after each</name>
  <files>packages/**, apps/*/package.json, apps/*/src/**, apps/*/tests/**, apps/*/Dockerfile, apps/*/Dockerfile.dockerignore, apps/*/vite.config.ts, apps/*/vitest.config.ts, package.json, pnpm-lock.yaml, infra/construir-imagens.sh</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/infra.csv (kind package)
    - apps/api/Dockerfile, apps/workers/Dockerfile, apps/crm/Dockerfile, apps/gestao-vite/Dockerfile, apps/desk-vite/Dockerfile
    - infra/construir-imagens.sh
  </read_first>
  <action>
    For each approved package row, in this order: tempo-real, armazenamento, autenticacao (lowest blast radius first), then any other package rows:
    1. `node tools/std/move-files.ts --map STD/map --scopes infra --kinds package --ids <row id>` (moves `packages/<old>` to `packages/<new>`).
    2. `node tools/std/rewrite-literals.ts --map STD/map --scopes infra --kinds package --ids <row id>` (package.json `name`, dependents' dependency keys, `@pipe/<old>` specifiers, alias keys, `--filter` args in package.json scripts, Dockerfiles and `*.sh`).
    3. Hand-check every Dockerfile / `.dockerignore` for `packages/<old>` paths and `@pipe/<old>` filters (`rg -n "packages/<old>|@pipe/<old>" apps infra package.json` must return nothing).
    4. `pnpm install` (regenerates lockfile), `pnpm typecheck`, `pnpm build`.
    5. Build each image whose Dockerfile references the package: `MSYS_NO_PATHCONV=1 SEM_PULL=1 bash infra/construir-imagens.sh std-check <app>` (api, workers as applicable) — exit 0.
    6. Commit `refactor(01-28): rename package @pipe/<old> -> @pipe/<new>` (one commit per package).
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - for every applied package row: `rg -n "@pipe/<old>|packages/<old>" apps packages infra package.json --glob '!pnpm-lock.yaml'` returns nothing (commands and outputs listed in SUMMARY)
    - `pnpm install --frozen-lockfile` exits 0
    - docker build of api and workers images exited 0 after the last package rename (commands listed in SUMMARY)
    - one git commit per renamed package
  </acceptance_criteria>
  <done>Packages renamed and images build.</done>
</task>

<task type="auto">
  <name>Task 2: Slice 4a gate</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-4a-*</files>
  <read_first>
    - tools/std/gate.sh
  </read_first>
  <action>
    `bash tools/std/gate.sh slice-4a` (the gate's test-count comparison maps old package names to new via the applied package rows). All PASS. Commit `chore(01-28): gate slice-4a green`.
  </action>
  <verify>
    <automated>bash tools/std/gate.sh slice-4a</automated>
  </verify>
  <acceptance_criteria>
    - `bash tools/std/gate.sh slice-4a` exits 0
    - slice-4a-test-counts.json contains the new package names with passed counts equal to baseline counts of the old names
  </acceptance_criteria>
  <done>Slice 4a green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → container images | Dockerfiles copy explicit manifests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-28-01 | Denial of service | image fails to build/start after rename (Pitfall 6) | mitigate | one package at a time + real docker build + frozen-lockfile install |
| T-01-28-02 | Tampering | lockfile resolution drift | mitigate | lockfile regenerated by pnpm only; no version changes; `--frozen-lockfile` verify |
</threat_model>

<verification>
Frozen install, typecheck, docker builds and gate green.
</verification>

<success_criteria>
PT package names eliminated per map with deployable images.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-28-SUMMARY.md` when done
</output>
