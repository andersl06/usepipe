---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 36
type: execute
wave: 6
depends_on: ["01-09"]
files_modified:
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/css.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/map-codex1-css-*.json
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/sample-css.csv
autonomous: true
requirements: [STD-02, STD-10]

must_haves:
  truths:
    - "Every candidate row of css.csv (css-class, css-var, data-attr) has a proposed English name or KEEP with decision_ref (D-35, W6)"
    - "Class names sharing a prefix family keep one consistent English prefix (e.g. every dk-conversa-* -> dk-conversation-*)"
    - "check-map reports 0 errors for scope css at status=proposed"
  artifacts:
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/css.csv"
      provides: "CSS class, custom property and data-* attribute map (D-35)"
  key_links:
    - from: "std/map/css.csv"
      to: "tools/std/rename-css.ts (plan 01-25)"
      via: "approved rows applied in slice 3e"
      pattern: "css-class|css-var|data-attr"
---

<objective>
Propose the CSS map (D-35): ~3,025 class selectors, 482 custom properties and the data-* attributes (W6), split out of plan 01-10 so the Sonnet review is bounded by CSS file group (W11).

Purpose: Gate 2 approves the CSS map together with the rest (D-03/D-35); plan 01-25 applies it.
Output: `std/map/css.csv` at status=proposed, `std/reports/sample-css.csv`.

Model routing: Codex account 1 proposes; Claude (Sonnet) reviews per file group. No Haiku.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/GLOSSARY.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/CONVENTIONS-EN.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

File groups (by `declared_at` path): G1 `packages/ui/**`; G2 `apps/desk-vite/**`; G3 `apps/gestao-vite/**`; G4 `apps/crm/**` + `apps/site/**`; G5 data-attr rows (any path).
Codex calls go only through `tools/std/codex-run.sh` (worktree of HEAD).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Codex account 1 proposes CSS names per file group</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/css.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/map-codex1-css-*.json</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/TEMPLATE-map.md
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/css.csv (header + first 50 rows)
  </read_first>
  <action>
    For each file group G1..G5: `node tools/std/codex-prompt.ts --template STD/prompts/TEMPLATE-map.md --rows STD/map --filter scope=css --status candidate --chunk 300 --out-dir STD/prompts/map-css-<group>` (filter the chunk rows to the group's paths / kind data-attr; codex-prompt sorts by declared_at so a CSS file's rows stay together); run each chunk with `bash tools/std/codex-run.sh 1 tools/std/schemas/map-rows.schema.json <chunk> STD/out/map-codex1-css-<group>-NNN.json`; merge with `node tools/std/merge-proposals.ts --in "STD/out/map-codex1-css-<group>-*.json" --target STD/map --owner codex1`. Add to each prompt (after the template text): keep existing app prefixes (`dk-`, etc.) unchanged, translate only PT tokens using the approved glossary, keep one English form per PT prefix family across all files, data-attr rows keep the `data-` prefix and translate name and value tokens separately. Commit `docs(01-36): CSS map proposals`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/css.csv','utf8');const n=(t.match(/,candidate,/g)||[]).length;if(n)process.exit(1);console.log('candidate=0')"</automated>
  </verify>
  <acceptance_criteria>
    - no css.csv row at status candidate
    - css.csv has only kinds css-class, css-var and data-attr
    - codex-log-1.csv has these calls, all exit 0
  </acceptance_criteria>
  <done>All CSS rows proposed.</done>
</task>

<task type="auto">
  <name>Task 2: Sonnet review per file group; prefix-family consistency; check-map green</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/css.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/sample-css.csv</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/GLOSSARY.md
  </read_first>
  <action>
    1. Prefix-family check (mechanical, node one-liner in the scratch dir): group css-class rows by their longest shared PT-token prefix (`splitIdentifier` of old up to the first English token); every group must map that prefix to one English form — list inconsistent groups and fix them (owner=sonnet).
    2. `node tools/std/check-map.ts --map STD/map --scopes css --sample 0.1 --seed 5 --out STD/reports/sample-css.csv` (stratified by scope; add group column by path). Review one group at a time (G1..G5), committing after each group (`docs(01-36): review CSS <group>`); review all sampled rows, every css-var row (482: variables are shared tokens) and every data-attr row. Record per-group rows reviewed, changed and sample error rate in SUMMARY; a group with error rate > 10% is reviewed entirely.
    3. `node tools/std/check-map.ts --map STD/map --scopes css --require-status proposed --glossary STD/GLOSSARY.md` until 0 errors. Commit `docs(01-36): CSS map reviewed (D-35)`.
  </action>
  <verify>
    <automated>node tools/std/check-map.ts --map .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map --scopes css --require-status proposed --glossary .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/GLOSSARY.md</automated>
  </verify>
  <acceptance_criteria>
    - check-map for scope css exits 0
    - SUMMARY lists 0 inconsistent prefix families after fixes and the per-group review table
  </acceptance_criteria>
  <done>CSS map ready for gate 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| map → styling | class names are an untyped contract between CSS and markup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-36-01 | Tampering | two classes merged into one new name | mitigate | check-map collision rule (6); plan 01-25 compares distinct selector counts before/after |
| T-01-36-02 | Information disclosure | Codex reading secrets | mitigate | codex-run worktree without .env*/keys; template rule |
</threat_model>

<verification>
check-map 0 errors for css; per-group review recorded.
</verification>

<success_criteria>
CSS map proposed and reviewed for gate 2.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-36-SUMMARY.md` when done
</output>
