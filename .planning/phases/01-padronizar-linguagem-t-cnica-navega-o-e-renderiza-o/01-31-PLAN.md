---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 31
type: execute
wave: 26
depends_on: ["01-30"]
files_modified:
  - docs/**
  - PROJECT-HANDOFF.md
  - README.md
  - infra/README.md
  - apps/*/README.md
  - .planning/**/*.md
  - apps/**
  - packages/**
  - tools/std/stale-refs.ts
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/exceptions.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-5-*
autonomous: true
requirements: [STD-10, STD-11]

must_haves:
  truths:
    - "No tracked technical doc, README, example or script references an old (applied) identifier, path, endpoint, route, script or package name, except literal historical records classified C"
    - "Doc file/dir names follow the approved rows; every reference to a renamed doc path (including .planning) is updated"
    - "The STD-11 scan reports 0 unclassified findings; every remaining PT finding is A, B (points to persisted.csv) or C (justified exception)"
    - "Slice 5 gate passes; tag std-slice-5-end exists"
  artifacts:
    - path: "tools/std/stale-refs.ts"
      provides: "old-name reference sweep over tracked non-code files"
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-5-scan-summary.md"
      provides: "final scan summary"
      contains: "Unclassified: 0"
  key_links:
    - from: "std/exceptions.csv"
      to: "std/reports/slice-5-scan.csv"
      via: "classification of every remaining finding"
      pattern: "exception_ref"
---

<objective>
Slice 5 (D-19 step 5): residual sweep — stale references in docs/examples/scripts, doc file names, residual identifiers and comments — until the STD-11 scan has zero unclassified findings.

Purpose: ROADMAP success criterion 7 and STD-11 require that no unclassified PT technical naming remains; D-07 requires docs citing renamed commands to be updated.
Output: updated docs, stale-refs tool, final scan with 0 unclassified, tag `std-slice-5-end`.

Model routing: stale-ref fixes that are pure old->new substitutions from applied rows are Haiku-eligible; classification of remaining findings (A/B/C with justification) and any residual comment triage are Sonnet (residual comments may be sent to Codex account 1/2 by scope via the comment workflow).
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-15-PLAN.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Docs policy for this phase: technical references inside docs (code spans, paths, commands, endpoint/route strings, identifiers, package/script names) are updated to the new names. Portuguese prose of docs/specs (product/design narrative) is not an identifier and stays; plan 01-02 records this scanner rule as the explicit global category A `markdown prose` row `**/*.md,*,md-prose,A,...,STD-10` in `STD/exceptions.csv`. The scanner examines `.md` paths, not prose. Doc FILE and DIR names are identifiers: follow infra.csv rows (slice 5) — if the owner marked them KEEP at gate 2, they carry a C justification in exceptions.csv.
`referencias-blip/**` is out of scope. Dated history files under `.planning/` (e.g. prior SUMMARYs, CONTEXT, RESEARCH) are records of the past: do not rewrite their old names except path references that would otherwise be broken links; classify as C (historical record).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stale-reference sweep tool and doc updates; doc file renames</name>
  <files>tools/std/stale-refs.ts, docs/**, PROJECT-HANDOFF.md, README.md, infra/README.md, apps/*/README.md, .planning/**/*.md</files>
  <read_first>
    - tools/std/lib/map.ts
    - PROJECT-HANDOFF.md, README.md, infra/README.md
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/infra.csv (docs file/dir rows)
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/exceptions.csv (global category A `md-prose` row from 01-02)
  </read_first>
  <action>
    1. Create `tools/std/stale-refs.ts` CLI `node tools/std/stale-refs.ts --map STD/map --out <csv>`: for every row with status applied|verified and new not in KEEP/STATE, whose `old` is >= 5 chars and is itself PT (`isPtToken` on at least one token), run `git grep -n -w -F <old>` over tracked files excluding `referencias-blip/**`, `.planning/phases/01-*/std/**`, `pnpm-lock.yaml`, `**/dist/**`; output `file,line,old,new,kind,snippet`.
    2. Apply doc file/dir rows: `move-files.ts --scopes infra --kinds file,dir` restricted to `docs/**` rows (`--ids`); then rewrite path references to renamed docs across all tracked `.md` (incl. `.planning/**`) — exact old path -> new path.
    3. Run stale-refs; for each hit in docs/READMEs/examples/scripts/code: replace with `new` when it is a technical reference (code span, path, command, identifier); if it is historical narrative in `.planning/` or a literal quote, leave it and add a C row to exceptions.csv (glob = that file, pattern = the old token, justification = "historical record" or "literal quote", ref = D-17 or STD-11). Re-run until only classified hits remain.
    Commit `docs(01-31): update technical references and doc names to English identifiers`.
  </action>
  <verify>
    <automated>node tools/std/stale-refs.ts --map .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map --out .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-5-stale-refs.csv</automated>
  </verify>
  <acceptance_criteria>
    - every row of slice-5-stale-refs.csv is covered by an exceptions.csv C row (checked by scan in Task 2) or the file is under .planning history
    - `rg -n "pnpm (run )?banco:" PROJECT-HANDOFF.md README.md infra/README.md` returns nothing
    - no broken relative links to renamed docs: every `docs/...md` path mentioned in tracked `.md` files exists (node check in SUMMARY)
    - `STD/exceptions.csv` contains the global `**/*.md,*,md-prose,A,` row with justification describing Markdown prose and `ref=STD-10` (the `markdown prose` exception from 01-02)
  </acceptance_criteria>
  <done>Docs reference current names.</done>
</task>

<task type="auto">
  <name>Task 2: Final scan to zero unclassified; exceptions audit; slice 5 gate; tag</name>
  <files>apps/**, packages/**, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/exceptions.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-5-*</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/exceptions.csv
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-02-PLAN.md (Task 2 `md-prose` category A row)
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/persisted.csv
  </read_first>
  <action>
    1. `node tools/std/scan-pt.ts --out STD/reports/slice-5-scan.csv --summary STD/reports/slice-5-scan-summary.md --map STD/map`. For each unclassified finding: technical identifier with an applied-row equivalent -> fix the code (approved-derived row, Sonnet, notes `post-gate2-derived`, applied with the tools); residual comment -> comment workflow (Codex by scope, Sonnet review, apply); persisted value -> it must already be in persisted.csv (add the missing B exception row pointing to its id; if it is not in persisted.csv, Sonnet adds it with a decision and lists it in SUMMARY for owner visibility); product text in a technical position (e.g. a toast string passed through a technical call) -> A row with exact pattern. Repeat until `Unclassified: 0`.
    2. Exceptions audit (Sonnet): list exception rows with > 50 matches and confirm each is intentionally broad (env names, SQL names, JSX text rule); tighten any over-broad regex. Every row must have category, justification and ref.
    3. `node tools/std/scan-pt.ts ... --fail-on-unclassified` exit 0; `bash tools/std/gate.sh slice-5` all PASS. Commit `refactor(01-31): residual renames and classification; STD-11 scan unclassified=0; gate slice-5 green`; `git tag std-slice-5-end`.
  </action>
  <verify>
    <automated>node tools/std/scan-pt.ts --out .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-5-scan.csv --summary .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-5-scan-summary.md --map .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map --fail-on-unclassified && bash tools/std/gate.sh slice-5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -x "Unclassified: 0" STD/reports/slice-5-scan-summary.md` matches
    - every exceptions.csv data row has non-empty category (A|B|C), justification and ref; every B row ref points to an existing persisted.csv id
    - `grep -c '^\*\*/\*\.md,\*,md-prose,A,' STD/exceptions.csv` prints 1; that row has non-empty justification and `STD-10` in its ref column
    - `bash tools/std/gate.sh slice-5` exits 0; tag std-slice-5-end at HEAD
  </acceptance_criteria>
  <done>STD-10/STD-11 scan proof achieved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exceptions file → phase completion | an over-broad exception could hide real PT naming |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-31-01 | Repudiation | over-broad exception rows | mitigate | audit rows with >50 matches; every row needs category/justification/ref; B rows must reference persisted.csv ids |
| T-01-31-02 | Tampering | persisted value "fixed" during residual sweep | mitigate | persisted values are only classified (B), never renamed; ddl check + gate |
</threat_model>

<verification>
Scan unclassified=0 with `--fail-on-unclassified`; gate slice-5 green; tag present.
</verification>

<success_criteria>
ROADMAP criterion 7 satisfied by automated scan.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-31-SUMMARY.md` when done
</output>
