---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 10
type: execute
wave: 6
depends_on: ["01-09", "01-35"]
files_modified:
  - .planning/PROJECT.md
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-db.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-contracts.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-autenticacao.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-armazenamento.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-tempo-real.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-mcp.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/workers.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/api.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/infra.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/map-codex1-*.json
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/sample-backend.csv
autonomous: true
requirements: [STD-01, STD-02, STD-08, STD-10]

must_haves:
  truths:
    - "PROJECT.md states the new technical-language rule and no longer says 'todo em português' (STD-08)"
    - "Every candidate row of the backend and infra scopes has a proposed English name or KEEP/REMOVE with decision_ref (CSS is plan 01-36)"
    - "Every wire-contracts rename-with-db-key key has a wire-key row whose new equals the Drizzle ts-prop new (W4 input)"
    - "check-map reports 0 errors for these scopes at status=proposed"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "STD-08 rule"
      contains: "CONVENTIONS-EN.md"
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/api.csv"
      provides: "API old->new rows incl. endpoints"
  key_links:
    - from: "std/map/*.csv"
      to: "std/GLOSSARY.md"
      via: "check-map --glossary consistency"
      pattern: "glossary"
---

<objective>
Record the approved language rule in PROJECT.md (STD-08) and propose the old->new map for the backend and infra scopes (D-03 gate 2 input). The CSS map (D-35, ~3,000 selectors) moved to plan 01-36 so this plan's Sonnet review stays bounded (W11).

Purpose: Gate 2 needs a complete, mechanically valid map. Backend scopes are one coherent vocabulary block for Codex account 1 (RESEARCH split).
Output: map CSVs for packages-db, packages-contracts, packages-autenticacao, packages-armazenamento, packages-tempo-real, packages-mcp, workers, api, infra at status=proposed; PROJECT.md updated.

Model routing: Codex account 1 proposes names; Claude (Sonnet) enforces fixed decisions, reviews the sample plus all breaking kinds, and writes PROJECT.md. No Haiku.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-CONTEXT.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/GLOSSARY.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/CONVENTIONS-EN.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

SCOPES (this plan) = packages-db,packages-contracts,packages-autenticacao,packages-armazenamento,packages-tempo-real,packages-mcp,workers,api,infra

Fixed name decisions the map must contain (owner-locked):
- D-12: endpoint `/v1/auth/google/retorno` -> `/v1/auth/google/callback`; D-13: `/v1/auth/sso/retorno` -> `/v1/auth/sso/callback`.
- D-15: `webhooks/whatsapp|instagram|messenger` rows = KEEP.
- D-38: cookie `pipe_sessao` renames (e.g. `pipe_session`); all Prometheus metric names rename (`pipe_fila_profundidade`, `pipe_fila_idade_item_mais_velho_segundos`, `pipe_http_requisicoes_total`, `pipe_mensagem_entrega_total`, `pipe_migration_pendente`).
- D-10: 10 queues (`pipe-entrada`, `pipe-entrega`, `pipe-agregacao`, `pipe-espelho-crm`, `pipe-midia`, `pipe-sla`, `pipe-process-http`, `pipe-dicionario-crm`, `pipe-importacao`, `pipe-instagram-token`) and 7 scheduler ids (`varredura-outbox`, `metrica-diaria`, `varredura-espelho-crm`, `varredura-midia`, `varredura-sla`, `varredura-dicionario-crm`, `renovacao-token-instagram`) rename.
- D-07: root scripts (`banco:subir`, `banco:descer`, `banco:migrar`, `banco:semear`, `seed:demo`, `desk`, `ponte`) and workspace scripts (`gerar`, `gerar:custom`, `migrar`, `semear`, `semear:demo`, `provisionar`, `importar:fluxo`, `test:observar`) rename; `package` rows for `@pipe/autenticacao`, `@pipe/armazenamento`, `@pipe/tempo-real`; `app` rows for `gestao-vite`, `ponte` (D-04).
- D-06/D-36: no env var name rows (they are exceptions); `infra` shell-var rows whose names are env vars must not exist.
Codex calls go only through `tools/std/codex-run.sh` (worktree of HEAD): GLOSSARY.md, CONVENTIONS-EN.md and the candidate map CSVs are committed before the first call.
</context>

<tasks>

<task type="auto">
  <name>Task 1: STD-08 — update PROJECT.md language rule</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (Constraints ~line 75 and Key Decisions ~line 89)
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/CONVENTIONS-EN.md
  </read_first>
  <action>
    Replace the constraint that says technical code is "todo em português" (around line 75) with the approved rule, written in Portuguese like the rest of PROJECT.md: linguagem técnica (arquivos, pastas, identificadores, rotas, endpoints, nomes de teste, comentários técnicos, CSS) em inglês conforme `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/CONVENTIONS-EN.md` e o glossário aprovado; texto visível ao usuário continua em português/localizado; nomes SQL, valores e contratos persistidos (incluindo chaves de documentos jsonb) ficam como estão até decisão própria (STD-06, `persisted.csv`); variáveis de ambiente mantêm nome (D-06/D-36). Make the Key Decisions entry (~line 89) reference the same document and gate 1 approval date. Do not change other sections.
  </action>
  <verify>
    <automated>test "$(grep -c 'todo em português' .planning/PROJECT.md)" = "0" && grep -c "CONVENTIONS-EN.md" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "todo em português" .planning/PROJECT.md` prints 0
    - `grep -c "CONVENTIONS-EN.md" .planning/PROJECT.md` >= 1
    - PROJECT.md mentions that user-visible text stays in Portuguese
  </acceptance_criteria>
  <done>STD-08 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Codex account 1 proposes names for SCOPES; fixed decisions and wire-key rows</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-db.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-contracts.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-autenticacao.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-armazenamento.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-tempo-real.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-mcp.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/workers.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/api.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/infra.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/map-codex1-*.json</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/TEMPLATE-map.md
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/wire-contracts.csv
    - apps/api/src/servidor.ts, apps/api/src/autenticacao.ts (fluxoDaRota), apps/api/src/filas.ts, apps/workers/src/filas.ts
  </read_first>
  <action>
    1. For each scope in SCOPES: `node tools/std/codex-prompt.ts --template STD/prompts/TEMPLATE-map.md --rows STD/map --filter scope=<scope> --status candidate --chunk 300 --out-dir STD/prompts/map-<scope>`; run every chunk with `bash tools/std/codex-run.sh 1 tools/std/schemas/map-rows.schema.json <chunk> STD/out/map-codex1-<scope>-NNN.json`; merge with `node tools/std/merge-proposals.ts --in "STD/out/map-codex1-<scope>-*.json" --target STD/map --owner codex1`. Order chunks so a symbol's rows for one file stay in one chunk (codex-prompt sorts by declared_at).
    2. Enforce the fixed decisions in context by editing rows directly (owner=sonnet): callbacks, webhook KEEPs, cookie, metrics, queues, scheduler ids, scripts, packages, apps.
    3. For every key in `keys_affected` of `wire-contracts.csv` rows with decision `rename-with-db-key`, create one `wire-key` row in `packages-db.csv` (old = old TS key, new = the `new` of the Drizzle ts-prop row for that key — never a different spelling, `consumers` = semicolon-separated files that read that key untyped — find with `rg -l "\.<old>\b|'<old>'" apps/*/src apps/*/tests packages/*/src`), decision_ref D-09. Where a packages-contracts ts-prop row has the same `old`, set its `new` to the same value now (check-map rule 10 enforces this at gate 2 across all scopes; W4).
    Commit: `docs(01-10): backend/infra rename map proposals`.
  </action>
  <verify>
    <automated>ls .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/map-codex1-*.json | wc -l && node -e "const fs=require('fs'),d='.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/';let n=0;for(const s of 'packages-db packages-contracts packages-autenticacao packages-armazenamento packages-tempo-real packages-mcp workers api infra'.split(' '))n+=(fs.readFileSync(d+s+'.csv','utf8').match(/,candidate,/g)||[]).length;if(n)process.exit(1);console.log('candidate=0')"</automated>
  </verify>
  <acceptance_criteria>
    - no row of SCOPES is at status candidate
    - `grep -c "/v1/auth/google/callback" STD/map/api.csv` >= 1 and `grep -c "/v1/auth/sso/callback" STD/map/api.csv` >= 1
    - `grep -c "pipe-entrada" STD/map/workers.csv STD/map/api.csv` total >= 1 with a non-KEEP new value
    - `grep -c "pipe_sessao" STD/map/packages-autenticacao.csv STD/map/api.csv` total >= 1 (cookie row present, D-38)
    - infra.csv contains rows of kind package for `@pipe/autenticacao`, `@pipe/armazenamento`, `@pipe/tempo-real` and kind app for `gestao-vite` and `ponte`
    - the check-map output for packages-db,packages-contracts contains no rule-10 (wire consistency) error (other rule errors are fixed in Task 3)
    - codex-log-1.csv has lines for these outputs, all exit 0
  </acceptance_criteria>
  <done>All backend/infra rows proposed; fixed decisions and wire-key rows in place.</done>
</task>

<task type="auto">
  <name>Task 3: Sonnet review of breaking kinds and sample; check-map green</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-db.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-contracts.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-autenticacao.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-armazenamento.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-tempo-real.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-mcp.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/workers.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/api.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/infra.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/sample-backend.csv</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/persisted.csv
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/GLOSSARY.md
  </read_first>
  <action>
    1. `node tools/std/check-map.ts --map STD/map --scopes <SCOPES> --sample 0.1 --seed 1 --out STD/reports/sample-backend.csv`; review every sampled row plus ALL rows of kinds endpoint, queue, job-name, ws-event, error-code, cookie, metric, wire-key, package, app, subpath-export, script, and every class/interface row whose old starts with Controlador|Servico|Guarda|Erro|Filtro|Modulo (D-05). Review in scope order, committing after each scope (`docs(01-10): review <scope>`), so the review never has to hold more than one scope at a time. Fix rows (owner=sonnet). Record in the SUMMARY per scope: rows reviewed, rows changed, sample error rate. If a scope's sample error rate > 10%, review that whole scope.
    2. Run `node tools/std/check-map.ts --map STD/map --scopes <SCOPES> --require-status proposed --glossary STD/GLOSSARY.md` until 0 errors. Warnings about glossary must each be fixed or carry `glossary-exception:` in notes.
    Commit: `docs(01-10): backend/infra rename map reviewed + PROJECT.md language rule`.
  </action>
  <verify>
    <automated>node tools/std/check-map.ts --map .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map --scopes packages-db,packages-contracts,packages-autenticacao,packages-armazenamento,packages-tempo-real,packages-mcp,workers,api,infra --require-status proposed --glossary .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/GLOSSARY.md</automated>
  </verify>
  <acceptance_criteria>
    - check-map command above exits 0
    - sample-backend.csv exists; SUMMARY has the per-scope review table
  </acceptance_criteria>
  <done>Backend and infra maps proposed, reviewed and mechanically valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| map → security-relevant names | guards, auth callbacks, cookies, scopes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10-01 | Spoofing | OAuth/SSO callback paths | mitigate | fixed rows per D-12/D-13; cutover plan re-registers exact URI and env value |
| T-01-10-02 | Elevation of privilege | scope strings or guard names mis-mapped | mitigate | scopes are persisted (not in map); guard class rows reviewed 100% by Sonnet; route-match compare later |
| T-01-10-03 | Session management | cookie rename | accept | owner accepted global logout (D-38) |
| T-01-10-04 | Denial of service | Drizzle key renamed but contracts/front props named differently | mitigate | wire-key new copied from the Drizzle ts-prop row; contracts rows aligned; check-map rule 10 |
</threat_model>

<verification>
check-map 0 errors for the 9 scopes; PROJECT.md grep checks pass.
</verification>

<success_criteria>
STD-08 done; backend/infra map ready for gate 2.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-10-SUMMARY.md` when done
</output>
