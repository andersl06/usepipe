---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 16
type: execute
wave: 11
depends_on: ["01-15"]
files_modified:
  - apps/workers/**
  - apps/ponte/src/**
  - apps/ponte/tests/**
  - apps/api/src/**
  - apps/api/tests/**
  - infra/observabilidade/alertas.yml
  - infra/observabilidade/prometheus.yml
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/queue-rename.md
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/workers.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/ponte.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/api.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-2a-*
autonomous: true
requirements: [STD-07, STD-10]

must_haves:
  truths:
    - "Workers and ponte identifiers, files and test titles follow the approved map"
    - "All 10 BullMQ queue names, job names and the 7 scheduler ids are renamed in producers (API) and consumers (workers + 7 API-hosted consumers) in the same plan (D-10)"
    - "Prometheus metric names are renamed in code and in infra/observabilidade/alertas.yml together (D-38)"
    - "The old->new queue and scheduler table needed by the cutover drain is recorded"
    - "Gate passes"
  artifacts:
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/queue-rename.md"
      provides: "old/new queue + scheduler ids for drain"
      contains: "pipe-entrada"
  key_links:
    - from: "apps/api/src/filas.ts (producers)"
      to: "apps/workers/src/filas.ts (names)"
      via: "shared constants exported by @pipe/workers"
      pattern: "Queue"
    - from: "metric names in code"
      to: "infra/observabilidade/alertas.yml"
      via: "same renamed strings"
      pattern: "pipe_"
---

<objective>
Slice 2a (D-19 step 2): apply the approved map for `apps/workers` and `apps/ponte`, and rename BullMQ queues/jobs/schedulers and Prometheus metrics end to end.

Purpose: Queue names are string contracts between API producers and consumers; renaming both sides together keeps the branch green (D-10). Metric names and alert rules must move together (D-38).
Output: renamed workers/ponte, renamed queue/job/metric strings, `std/queue-rename.md`, gate report.

Model routing (D-25): Haiku-eligible tool application; Sonnet writes `queue-rename.md` and resolves any unmatched literal. Escalation rules as plan 01-13.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-13-PLAN.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Slice rules: plan 01-13 context. Scopes: workers, ponte (all TS kinds + test-title + file/dir), plus kinds queue, job-name, metric from workers.csv and api.csv.

Facts:
- Queue names in `apps/workers/src/filas.ts`, producers in `apps/api/src/filas.ts`; `@pipe/workers` exports `.` and `./whatsapp` (subpath-export rows in workers.csv); API hosts 7 consumers.
- 10 queues: `pipe-entrada`, `pipe-entrega`, `pipe-agregacao`, `pipe-espelho-crm`, `pipe-midia`, `pipe-sla`, `pipe-process-http`, `pipe-dicionario-crm`, `pipe-importacao`, `pipe-instagram-token` (API-local). 7 schedulers: `varredura-outbox`, `metrica-diaria`, `varredura-espelho-crm`, `varredura-midia`, `varredura-sla`, `varredura-dicionario-crm`, `renovacao-token-instagram`. process-http jobIds use prefix `process-http-<id>`.
- Metrics: `pipe_fila_profundidade`, `pipe_fila_idade_item_mais_velho_segundos`, `pipe_http_requisicoes_total`, `pipe_mensagem_entrega_total`, `pipe_migration_pendente`; referenced by `infra/observabilidade/alertas.yml`.
- Dev/test uses `PIPE_FILAS=memoria` (env name and value stay, D-06).
- ponte depends on `@pipe/api` subpath exports (`./dominio/fluxo` etc.) — those subpaths rename in plan 01-17; ponte's own identifiers rename here.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workers and ponte identifiers, files, test titles</name>
  <files>apps/workers/**, apps/ponte/src/**, apps/ponte/tests/**, apps/api/src/**, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/workers.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/ponte.csv</files>
  <read_first>
    - apps/workers/src/filas.ts, apps/workers/src/main.ts
    - apps/workers/package.json (exports), apps/workers/vitest.config.ts
    - apps/ponte/src/main.ts
  </read_first>
  <action>
    `rename-symbols.ts --scopes workers,ponte --kinds symbol,ts-prop,ts-local,wire-key` (dry-run then apply); `move-files.ts --scopes workers,ponte --kinds file,dir`; `rewrite-literals.ts --scopes workers,ponte --kinds subpath-export,test-title,error-code,query-param`. `pnpm typecheck` exit 0. Check `apps/workers/Dockerfile` for paths reported in move leftovers and fix them now (Dockerfile is technical config). Commit `refactor(01-16): rename workers and ponte per approved map`.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - tools report missing=0 / 0 conflicts
    - `pnpm typecheck` exits 0
    - no approved TS/file/dir/test-title row in workers.csv or ponte.csv remains at status approved
  </acceptance_criteria>
  <done>Workers and ponte renamed.</done>
</task>

<task type="auto">
  <name>Task 2: Queues, job names, schedulers, metrics + alert rules; queue-rename.md; gate</name>
  <files>apps/workers/src/**, apps/api/src/**, apps/api/tests/**, infra/observabilidade/alertas.yml, infra/observabilidade/prometheus.yml, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/queue-rename.md, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-2a-*</files>
  <read_first>
    - apps/api/src/filas.ts (producers; file path may already be renamed — resolve via map)
    - infra/observabilidade/alertas.yml, infra/observabilidade/prometheus.yml
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-RESEARCH.md (Queue drain runbook table)
  </read_first>
  <action>
    1. BEFORE rewriting, write `STD/queue-rename.md`: table `old queue | new queue | consumer (api/workers) | durable source/sweep | lost-job impact` for all 10 queues (impact column copied from RESEARCH drain table: `pipe-entrada`, `pipe-process-http`, `pipe-importacao` = unrecoverable, must reach zero), and table `old scheduler id | new scheduler id | queue | pattern` for the 7 schedulers, plus old/new job name rows and the `process-http-<id>` jobId prefix mapping. Values from approved rows in workers.csv/api.csv.
    2. `node tools/std/rewrite-literals.ts --map STD/map --scopes workers,api --kinds queue,job-name,metric --dry-run`, then apply. Rewrite metric names in `infra/observabilidade/alertas.yml` (and `prometheus.yml` if it names metrics) to the same new strings — YAML is not covered by the tool, edit the exact `expr:` tokens by hand from the approved metric rows.
    3. Verify no old queue/scheduler/metric string remains in code: for each old value `rg -n -F "'<old>'" apps packages` returns nothing (except `STD/queue-rename.md`).
    4. `bash tools/std/gate.sh slice-2a` all PASS (queue tests run with `PIPE_FILAS=memoria` as before).
    Commit `refactor(01-16): rename queues, job names, schedulers and metrics (D-10, D-38); gate slice-2a green`.
  </action>
  <verify>
    <automated>bash tools/std/gate.sh slice-2a</automated>
  </verify>
  <acceptance_criteria>
    - `STD/queue-rename.md` lists 10 queues and 7 scheduler ids with old and new values
    - `rg -n -F "pipe-entrada" apps packages infra` returns nothing
    - `rg -n -F "varredura-outbox" apps packages` returns nothing
    - `rg -n "pipe_fila_profundidade|pipe_http_requisicoes_total" apps packages infra` returns nothing
    - `bash tools/std/gate.sh slice-2a` exits 0
  </acceptance_criteria>
  <done>Queues and metrics renamed end to end; drain table recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API producers → Redis → consumers | queue names bind both sides |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-16-01 | Denial of service | producer/consumer queue name mismatch | mitigate | both sides rewritten from the same approved rows in one plan; queue tests in gate; old-string grep returns nothing |
| T-01-16-02 | Denial of service | in-flight jobs under old names lost at deploy | mitigate | queue-rename.md feeds the cutover drain (plan 01-33); unrecoverable queues flagged |
| T-01-16-03 | Repudiation | alerts silently stop firing after metric rename | mitigate | alertas.yml updated in same plan; smoke checklist verifies rules load (D-38) |
</threat_model>

<verification>
Gate slice-2a green; no old queue/metric strings.
</verification>

<success_criteria>
Workers/ponte English; queue and metric contracts renamed consistently with drain data recorded.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-16-SUMMARY.md` when done
</output>
