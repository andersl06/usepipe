---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 19
type: execute
wave: 14
depends_on: ["01-18"]
files_modified:
  - apps/api/**
  - apps/desk-vite/**
  - apps/gestao-vite/**
  - apps/crm/src/**
  - packages/autenticacao/src/**
  - packages/contracts/src/**
  - packages/tempo-real/src/**
  - apps/gestao-vite/README.md
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-2d-*
autonomous: true
requirements: [STD-06, STD-07, STD-10]

must_haves:
  truths:
    - "Non-persisted error codes are renamed in throw sites, front comparisons and tests; persisted codes (stored in erro_codigo/ultimo_erro, API-key scopes) are unchanged (D-10, D-40)"
    - "Non-db wire keys classified rename in wire-contracts.csv are renamed in API DTOs, contracts types and every front consumer together (D-09)"
    - "WebSocket event names are renamed in the API gateway and the tempo-real client together (D-10)"
    - "Session cookie pipe_sessao (and pipe_desafio if mapped) is renamed in the session package, API session, WS upgrade, CRM session helper + middleware and Vite dev proxies (D-38)"
    - "Gate passes"
  artifacts:
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-2d-gate.md"
      provides: "gate result"
  key_links:
    - from: "cookie name constant in session package"
      to: "WS upgrade handler + CRM middleware"
      via: "same new cookie name"
      pattern: "pipe_session|COOKIE"
---

<objective>
Slice 2d: rename the remaining API string contracts — non-persisted error codes, non-db wire keys, WebSocket events, and the session cookie — with every consumer.

Purpose: These contracts cross API <-> fronts/CRM/WS; persisted ones must stay (STD-06), so this plan applies only rows that the gate-2-approved persisted inventory left in the map.
Output: renamed contracts, gate report.

Model routing (D-25): Haiku-eligible for approved rows via tools; Sonnet verifies the persisted-code invariant and the cookie wiring across the 6 sites.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-13-PLAN.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Slice rules: plan 01-13 context.

Facts (original paths; resolve via map):
- Error codes: `ErroPipe.x('code')` (169) + `new ErroPipe(...)` (74), 108 unique codes; fronts compare codes (e.g. `arquivo_vazio`). Persisted codes are in `STD/persisted.csv` (kind error-code) and have no map rows.
- `pipe_sessao` appears in `packages/autenticacao/src/sessao.ts`, `apps/api/src/sessao.ts`, `apps/api/src/eventos-ws.ts` (WS upgrade auth), `apps/crm/src/lib/sessao.ts`, `apps/crm/src/middleware.ts`, `apps/desk-vite/vite.config.ts`, `apps/gestao-vite/vite.config.ts`, `apps/gestao-vite/README.md`. D-38: rename, global logout accepted.
- WS events: API gateway (`eventos-ws.ts`) and `packages/tempo-real` client (no front consumer yet, RT-01).
- Wire keys: `STD/wire-contracts.csv` rows with decision `rename-with-db-key` were applied in plan 01-13; this plan applies `wire-key` rows in api.csv/packages-contracts.csv (non-db origin).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Error codes, wire keys, WS events</name>
  <files>apps/api/**, apps/desk-vite/src/**, apps/desk-vite/tests/**, apps/gestao-vite/src/**, apps/gestao-vite/tests/**, apps/crm/src/**, packages/contracts/src/**, packages/tempo-real/src/**</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/persisted.csv (kind error-code)
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/wire-contracts.csv
    - API error class file (resolved from apps/api/src/dominio/erros.ts via map)
  </read_first>
  <action>
    1. Record the list of persisted error-code values (persisted.csv) and `rg -c -F "'<code>'"` count for each across apps/packages (before).
    2. `rewrite-literals.ts --scopes api,packages-contracts --kinds wire-key` (dry-run first, then apply) for untyped consumers listed in each row's `consumers`. Typed consumers already follow the DTO/contract ts-prop rows applied in plans 01-14/01-17; `wire-key` is never a rename-symbols kind. This plan is the single owner of api and packages-contracts wire-key rows (plan 01-14 does not apply them).
    3. `rewrite-literals.ts --scopes api,packages-contracts,packages-tempo-real --kinds error-code,ws-event` (dry-run first; apply).
    4. Recount persisted codes: every count must equal step 1 (persisted codes untouched). Record the table in SUMMARY.
    5. `pnpm typecheck` exit 0. Commit `refactor(01-19): rename non-persisted error codes, wire keys, ws events`.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` exits 0
    - persisted error-code occurrence counts identical before/after (table in SUMMARY)
    - no approved error-code/ws-event/wire-key row in api.csv, packages-contracts.csv, packages-tempo-real.csv remains at status approved
  </acceptance_criteria>
  <done>Codes, wire keys and events renamed; persisted codes intact.</done>
</task>

<task type="auto">
  <name>Task 2: Session cookie rename across all sites; gate</name>
  <files>packages/autenticacao/src/**, apps/api/src/**, apps/crm/src/**, apps/desk-vite/vite.config.ts, apps/gestao-vite/vite.config.ts, apps/gestao-vite/README.md, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-2d-*</files>
  <read_first>
    - packages/autenticacao/src/sessao.ts (resolved via map)
    - apps/api/src/eventos-ws.ts (resolved via map; WS upgrade cookie parsing)
    - apps/crm/src/middleware.ts, apps/crm/src/lib/sessao.ts
    - apps/desk-vite/vite.config.ts, apps/gestao-vite/vite.config.ts
  </read_first>
  <action>
    `rewrite-literals.ts --map STD/map --scopes packages-autenticacao,api,crm --kinds cookie` (dry-run, apply). Then hand-edit non-TS sites from the approved cookie rows: comments/docs strings in both `vite.config.ts` files and `apps/gestao-vite/README.md`. Verify exactly one definition of the cookie name constant is used by API session, WS upgrade and CRM (if CRM duplicates the literal, both literals now equal the new name). Cookie attributes (HttpOnly, Secure, SameSite, path, maxAge) must be byte-identical: diff the attribute object before/after. Run `bash tools/std/gate.sh slice-2d` (all PASS; session + WS tests included). Commit `refactor(01-19): rename session cookie (D-38); gate slice-2d green`.
  </action>
  <verify>
    <automated>bash tools/std/gate.sh slice-2d && test -z "$(rg -n -F pipe_sessao apps packages --glob '!**/dist/**')"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n -F pipe_sessao apps packages --glob '!**/dist/**'` returns nothing
    - cookie attribute object diff is empty except the name (recorded in SUMMARY)
    - `bash tools/std/gate.sh slice-2d` exits 0
  </acceptance_criteria>
  <done>Cookie renamed coherently; slice 2d green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser cookie → API/WS/CRM | session authentication |
| DB stored codes → code | persisted error values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-19-01 | Spoofing | WS upgrade still reading old cookie name -> auth bypass or lockout | mitigate | all 6 sites from the same approved row; `rg pipe_sessao` empty; WS/session tests in gate |
| T-01-19-02 | Session management | cookie attributes changed accidentally | mitigate | attribute object diff must be name-only |
| T-01-19-03 | Tampering | persisted error codes renamed in code -> mismatch with DB rows | mitigate | before/after occurrence counts of persisted codes must be equal |
| T-01-19-04 | Session management | global logout at deploy | accept | owner accepted (D-38) |
</threat_model>

<verification>
Gate slice-2d green; no `pipe_sessao`; persisted code counts unchanged.
</verification>

<success_criteria>
All API string contracts renamed per map except persisted ones.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-19-SUMMARY.md` when done
</output>
