---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 35
type: execute
wave: 5
depends_on: ["01-08"]
files_modified:
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/wire-contracts.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/wire-*.json
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/wire-codex1/**
autonomous: true
requirements: [STD-05, STD-06]

must_haves:
  truths:
    - "Every API endpoint is classified for wire-key impact (raw Drizzle rows, mapped DTO, jsonb, none) per D-09"
    - "Keys read from or written to jsonb are classified persisted and never renamed on the wire"
    - "Every rename-with-db-key decision lists the affected keys, so plan 01-10 can create wire-key rows and check-map rule 10 can prove front/contract props follow the same names"
  artifacts:
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/wire-contracts.csv"
      provides: "D-09 per-endpoint classification"
      contains: "endpoint,method,response_source,keys_affected,decision,decision_ref,notes"
  key_links:
    - from: "std/wire-contracts.csv rename-with-db-key rows"
      to: "std/map/packages-db.csv wire-key rows (plan 01-10)"
      via: "keys_affected"
      pattern: "rename-with-db-key"
---

<objective>
Classify every API endpoint's JSON contract for key-rename impact (D-09), split out of plan 01-08 to keep each Sonnet review bounded (W11).

Purpose: raw Drizzle rows put TS keys on the wire; renaming a Drizzle key changes the JSON every front reads. jsonb content on the wire is persisted data.
Output: `std/wire-contracts.csv`.

Model routing: Codex account 1 lists response sources per endpoint; Claude (Sonnet) decides and writes the CSV.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-CONTEXT.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-08-SUMMARY.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

wire-contracts.csv columns: `endpoint,method,response_source,keys_affected,decision,decision_ref,notes`; `response_source` in raw-rows|mapped|jsonb|none; `decision` in `rename-with-db-key` (wire key follows the Drizzle TS key rename in the same slice as its consumers), `keep` (API maps back to the old key), `persisted` (jsonb/stored -> must already be in persisted.csv). `keys_affected` = semicolon-separated old keys.
Codex calls go only through `tools/std/codex-run.sh` (worktree of HEAD; 01-08 outputs are committed).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Codex account 1 lists the response source of every endpoint</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/wire-*.json, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/wire-codex1/**</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/api.csv (endpoint rows)
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-routes.json (handler + file per route)
    - apps/api/src/controladores/ (one controller returning rows directly, found via grep for `return await db.select` / `.returning()`)
  </read_first>
  <action>
    Write `STD/prompts/wire-codex1/TEMPLATE-wire.md` (English; same safety preamble as the other templates, incl. the no-.env/no-keys rule) asking, for each route of `baseline-routes.json` (method, path, controller, handler, file:line): open the handler and every function it returns through; answer `response_source` (raw-rows when the response contains objects produced by Drizzle `select()`/`.returning()`/relational queries without a key-renaming map; mapped when every response key is written explicitly; jsonb when a response value is a jsonb column document; none for empty/primitive), `keys_affected` = the response keys that are Drizzle TS keys (raw-rows) or jsonb document roots (jsonb), and any request-body keys that are written straight into a Drizzle insert/update. Output schema: reuse `tools/std/schemas/map-rows.schema.json` shape `{rows:[{id, new, persisted, category, decision_ref, notes}]}` with `id` = `METHOD path`, `new` = response_source, `notes` = `keys:<k1;k2>|req:<k;..>|evidence:<file:line>`. Chunk 60 routes per prompt, run each with `bash tools/std/codex-run.sh 1 tools/std/schemas/map-rows.schema.json <chunk> STD/out/wire-codex1-NNN.json`. Commit `docs(01-35): wire response-source proposals`.
  </action>
  <verify>
    <automated>ls .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/wire-codex1-*.json | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - the union of `id`s in `STD/out/wire-codex1-*.json` equals the set of `METHOD path` of baseline-routes.json (node check printed in SUMMARY)
    - codex-log-1.csv has these calls, all exit 0
  </acceptance_criteria>
  <done>Raw classification per endpoint available.</done>
</task>

<task type="auto">
  <name>Task 2: Sonnet decides and writes wire-contracts.csv</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/wire-contracts.csv</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/persisted.csv
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/jsonb-reach.csv
  </read_first>
  <action>
    Write `STD/wire-contracts.csv` with one row per endpoint row of `STD/map/api.csv` plus one per already-English route that returns raw rows. Default decisions: `rename-with-db-key` for raw-rows endpoints whose keys come from Drizzle TS keys (all consumers are Pipe-controlled, STD-05 coordinated cut); `persisted` for any key that is a jsonb document or a jsonb-reached prop (it must exist in persisted.csv — add it there with a B exception if missing, and list it in SUMMARY); `keep` only when a consumer outside the repo exists (none expected). Sonnet verifies by opening the handler for: every `jsonb` and `mapped` row, and a 15% seeded sample of `raw-rows` rows (seed 4); a sample error rate > 10% triggers review of all raw-rows rows. Commit `docs(01-35): wire-contract classification (D-09)`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),d='.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/';const w=fs.readFileSync(d+'wire-contracts.csv','utf8').trim().split('\n');if(w[0]!=='endpoint,method,response_source,keys_affected,decision,decision_ref,notes')process.exit(1);const bad=w.slice(1).filter(l=>!/,(rename-with-db-key|keep|persisted),/.test(l));if(bad.length)process.exit(1);console.log(w.length-1)"</automated>
  </verify>
  <acceptance_criteria>
    - wire-contracts.csv has at least one row per endpoint row in STD/map/api.csv and no empty `decision`
    - every `persisted` row's keys exist in persisted.csv
    - every `rename-with-db-key` row has a non-empty `keys_affected`
  </acceptance_criteria>
  <done>D-09 classification complete for plan 01-10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → fronts (wire) | JSON keys produced from Drizzle rows or jsonb |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-35-01 | Denial of service | fronts reading old JSON keys after Drizzle rename (Pitfall 10) | mitigate | rename-with-db-key rows list keys; 01-10 creates wire-key rows; check-map rule 10 aligns contracts/front props |
| T-01-35-02 | Tampering | jsonb content renamed on the wire | mitigate | persisted decision; keys must be in persisted.csv |
| T-01-35-03 | Information disclosure | Codex reading secrets | mitigate | codex-run worktree without .env*/keys; template rule |
</threat_model>

<verification>
wire-contracts.csv complete with decisions; persisted keys cross-checked.
</verification>

<success_criteria>
Every endpoint's wire contract classified before the map is proposed.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-35-SUMMARY.md` when done
</output>
