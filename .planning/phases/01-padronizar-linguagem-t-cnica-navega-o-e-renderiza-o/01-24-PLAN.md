---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 24
type: execute
wave: 22
depends_on: ["01-23"]
files_modified:
  - apps/gestao-vite/src/lib/filter-memory.ts
  - apps/gestao-vite/tests/filter-memory.test.ts
  - apps/gestao-vite/src/**
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/gestao-vite.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-3d-*
autonomous: true
requirements: [STD-04, STD-12]

must_haves:
  truths:
    - "Filters on the D-30 screens live in React state and the last filter is restored from localStorage per screen, tenant and user; they no longer appear in the query string (D-30)"
    - "A stored filter from another tenant or user is never applied; invalid/stale stored values are dropped"
    - "Wizard step, Gestão contact-detail ticket and per-screen deep-link behavior follow the gate-2-approved nav contract (D-31, D-29 Gestão, D-34)"
  artifacts:
    - path: "apps/gestao-vite/src/lib/filter-memory.ts"
      provides: "filterStorageKey, loadFilters, saveFilters"
      exports: ["filterStorageKey", "loadFilters", "saveFilters"]
    - path: "apps/gestao-vite/tests/filter-memory.test.ts"
      provides: "node:test for the helper"
  key_links:
    - from: "D-30 screens"
      to: "filter-memory.ts"
      via: "useState initialised from loadFilters; saveFilters on change"
      pattern: "loadFilters"
    - from: "filterStorageKey"
      to: "session context tenant + user ids"
      via: "key parts"
      pattern: "pipe:"
---

<objective>
Implement the Gestão part of the navigation contract: filters in state with remembered last filter (D-30), and the owner's gate-2 decisions for wizard steps (D-31), Gestão contact detail (D-29) and deep links (D-34).

Purpose: STD-12 implementation for Gestão; STD-04 classification applied.
Output: tested filter-memory helper, updated screens, gate report.

Model routing: Claude (Sonnet) — behavior change from the approved contract (not Haiku, D-25).
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/nav-contract.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Keep file basenames `filter-memory.ts` / `filter-memory.test.ts`; place the helper in the renamed `lib` folder (resolve via map). Key format (nav contract): `pipe:<app>:<screen>:filters:v1:<tenantId>:<userId>` with app = `gestao` (or the approved new app name) and screen = a stable kebab-case id per screen listed in nav-contract.md.
The screens to change are exactly the rows of nav-contract.md `## Per-screen table` whose decided location is react-state/storage for filters (D-30: operation monitoring queue/agent filters, flow log, analysis period screens, novidades search) plus the rows decided at gate 2 for D-31/D-29/D-34. The corresponding query-param rows in gestao-vite.csv have `new=STATE`.
Tenant and user ids come from the session context (renamed from `contexto/sessao`); account switching happens in the same browser, which is why both ids are part of the key.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: filter-memory helper with node:test (RED -> GREEN)</name>
  <files>apps/gestao-vite/src/lib/filter-memory.ts, apps/gestao-vite/tests/filter-memory.test.ts</files>
  <read_first>
    - apps/gestao-vite/tests (an existing node:test file for style)
    - apps/gestao-vite/src session context (renamed from contexto/sessao) for tenant/user id field names
  </read_first>
  <behavior>
    - filterStorageKey('gestao','monitoring','t1','u1') === 'pipe:gestao:monitoring:filters:v1:t1:u1'
    - keys differ when tenantId or userId differ
    - saveFilters then loadFilters with a validator returning the value round-trips
    - loadFilters returns null and removes the key when stored JSON is invalid
    - loadFilters returns null and removes the key when the validator returns null (stale ids)
    - loadFilters with a missing tenantId or userId (empty string) returns null and never reads storage
    - a storage object is injectable (in-memory Map-backed stub in tests; defaults to globalThis.localStorage)
  </behavior>
  <action>
    Tests first (`node:test`, `node:assert/strict`), run `pnpm --filter @pipe/gestao-vite test` -> RED. Implement `filterStorageKey(app, screen, tenantId, userId): string`, `loadFilters<T>(key: string | null, validate: (v: unknown) => T | null, storage?: Pick<Storage,'getItem'|'setItem'|'removeItem'>): T | null`, `saveFilters(key: string | null, value: unknown, storage?)` (no-op for null key), where `filterStorageKey` returns null if tenantId or userId is empty. try/catch around JSON.parse and storage access (private mode / quota) returning null. GREEN. Commits `test(01-24): filter memory helper` / `feat(01-24): filter memory helper`.
  </action>
  <verify>
    <automated>pnpm --filter @pipe/gestao-vite test</automated>
  </verify>
  <acceptance_criteria>
    - 7 `test(` cases for the behavior list exist and pass
    - `grep -c "filters:v1" apps/gestao-vite/src/lib/filter-memory.ts` >= 1
  </acceptance_criteria>
  <done>Helper tested.</done>
</task>

<task type="auto">
  <name>Task 2: Apply D-30 to listed screens and gate-2 decisions for D-31/D-29/D-34; gate</name>
  <files>apps/gestao-vite/src/**, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/gestao-vite.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-3d-*</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/nav-contract.md (## Gestão, ## Per-screen table)
    - each screen file listed there (renamed paths; originals: operacao/monitoramento.tsx, fluxo/log/log.tsx, fluxo/analise/**, novidades/page.tsx, componentes/filtros-rapidos.tsx, criar/fluxo/page.tsx, criar/roteador/page.tsx, fluxo/contatos/detalhe/detalhe.tsx)
  </read_first>
  <action>
    1. D-30 screens: replace `useSearchParams` filter reads/writes with `useState` initialised from `loadFilters(filterStorageKey(app, screen, tenantId, userId), validate)` and a `useEffect` calling `saveFilters` on change; each screen gets a small `validate` that checks shape and drops ids not present in the currently loaded option lists (queues/agents) — when options load after the filter, re-validate once. Remove the filter params from any `navigate`/links that previously carried them. Shared quick-filter component (renamed `componentes/filtros-rapidos.tsx`) gets filter value + setter via props instead of search params.
    2. D-31: implement the wizard-step decision recorded in nav-contract.md (if step-in-path: add the approved step route segments and read the step from the path; if state: `useState` step and remove `?passo=`); certificates wizard stays `useState` unless the contract says otherwise.
    3. D-29 Gestão contact detail and D-34 rows: apply exactly what nav-contract.md says (default recommendation was keep path + `?ticketId`-style query, deep-linkable) — only renames, no behavior change, when the decision is "keep".
    4. Mark STATE rows applied. Check: for each STATE query-param row, `rg -n "'<old>'|'<new>'" <screen file>` shows no search-param access left.
    5. `bash tools/std/gate.sh slice-3d` all PASS.
    Commit `feat(01-24): gestao filters in state with remembered last filter (D-30); wizard/contact/deep-link decisions (D-31/D-29/D-34)`.
  </action>
  <verify>
    <automated>bash tools/std/gate.sh slice-3d</automated>
  </verify>
  <acceptance_criteria>
    - `bash tools/std/gate.sh slice-3d` exits 0
    - every D-30 screen file from nav-contract.md imports `loadFilters` (`rg -l loadFilters apps/gestao-vite/src` count >= number of D-30 screens)
    - no STATE query-param row remains at status approved in gestao-vite.csv
    - SUMMARY lists each nav-contract row implemented with file paths
  </acceptance_criteria>
  <done>Gestão nav contract implemented; manual checks pending in 01-32.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → app state | browser-persisted, shared across accounts in the same browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-24-01 | Information disclosure | filters of tenant A applied in tenant B | mitigate | key includes tenantId + userId; null key when either missing (tested) |
| T-01-24-02 | Tampering | malformed stored filter crashing screens | mitigate | JSON parse guarded; validator drops invalid values and removes key (tested) |
</threat_model>

<verification>
Helper tests pass; gate slice-3d green.
</verification>

<success_criteria>
Gestão follows the approved nav contract.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-24-SUMMARY.md` when done
</output>
