---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 30
type: execute
wave: 28
depends_on: ["01-29"]
files_modified:
  - apps/ponte/**
  - apps/site/**
  - package.json
  - pnpm-lock.yaml
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/infra.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/site.csv
autonomous: true
requirements: [STD-07, STD-10]

must_haves:
  truths:
    - "Ponte and site follow the approved map (D-39)."
    - "Site image builds and local links resolve."
  artifacts: []
  key_links: []
---

<objective>
Rename ponte and site from approved rows (D-39).

Purpose: Complete site and ponte before the infra gate.
Output: renamed ponte/site and site image.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-13-PLAN.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-15-PLAN.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Slice rules: plan 01-13 context. Comment workflow: plan 01-15 context.
Facts: root scripts `banco:subir`, `banco:descer`, `banco:migrar`, `banco:semear`, `seed:demo`, `desk`, `ponte`; workspace scripts `gerar`, `gerar:custom`, `migrar`, `semear`, `semear:demo` (db), `provisionar`, `importar:fluxo` (api), `test:observar` (ai, core), `seed:crm` (crm). Infra PT file names: `infra/construir-imagens.sh`, `infra/compose/implantar.sh`, `infra/compose/backup/restaurar-teste.sh`, `infra/compose/segredos/`, `infra/compose/postgres/init/10-papel-app.sh`, `infra/compose/env.prod.exemplo`. Shell variables like `VERSAO`, `RAIZ`, `REGISTRO`, `APPS`, `PULL` are shell-var rows ONLY when they are not env vars read by compose/containers (env names stay, D-06/D-36; `PIPE_VERSAO` stays). The VPS runs `deploy.sh` from `/opt/pipe` — if the approved map renames `deploy.sh`/`implantar.sh` or their directory, the cutover runbook (plan 01-33) must use the new names. `apps/site` is static (1 TS file, `index.html`, `blog/`, `ferramentas/`, `assets/`, `nginx.conf`, own Dockerfile and docker-compose.yml); site directory names are public URLs of the site — follow the approved rows exactly.
Never run `drizzle-kit generate`: the renamed `gerar` script still runs it; do not invoke it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: ponte app, site files/dirs and config</name>
  <files>apps/ponte/**, apps/site/**, package.json, pnpm-lock.yaml, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/infra.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/site.csv</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/site.csv
    - apps/site/Dockerfile, apps/site/nginx.conf, apps/site/docker-compose.yml, apps/site/index.html (links to blog/ferramentas/assets)
    - apps/ponte/package.json
  </read_first>
  <action>
    1. ponte: `move-files.ts --scopes infra --kinds app --ids <ponte row>` + package name row via `rewrite-literals.ts --kinds package --ids <row>`; root `ponte` script filter value updated (key renamed in plan 01-39 Task 1); `pnpm install`.
    2. site: `move-files.ts --scopes site --kinds file,dir` (dry-run, apply); then fix every reference inside `apps/site/index.html`, blog/ferramentas HTML files, `nginx.conf` (locations/rewrites), site Dockerfile and docker-compose.yml to the moved paths (leftovers report + `rg -n "<old path>" apps/site`). Build the site image: `MSYS_NO_PATHCONV=1 SEM_PULL=1 bash infra/construir-imagens.sh std-check site` (name of the build script as renamed later in plan 01-39 Task 1 — run this before Task 2 renames it, so use the current name).
    3. `pnpm typecheck` exit 0. Commit `refactor(01-30): rename ponte app and site files (D-39)`.
  </action>
  <verify>
    <automated>pnpm typecheck && test -z "$(rg -n '@pipe/ponte' apps packages package.json 2>/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "@pipe/ponte" apps packages package.json` returns nothing when the ponte package row is not KEEP
    - site image build exit 0 (recorded in SUMMARY)
    - every href/src in apps/site/**/*.html pointing to a local path resolves to an existing tracked file (node check listed in SUMMARY)
  </acceptance_criteria>
  <done>ponte and site renamed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy scripts → production secrets | sops/age key handling on the VPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-30-01 | Information disclosure | shell variable rename breaking secret-path handling (SOPS_AGE_KEY_FILE, umask) | mitigate | env names never renamed; `bash -n` + Sonnet review of deploy/implantar/backup edits; security comments 100% reviewed |
| T-01-30-02 | Tampering | accidental drizzle generate via renamed script | mitigate | explicit instruction never to run it; ddl check in gate |
| T-01-30-03 | Denial of service | VPS operator runs old script names | mitigate | cutover runbook (01-33) lists new script names/paths |
</threat_model>

<verification>
Typecheck and site image build pass.
</verification>

<success_criteria>
Ponte and site renamed.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-30-SUMMARY.md` when done
</output>
