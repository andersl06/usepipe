---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 30
type: execute
wave: 25
depends_on: ["01-29"]
files_modified:
  - apps/ponte/**
  - apps/site/**
  - infra/**
  - package.json
  - pnpm-lock.yaml
  - packages/*/package.json
  - apps/*/package.json
  - apps/*/Dockerfile
  - docker-compose.yml
  - PROJECT-HANDOFF.md
  - README.md
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/infra.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/site.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/infra.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/site.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-4-*
autonomous: true
requirements: [STD-07, STD-10]

must_haves:
  truths:
    - "apps/ponte and apps/site follow the approved map (D-39): ponte app dir/package; site files, dirs and technical config (Dockerfile, nginx.conf, compose)"
    - "Infra file names, shell function/variable names (never env var names), root and workspace package.json script keys follow the map (D-07); every doc/script that invokes a renamed script is updated in the same plan"
    - "Infra and site comments are triaged per D-16/D-17"
    - "Slice 4 gate passes; tag std-slice-4-end exists"
  artifacts:
    - path: "package.json"
      provides: "English root script keys"
  key_links:
    - from: "root package.json scripts"
      to: "PROJECT-HANDOFF.md / README.md / infra/README.md commands"
      via: "pnpm <script> references rewritten together"
      pattern: "pnpm "
---

<objective>
Slice 4c: rename ponte and site apps, infra files and shell identifiers, and all package.json script keys (D-07, D-39), triage infra/site comments, and close slice 4.

Purpose: Finishes the infra/naming layer so only residual references remain for slice 5.
Output: renamed ponte/site/infra/scripts, updated command docs, gate report, tag `std-slice-4-end`.

Model routing: script/file renames Haiku-eligible from approved rows; shell edits and comment review by Sonnet; infra comments triaged by Codex account 1, site comments by Codex account 2.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-13-PLAN.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-15-PLAN.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Slice rules: plan 01-13 context. Comment workflow: plan 01-15 context.
Facts: root scripts `banco:subir`, `banco:descer`, `banco:migrar`, `banco:semear`, `seed:demo`, `desk`, `ponte`; workspace scripts `gerar`, `gerar:custom`, `migrar`, `semear`, `semear:demo` (db), `provisionar`, `importar:fluxo` (api), `test:observar` (ai, core), `seed:crm` (crm). Infra PT file names: `infra/construir-imagens.sh`, `infra/compose/implantar.sh`, `infra/compose/backup/restaurar-teste.sh`, `infra/compose/segredos/`, `infra/compose/postgres/init/10-papel-app.sh`, `infra/compose/env.prod.exemplo`. Shell variables like `VERSAO`, `RAIZ`, `REGISTRO`, `APPS`, `PULL` are shell-var rows ONLY when they are not env vars read by compose/containers (env names stay, D-06/D-36; `PIPE_VERSAO` stays). The VPS runs `deploy.sh` from `/opt/pipe` — if the approved map renames `deploy.sh`/`implantar.sh` or their directory, the cutover runbook (plan 01-33) must use the new names. `apps/site` is static (1 TS file, `index.html`, `blog/`, `ferramentas/`, `assets/`, `nginx.conf`, own Dockerfile and docker-compose.yml); site directory names are public URLs of the site — follow the approved rows exactly.
Never run `drizzle-kit generate`: the renamed `gerar` script still runs it; do not invoke it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: ponte app, site files/dirs and config</name>
  <files>apps/ponte/**, apps/site/**, package.json, pnpm-lock.yaml, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/infra.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/site.csv</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/site.csv
    - apps/site/Dockerfile, apps/site/nginx.conf, apps/site/docker-compose.yml, apps/site/index.html (links to blog/ferramentas/assets)
    - apps/ponte/package.json
  </read_first>
  <action>
    1. ponte: `move-files.ts --scopes infra --kinds app --ids <ponte row>` + package name row via `rewrite-literals.ts --kinds package --ids <row>`; root `ponte` script filter value updated (key renamed in Task 2); `pnpm install`.
    2. site: `move-files.ts --scopes site --kinds file,dir` (dry-run, apply); then fix every reference inside `apps/site/index.html`, blog/ferramentas HTML files, `nginx.conf` (locations/rewrites), site Dockerfile and docker-compose.yml to the moved paths (leftovers report + `rg -n "<old path>" apps/site`). Build the site image: `MSYS_NO_PATHCONV=1 SEM_PULL=1 bash infra/construir-imagens.sh std-check site` (name of the build script as renamed later in Task 2 — run this before Task 2 renames it, so use the current name).
    3. `pnpm typecheck` exit 0. Commit `refactor(01-30): rename ponte app and site files (D-39)`.
  </action>
  <verify>
    <automated>pnpm typecheck && test -z "$(rg -n '@pipe/ponte' apps packages package.json 2>/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "@pipe/ponte" apps packages package.json` returns nothing when the ponte package row is not KEEP
    - site image build exit 0 (recorded in SUMMARY)
    - every href/src in apps/site/**/*.html pointing to a local path resolves to an existing tracked file (node check listed in SUMMARY)
  </acceptance_criteria>
  <done>ponte and site renamed.</done>
</task>

<task type="auto">
  <name>Task 2: Infra file names, shell identifiers, package.json script keys, command docs</name>
  <files>infra/**, package.json, packages/*/package.json, apps/*/package.json, apps/*/Dockerfile, docker-compose.yml, PROJECT-HANDOFF.md, README.md</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/infra.csv (kinds file, dir, shell-var, script)
    - infra/construir-imagens.sh, infra/compose/deploy.sh, infra/compose/implantar.sh, infra/README.md, PROJECT-HANDOFF.md, README.md
  </read_first>
  <action>
    1. `move-files.ts --scopes infra --kinds file,dir` (infra file/dir rows); update references to moved infra files in other infra scripts, READMEs, compose files (`rg -n "<old name>"` per row).
    2. Shell identifiers (Sonnet, by hand from approved shell-var rows): rename each variable/function consistently within its script (all `$VAR`, `${VAR}`, `${VAR:-..}` uses); skip anything listed as an env name in exceptions.csv. `bash -n <script>` for every edited script; `shellcheck` if available.
    3. Script keys: `rewrite-literals.ts --scopes infra,packages-db,api,packages-ai,packages-core,crm --kinds script` (package.json keys and `pnpm <script>` / `pnpm run <script>` / `pnpm --filter X <script>` occurrences in `*.md`, `*.sh`, Dockerfiles). Hand-check PROJECT-HANDOFF.md, README.md, infra/README.md, apps/*/README.md command lines (D-07).
    4. Verify: every script key in every package.json is English per map (`node` one-liner listing keys vs map old values -> no old key remains); `pnpm run` of the renamed root DB-up script works (`docker compose up -d postgres redis` via the script) — do not run the renamed generate script.
    Commit `refactor(01-30): rename infra files, shell identifiers and scripts (D-07)`.
  </action>
  <verify>
    <automated>for f in $(git ls-files 'infra/**/*.sh' 'infra/*.sh'); do bash -n "$f" || exit 1; done && node -e "const p=require('./package.json');const bad=Object.keys(p.scripts).filter(k=>/banco|semear|migrar|gerar|provisionar|importar|observar|ponte/.test(k));if(bad.length){console.error(bad);process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - all tracked infra `.sh` files pass `bash -n`
    - no package.json (root or workspace) has a script key equal to an `old` value of an applied script row
    - `rg -n "pnpm (run )?banco:|pnpm (run )?semear|pnpm --filter @pipe/db (migrar|semear|gerar)" --glob '!.planning/**' .` returns nothing
    - no env var name was changed: `git diff std-slice-3-end -- .env.example infra/compose/*exemplo* | rg "^[-+][A-Z_]+="` shows only the value change of GOOGLE_URL_RETORNO already made in slice 2 (i.e. nothing new)
  </acceptance_criteria>
  <done>Infra and scripts renamed with docs in sync.</done>
</task>

<task type="auto">
  <name>Task 3: Infra and site comments; slice 4 gate; tag std-slice-4-end</name>
  <files>infra/**, apps/site/**, apps/*/Dockerfile, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/infra.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/site.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-4-*</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/infra.csv
    - infra/compose/deploy.sh (security-relevant comments: secrets, sops, migration order)
  </read_first>
  <action>
    Comment workflow steps 1-6: infra on account 1, site on account 2. Force sensitivity=security for comments in files handling secrets/backup/deploy (`deploy.sh`, `implantar.sh`, `segredos/**`, `backup/**`, postgres init, sops/age lines) and architecture for Dockerfiles; Sonnet 100% review of those. Then `bash tools/std/gate.sh slice-4` all PASS, plus rebuild all images once: `MSYS_NO_PATHCONV=1 SEM_PULL=1 bash infra/<renamed build script> std-check` exit 0. Commit `docs(01-30): infra/site comment triage; gate slice-4 green`; `git tag std-slice-4-end`.
  </action>
  <verify>
    <automated>bash tools/std/gate.sh slice-4 && git rev-parse -q --verify refs/tags/std-slice-4-end</automated>
  </verify>
  <acceptance_criteria>
    - comments/infra.csv and comments/site.csv rows all `applied`
    - full image build of all apps exit 0 (recorded in SUMMARY)
    - `bash tools/std/gate.sh slice-4` exits 0; tag std-slice-4-end at HEAD
  </acceptance_criteria>
  <done>Slice 4 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy scripts → production secrets | sops/age key handling on the VPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-30-01 | Information disclosure | shell variable rename breaking secret-path handling (SOPS_AGE_KEY_FILE, umask) | mitigate | env names never renamed; `bash -n` + Sonnet review of deploy/implantar/backup edits; security comments 100% reviewed |
| T-01-30-02 | Tampering | accidental drizzle generate via renamed script | mitigate | explicit instruction never to run it; ddl check in gate |
| T-01-30-03 | Denial of service | VPS operator runs old script names | mitigate | cutover runbook (01-33) lists new script names/paths |
</threat_model>

<verification>
Scripts syntax-check; gate slice-4 green; all images build; tag present.
</verification>

<success_criteria>
Slice 4 (infra + app/package names + scripts) complete.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-30-SUMMARY.md` when done
</output>
