---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 23
type: execute
wave: 18
depends_on: ["01-22"]
files_modified:
  - apps/gestao-vite/src/**
  - apps/gestao-vite/tests/**
  - apps/gestao-vite/index.html
  - apps/gestao-vite/Dockerfile
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/gestao-vite.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/allowed-test-removals.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-3c-*
autonomous: true
requirements: [STD-07, STD-10]

must_haves:
  truths:
    - "Gestão identifiers, files, folders, routes (incl. the twice-mounted contact route tree) and all five path builders follow the approved map in one change"
    - "Legacy redirects ROTAS_ANTIGAS_SEM_CONTATO and ParaOCanalDoBot are deleted (D-14)"
    - "The Gestão->Desk conversation link builder (src/lib/desk-url.ts) and tests/desk-url.test.ts are deleted (D-28)"
    - "Route-literal tests (fluxo-detalhe, canal-do-fluxo, equipe, atendentes) use the new paths and pass"
    - "Gate passes with the documented test removal"
  artifacts:
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/allowed-test-removals.csv"
      provides: "documented removal of desk-url tests"
      contains: "D-28"
  key_links:
    - from: "apps/gestao-vite/src/App.tsx route segments"
      to: "path builders (contato, casca, itens, configuracoes/navegacao, growth/navegacao)"
      via: "same segment rows applied in one run"
      pattern: "path="
---

<objective>
Slice 3c: apply the approved map to `apps/gestao-vite` (287 files) and execute the Gestão removals decided by the owner (D-14, D-28).

Purpose: Gestão has duplicated path builders that already diverged once; renaming all segments in one run keeps them consistent.
Output: renamed Gestão, removals, gate report.

Model routing (D-25): renames Haiku-eligible via tools; removals (approved REMOVE rows) executed by the same executor but verified by Sonnet (dead-code check). Escalation rules as plan 01-13.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-13-PLAN.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Slice rules: plan 01-13 context. Scope gestao-vite; kinds symbol, ts-prop, ts-local, file, dir, front-route, query-param (only rows whose new is not STATE), storage-key, test-title. STATE rows and D-30/D-31/D-34 behavior are plan 01-24; CSS is plan 01-25.

Facts (pre-rename paths): routes in `src/App.tsx:254-311` with `rotasDoContato` mounted under `/fluxo/:id` and `/roteador/:id`; legacy redirects `ROTAS_ANTIGAS_SEM_CONTATO` and `ParaOCanalDoBot` at `src/App.tsx:86-90, 222-244`; path builders `paginas/fluxo/contato.tsx:16-22`, `paginas/operacao/casca.tsx:36-99`, `paginas/fluxo/itens.ts:67-167`, `paginas/fluxo/configuracoes/navegacao.tsx:23-69`, `paginas/fluxo/growth/navegacao.tsx`; onboarding/public pattern in `componentes/exigir-sessao.tsx:13`; `src/lib/desk-url.ts` (`urlDaConversaNoDesk`) has no consumer in src; literal-path tests `tests/fluxo-detalhe.test.ts`, `tests/canal-do-fluxo.test.ts`, `tests/equipe.test.ts`, `tests/atendentes.test.ts`; vite alias to `packages/ui/src/estilos.css` (path may already be renamed in slice 1).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gestão identifiers, files, folders</name>
  <files>apps/gestao-vite/src/**, apps/gestao-vite/tests/**, apps/gestao-vite/index.html, apps/gestao-vite/Dockerfile, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/gestao-vite.csv</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/gestao-vite.csv
    - apps/gestao-vite/src/App.tsx
    - apps/gestao-vite/vite.config.ts
  </read_first>
  <action>
    `rename-symbols.ts --scopes gestao-vite --kinds symbol,ts-prop,ts-local` (dry-run, apply; REMOVE rows are skipped by the tool); `move-files.ts --scopes gestao-vite --kinds file,dir` (dry-run, apply); fix `index.html`/Dockerfile paths from the leftovers report. `pnpm typecheck` exit 0. Commit `refactor(01-23): rename gestao identifiers and files`.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - tools report missing=0, 0 conflicts
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Gestão code renamed.</done>
</task>

<task type="auto">
  <name>Task 2: Routes, path builders, test titles, D-14/D-28 removals; gate</name>
  <files>apps/gestao-vite/src/**, apps/gestao-vite/tests/**, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/allowed-test-removals.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-3c-*</files>
  <read_first>
    - apps/gestao-vite/src/App.tsx (renamed)
    - the 5 path-builder files (renamed paths)
    - apps/gestao-vite/tests (the 4 literal-path tests, renamed)
  </read_first>
  <action>
    1. `rewrite-literals.ts --scopes gestao-vite --kinds front-route,query-param,storage-key,test-title` (dry-run; confirm each path builder file appears in the planned edits; apply).
    2. Segment consistency check: extract all static segments from Route paths and from the 5 path-builder files; every segment must be a `new` value of an applied front-route row or an English KEEP; `rg` for each old segment in `apps/gestao-vite/src` returns nothing.
    3. D-14: delete `ROTAS_ANTIGAS_SEM_CONTATO` (constant + the routes it generates) and `ParaOCanalDoBot` (component + its routes) from `App.tsx`; no PT->EN redirect is added anywhere.
    4. D-28: `git rm` the renamed `src/lib/desk-url.ts` and `tests/desk-url.test.ts`; confirm `rg -n "desk-url|urlDaConversaNoDesk" apps/gestao-vite` returns nothing; append `@pipe/gestao-vite,<number of tests in desk-url.test.ts at baseline>,D-28` to `STD/reports/allowed-test-removals.csv` (create with header `package,count,decision_ref`).
    5. Mark REMOVE rows applied. `bash tools/std/gate.sh slice-3c` all PASS.
    Commit `refactor(01-23): rename gestao routes/builders/tests; remove legacy redirects (D-14) and desk link builder (D-28); gate slice-3c green`.
  </action>
  <verify>
    <automated>bash tools/std/gate.sh slice-3c && test -z "$(rg -n 'ROTAS_ANTIGAS_SEM_CONTATO|ParaOCanalDoBot|urlDaConversaNoDesk' apps/gestao-vite)"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'ROTAS_ANTIGAS_SEM_CONTATO|ParaOCanalDoBot|urlDaConversaNoDesk' apps/gestao-vite` returns nothing
    - `git ls-files apps/gestao-vite | grep -c desk-url` prints 0
    - allowed-test-removals.csv contains a D-28 row
    - `bash tools/std/gate.sh slice-3c` exits 0
  </acceptance_criteria>
  <done>Gestão renamed and cleaned; slice 3c green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated user → Gestão routes | session guard and onboarding pattern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-23-01 | Elevation of privilege | onboarding/public route regex in session guard not updated -> protected page treated as public or lockout | mitigate | its literal is a front-route row applied here; segment consistency check; gate tests |
| T-01-23-02 | Repudiation | test count drop hides lost tests | mitigate | only the documented D-28 removal is allowed in test-count compare |
</threat_model>

<verification>
Gate slice-3c green; removal greps empty.
</verification>

<success_criteria>
Gestão English per map; D-14/D-28 executed.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-23-SUMMARY.md` when done
</output>
