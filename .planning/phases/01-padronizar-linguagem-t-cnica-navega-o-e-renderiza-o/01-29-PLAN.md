---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 29
type: execute
wave: 24
depends_on: ["01-28"]
files_modified:
  - apps/gestao-vite/**
  - apps/desk-vite/**
  - package.json
  - pnpm-lock.yaml
  - infra/construir-imagens.sh
  - infra/compose/docker-compose.prod.yml
  - infra/compose/env.prod.exemplo
  - infra/compose/deploy.sh
  - infra/compose/implantar.sh
  - docker-compose.yml
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/infra.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-4b-*
autonomous: true
requirements: [STD-07, STD-10]

must_haves:
  truths:
    - "The Gestão app directory and package name follow the approved app row; Dockerfile, dockerignore, build script APPS list, compose service/image names and Traefik router labels are updated together (D-04)"
    - "If the approved map also renames desk-vite, it is renamed separately after Gestão, with its own build validation (one high-risk app at a time, D-19)"
    - "The Gestão image (and Desk image if renamed) builds with the production VITE args and MSYS_NO_PATHCONV=1; docker compose config validates"
    - "Gate passes"
  artifacts:
    - path: "infra/compose/docker-compose.prod.yml"
      provides: "renamed service/image/router names"
  key_links:
    - from: "infra/construir-imagens.sh APPS list"
      to: "apps/<new app dir>"
      via: "image name ghcr.io/pipe/<app>"
      pattern: "APPS="
    - from: "docker-compose.prod.yml image: ghcr.io/pipe/<app>:${PIPE_VERSAO}"
      to: "construir-imagens.sh output tags"
      via: "same app name"
      pattern: "ghcr.io/pipe/"
---

<objective>
Slice 4b: rename the Gestão app (and the Desk app only if the map says so), one app at a time, including compose/Traefik/image names, with real image builds.

Purpose: App directory names are baked into Dockerfiles, image names, compose services and routers; a missed reference breaks deploy (Desk VPS incident).
Output: renamed app(s), updated infra, gate report.

Model routing: Claude (Sonnet) — infra edits and build validation are judgment-heavy; renames themselves come from approved rows.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-13-PLAN.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Rows: kind `app` in infra.csv for `gestao-vite` (and `desk-vite` only if present and not KEEP). `ponte` app row is plan 01-30.
Facts: `infra/construir-imagens.sh` default `APPS=(api workers desk-vite gestao-vite crm site)`, registry `ghcr.io/pipe`, bakes `VITE_*`/`NEXT_PUBLIC_*` at build time; `infra/compose/docker-compose.prod.yml` has services/images/Traefik routers for `gestao` and `desk` hostnames; `infra/compose/deploy.sh <tag>` runs on the VPS; env names (`VITE_PORTA`, `VITE_URL_API`, `PIPE_*`) stay (D-06/D-36). References to `gestao-vite` outside the lockfile: 47 files / 122 hits (RESEARCH). Root scripts reference `@pipe/desk-vite` (`desk` script) — script KEY renames happen in plan 01-30, but filter VALUES change here.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the Gestão app and all build/deploy references; build image</name>
  <files>apps/gestao-vite/**, package.json, pnpm-lock.yaml, infra/construir-imagens.sh, infra/compose/docker-compose.prod.yml, infra/compose/env.prod.exemplo, infra/compose/deploy.sh, infra/compose/implantar.sh, docker-compose.yml</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/infra.csv (kind app)
    - apps/gestao-vite/Dockerfile, apps/gestao-vite/Dockerfile.dockerignore, apps/gestao-vite/package.json
    - infra/construir-imagens.sh, infra/compose/docker-compose.prod.yml
  </read_first>
  <action>
    1. `node tools/std/move-files.ts --map STD/map --scopes infra --kinds app --ids <gestao row id>`; `node tools/std/rewrite-literals.ts --map STD/map --scopes infra --kinds package --ids <row for @pipe/gestao-vite package name, if a package row exists>` (else edit `apps/<new>/package.json` name from the approved row).
    2. Update by hand from the approved app row: `apps/<new>/Dockerfile` (WORKDIR/COPY/`--filter`), its `.dockerignore`, `infra/construir-imagens.sh` APPS default + any case branches keyed on the app name, `infra/compose/docker-compose.prod.yml` service name, `image: ghcr.io/pipe/<new>:${PIPE_VERSAO}`, Traefik router/service label names (hostnames stay unless the map row says otherwise), `deploy.sh`/`implantar.sh` references to the service name, root `package.json` filter values.
    3. `rg -n "gestao-vite" --glob '!pnpm-lock.yaml' --glob '!.planning/**' --glob '!referencias-blip/**' .` returns only docs (listed for slice 5) — fix any code/config hit now.
    4. `pnpm install`, `pnpm typecheck`, `pnpm build`; `MSYS_NO_PATHCONV=1 SEM_PULL=1 bash infra/construir-imagens.sh std-check <new app>` exit 0; `docker compose -f infra/compose/docker-compose.prod.yml --env-file infra/compose/env.prod.exemplo config -q` exit 0.
    Commit `refactor(01-29): rename gestao app (dir, package, image, compose, traefik)`.
  </action>
  <verify>
    <automated>docker compose -f infra/compose/docker-compose.prod.yml --env-file infra/compose/env.prod.exemplo config -q && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `test -d apps/gestao-vite` fails (dir renamed) when the row is not KEEP
    - `rg -n "gestao-vite" apps infra package.json docker-compose.yml` returns nothing
    - image build command exit 0 (recorded in SUMMARY)
    - compose config validation exits 0
  </acceptance_criteria>
  <done>Gestão app renamed and deployable.</done>
</task>

<task type="auto">
  <name>Task 2: Desk app rename if mapped; slice 4b gate</name>
  <files>apps/desk-vite/**, package.json, pnpm-lock.yaml, infra/construir-imagens.sh, infra/compose/docker-compose.prod.yml, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-4b-*</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/infra.csv (desk-vite app row, if any)
    - apps/desk-vite/Dockerfile
  </read_first>
  <action>
    If infra.csv has an approved non-KEEP app row for `desk-vite`, repeat Task 1 steps for it (separate commit), building with `MSYS_NO_PATHCONV=1 SEM_PULL=1 bash infra/construir-imagens.sh std-check <new desk app>` which passes `VITE_BASE=/desk/`; verify the built `index.html` references `/desk/assets/` (not a Windows path). Otherwise record "desk-vite: KEEP per map" in SUMMARY. Then `bash tools/std/gate.sh slice-4b` all PASS. Commit `chore(01-29): gate slice-4b green`.
  </action>
  <verify>
    <automated>bash tools/std/gate.sh slice-4b</automated>
  </verify>
  <acceptance_criteria>
    - `bash tools/std/gate.sh slice-4b` exits 0
    - SUMMARY states desk app outcome (renamed with build evidence, or KEEP)
  </acceptance_criteria>
  <done>Slice 4b green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Traefik → containers | routers select services by label names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-29-01 | Denial of service | Traefik router/service label mismatch after rename | mitigate | compose config validation; labels renamed together; smoke at cutover |
| T-01-29-02 | Tampering | MSYS path conversion baking a Windows path into the Desk build | mitigate | MSYS_NO_PATHCONV=1 and index.html asset path check |
</threat_model>

<verification>
Images build; compose config valid; gate green.
</verification>

<success_criteria>
High-risk app renames done one at a time with deploy artifacts validated.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-29-SUMMARY.md` when done
</output>
