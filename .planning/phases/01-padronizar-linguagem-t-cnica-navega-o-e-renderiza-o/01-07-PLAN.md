---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 07
type: execute
wave: 4
depends_on: ["01-03", "01-06"]
files_modified:
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/nav-contract.md
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/codex2-nav.json
autonomous: true
requirements: [STD-04, STD-05, STD-12]

must_haves:
  truths:
    - "Every front screen and URL/state item of desk-vite, gestao-vite and crm is classified path / query / react-state / router-state / storage / NEEDS VALIDATION, with Blip evidence or NEEDS VALIDATION"
    - "The Desk selectedConversationId canonical form is documented: React state, history marker without id, F5 returns to list (D-27, D-32)"
    - "The compatibility strategy is a coordinated v1 cut with no /v2, justified by the consumer list (STD-05)"
    - "Rendering: Desk and Gestão stay client-side SPA; CRM keeps Server Components (D-33)"
  artifacts:
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/nav-contract.md"
      provides: "STD-12 contract + STD-04 per-screen table + STD-05 compat section"
      contains: "selectedConversationId"
  key_links:
    - from: "std/nav-contract.md"
      to: "std/reports/baseline-route-consumers.csv"
      via: "consumer list cited in compat section"
      pattern: "baseline-route-consumers"
---

<objective>
Produce the navigation/rendering contract (STD-12), the per-screen URL-vs-state classification (STD-04) and the compatibility strategy (STD-05).

Purpose: Route translation and "where state lives" are separate problems (D-26). The map (plans 01-10/01-11) needs these decisions to know which routes/params are renamed, removed (REMOVE) or leave the URL (STATE). Owner approves this document at gate 2 (plan 01-12).
Output: `std/nav-contract.md`.

Model routing: Codex account 2 proposes the classification (read-only via `tools/std/codex-run.sh 2`); Claude (Sonnet) verifies evidence and writes the contract (URL-vs-state is a semantic decision; never Haiku, D-25).
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-CONTEXT.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-RESEARCH.md
@docs/specs/2026-09-07-arquitetura-de-front.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Facts:
- Desk routes (`apps/desk-vite/src/App.tsx:31-45`): `/entrar`, `/convite/:token`, `/`, `/chat`, `/chat/:id`, `/contacts`, `/contacts/:id`, `/analytics`, `/activeMessage/send`, `/bulk-ticket`, `/preferences`, `*`. `BrowserRouter` with `basename={import.meta.env.BASE_URL}` (`/desk` on the VPS). React Router installed is 7.9.3 (declarative; no data-router APIs).
- `useSearchParams` users: gestao-vite — `componentes/filtros-rapidos.tsx`, `paginas/cadastros/atendentes-edicao.tsx`, `paginas/cadastros/atendentes-permissoes.tsx`, `paginas/contrato/membros/page.tsx`, `paginas/contrato/page.tsx`, `paginas/criar/fluxo/page.tsx`, `paginas/criar/roteador/page.tsx`, `paginas/entrar.tsx`, `paginas/fluxo/analise/dashboard/dashboard.tsx`, `paginas/fluxo/analise/dicionario-de-dados/dicionario.tsx`, `paginas/fluxo/analise/jornada/pagina.tsx`, `paginas/fluxo/analise/mensagens-ativas/pagina.tsx`, `paginas/fluxo/analise/visao-geral/pagina.tsx`, `paginas/fluxo/contatos/detalhe/detalhe.tsx`, `paginas/fluxo/log/log.tsx`, `paginas/minha-conta/page.tsx`, `paginas/novidades/page.tsx`, `paginas/operacao/historico.tsx`, `paginas/operacao/monitoramento.tsx`, `paginas/operacao/monitoria.tsx`, `paginas/operacao/relatorios-atendimento.tsx`, `paginas/operacao/relatorios-esforco.tsx`, `paginas/operacao/relatorios-satisfacao.tsx`, `paginas/trocar-conta/sem-acesso/page.tsx`; desk-vite — `paginas/contatos/page.tsx`, `paginas/entrar.tsx`, `paginas/mensagem-ativa/page.tsx`.
- Research Pattern 8 pitfalls: do NOT store the conversation id in `navigate(..., { state })` (history.state survives F5); `apps/gestao-vite/src/lib/desk-url.ts` has no consumer in src (only its test).
- Blip evidence locations: CONTEXT.md `<canonical_refs>` "Evidência Blip" list (file + line numbers).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate and classify every screen/state item (Codex 2 proposal, Sonnet verification)</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/codex2-nav.json</files>
  <read_first>
    - apps/desk-vite/src/App.tsx
    - apps/desk-vite/src/paginas/atendimentos/page.tsx
    - apps/desk-vite/src/paginas/atendimentos/painel.tsx (line ~160 contact link)
    - apps/desk-vite/src/paginas/contatos/page.tsx (lines 30-50)
    - apps/gestao-vite/src/App.tsx
    - apps/gestao-vite/src/paginas/fluxo/contatos/detalhe/detalhe.tsx (lines 20-35)
    - apps/crm/src/middleware.ts
    - referencias-blip/portal/INDICE.md
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/TEMPLATE-nav.md
  </read_first>
  <action>
    Build the prompt: TEMPLATE-nav.md + the list of apps/routes/useSearchParams files above + the CONTEXT canonical_refs Blip evidence list; write it to `STD/prompts/nav/nav-001.md`. Run `bash tools/std/codex-run.sh 2 tools/std/schemas/nav-classification.schema.json STD/prompts/nav/nav-001.md STD/out/codex2-nav.json`. If the output misses screens, rerun with a per-app prompt (desk, gestao, crm) and concatenate.
    Sonnet verification: for every screen row, open the cited `referencias-blip/...` file at the cited line and confirm the evidence says what the row claims; any row whose evidence does not hold becomes `NEEDS VALIDATION`. Rows for decisions already locked must match them exactly: D-27 (Desk conversation -> react-state, deep_link no, survives_f5 no), D-29 (Desk contact+ticket selection -> react-state, URL only `/contacts`), D-30 (filters on monitoring queue/agent, log, analysis period, novidades search -> react-state + last filter in storage), D-32 (Desk back/forward -> NEEDS VALIDATION annotation), D-28 (Gestão never opens a Desk conversation by id). Record the edits in the JSON (`notes: "sonnet: ..."`).
  </action>
  <verify>
    <automated>node -e "const j=JSON.parse(require('fs').readFileSync('.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/codex2-nav.json','utf8'));const apps=new Set(j.screens.map(s=>s.app));if(j.screens.length<60||!['desk-vite','gestao-vite','crm'].every(a=>apps.has(a)))process.exit(1);console.log(j.screens.length)"</automated>
  </verify>
  <acceptance_criteria>
    - codex2-nav.json has >= 60 screen rows covering apps desk-vite, gestao-vite, crm
    - every one of the 27 `useSearchParams` files listed in context appears in at least one row (`notes` or `route`)
    - `STD/reports/codex-log-2-codex2-nav.csv` has a line with this output file and exit 0 (per-app reruns pass job `codex2-nav` as the wrapper's 5th argument)
  </acceptance_criteria>
  <done>Classification proposal exists and was evidence-checked.</done>
</task>

<task type="auto">
  <name>Task 2: Write nav-contract.md (STD-12 rules, STD-04 table, STD-05 compat)</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/nav-contract.md</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/codex2-nav.json
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-route-consumers.csv
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-RESEARCH.md (Pattern 8, Runtime State Inventory "Live service config")
    - apps/desk-vite/src/componentes/casca.tsx and apps/desk-vite/src/componentes/trilho.tsx
    - apps/desk-vite/src/lib/consulta.ts (useLeitura null-key behavior)
  </read_first>
  <action>
    Write `STD/nav-contract.md` with `Status: PROPOSED (owner approval at gate 2, plan 01-12)` and sections:
    1. `## Rules` (STD-12): when to use path param (navigable, deep-linkable resource), query param (shareable search/filter/pagination only when the screen is classified shareable), React state (ephemeral UI selection), router state (`location.state`: only markers without ids, never data that must not survive F5), store/context (cross-component ephemeral selection in one app shell), localStorage (remembered preferences/last filter, key includes tenant+user), deep link, refresh persistence, back/forward, client vs server rendering (D-33: Desk/Gestão SPA; CRM Server Components kept until CRM-01; stable Blip URL does not imply SSR).
    2. `## Desk` — selectedConversationId (D-27): React state in a Casca-level context so the rail can reset it; opening pushes `navigate('/chat', { state: { panel: 'conversation' } })` (marker only, no id); on every `location.key` change, if `location.state?.panel !== 'conversation'` clear selection; F5 -> state empty -> list; not-owned guard -> clear selection instead of `navigate('/', {replace:true})`; `/chat/:id` route removed (REMOVE, D-27); `useLeitura(id ? path : null)` keeps working with the key from state. Contacts (D-29): route `/contacts` only; contact and ticket selection in React state; marker `{ panel: 'contact' }` for back; the conversation drawer link (`painel.tsx` `href=/contacts/${contatoId}?ticket=`) becomes in-app navigation that sets a pending selection in the same Casca-level context, then `navigate('/contacts', { state: { panel: 'contact' } })` — ids are NOT put in location.state (decision: transient context, so F5 on contacts returns to the list, consistent with D-27); `/contacts/:id` removed and `?ticket=` becomes STATE. Back/forward (D-32): as above, marked NEEDS VALIDATION against live Blip.
    3. `## Gestão` — D-28 (remove `src/lib/desk-url.ts` + `tests/desk-url.test.ts`; Gestão shows conversations only in its own preview); D-14 (remove `ROTAS_ANTIGAS_SEM_CONTATO` and `ParaOCanalDoBot`); D-29 Gestão contact detail `contatos/:contatoId?ticketId=` classification with Blip Portal evidence (`/users/:id?ticketId`, portal.js :247682-247683) -> recommendation keep path + query (deep-linkable), owner decides at gate 2; D-30 list of screens whose filters move to React state + last filter in localStorage with key `pipe:<app>:<screen>:filters:v1:<tenantId>:<userId>` (validator drops invalid/stale values, versioned key); D-31 wizard (create flow/router `?passo=`, certificates `useState`) with Blip evidence `/application/create/{router|name|marketplace}` -> recommendation step in path, owner decides at gate 2; D-34 per-screen deep-link/F5 decision.
    4. `## CRM` — routes are App Router folders; rename = URL change; middleware/estrutura-crm public-route regexes stay duplicated but must change together; Server Components untouched (STD-09, D-33).
    5. `## Per-screen table` (STD-04): `app | screen | current route | state item | current location | decided location | deep link | survives F5 | back/forward | Blip evidence (file:line) | decision_ref | status (DECIDED / OWNER DECIDES AT GATE 2 / NEEDS VALIDATION)` — one row per item from codex2-nav.json after Sonnet verification.
    6. `## Compatibility strategy (STD-05)` — consumers of every API path are Pipe-controlled (cite `reports/baseline-route-consumers.csv` counts per app: desk, gestão, crm, ponte, packages, tests); decision: coordinated cut of v1, no `/v2`, no PT->EN redirects (D-14); external registrations handled explicitly: Google OAuth redirect -> `/v1/auth/google/callback` re-registered by owner + `GOOGLE_URL_RETORNO` value change in `.env`, `.env.example`, `infra/compose/env.prod.exemplo`, VPS env (D-12, D-36); SSO `/v1/auth/sso/callback` (D-13); Meta webhooks unchanged (D-15); tracked link `/l/:codigo` unchanged; signed attachment URLs (15 min) break at cutover (accepted); pending invites resent (D-14); bookmarks break (D-14); session cookie rename logs everyone out (D-38); queues drained at cutover (D-10).
    Commit: `docs(01-07): navigation/rendering contract draft (STD-04/05/12)`.
  </action>
  <verify>
    <automated>grep -c "selectedConversationId\|panel: 'conversation'\|coordinated cut\|NEEDS VALIDATION\|pipe:<app>:<screen>:filters:v1" .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/nav-contract.md</automated>
  </verify>
  <acceptance_criteria>
    - nav-contract.md contains headings `## Rules`, `## Desk`, `## Gestão`, `## CRM`, `## Per-screen table`, `## Compatibility strategy (STD-05)`
    - contains the exact strings `panel: 'conversation'`, `pipe:<app>:<screen>:filters:v1:<tenantId>:<userId>`, `/v1/auth/google/callback`, `no /v2`
    - per-screen table row count (lines starting with `|` in that section, minus header) >= number of rows in codex2-nav.json
    - no table row has an empty `status` cell
    - document states conversation id is never stored in `location.state`
  </acceptance_criteria>
  <done>Contract draft ready for gate 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser history/storage → app | history.state and localStorage persist across reloads and accounts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07-01 | Information disclosure | last-filter localStorage across tenant switch | mitigate | key includes tenantId + userId; validator drops stale ids |
| T-01-07-02 | Spoofing | post-login `destino` redirect | mitigate | contract states `destinoAbsoluto` internal-path check stays unchanged; only identifiers rename |
| T-01-07-03 | Repudiation | Blip evidence misquoted by Codex | mitigate | Sonnet opens each cited file:line; unverifiable rows become NEEDS VALIDATION |
</threat_model>

<verification>
nav-contract.md exists with all sections and per-screen statuses; codex call logged.
</verification>

<success_criteria>
STD-04 table, STD-05 strategy and STD-12 rules drafted with evidence, ready for owner approval at gate 2.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-07-SUMMARY.md` when done
</output>
