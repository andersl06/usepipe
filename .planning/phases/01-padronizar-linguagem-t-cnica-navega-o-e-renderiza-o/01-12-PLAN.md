---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 12
type: execute
wave: 7
depends_on: ["01-10", "01-11", "01-36"]
files_modified:
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/GATE2-REVIEW.md
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/persisted.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/exceptions.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/nav-contract.md
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/wire-contracts.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-0-*
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/gate-order.txt
autonomous: false
requirements: [STD-02, STD-04, STD-05, STD-06, STD-10, STD-12]

must_haves:
  truths:
    - "Before approval, every approved-to-be row is proven applicable by dry-runs (declarations found, no move conflicts)"
    - "The owner approved the old->new map per app, the CSS map, persisted.csv decisions, wire-contracts decisions and nav-contract.md (D-03 gate 2, D-35)"
    - "All map rows are status=approved and check-map passes with --require-status approved"
    - "After the owner's edits, the dry-runs are re-run on the approved rows and still report missing=0 / 0 conflicts (W13)"
    - "A slice-0 gate run with the approved-map lexicon re-baselines the PT scan, so later slice gates compare Unclassified counts with the same lexicon"
    - "The breaking-changes list covers every externally visible rename, including apps/site public URLs and doc paths (W10)"
    - "Tag std-slice-0-end marks the end of slice 0 (tools + inventory + gates)"
  artifacts:
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/GATE2-REVIEW.md"
      provides: "owner review packet"
      contains: "Breaking changes"
  key_links:
    - from: "std/map/*.csv status=approved"
      to: "rename-symbols / move-files / rewrite-literals"
      via: "tools only consume approved rows"
      pattern: "approved"
---

<objective>
Prepare and run owner gate 2 (D-03): prove the map is applicable, present a review packet, record approval, and close slice 0.

Purpose: Haiku-eligible mechanical renaming may only start after this gate (D-25, STD-07).
Output: `std/GATE2-REVIEW.md`, all map rows approved, nav-contract/persisted/wire-contracts APPROVED, git tag `std-slice-0-end`.

Model routing: Claude (Sonnet) runs dry-runs, reconciles persisted candidates and writes the packet. Owner approves.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-10-SUMMARY.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-11-SUMMARY.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-36-SUMMARY.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile persisted candidates, run dry-runs, write GATE2-REVIEW.md</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/GATE2-REVIEW.md, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/persisted.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/exceptions.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/persisted.csv
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/nav-contract.md
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/wire-contracts.csv
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/inventory-summary.md
  </read_first>
  <action>
    1. If `STD/persisted-candidates-*.csv` exist: Sonnet reviews each row; confirmed ones append to `STD/persisted.csv` with a decision and get an exceptions.csv B row; rejected ones go back to their map CSV at status=proposed with a proposed name. Delete the candidates files after reconciliation.
    2. Validate: `node tools/std/check-map.ts --map STD/map --scopes all --require-status proposed --glossary STD/GLOSSARY.md` -> 0 errors (this is the first run with every scope selected, so rule 10 wire consistency between packages-db wire-key rows and contracts/api/front prop rows is fully evaluated here; fix mismatches by aligning the contracts/front row to the Drizzle name, W4).
    3. Dry-runs with `--status proposed --dry-run` (the tools refuse `--status proposed` without `--dry-run`; `<map>` below is `STD/map`): `node tools/std/rename-symbols.ts --map <map> --scopes all --kinds symbol,ts-prop,ts-local,wire-key --dry-run` (missing must be 0), `node tools/std/move-files.ts --map <map> --scopes all --dry-run` (0 conflicts: no two sources to one target, no target already tracked), `node tools/std/rewrite-literals.ts --map <map> --scopes all --kinds endpoint,front-route,query-param,queue,job-name,ws-event,error-code,test-title,cookie,storage-key,metric,script,subpath-export,package,literal-value --dry-run` (record unmatched counts). Fix map rows until missing=0 and conflicts=0; unmatched literal rows must be explained in notes (e.g. only referenced in docs) or fixed.
    4. Write `STD/GATE2-REVIEW.md`: per scope table of counts by kind (proposed rows, KEEP, REMOVE, STATE); `## Breaking changes` listing every endpoint, front-route, cookie, metric, queue, job-name, ws-event, error-code, package, app, script row (old -> new) grouped by kind, plus (W10) every `site` scope file/dir row that changes a public URL of `apps/site` (served HTML pages, assets and any path referenced from outside, e.g. links in e-mails or the Meta/Google app pages) and every doc-path row (file/dir rows under `docs/**` and root/app README-level docs in infra.csv) with the count of inbound references from `STD/reports/move-leftovers-*`/`git grep -n -F <old path>`; `## Removals` (all REMOVE rows with decision_ref); `## Owner decisions still open` (nav-contract rows marked OWNER DECIDES AT GATE 2: D-29 Gestão contact detail, D-31 wizard, D-34 deep-link table; any AMBIGUOUS left); `## Persisted (STD-06)` summary of persisted.csv by decision; `## Wire contracts (D-09)` counts by decision; `## CSS (D-35)` counts and the 30 most-used class renames; `## How to review` (open `STD/map/<scope>.csv` in a spreadsheet; edit `new` or add a note; unchanged rows are accepted).
    Commit: `docs(01-12): gate 2 review packet`.
  </action>
  <verify>
    <automated>node tools/std/check-map.ts --map .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map --scopes all --require-status proposed --glossary .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/GLOSSARY.md && grep -c "## Breaking changes" .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/GATE2-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - check-map exits 0 for all scopes at status proposed
    - rename-symbols dry-run reports `missing=0`; move-files dry-run reports 0 conflicts (numbers copied into GATE2-REVIEW.md)
    - no `STD/persisted-candidates-*.csv` file remains
    - GATE2-REVIEW.md `## Breaking changes` has subsections for site public URLs and doc paths (each may say "none" only when site.csv / infra.csv have no such file/dir rows)
    - GATE2-REVIEW.md contains sections `## Breaking changes`, `## Removals`, `## Owner decisions still open`, `## Persisted (STD-06)`, `## Wire contracts (D-09)`, `## CSS (D-35)`, `## How to review`
  </acceptance_criteria>
  <done>Map proven applicable; review packet ready.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: OWNER GATE 2 — approve map, CSS map, persisted, wire contracts, nav contract (D-03, D-35)</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/GATE2-REVIEW.md</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/GATE2-REVIEW.md
  </read_first>
  <action>Present GATE2-REVIEW.md and the files it links. Wait for the owner's approval or edits.</action>
  <what-built>Complete old->new map per scope (18 CSVs incl. css.csv), persisted inventory with decisions, wire-contract classification, and the navigation/rendering contract, all mechanically validated and dry-run-proven.</what-built>
  <how-to-verify>
    1. Read `STD/GATE2-REVIEW.md` sections `Breaking changes`, `Removals`, `Owner decisions still open`.
    2. Decide each open item (Gestão contact detail URL, wizard step in path or state, deep-link table rows).
    3. Open the per-app CSVs you care about in a spreadsheet (`STD/map/<scope>.csv`); change `new` where you disagree.
    4. Check `STD/persisted.csv` decisions and `STD/nav-contract.md`.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/GATE2-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - owner response recorded in the SUMMARY (approved, or list of edits)
  </acceptance_criteria>
  <resume-signal>Type "approved" or list the rows/decisions to change</resume-signal>
  <done>Owner has decided.</done>
</task>

<task type="auto">
  <name>Task 3: Record approval, flip rows to approved, tag std-slice-0-end</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/nav-contract.md, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/persisted.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/wire-contracts.csv</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/GATE2-REVIEW.md
    - owner edits from the checkpoint response
  </read_first>
  <action>
    Apply every owner edit to the CSVs/nav-contract (owner=sonnet, notes `owner-gate2`), update nav-contract rows from OWNER DECIDES AT GATE 2 to DECIDED with the owner's choice, set `Status: APPROVED <date>` in nav-contract.md; persisted.csv and wire-contracts.csv stay pure CSV, so record their approval in GATE2-REVIEW.md `## Approval` section (date + owner response). Re-run check-map (0 errors), then `node tools/std/check-map.ts --map STD/map --scopes all --approve`, then `node tools/std/check-map.ts --map STD/map --scopes all --require-status approved --glossary STD/GLOSSARY.md` (0 errors). W13 — owner edits change rows after the Task 1 dry-runs, so re-run all three dry-runs on the approved rows (default `--status approved`): `rename-symbols.ts --scopes all --kinds symbol,ts-prop,ts-local,wire-key --dry-run` (missing=0), `move-files.ts --scopes all --dry-run` (0 conflicts), `rewrite-literals.ts --scopes all --kinds endpoint,front-route,query-param,queue,job-name,ws-event,error-code,test-title,cookie,storage-key,metric,script,subpath-export,package,literal-value --dry-run` (unmatched rows explained in notes); fix and re-approve until clean; copy the numbers into GATE2-REVIEW.md `## Approval`. Commit `docs(01-12): gate 2 approved (rename map, CSS map, persisted, nav contract)`. Then re-baseline the gates with the approved-map lexicon (the PT scan lexicon changes when `new` values are filled, so the pre-gate-2 scan counts are not comparable): run `bash tools/std/gate.sh slice-0` — all steps PASS, step 9 reports `PASS (re-baselined: lexicon <hash>)`; commit `docs(01-12): slice-0 gate with approved lexicon` and run `git tag std-slice-0-end`.
  </action>
  <verify>
    <automated>node tools/std/check-map.ts --map .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map --scopes all --require-status approved --glossary .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/GLOSSARY.md && test "$(grep -c FAIL .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-0-gate.md)" = "0" && git rev-parse -q --verify refs/tags/std-slice-0-end</automated>
  </verify>
  <acceptance_criteria>
    - check-map with `--require-status approved` exits 0
    - `grep -c "Status: APPROVED" STD/nav-contract.md` = 1 and nav-contract has no `OWNER DECIDES AT GATE 2` left
    - GATE2-REVIEW.md has an `## Approval` section with a date and the post-edit dry-run numbers (missing=0, conflicts=0)
    - `STD/reports/slice-0-gate.md` has no FAIL; `STD/reports/slice-0-scan-summary.md` has a `Lexicon:` line different from `none`; `gate-order.txt` ends with `slice-0`
    - `git rev-parse -q --verify refs/tags/std-slice-0-end` succeeds and points at HEAD
  </acceptance_criteria>
  <done>Gate 2 closed; slices may start.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner approval → mechanical execution | after this gate, lighter models apply rows without judgment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12-01 | Repudiation | approval record | mitigate | owner response recorded in SUMMARY and GATE2-REVIEW.md `## Approval`; tag marks the approved state |
| T-01-12-02 | Tampering | proposed rows applied without approval | mitigate | tools consume only status=approved; `--approve` refuses when check-map has errors |
</threat_model>

<verification>
All rows approved, check-map green, tag present.
</verification>

<success_criteria>
Gate 2 approved; STD-02 map approved before any rename.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-12-SUMMARY.md` when done
</output>
