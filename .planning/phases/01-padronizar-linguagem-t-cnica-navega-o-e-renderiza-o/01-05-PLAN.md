---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04", "01-34"]
files_modified:
  - tools/std/inventory.ts
  - tools/std/inventory.test.ts
  - tools/std/lib/jsonb-reach.ts
  - tools/std/jsonb-reach.test.ts
  - tools/std/test-counts.ts
  - tools/std/gate.sh
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-gate.md
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-test-counts.json
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-js-specifiers.txt
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/gate-order.txt
autonomous: true
requirements: [STD-02, STD-03, STD-06, STD-07, STD-11]

must_haves:
  truths:
    - "Inventory extraction is deterministic: every PT candidate (paths, declarations, properties, routes, endpoints, queues, codes, test titles, scripts, cookies, storage keys, metrics, CSS, data-* attributes, string-literal union members, shell vars, comments) becomes a CSV row"
    - "Every TS type that reaches a jsonb column (directly via $type<>, via insert/update/set values, via casts/annotations on selected jsonb values, or via the known sinks) is traced transitively, and every property / literal member of those types is inventoried with persisted=unknown and notes jsonb:<table>.<column> (STD-06, D-09)"
    - "Every front route has a per-dependent file:line list (navigate/Link/redirect/href/path-builder/tests/docs/regex), not just a count (STD-03)"
    - "One command (tools/std/gate.sh <label>) runs the full D-20 slice gate and fails on any regression, including a jsonb-key disappearance"
    - "Gate comparisons (routes, test counts, flaky-test paths) translate names only through rows already applied, and PT-scan counts are compared only between scans with the same lexicon"
    - "Baseline gate passes and stores per-package test counts for later comparison"
  artifacts:
    - path: "tools/std/inventory.ts"
      provides: "candidate map rows per scope + comment rows per scope + front-route dependents"
    - path: "tools/std/lib/jsonb-reach.ts"
      provides: "transitive jsonb type reach"
      exports: ["traceJsonbReach"]
    - path: "tools/std/gate.sh"
      provides: "slice gate"
    - path: "tools/std/test-counts.ts"
      provides: "turbo test log parser + compare"
      exports: ["parseTestCounts", "compareCounts"]
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-test-counts.json"
      provides: "per-package counts"
  key_links:
    - from: "tools/std/gate.sh"
      to: "route-match.ts, scan-pt.ts, ddl-snapshot.sh, test-counts.ts, jsonb-keys.ts"
      via: "sequential steps with PASS/FAIL"
      pattern: "jsonb-keys.ts"
    - from: "tools/std/inventory.ts"
      to: "tools/std/lib/jsonb-reach.ts"
      via: "persisted=unknown + notes jsonb: for reached props"
      pattern: "traceJsonbReach"
    - from: "tools/std/inventory.ts"
      to: "tools/std/pt-detect.ts"
      via: "candidate filter"
      pattern: "from './pt-detect.ts'"
---

<objective>
Build the deterministic inventory extractor (including jsonb type reach and per-route dependents) and the slice gate, then run the gate on the baseline.

Purpose: STD-02/STD-10 completeness is guaranteed mechanically (Research Pattern 1: deterministic inventory first, LLM second). STD-06: property names of TS shapes serialized into jsonb ARE persisted keys (stored flows, in-flight executions, outbox replay, audit, CRM data); they must be found by type tracing, not by the `jsonb(` call site alone, or rename-symbols would rename them in slice 1 and neither the DDL snapshot nor fresh-seed tests would notice. D-20 requires a uniform after-slice gate.
Output: `tools/std/inventory.ts`, `tools/std/lib/jsonb-reach.ts`, `tools/std/gate.sh`, `tools/std/test-counts.ts`, baseline gate report and counts.

Model routing: Claude (Sonnet). No Codex.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-RESEARCH.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-04-SUMMARY.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-34-SUMMARY.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Tool conventions as in plan 01-02. Map CSV contract as in plan 01-04 (`tools/std/lib/map.ts`: `MAP_COLUMNS`, `readMap`, `writeMap`; ts-morph project from `tools/std/lib/project.ts` `loadWorkspaceProject`).

Scopes (one CSV each; file path decides scope): `packages-core`, `packages-db`, `packages-contracts`, `packages-ui`, `packages-ai`, `packages-autenticacao`, `packages-armazenamento`, `packages-tempo-real`, `packages-mcp`, `workers`, `api`, `ponte`, `desk-vite`, `gestao-vite`, `crm`, `site`, `infra` (root files, `infra/**`, Dockerfiles, compose, `*.sh`, root `package.json` scripts, `docs/**` paths, package/app names), `css` (all `.css` files, CSS in `apps/site/**/*.html`, and every `data-attr` row wherever it appears; D-35).

Slice numbers for the `slice` column (D-19): packages-* = 1; workers, api, ponte = 2; desk-vite, gestao-vite, crm, css = 3; infra, site, and `package`/`app` kinds = 4; test-title rows keep their workspace slice; docs paths = 5.

jsonb facts (packages/db/src/schema, ~87 `jsonb(` columns): e.g. `bloco.conteudo`/`posicao` and `transicao.condicao` (automacao.ts:260-303), `execucaoFluxo.contexto` (automacao.ts:336), `posicaoNoRoteador.contexto` (:211), `execucaoPasso.entrada/saida` (:365), `processHttpExecucao.entrada/contexto/pedido/resposta` (:395-399), `entregaWebhook.payload` (:700), the outbox table `outboxMensagem` (conversas.ts:405) replayed by the D-10 sweep, `logAuditoria.antes/depois` (identidade.ts:513), CRM `conta.atributos`, `lead.utm/customizados` (crm.ts), `contato.atributos`, `templateMensagem.variaveis` (conversas.ts). Only one column uses `$type<>` today (automacao.ts:130), so most shapes arrive through values: the flow engine types in `packages/core/src/fluxo/**` (`Contexto`, `MensagemDeEntrada`, `MensagemDeSaida`, `PedidoDeHttp`, `RespostaDeHttp`, `CursorDeProcessHttp`, `ListaDeAcoesSuspensa = 'entrada' | 'conteudo' | 'saida'`, `FONTES_DE_VARIAVEL`, block/entry types in `modelos.ts`/`acoes.ts`/`condicao.ts`/`editor.ts`).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic inventory extractor with per-route dependents</name>
  <files>tools/std/inventory.ts, tools/std/inventory.test.ts</files>
  <read_first>
    - tools/std/pt-detect.ts, tools/std/lib/map.ts, tools/std/scan-pt.ts (reuse its position walkers; import them, do not duplicate), tools/std/route-match.ts (reuse normalizePath)
    - apps/gestao-vite/src/App.tsx (route tree, rotasDoContato mounted twice)
    - apps/api/src/filas.ts and apps/workers/src/filas.ts (queue constants, scheduler ids)
    - packages/db/src/schema/identidade.ts (TS key vs SQL name)
  </read_first>
  <behavior>
    - A fixture source `export class ControladorAnexos { buscarAnexo(idAnexo: string) {} }` yields rows kind=symbol old=ControladorAnexos, kind=symbol old=buscarAnexo, kind=ts-local old=idAnexo, each with declared_at `<file>:<line>`
    - English identifiers (`status`, `useState`) yield no rows
    - A pgTable column `contatoId: text('contato_id')` yields kind=ts-prop old=contatoId with notes containing `sql=contato_id` and no row for `contato_id`
    - Rows get stable ids `<scope>-<kind>-<sha1(old+declared_at) first 8>` so reruns do not reshuffle ids
    - A comment `// Busca a conversa quando existe` produces a comments row; `// eslint-disable-next-line` does not
    - `type Painel = 'aberto' | 'fechado'` yields two kind=literal-value rows with persisted=unknown; `<div data-painel="aberto">` yields kind=data-attr rows `data-painel` and `data-painel=aberto` in scope css
    - A route `/fluxo/:id/contatos` with consumers `navigate(\`/fluxo/${id}/contatos\`)` in one file and `'/fluxo/x/contatos'` in a test yields two front-route-dependents rows (file:line, kind navigate / test) for that route row id
  </behavior>
  <action>
    Create `tools/std/inventory.ts`, CLI `node tools/std/inventory.ts --out STD [--dry-run]`, exporting `extract(root): {rows: MapRow[], comments: CommentRow[], routeDependents: RouteDependent[]}`.
    Candidates (only when at least one token is PT via `isPtToken`): `file`/`dir` (tracked path segments; one row per unique dir path and per file basename), `symbol` (functions, classes, interfaces, type aliases, enums, enum members, exported/module-level variables, methods, class properties), `ts-local` (parameters, locals, destructured bindings), `ts-prop` (interface/type members, object-literal keys in exported objects, Drizzle table keys with `notes` = `sql=<column>`), `literal-value` (string-literal union members, `as const` string members, enum-like const object string values — W6), `data-attr` (JSX/HTML `data-*` attribute names as `data-x` and literal values as `data-x=value`, `dataset.x`, attribute API args, CSS `[data-x=...]` selectors; scope always `css`, `consumers` = occurrence count in TSX/TS/HTML/CSS — W6), `endpoint` (joined Controller+method paths, full path with param names), `front-route` (Route path values in desk-vite/gestao-vite App.tsx including nested/relative routes resolved to full pattern and the duplicated `rotasDoContato` under `/fluxo/:id` and `/roteador/:id`; CRM `apps/crm/src/app/**` folders containing `page.tsx`/`route.ts`/`layout.tsx`), `query-param`, `queue`, `job-name` (incl. the 7 scheduler ids), `ws-event`, `error-code`, `test-title` (English titles excluded by detector), `script` (package.json scripts keys, all workspaces + root), `cookie`, `storage-key`, `metric`, `subpath-export` (package.json exports keys), `package` (`@pipe/<pt>`), `app` (`apps/<pt>` dir names), `css-class`/`css-var` (scope `css`, `declared_at` = css file:line; `consumers` = count of whole-token occurrences in TSX/TS/HTML), `shell-var` (assignments `NAME=` and function names in tracked `*.sh`, excluding names present in the env exceptions of `STD/exceptions.csv`).
    Fill: `consumers` = count of identifier/literal occurrences repo-wide (approximate, document as such in the summary), `persisted` = `unknown` for kinds error-code, storage-key, cookie, ws-event, job-name, queue, wire-key, literal-value, and for every ts-prop / literal-value row flagged by `traceJsonbReach` (Task 2; `notes` += `jsonb:<table>.<column>[;...]`, plus `sql=`-less Drizzle keys of jsonb columns); `no` otherwise; `status` = candidate; `new`, `category`, `decision_ref`, `owner` empty. Until Task 2 lands, call it through an import that Task 2 fills (write Task 1 tests with a stub reach set).
    Front-route dependents (W5, STD-03): for every front-route row, find every dependent in tracked files of the owning app (`src/**`, `tests/**`), `apps/*/src/**` of other apps (cross-app links), `docs/**/*.md`, `*.md` at repo root and `apps/*/README.md`: `navigate(...)`/`redirect(...)`/`<Navigate to>`/`<Link to>`/`href=`/`window.location` assignments, template/string literals in path-builder functions (e.g. gestao `paginas/fluxo/contato.tsx`, `paginas/operacao/casca.tsx`, `paginas/fluxo/itens.ts`, `configuracoes/navegacao.tsx`, `growth/navegacao.tsx`), test literals, doc mentions, and regex literals/`new RegExp` strings (CRM `middleware.ts:15`, `estrutura-crm.tsx:69`, gestao `exigir-sessao.tsx:13`) — a literal matches a route when its `normalizePath` equals the route pattern or has it as a segment-boundary prefix. Write `STD/reports/front-route-dependents.csv` columns `route_row_id,app,route,file,line,kind,snippet` with kind in navigate|link|redirect|href|path-builder|test|doc|regex|other; fill the route row's `consumers` with the dependent count.
    Comments: `STD/comments/<scope>.csv` columns `id,scope,file,start_line,end_line,kind,pt_score,text,action,new_text,sensitivity,evidence,reviewed_by,status` (last six empty; kind in line|block|jsdoc|hash|css; only comments where `isPtComment` is true; skip lint/ts directives and license headers; sources: TS/JS files, `.css` files (scope css), and hash-comments in tracked `*.sh`, `Dockerfile*`, `*.dockerignore`, `*.yml`, `*.yaml`, `nginx.conf` (scope infra or site by path)).
    Also write `STD/inventory-summary.md`: counts per scope x kind, comment counts per scope, total rows, route-dependent counts per app.
    Tests in `tools/std/inventory.test.ts` for the behavior cases (in-memory source files).
    Do NOT run the real inventory in this plan (plan 01-08 runs it); `--dry-run` on the repo may be used to check it finishes (< 5 min) and prints totals.
  </action>
  <verify>
    <automated>node --test tools/std/inventory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tools/std/inventory.test.ts` exits 0 with every behavior case as its own test
    - `grep -c "front-route-dependents.csv" tools/std/inventory.ts` >= 1
    - `grep -c "literal-value\|data-attr" tools/std/inventory.ts` >= 2
  </acceptance_criteria>
  <done>Inventory extractor ready and tested, including dependents and the W6 kinds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: jsonb type reach (STD-06 blocker) wired into the inventory</name>
  <files>tools/std/lib/jsonb-reach.ts, tools/std/jsonb-reach.test.ts, tools/std/inventory.ts</files>
  <read_first>
    - tools/std/lib/project.ts (loadWorkspaceProject)
    - packages/db/src/schema/automacao.ts (lines 200-420 and 690-710), packages/db/src/schema/conversas.ts (lines 150-260 and 330-425), packages/db/src/schema/identidade.ts (lines 500-520), packages/db/src/schema/crm.ts (lines 25-90)
    - packages/core/src/fluxo/contexto.ts, packages/core/src/fluxo/modelos.ts
  </read_first>
  <behavior>
    - Fixture: `bloco = pgTable('bloco', { conteudo: jsonb('conteudo').$type<Bloco>() })`, `interface Bloco { acoes: Acao[] }`, `interface Acao { tipo: 'enviar' | 'esperar'; alvo: Alvo }`, `interface Alvo { canalId: string }` -> reach contains props `acoes`, `tipo`, `alvo`, `canalId` and literal members `enviar`, `esperar`, each with via `$type` and column `bloco.conteudo`
    - Fixture: `db.insert(execucao).values({ contexto: ctx })` with `ctx: Contexto` (no `$type`) -> every prop of `Contexto` (and of types nested in it: arrays, unions, Record values, generic args, Partial/Pick wrappers) is reached with via `insert-values`
    - Fixture: `db.update(execucao).set({ contexto: novo })` and `.onConflictDoUpdate({ set: { contexto: novo } })` -> reached with via `update-set`
    - Fixture: `const c = linha.contexto as Contexto` and `const c: Contexto = linha.contexto` where `linha` comes from `db.select().from(execucao)` -> reached with via `read-cast`
    - A type used only in memory (never flowing to a jsonb column) is NOT reached; `Record<string, string>` value maps yield no prop rows (their keys are data), but the Record-typed property itself is reached
    - Inventory rows for reached ts-prop / literal-value get persisted=unknown and notes `jsonb:<table>.<column>`
  </behavior>
  <action>
    Create `tools/std/lib/jsonb-reach.ts` exporting `traceJsonbReach(project): { reached: Map<declKey, {table, column, via, rootType}[]>, report: Row[] }` using the ts-morph type checker over the whole workspace project:
    1. Roots: every property assignment in a `pgTable(...)` column object whose initializer chain starts with `jsonb(` — record table export name, TS key and SQL column name.
    2. Types reaching each root: (a) the type argument of `.$type<T>()` on that column; (b) the type of the value expression for that key in object literals passed to `.values(...)` of `insert(<table>)`, `.set(...)` of `update(<table>)`, `onConflictDoUpdate({ set })`, and any object typed `typeof <table>.$inferInsert` / `InferInsertModel<typeof table>` (resolve the table from the call chain receiver); (c) reads: `as T`, `satisfies T`, variable/parameter/return annotations whose initializer or returned expression is a property access `<x>.<jsonb key>` where `<x>`'s type is the table's select row type (`$inferSelect` / result of `select().from(<table>)` / relational query), plus generic arguments of `JSON.parse<T>`-style helpers applied to those values; (d) known sinks listed in context (the exported types of `packages/core/src/fluxo/**`) are added as roots attributed to the columns that store flow content/context (`bloco.conteudo`, `bloco.posicao`, `transicao.condicao`, `execucaoFluxo.contexto`, `posicaoNoRoteador.contexto`, `execucaoPasso.entrada/saida`, `processHttpExecucao.*`) even when no call site is found; plus the payload types of the outbox writer (the function inserting into `outboxMensagem`), `entregaWebhook.payload` writer, audit writer (`logAuditoria.antes/depois`) and CRM `atributos`/`customizados`/`utm` writers.
    3. Transitive closure: from each reached type, walk property types, union/intersection members, array/tuple element types, `Record<K, V>` value types, `Partial/Required/Pick/Omit/Readonly` wrappers and generic type arguments; for `as const` arrays feeding a type (`(typeof X)[number]`) include the array's string members. Collect every property declaration (interface/type member, class property, object-literal key when the object type is anonymous) and every string-literal member. Stop at lib types (`Date`, `string`, DOM, node) and at `Record<string, …>` keys (keys are data; the value type is still walked).
    4. Write `STD/reports/jsonb-reach.csv` (`table,column,via,root_type,kind,name,declared_at`) when called from the inventory CLI (not in `--dry-run`).
    Wire into `inventory.ts`: every ts-prop and literal-value row whose declaration is in `reached` gets persisted=unknown and notes `jsonb:<table>.<column>` (all columns joined by `;`), even if its name is fully English (English names still need the persisted flag so they are never renamed later — create the row with `new`=`KEEP` candidate only when the name is English; plan 01-08 classifies). Tests in `tools/std/jsonb-reach.test.ts` for every behavior case using an in-memory ts-morph Project.
    Run `node tools/std/inventory.ts --out STD --dry-run`; it must print a `jsonb-reach` total > 0 and per-column counts, including non-zero counts for `bloco.conteudo`, `execucaoFluxo.contexto`, `logAuditoria.antes` (if a column shows 0, add the missing writer/reader pattern to step 2 and a test for it). Commit `feat(01-05): inventory extractor with jsonb type reach and route dependents`.
  </action>
  <verify>
    <automated>node --test tools/std/jsonb-reach.test.ts tools/std/inventory.test.ts && node tools/std/inventory.ts --out .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tools/std/jsonb-reach.test.ts` exits 0 with the 6 behavior cases as separate tests
    - `--dry-run` prints per-scope totals for all 18 scopes, a `jsonb-reach` total > 0 with non-zero counts for `bloco.conteudo`, `execucaoFluxo.contexto` and `logAuditoria.antes`, and writes no file (`git status --porcelain .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map` empty)
    - dry-run totals show kind `endpoint` count >= 150 and kind `front-route` count >= 80
    - dry-run shows the props of `Contexto` (packages/core/src/fluxo/contexto.ts) and the members `entrada`, `conteudo`, `saida` of `ListaDeAcoesSuspensa` as jsonb-reached
  </acceptance_criteria>
  <done>No jsonb-serialized property can enter the map as persisted=no without an explicit classification in plan 01-08.</done>
</task>

<task type="auto">
  <name>Task 3: Slice gate script, test-count parser, baseline gate run</name>
  <files>tools/std/gate.sh, tools/std/test-counts.ts, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-gate.md, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-test-counts.json</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/baseline.md
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-VALIDATION.md (Sampling Rate)
    - tools/std/route-match.ts, tools/std/scan-pt.ts, tools/std/ddl-snapshot.sh, tools/std/jsonb-keys.ts (CLI flags)
  </read_first>
  <action>
    `tools/std/test-counts.ts`: `parseTestCounts(log): Record<pkg,{passed,failed,skipped,files_failed:string[]}>` from turbo-prefixed lines (`@pipe/x:test: ...`): vitest `Tests  N passed (M)` / `N failed`, `FAIL  <file>`; node:test `# pass N`, `# fail N`, `# skipped N`; `compareCounts(current, baseline, renames, allowedRemovals)` where renames maps old package names to new from map rows kind package/app with status applied|verified ONLY (approved-but-unapplied package rows must not translate baseline keys, otherwise every gate before slice 4 fails) and `allowedRemovals` comes from `STD/reports/allowed-test-removals.csv` (`package,count,decision_ref`) — returns failures when current passed < baseline passed - allowed. CLI: `node tools/std/test-counts.ts --log <log> --out <json> [--compare <baseline.json> --map STD/map]`, exit 1 on shortfall. Add a unit check in the SUMMARY: an approved (unapplied) package row leaves baseline keys untranslated.
    `tools/std/gate.sh <label>` (bash, `set -uo pipefail`, runs from repo root, writes `STD/reports/<label>-gate.md` with one `PASS`/`FAIL` line per step and exits 1 if any FAIL). Steps in order: (1) `git clean -fdX -- 'apps/*/dist' 'packages/*/dist' 'apps/*/.next'` and remove `.turbo` cache dirs; (2) `pnpm install`; (3) `pnpm typecheck`; (4) `pnpm build`; (5) `pnpm turbo run test --continue` logged to the scratch dir; if it fails and every failing file is one of `tests/instagram.test.ts|tests/fluxo.test.ts|tests/messenger.test.ts` (or their renamed paths via map rows with status applied|verified only), rerun each isolated with `pnpm --filter <api package> exec vitest run <file>`; PASS only if isolated reruns pass; always run the 3 isolated as well; (6) `node tools/std/test-counts.ts --log <log> --out STD/reports/<label>-test-counts.json` plus `--compare STD/reports/baseline-test-counts.json --map STD/map` unless label is `baseline`; (7) `bash tools/std/ddl-snapshot.sh check`; (8) `node tools/std/route-match.ts --emit STD/reports/<label>-routes.json --consumers STD/reports/<label>-route-consumers.csv --allow STD/route-drift-allow.csv --check` plus `--compare STD/reports/baseline-routes.json --map STD/map` unless baseline (route-match translates only applied|verified rows, plan 01-03); (9) PT-scan trend with a frozen lexicon: if `STD/reports/gate2-lexicon.txt` exists (written by plan 01-12 at gate 2 approval) run `node tools/std/scan-pt.ts --out STD/reports/<label>-scan.csv --summary STD/reports/<label>-scan-summary.md --map STD/map --lexicon-file STD/reports/gate2-lexicon.txt`, otherwise the same command without `--lexicon-file` (only `baseline` runs before gate 2; a missing `STD/map` is tolerated and yields `Lexicon: none`); read its `Lexicon:` and `Unclassified:` lines; find the latest previous gate in `STD/reports/gate-order.txt` whose scan summary has the SAME `Lexicon:` value and FAIL when the current `Unclassified` is higher. The fingerprint depends only on the gate-2-approved rows frozen in `gate2-lexicon.txt`, so post-gate2-derived rows, jsonb-guard KEEP rows and rows moved to persisted.csv never change it and can never skip the "count must not rise" comparison. If no previous gate has the same fingerprint: write `PASS (re-baselined: lexicon <hash>)` only for labels `baseline` and `slice-0` (the single gate-2 re-baseline); for any other label write `FAIL (lexicon changed outside gate 2)` and the executor stops and reports. Residual PT that only derived rows can detect is caught by the per-slice closing scans with `--map STD/map` (plans 01-15, 01-37, 01-27) and by the final `--fail-on-unclassified` scan (plan 01-31); append label to `gate-order.txt`; (10) backend `.js` specifier check: count of `rg -n --pcre2 "from '\.{1,2}/[^']*(?<!\.js)'"` over `apps/api/src apps/api/tests apps/workers/src apps/workers/tests apps/ponte/src apps/ponte/tests` and every `packages/*/src` except `packages/ui` (use the current dir names resolved from the map when renamed; glob `packages/*/src` covers renamed packages) must be <= the baseline count stored in `STD/reports/baseline-js-specifiers.txt` (written when label is baseline); (11) `node tools/std/jsonb-keys.ts --check` (plan 01-34: every jsonb fixture key present in code at baseline is still present as a property name) — this also runs the api jsonb-compat test as part of step 5. Accept `--skip-tests` only for label `dry` (never in slice gates).
    Run `bash tools/std/gate.sh baseline`. It must PASS. Commit: `feat(01-05): slice gate; baseline gate green`.
  </action>
  <verify>
    <automated>bash tools/std/gate.sh baseline && grep -c "FAIL" .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-gate.md | grep -x 0</automated>
  </verify>
  <acceptance_criteria>
    - `bash tools/std/gate.sh baseline` exits 0
    - `STD/reports/baseline-gate.md` has 11 lines starting with `PASS`
    - `STD/reports/baseline-test-counts.json` has a key per workspace with a test script (13 packages as in baseline.md) and passed counts equal to baseline.md plus the jsonb-compat tests added by plan 01-34
    - `STD/reports/baseline-js-specifiers.txt` exists and contains an integer
    - `STD/reports/gate-order.txt` first line is `baseline`
    - `grep -c "applied|verified\|applied,verified" tools/std/test-counts.ts tools/std/gate.sh` total >= 2 (renames only through applied rows)
  </acceptance_criteria>
  <done>Gate runs green on baseline; every later slice uses `bash tools/std/gate.sh <label>`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate verdict → slice progression | a false PASS lets a regression into the next slice |
| code → jsonb data | TS property names are the stored keys of jsonb documents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Repudiation | gate.sh | mitigate | Per-step PASS/FAIL report committed per label; test counts compared to baseline so "0 tests ran" cannot pass (Pitfall 8) |
| T-01-05-02 | Tampering | flaky-test exemption hiding a real failure | mitigate | Exemption only for the 3 named files and only if their isolated rerun passes |
| T-01-05-03 | Tampering | stale dist masking breakage (Pitfall 4) | mitigate | gate deletes all dist/.next/.turbo before typecheck/build |
| T-01-05-04 | Tampering | jsonb keys renamed with their TS types (stored flows/executions/outbox/audit/CRM unreadable) | mitigate | traceJsonbReach marks every reached prop persisted=unknown; gate step 11 jsonb-keys check + api jsonb-compat test every slice |
| T-01-05-05 | Repudiation | scan count compared across different lexicons | mitigate | `Lexicon:` fingerprint frozen at gate 2 (`gate2-lexicon.txt`); only same-lexicon comparison; re-baseline allowed only for `baseline` and `slice-0`, any other lexicon change FAILs |
</threat_model>

<verification>
Inventory and jsonb-reach tests pass; `bash tools/std/gate.sh baseline` exits 0 with 11 PASS lines.
</verification>

<success_criteria>
Deterministic inventory (with jsonb reach and route dependents) and a single-command slice gate exist, and the baseline passes the gate.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-05-SUMMARY.md` when done
</output>
