---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04"]
files_modified:
  - tools/std/inventory.ts
  - tools/std/inventory.test.ts
  - tools/std/test-counts.ts
  - tools/std/gate.sh
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-gate.md
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-test-counts.json
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-js-specifiers.txt
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/gate-order.txt
autonomous: true
requirements: [STD-02, STD-03, STD-07, STD-11]

must_haves:
  truths:
    - "Inventory extraction is deterministic: every PT candidate (paths, declarations, properties, routes, endpoints, queues, codes, test titles, scripts, cookies, storage keys, metrics, CSS, shell vars, comments) becomes a CSV row"
    - "One command (tools/std/gate.sh <label>) runs the full D-20 slice gate and fails on any regression"
    - "Baseline gate passes and stores per-package test counts for later comparison"
  artifacts:
    - path: "tools/std/inventory.ts"
      provides: "candidate map rows per scope + comment rows per scope"
    - path: "tools/std/gate.sh"
      provides: "slice gate"
    - path: "tools/std/test-counts.ts"
      provides: "turbo test log parser + compare"
      exports: ["parseTestCounts", "compareCounts"]
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-test-counts.json"
      provides: "per-package counts"
  key_links:
    - from: "tools/std/gate.sh"
      to: "route-match.ts, scan-pt.ts, ddl-snapshot.sh, test-counts.ts"
      via: "sequential steps with PASS/FAIL"
      pattern: "route-match.ts"
    - from: "tools/std/inventory.ts"
      to: "tools/std/pt-detect.ts"
      via: "candidate filter"
      pattern: "from './pt-detect.ts'"
---

<objective>
Build the deterministic inventory extractor and the slice gate, then run the gate on the baseline.

Purpose: STD-02/STD-10 completeness is guaranteed mechanically (Research Pattern 1: deterministic inventory first, LLM second). D-20 requires a uniform after-slice gate; one script keeps every slice plan's verification identical and cheap in context.
Output: `tools/std/inventory.ts`, `tools/std/gate.sh`, `tools/std/test-counts.ts`, baseline gate report and counts.

Model routing: Claude (Sonnet). No Codex.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-RESEARCH.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-04-SUMMARY.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Tool conventions as in plan 01-02. Map CSV contract as in plan 01-04 (`tools/std/lib/map.ts`: `MAP_COLUMNS`, `readMap`, `writeMap`).

Scopes (one CSV each; file path decides scope): `packages-core`, `packages-db`, `packages-contracts`, `packages-ui`, `packages-ai`, `packages-autenticacao`, `packages-armazenamento`, `packages-tempo-real`, `packages-mcp`, `workers`, `api`, `ponte`, `desk-vite`, `gestao-vite`, `crm`, `site`, `infra` (root files, `infra/**`, Dockerfiles, compose, `*.sh`, root `package.json` scripts, `docs/**` paths, package/app names), `css` (all `.css` files and CSS in `apps/site/**/*.html`; D-35).

Slice numbers for the `slice` column (D-19): packages-* = 1; workers, api, ponte = 2; desk-vite, gestao-vite, crm, css = 3; infra, site, and `package`/`app` kinds = 4; test-title rows keep their workspace slice; docs paths = 5.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic inventory extractor</name>
  <files>tools/std/inventory.ts, tools/std/inventory.test.ts</files>
  <read_first>
    - tools/std/pt-detect.ts, tools/std/lib/map.ts, tools/std/scan-pt.ts (reuse its position walkers; import them, do not duplicate)
    - apps/gestao-vite/src/App.tsx (route tree, rotasDoContato mounted twice)
    - apps/api/src/filas.ts and apps/workers/src/filas.ts (queue constants, scheduler ids)
    - packages/db/src/schema/identidade.ts (TS key vs SQL name)
  </read_first>
  <behavior>
    - A fixture source `export class ControladorAnexos { buscarAnexo(idAnexo: string) {} }` yields rows kind=symbol old=ControladorAnexos, kind=symbol old=buscarAnexo, kind=ts-local old=idAnexo, each with declared_at `<file>:<line>`
    - English identifiers (`status`, `useState`) yield no rows
    - A pgTable column `contatoId: text('contato_id')` yields kind=ts-prop old=contatoId with notes containing `sql=contato_id` and no row for `contato_id`
    - Rows get stable ids `<scope>-<kind>-<sha1(old+declared_at) first 8>` so reruns do not reshuffle ids
    - A comment `// Busca a conversa quando existe` produces a comments row; `// eslint-disable-next-line` does not
  </behavior>
  <action>
    Create `tools/std/inventory.ts`, CLI `node tools/std/inventory.ts --out STD [--dry-run]`, exporting `extract(root): {rows: MapRow[], comments: CommentRow[]}`.
    Candidates (only when at least one token is PT via `isPtToken`): `file`/`dir` (tracked path segments; one row per unique dir path and per file basename), `symbol` (functions, classes, interfaces, type aliases, enums, enum members, exported/module-level variables, methods, class properties), `ts-local` (parameters, locals, destructured bindings), `ts-prop` (interface/type members, object-literal keys in exported objects, Drizzle table keys with `notes` = `sql=<column>`), `endpoint` (joined Controller+method paths, full path with param names), `front-route` (Route path values in desk-vite/gestao-vite App.tsx including nested/relative routes resolved to full pattern and the duplicated `rotasDoContato` under `/fluxo/:id` and `/roteador/:id`; CRM `apps/crm/src/app/**` folders containing `page.tsx`/`route.ts`/`layout.tsx`), `query-param`, `queue`, `job-name` (incl. the 7 scheduler ids), `ws-event`, `error-code`, `test-title` (English titles excluded by detector), `script` (package.json scripts keys, all workspaces + root), `cookie`, `storage-key`, `metric`, `subpath-export` (package.json exports keys), `package` (`@pipe/<pt>`), `app` (`apps/<pt>` dir names), `css-class`/`css-var` (scope `css`, `declared_at` = css file:line; `consumers` = count of whole-token occurrences in TSX/TS/HTML), `shell-var` (assignments `NAME=` and function names in tracked `*.sh`, excluding names present in the env exceptions of `STD/exceptions.csv`).
    Fill: `consumers` = count of identifier/literal occurrences repo-wide (approximate, document as such in the summary), `persisted` = `unknown` for kinds error-code, storage-key, cookie, ws-event, job-name, queue, wire-key, and for ts-prop of jsonb columns (`notes` += `jsonb`); `no` otherwise; `status` = candidate; `new`, `category`, `decision_ref`, `owner` empty.
    Comments: `STD/comments/<scope>.csv` columns `id,scope,file,start_line,end_line,kind,pt_score,text,action,new_text,sensitivity,evidence,reviewed_by,status` (last six empty; kind in line|block|jsdoc|hash|css; only comments where `isPtComment` is true; skip lint/ts directives and license headers; sources: TS/JS files, `.css` files (scope css), and hash-comments in tracked `*.sh`, `Dockerfile*`, `*.dockerignore`, `*.yml`, `*.yaml`, `nginx.conf` (scope infra or site by path)).
    Also write `STD/inventory-summary.md`: counts per scope x kind, comment counts per scope, total rows.
    Tests in `tools/std/inventory.test.ts` for the behavior cases (in-memory source files).
    Do NOT run the real inventory in this plan (plan 01-08 runs it); `--dry-run` on the repo may be used to check it finishes (< 5 min) and prints totals.
  </action>
  <verify>
    <automated>node --test tools/std/inventory.test.ts && node tools/std/inventory.ts --out .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tools/std/inventory.test.ts` exits 0
    - `--dry-run` prints per-scope totals for all 18 scopes and writes no file (`git status --porcelain .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map` empty)
    - dry-run totals show kind `endpoint` count >= 150 and kind `front-route` count >= 80
  </acceptance_criteria>
  <done>Inventory extractor ready and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Slice gate script, test-count parser, baseline gate run</name>
  <files>tools/std/gate.sh, tools/std/test-counts.ts, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-gate.md, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-test-counts.json</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/baseline.md
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-VALIDATION.md (Sampling Rate)
    - tools/std/route-match.ts, tools/std/scan-pt.ts, tools/std/ddl-snapshot.sh (CLI flags)
  </read_first>
  <action>
    `tools/std/test-counts.ts`: `parseTestCounts(log): Record<pkg,{passed,failed,skipped,files_failed:string[]}>` from turbo-prefixed lines (`@pipe/x:test: ...`): vitest `Tests  N passed (M)` / `N failed`, `FAIL  <file>`; node:test `# pass N`, `# fail N`, `# skipped N`; `compareCounts(current, baseline, renames, allowedRemovals)` where renames maps old package names to new (from approved map rows kind package/app) and `allowedRemovals` comes from `STD/reports/allowed-test-removals.csv` (`package,count,decision_ref`) — returns failures when current passed < baseline passed - allowed. CLI: `node tools/std/test-counts.ts --log <log> --out <json> [--compare <baseline.json> --map STD/map]`, exit 1 on shortfall.
    `tools/std/gate.sh <label>` (bash, `set -uo pipefail`, runs from repo root, writes `STD/reports/<label>-gate.md` with one `PASS`/`FAIL` line per step and exits 1 if any FAIL). Steps in order: (1) `git clean -fdX -- 'apps/*/dist' 'packages/*/dist' 'apps/*/.next'` and remove `.turbo` cache dirs; (2) `pnpm install`; (3) `pnpm typecheck`; (4) `pnpm build`; (5) `pnpm turbo run test --continue` logged to the scratch dir; if it fails and every failing file is one of `tests/instagram.test.ts|tests/fluxo.test.ts|tests/messenger.test.ts` (or their approved renamed paths via map), rerun each isolated with `pnpm --filter <api package> exec vitest run <file>`; PASS only if isolated reruns pass; always run the 3 isolated as well; (6) `node tools/std/test-counts.ts --log <log> --out STD/reports/<label>-test-counts.json` plus `--compare STD/reports/baseline-test-counts.json --map STD/map` unless label is `baseline`; (7) `bash tools/std/ddl-snapshot.sh check`; (8) `node tools/std/route-match.ts --emit STD/reports/<label>-routes.json --consumers STD/reports/<label>-route-consumers.csv --allow STD/route-drift-allow.csv --check` plus `--compare STD/reports/baseline-routes.json --map STD/map` unless baseline; (9) `node tools/std/scan-pt.ts --out STD/reports/<label>-scan.csv --summary STD/reports/<label>-scan-summary.md --map STD/map` and compare its `Unclassified:` count to the previous gate's (latest `*-scan-summary.md` by the order recorded in `STD/reports/gate-order.txt`): FAIL if higher; append label to `gate-order.txt`; (10) backend `.js` specifier check: count of `rg -n --pcre2 "from '\.{1,2}/[^']*(?<!\.js)'"` over `apps/api/src apps/api/tests apps/workers/src apps/workers/tests apps/ponte/src apps/ponte/tests` and every `packages/*/src` except `packages/ui` (use the current dir names resolved from the map when renamed; glob `packages/*/src` covers renamed packages) must be <= the baseline count stored in `STD/reports/baseline-js-specifiers.txt` (written when label is baseline). Accept `--skip-tests` only for label `dry` (never in slice gates).
    Run `bash tools/std/gate.sh baseline`. It must PASS. Commit: `feat(01-05): inventory extractor and slice gate; baseline gate green`.
  </action>
  <verify>
    <automated>bash tools/std/gate.sh baseline && grep -c "FAIL" .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-gate.md | grep -x 0</automated>
  </verify>
  <acceptance_criteria>
    - `bash tools/std/gate.sh baseline` exits 0
    - `STD/reports/baseline-gate.md` has 10 lines starting with `PASS`
    - `STD/reports/baseline-test-counts.json` has a key per workspace with a test script (13 packages as in baseline.md) and passed counts equal to baseline.md
    - `STD/reports/baseline-js-specifiers.txt` exists and contains an integer
    - `STD/reports/gate-order.txt` first line is `baseline`
  </acceptance_criteria>
  <done>Gate runs green on baseline; every later slice uses `bash tools/std/gate.sh <label>`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate verdict → slice progression | a false PASS lets a regression into the next slice |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Repudiation | gate.sh | mitigate | Per-step PASS/FAIL report committed per label; test counts compared to baseline so "0 tests ran" cannot pass (Pitfall 8) |
| T-01-05-02 | Tampering | flaky-test exemption hiding a real failure | mitigate | Exemption only for the 3 named files and only if their isolated rerun passes |
| T-01-05-03 | Tampering | stale dist masking breakage (Pitfall 4) | mitigate | gate deletes all dist/.next/.turbo before typecheck/build |
</threat_model>

<verification>
Inventory tests pass; `bash tools/std/gate.sh baseline` exits 0 with 10 PASS lines.
</verification>

<success_criteria>
Deterministic inventory and a single-command slice gate exist, and the baseline passes the gate.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-05-SUMMARY.md` when done
</output>
