---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 32
type: execute
wave: 31
depends_on: ["01-31"]
files_modified:
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/FINAL-REVIEW.md
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/smoke-checklist.md
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/final-*
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/final-post-gate2-derived-ack.csv
autonomous: false
requirements: [STD-03, STD-06, STD-09, STD-11, STD-12]

must_haves:
  truths:
    - "Meta webhook paths webhooks/whatsapp, webhooks/instagram and webhooks/messenger are unchanged (D-15)"
    - "Full regression is green on the final tree: typecheck, tests (counts >= baseline minus documented removals), builds, every docker image, DDL unchanged, routes/guards equal to baseline through the map, zero orphans, zero unclassified PT"
    - "A Sonnet final review covers security invariants (guards, scopes, open redirect, body limits, flow keys, cookie attributes), persisted invariants, CRM invariance and nav-contract implementation"
    - "The owner walked the local smoke checklist (navigation, F5, back/forward, deep links, filters per tenant/user, auth callbacks, invite) and approved"
    - "The owner explicitly acknowledged every post-gate2-derived map row; unacknowledged rows are findings and prevent sign-off"
  artifacts:
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/FINAL-REVIEW.md"
      provides: "final review with invariant checks"
      contains: "Invariants"
  key_links:
    - from: "std/FINAL-REVIEW.md"
      to: "std/reports/final-gate.md"
      via: "cites gate evidence"
      pattern: "final-gate"
---

<objective>
Final regression and review before cutover (STD-11): full gate, all images, invariant review by Sonnet, and the owner's local smoke walk.

Purpose: D-20/STD-11 — the phase is not done until regression across fronts, API, workers, auth, callbacks, redirects, deep links, refresh and back/forward is proven.
Output: `std/FINAL-REVIEW.md`, filled local rows of `std/smoke-checklist.md`, final reports.

Model routing (D-24): Claude (Sonnet) runs and reviews; owner verifies in the browser.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/smoke-checklist.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/nav-contract.md
@PROJECT-HANDOFF.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Local environment notes (PROJECT-HANDOFF): start the API in its own PowerShell window with the root `.env`; Vite listens on `[::1]`; Docker Desktop must be running. Google login locally needs the local callback `http://localhost:<api port>/v1/auth/google/callback` registered in Google Cloud Console (owner) and the local `.env` value of `GOOGLE_URL_RETORNO` already updated in plan 01-18.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final gate, image builds, invariant review</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/FINAL-REVIEW.md, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/final-*</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-gate.md and slice-5-gate.md
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/persisted.csv
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-RESEARCH.md (Security Domain)
  </read_first>
  <action>
    1. `bash tools/std/gate.sh final` (all PASS) and build every image with the renamed build script: `MSYS_NO_PATHCONV=1 SEM_PULL=1 bash infra/<build script> std-final` (exit 0).
    2. Write `STD/FINAL-REVIEW.md` with `## Evidence` (links to final-gate.md, test counts vs baseline table, image build log summary) and `## Invariants` — one checked line each, with the command used and its output: (a) route-match compare vs baseline through map = no difference (routes + guards); (b) `@Escopos` literal list equal to baseline (D-40); (c) persisted error-code occurrence counts equal to baseline; (d) `bash tools/std/ddl-snapshot.sh check` = `SQL unchanged` and `git diff limpeza -- packages/db/drizzle` empty (D-08); (e) destinoAbsoluto body identifier-only diff vs `limpeza` (normalize via map); (f) body-limit paths match live routes (route-match internal strings); (g) flow-key tests pass; (h) cookie attributes identical except name (D-38); (i) CRM `'use client'` set identical (STD-09); (j) Desk: no ids in URL/location.state (greps from 01-22); Gestão: D-30 screens use filter-memory; (k) no PT->EN redirects exist (D-14): `rg -n "Navigate.*replace" apps/*/src` reviewed; (l) webhook paths unchanged (D-15); (m) env names unchanged except none (only the GOOGLE_URL_RETORNO value) (D-06/D-36); (n) scan `Unclassified: 0`; (o) jsonb persistence (STD-06): `node tools/std/jsonb-keys.ts --check` passes; `pnpm --filter <api package> exec vitest run <renamed path of tests/jsonb-compat.test.ts>` passes; `git diff std-slice-0-end -- <renamed apps/api tests>/fixtures/jsonb` is empty, or every re-recorded golden is listed with Sonnet's proof that the key is not stored in jsonb.
    3. Sonnet diff sampling: for each slice tag range, read 10 random changed hunks and confirm they are map-driven; list any non-map change with justification.
    Commit `docs(01-32): final regression and invariant review`.
  </action>
  <verify>
    <automated>bash tools/std/gate.sh final && grep -c "^- \[x\]" .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/FINAL-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - `bash tools/std/gate.sh final` exits 0
    - FINAL-REVIEW.md `## Invariants` has 15 lines (a)-(o), each `- [x]` with command + output
    - all images built (exit 0 recorded)
  </acceptance_criteria>
  <done>Automated regression and invariant review complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner local smoke walk (STD-11, STD-12)</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/smoke-checklist.md</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/smoke-checklist.md
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv (rows with notes containing `post-gate2-derived`)
  </read_first>
  <action>Start the local stack for the owner (API in its own PowerShell window per PROJECT-HANDOFF, Desk, Gestão, CRM dev servers), print the URLs. Enumerate every `STD/map/*.csv` row whose `notes` contains `post-gate2-derived`, retaining source CSV, row id, old, new and decision_ref in `STD/reports/final-post-gate2-derived-ack.csv` with an `ack` column. Present that complete list and the `env=local` smoke rows to the owner. On resume, record explicit owner acknowledgement per row in `ack` (use `yes` only for acknowledged rows); record every unacknowledged row as a finding in FINAL-REVIEW.md and block sign-off until resolved. Fill `result` and `date` for each local row from the owner's answers and commit `docs(01-32): local smoke results and derived-map acknowledgements`.</action>
  <what-built>The fully renamed monorepo with the new navigation contract, running locally.</what-built>
  <how-to-verify>
    1. In Google Cloud Console, add the local redirect `http://localhost:<api port>/v1/auth/google/callback` (keep the old one until cutover), then log in with Google.
    2. Desk: open a conversation, press F5 -> the list shows; open again, press browser Back -> the conversation closes and you stay in the Desk; open a contact from the conversation drawer -> contacts screen shows it, URL is `/contacts` with no id; F5 -> contact list.
    3. Desk rail: go /chat -> /contacts -> /analytics, then Back/Forward -> normal history.
    4. Gestão: open monitoring, log, an analysis screen and novidades; set filters; reload -> filters restored; switch account (tenant) -> filters NOT carried over.
    5. Gestão deep links: open two deep-linkable screens by pasting their URL in a new tab; F5 keeps them.
    6. Invite: create an invite and open the link (new invite path).
    7. CRM: sign in, open two pages, sign out -> redirect to sign-in with the destination param.
    8. Upload an attachment and a contact import file (body limits).
    9. Visual sanity of Desk, Gestão, CRM and site after the CSS rename.
    10. Review the `post-gate2-derived` rows listed from `STD/map/*.csv` in `STD/reports/final-post-gate2-derived-ack.csv`; explicitly acknowledge each old -> new choice. Any row left without acknowledgement is a blocking finding.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/final-post-gate2-derived-ack.csv && grep -c "| local |" .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/smoke-checklist.md</automated>
  </verify>
  <acceptance_criteria>
    - every `env=local` row of smoke-checklist.md has a non-empty result and date after resume
    - no local row result is FAIL (a FAIL returns to a fix task before cutover)
    - `STD/reports/final-post-gate2-derived-ack.csv` exists with one row for every `post-gate2-derived` row in `STD/map/*.csv`, including source CSV, row id, old, new, decision_ref and `ack` columns (header only if no matching rows)
    - every data row in `final-post-gate2-derived-ack.csv` has `ack=yes` from the owner; every other value or blank is recorded in FINAL-REVIEW.md as a finding and blocks sign-off
  </acceptance_criteria>
  <resume-signal>Type "approved" or list the failing checklist items</resume-signal>
  <done>Owner approved local behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| whole system | final verification before production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-32-01 | Elevation of privilege | any guard/scope drift across the phase | mitigate | invariants (a)(b)(g) re-checked on final tree |
| T-01-32-02 | Spoofing | open redirect / OAuth callback | mitigate | invariant (e) + manual Google login and CRM redirect in smoke |
| T-01-32-03 | Information disclosure | filters leaking across tenants | mitigate | smoke step 4 |
</threat_model>

<verification>
Final gate green; invariants checked; owner approved local smoke.
</verification>

<success_criteria>
Phase ready for cutover with proven regression coverage.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-32-SUMMARY.md` when done
</output>
