---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 21
type: execute
wave: 17
depends_on: ["01-37"]
files_modified:
  - apps/desk-vite/src/**
  - apps/desk-vite/tests/**
  - apps/desk-vite/index.html
  - apps/desk-vite/Dockerfile
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/desk-vite.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-3a-*
autonomous: true
requirements: [STD-07, STD-10]

must_haves:
  truths:
    - "Desk identifiers, files, folders (paginas, componentes, contexto, lib...), front routes, query params, storage keys (per persisted decisions) and test titles follow the approved map"
    - "REMOVE/STATE rows (/chat/:id, /contacts/:id, ?ticket=) are NOT applied here; plan 01-22 implements them"
    - "Desk builds with VITE_BASE=/desk/ and tests pass; gate passes"
  artifacts:
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-3a-gate.md"
      provides: "gate result"
  key_links:
    - from: "apps/desk-vite/src/App.tsx routes"
      to: "rail links and navigate() calls"
      via: "front-route rows applied together"
      pattern: "path="
---

<objective>
Slice 3a (D-19 step 3): apply the approved map to `apps/desk-vite` (identifiers, files, folders, routes, params, storage keys, test titles).

Purpose: Desk is small (50 files) and gets the navigation-contract change next (plan 01-22); renaming first means the behavior change lands on final names.
Output: renamed Desk, gate report.

Model routing (D-25): Haiku-eligible tool application; escalation rules as plan 01-13.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-13-PLAN.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Slice rules: plan 01-13 context. Scope: desk-vite. Kinds: symbol, ts-prop, ts-local, file, dir, front-route, query-param, storage-key, test-title. Exclude rows whose `new` is REMOVE or STATE (plan 01-22) and kinds css-class/css-var (plan 01-25).

Facts: Desk uses extensionless specifiers; tests run with `node --import tsx --test tests/*.test.ts` importing `../src/x.ts`; `BrowserRouter basename={import.meta.env.BASE_URL}`; docker build needs `MSYS_NO_PATHCONV=1` for `VITE_BASE=/desk/` in Git Bash. Storage keys `desk.pref.*` follow the persisted.csv decision (renamed only if its decision allows a reset).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Desk identifiers, files, folders</name>
  <files>apps/desk-vite/src/**, apps/desk-vite/tests/**, apps/desk-vite/index.html, apps/desk-vite/Dockerfile, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/desk-vite.csv</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/desk-vite.csv
    - apps/desk-vite/src/App.tsx, apps/desk-vite/src/main.tsx, apps/desk-vite/index.html
  </read_first>
  <action>
    `rename-symbols.ts --scopes desk-vite --kinds symbol,ts-prop,ts-local` (dry-run, apply); `move-files.ts --scopes desk-vite --kinds file,dir` (dry-run, apply); check `index.html` `src="/src/main.tsx"` and Dockerfile paths against the move leftovers report and fix. `pnpm typecheck` exit 0. Commit `refactor(01-21): rename desk identifiers and files`.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - tools report missing=0, 0 conflicts
    - `pnpm typecheck` exits 0
    - `git ls-files apps/desk-vite/src/paginas | wc -l` prints 0 if the `paginas` dir row is not KEEP
  </acceptance_criteria>
  <done>Desk code renamed.</done>
</task>

<task type="auto">
  <name>Task 2: Desk routes, params, storage keys, test titles; gate</name>
  <files>apps/desk-vite/src/**, apps/desk-vite/tests/**, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-3a-*</files>
  <read_first>
    - apps/desk-vite/src/App.tsx
    - apps/desk-vite/src rail component (resolved from componentes/trilho.tsx)
    - apps/desk-vite/src preferences lib (resolved from lib/preferencias.ts)
  </read_first>
  <action>
    `rewrite-literals.ts --scopes desk-vite --kinds front-route,query-param,storage-key,test-title,literal-value` excluding REMOVE/STATE rows (the tool skips them by definition; confirm in dry-run output). Sign-in route row: if the Desk sign-in route renames, the post-login redirect in the session guard component and the API-side login links (if any) must already match — grep the new and old sign-in paths across apps. `bash tools/std/gate.sh slice-3a` all PASS. Build check for the VPS base path: `MSYS_NO_PATHCONV=1 VITE_BASE=/desk/ pnpm --filter @pipe/desk-vite build` exit 0. Commit `refactor(01-21): rename desk routes, params, storage keys, test titles; gate slice-3a green`.
  </action>
  <verify>
    <automated>bash tools/std/gate.sh slice-3a</automated>
  </verify>
  <acceptance_criteria>
    - `bash tools/std/gate.sh slice-3a` exits 0
    - `MSYS_NO_PATHCONV=1 VITE_BASE=/desk/ pnpm --filter @pipe/desk-vite build` exits 0
    - `grep -c 'path="/chat/:id"' apps/desk-vite/src/App.tsx` still prints 1 (removal happens in 01-22)
  </acceptance_criteria>
  <done>Desk renamed; slice 3a green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated user → Desk routes | session guard component redirects to sign-in |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-21-01 | Spoofing | session guard redirect to renamed sign-in route | mitigate | sign-in route rows applied together; grep old/new sign-in paths across apps |
| T-01-21-02 | Information disclosure | renamed storage keys leaking prefs across users | accept | keys follow persisted.csv decision; no new data stored |
</threat_model>

<verification>
Gate slice-3a green; Desk builds with /desk/ base.
</verification>

<success_criteria>
Desk English per map, ready for nav-contract implementation.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-21-SUMMARY.md` when done
</output>
