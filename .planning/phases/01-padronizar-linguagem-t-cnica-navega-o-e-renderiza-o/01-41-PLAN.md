---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 41
type: execute
wave: 33
depends_on: ["01-33"]
files_modified:
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/cutover-log.md
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/history-commit-map.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-CONTEXT.md
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-41-SUMMARY.md
autonomous: false
requirements: [STD-11]

must_haves:
  truths:
    - "D-22 squash is default; no-squash requires an explicit owner amendment in CONTEXT and SUMMARY."
    - "All log writes are committed before history reconstruction."
    - "Backup refs are verified before branch reset; all committed changes are reproduced before ff-only merge."
    - "Original commits and SUMMARY hashes are mapped."
  artifacts: []
  key_links: []
---

<objective>
Record the history choice, reconstruct D-22 history and ff-merge to limpeza.

Purpose: Preserve committed cutover evidence while producing one commit per slice.
Output: backup refs, reconstructed history and commit map.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/queue-rename.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-RESEARCH.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

File paths `apps/api/src/drain-legacy-queues.ts` / `apps/api/tests/drain-legacy-queues.test.ts` use the renamed api folders (resolve the `src`/`tests` roots; they are not PT). Reuse the API's existing Redis connection helper (the module that builds the BullMQ connection from `PIPE_*` env; env names unchanged). BullMQ ^5.60: `Queue#removeJobScheduler(id)`, `Queue#getJobCounts(...types)`, `Queue#getJobs(['failed'])`, `Queue#obliterate({ force })`.
RESEARCH drain procedure (steps 1-6) and recoverability table are the source for the runbook. `pipe-entrada`, `pipe-process-http`, `pipe-importacao` have no sweep and must reach zero. Avoid the ~03:10 aggregation window. Meta webhook retry during the maintenance window is an assumption (A5): keep the window short.
Slice tags available: `std-slice-0-end` .. `std-slice-5-end`. Branches: the work branch `std/english-rename` was created from `limpeza` in plan 01-01 and is the checked-out branch during this plan; parallel work on `limpeza` is frozen (D-22), so the pre-phase `limpeza` head equals `git merge-base limpeza std/english-rename`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit history inputs after the cutover tag exists</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/cutover-log.md</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/cutover-log.md
    - existing 01-*-SUMMARY.md files and std-slice-0-end through std-slice-5-end tags
  </read_first>
  <action>Verify the tracked tree is clean and std-cutover-end resolves to the committed cutover log. Collect BASE=git merge-base limpeza std/english-rename, require BASE equals git rev-parse limpeza (D-22), current work HEAD, hashes for std-slice-0-end through std-slice-5-end and std-cutover-end, per-plan commit counts and original SUMMARY hashes. Append ## History inputs to cutover-log.md and commit docs(01-41): record history inputs. The std-cutover-end hash is recorded only now, after the tag exists. Do not reconstruct yet.</action>
  <verify><automated>git rev-parse -q --verify refs/tags/std-cutover-end && git log -1 --format=%H -- .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/cutover-log.md</automated></verify>
  <acceptance_criteria>
    - History inputs contains BASE, work HEAD, seven tag hashes and SUMMARY hashes
    - the entire History inputs update is committed before the owner choice
  </acceptance_criteria>
  <done>Tag and history inputs recorded in a committed log.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner confirms the D-22 history (default one commit per slice)</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/cutover-log.md, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-CONTEXT.md</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/cutover-log.md
    - existing 01-*-SUMMARY.md files (original commit hashes)
  </read_first>
  <action>Present the `## History inputs` of cutover-log.md. State that locked decision D-22 ("um commit por fatia") is the default and the plan's path: `squash` = six slice commits plus cutover and post-tag documentation commits built from the tag ranges, with backup branch `std/english-rename-work` and tag `std-english-rename-work-backup` keeping the per-plan history reachable, and `history-commit-map.csv` mapping every original commit and SUMMARY hash to its slice commit. Mention the factual trade-off (inside a squashed slice, `git log --follow` sees one rename per file per slice; the backup branch keeps the fine-grained history). Keeping per-plan commits (`no-squash`) is not a default option: it is available only if the owner explicitly amends D-22 in the reply. On resume, pass the explicit owner reply to Task 3. Squash is D-22 default; no-squash requires an explicit owner amendment with a reason. No reply means no merge or rewrite. Task 3 writes and commits the choice before reconstruction.</action>
  <what-built>Production cutover log, history inputs and the D-22 squash path ready to run.</what-built>
  <how-to-verify>Review the tag and SUMMARY hash list. Reply `squash` to confirm D-22 (one commit per slice), or `no-squash — amend D-22: <reason>` only if you want to change the locked decision.</how-to-verify>
  <verify><automated>test -f .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/cutover-log.md</automated></verify>
  <acceptance_criteria>
    - owner reply explicitly confirms squash or explicitly amends D-22 with a reason for no-squash
  </acceptance_criteria>
  <resume-signal>Reply `squash` (D-22 default) or `no-squash — amend D-22: <reason>`</resume-signal>
  <done>History strategy confirmed (D-22 or a recorded amendment).</done>
</task>

<task type="auto">
  <name>Task 3: Build the one-commit-per-slice history (or apply the recorded amendment), then merge into limpeza</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/cutover-log.md, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/history-commit-map.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-CONTEXT.md, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-41-SUMMARY.md</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/cutover-log.md (owner's History choice)
    - existing 01-*-SUMMARY.md files (original commit hashes)
  </read_first>
  <action>
    1. Write exactly one History choice line in cutover-log.md from the owner reply: squash by default; no-squash only after explicit D-22 amendment with date and reason in 01-CONTEXT.md and 01-41-SUMMARY.md. Commit docs(01-41): record owner history choice. If the owner has not replied, stop. Then abort before any history operation if `git status --porcelain --untracked-files=no` is non-empty or the current branch is not `std/english-rename`. Record WORK=`git rev-parse HEAD`, BASE=`git merge-base limpeza std/english-rename` (must equal `git rev-parse limpeza`; stop if limpeza moved, D-22 froze it) and the tag hashes. Create backup branch `git branch std/english-rename-work "$WORK"` and backup tag `git tag std-english-rename-work-backup "$WORK"`; verify `git rev-parse std/english-rename-work` and `git rev-parse std-english-rename-work-backup^{commit}` both print WORK. Keep these refs after the merge.
    2. `History choice: squash` (D-22 default): only after both backup refs are verified, run `git switch -C std/english-rename "$BASE"` — this resets the existing, checked-out branch name to the pre-phase base (the work stays reachable through the backup branch and tag; never `git branch -D` and never create a second branch with the same name). Then for source refs in order `std-slice-0-end` through `std-slice-5-end`, `std-cutover-end`, and WORK: confirm `git status --porcelain --untracked-files=no` is empty, run `git read-tree -u --reset <source-ref>` and commit each tree with messages `chore(std): slice 0 — baseline, tooling, inventory, approved glossary and map`, `refactor(std): slice 1 — packages`, `refactor(std): slice 2 — API and workers`, `refactor(std): slice 3 — fronts, CSS and CRM`, `refactor(std): slice 4 — infra, app and package names`, `refactor(std): slice 5 — residual references and final scan`, `chore(std): cutover tooling and log`, `docs(std): committed history inputs and owner choice` (plus attribution trailers). WORK is captured after the choice commit, so the final tree includes every committed log and CONTEXT/SUMMARY amendment. `git diff std/english-rename-work std/english-rename --stat` must be empty. `git switch limpeza` and `git merge --ff-only std/english-rename`; stop if fast-forward is impossible, without rebase or force. For every original per-plan commit and SUMMARY hash, map it to the slice commit whose source-ref interval contains it; hashes after std-cutover-end through WORK map to the post-tag documentation commit (`git merge-base --is-ancestor` against consecutive source refs).
    3. `History choice: no-squash (D-22 amended ...)` only: keep `std/english-rename` as is; `git switch limpeza` and `git merge --ff-only std/english-rename`; stop if fast-forward is impossible. Map every original per-plan commit and SUMMARY hash to itself with its slice.
    4. On `limpeza`, write and commit `STD/reports/history-commit-map.csv` with columns `old_commit,new_slice_commit,slice,history_choice` after the selected history is final (a separate documentation commit avoids a self-referential final slice hash). Verify the only tree difference versus the backed-up work branch is this mapping report and all SUMMARY hashes appear in it. Do not push.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain --untracked-files=no)" && test -f .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/history-commit-map.csv && test -z "$(git diff std/english-rename-work limpeza --stat -- . ':(exclude).planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/history-commit-map.csv')"</automated>
  </verify>
  <acceptance_criteria>
    - backup branch `std/english-rename-work` and tag `std-english-rename-work-backup` both point to original work HEAD, and both were verified before `git switch -C std/english-rename`
    - no `git read-tree -u --reset` runs when `git status --porcelain --untracked-files=no` has output
    - `history-commit-map.csv` has `old_commit,new_slice_commit,slice,history_choice` and covers every original per-plan commit and SUMMARY hash; no-squash rows map each hash to itself, squash rows map each hash to its new slice commit
    - `History choice: squash` (default): `git log --oneline <BASE>..limpeza` lists exactly the six slice commits, one cutover commit, one post-tag documentation commit, plus the mapping-report commit; `no-squash (D-22 amended ...)`: per-plan commits are reachable from limpeza and the amendment is in 01-CONTEXT.md and the SUMMARY
    - `git diff std/english-rename-work limpeza --stat -- . ':(exclude)STD/reports/history-commit-map.csv'` is empty (replace STD with its full phase path); the mapping report is the only added file
  </acceptance_criteria>
  <done>D-22 history (or the recorded amendment) delivered and merged back into limpeza.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Meta webhooks → API during maintenance | inbound messages may be retried or lost |
| Redis legacy queues → new workers | in-flight jobs under old names |
| Google OAuth → new callback | redirect URI registration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-41-01 | Denial of service | lost inbound jobs in pipe-entrada/process-http/importacao | mitigate | drain with old consumers running; zero counts required; failed jobs exported; obliterate refused unless drain ok |
| T-01-41-02 | Denial of service | Meta webhook deliveries during maintenance | accept | short window; Meta retry assumed (A5), documented in runbook |
| T-01-41-03 | Spoofing | OAuth redirect mismatch | mitigate | env value + Console registration steps in order; old /retorno removed only after login works |
| T-01-41-04 | Tampering | history rewrite losing work | mitigate | D-22 squash is the default and no-squash needs a recorded owner amendment; backup branch/tag verified before `git switch -C`; clean tracked tree before each read-tree; old-to-new hash mapping; empty diff check and ff-only merge |
</threat_model>

<verification>
Clean-tree checks, backup refs, tree equality, mapping and ff-only merge pass.
</verification>

<success_criteria>
D-22 history or explicit amendment merged into limpeza.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-41-SUMMARY.md` when done
</output>
