---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 15
type: execute
wave: 11
depends_on: ["01-14"]
files_modified:
  - packages/*/src/**
  - packages/*/tests/**
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/packages-*.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/comments-*.json
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/exceptions.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-1-*
autonomous: true
requirements: [STD-10, STD-11]

must_haves:
  truths:
    - "Every PT comment in packages/* is translated (meaning preserved), removed (redundant), or updated only with behavior evidence, per D-16"
    - "Security/architecture/integration/Meta/Blip comments were reviewed individually by Sonnet (D-24); literal evidence kept in original language with a C exception (D-17)"
    - "Slice 1 gate passes and tag std-slice-1-end exists"
  artifacts:
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-1-gate.md"
      provides: "slice 1 closing gate"
  key_links:
    - from: "std/comments/packages-*.csv"
      to: "tools/std/apply-comments.ts"
      via: "reviewed rows applied"
      pattern: "applied"
---

<objective>
Close slice 1: triage and apply comment changes for all packages (D-16/D-17), run the slice gate, tag `std-slice-1-end`.

Purpose: STD-10 includes technical comments; doing them per slice keeps context local and reviewable.
Output: updated package comments, comment CSVs at status applied, `slice-1-gate.md`, tag.

Model routing (D-23/D-24): Codex account 1 triages packages-db, packages-contracts, packages-autenticacao, packages-armazenamento, packages-tempo-real, packages-mcp; Codex account 2 triages packages-core, packages-ai, packages-ui. Sonnet reviews 100% of rows with sensitivity security/architecture/integration/meta-blip and a 10% sample of the rest; apply is mechanical.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-CONTEXT.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Comment workflow (binding for plans 01-15, 01-20, 01-37, 01-27, 01-31). A plan that triages one scope in several batches (01-20/01-37) names each batch's chunk dir and outputs `comments-<scope>-<batch>` and merges only that batch's glob, so a later merge never re-reads an earlier batch (merge-proposals also refuses to touch reviewed rows):
1. Chunk: `node tools/std/codex-prompt.ts --template STD/prompts/TEMPLATE-comments.md --rows STD/comments/<scope>.csv --chunk 150 --out-dir STD/prompts/comments-<scope>` (paths resolved to current files).
2. Run: `bash tools/std/codex-run.sh <account> tools/std/schemas/comment-triage.schema.json <chunk> STD/out/comments-<scope>-NNN.json`.
3. Merge: `node tools/std/merge-proposals.ts --in "STD/out/comments-<scope>-*.json" --target STD/comments --owner codex<1|2>`.
4. Sonnet review: every row with sensitivity in security|architecture|integration|meta-blip — open the code, confirm the translation keeps the exact technical meaning (D-17), confirm `update` rows are backed by real behavior (cite the line or test in `evidence`), confirm `keep-original` rows are literal quotes/payloads/official names; set `reviewed_by=sonnet`, `status=reviewed`. Then a seeded 10% sample of the remaining rows (same checks); if the sample error rate > 10%, review the whole scope; then set the remaining rows `reviewed_by=sample`, `status=reviewed`.
5. Apply: `node tools/std/apply-comments.ts --comments STD/comments --scopes <scopes> --dry-run`, then without `--dry-run`; not-found must be 0.
6. `pnpm typecheck` (comment edits can break JSDoc-typed JS or `// @ts-expect-error` placement).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage package comments (Codex 1 + 2) and Sonnet review</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/packages-*.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/comments-*.json</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/TEMPLATE-comments.md
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/packages-autenticacao.csv (security-heavy)
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/packages-armazenamento.csv (signed URLs)
  </read_first>
  <action>
    Run steps 1-4 of the comment workflow for scopes packages-db, packages-contracts, packages-autenticacao, packages-armazenamento, packages-tempo-real, packages-mcp (account 1) and packages-core, packages-ai, packages-ui (account 2). All comments in `packages/autenticacao/**` and `packages/armazenamento/**` are treated as sensitivity security regardless of the Codex label (100% Sonnet review). Record per scope in the SUMMARY: rows, translate/remove/update/keep-original counts, sample error rate.
    Commit `docs(01-15): comment triage for packages (reviewed)`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),d='.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/';let bad=0;for(const f of fs.readdirSync(d).filter(f=>f.startsWith('packages-'))){const t=fs.readFileSync(d+f,'utf8').split('\n').slice(1).filter(Boolean);for(const l of t)if(!/,reviewed$|,reviewed\r?$/.test(l))bad++}if(bad)process.exit(1);console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - every row of `STD/comments/packages-*.csv` has status `reviewed`
    - every row with sensitivity security|architecture|integration|meta-blip has `reviewed_by` = `sonnet`
    - every `update` row has a non-empty `evidence`
  </acceptance_criteria>
  <done>Package comments triaged and reviewed.</done>
</task>

<task type="auto">
  <name>Task 2: Apply comments, slice 1 gate, tag std-slice-1-end</name>
  <files>packages/*/src/**, packages/*/tests/**, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/packages-*.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/exceptions.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-1-*</files>
  <read_first>
    - tools/std/apply-comments.ts (CLI)
  </read_first>
  <action>
    Workflow steps 5-6 for all 9 package scopes. Then `node tools/std/scan-pt.ts --out STD/reports/slice-1-packages-scan.csv --summary STD/reports/slice-1-packages-scan-summary.md --map STD/map` and check: no unclassified finding of kind comment remains under `packages/` (filter the CSV by `file` starting with `packages/` and `kind=comment` and empty category). Fix by adding the missing comment rows to the comments CSV (Sonnet) and re-applying. Run `bash tools/std/gate.sh slice-1` (all PASS). Commit `docs(01-15): apply package comment triage; gate slice-1 green` and `git tag std-slice-1-end`.
  </action>
  <verify>
    <automated>bash tools/std/gate.sh slice-1 && git rev-parse -q --verify refs/tags/std-slice-1-end</automated>
  </verify>
  <acceptance_criteria>
    - apply-comments reports not-found=0 and refused=0
    - `awk -F, '$1 ~ /^"?packages\// && $3=="comment" && $6==""' STD/reports/slice-1-packages-scan.csv | wc -l` prints 0
    - `bash tools/std/gate.sh slice-1` exits 0
    - tag `std-slice-1-end` exists at HEAD
  </acceptance_criteria>
  <done>Slice 1 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| comments → future maintainers | security rationale lives in comments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15-01 | Tampering/Repudiation | security comment mistranslated or dropped (auth, signed URLs) | mitigate | 100% Sonnet review for autenticacao/armazenamento and all security-labelled rows; apply-comments refuses unreviewed sensitive rows |
| T-01-15-02 | Tampering | outdated comment "fixed" to match wrong assumption | mitigate | `update` requires evidence (code line or test) per D-16 category 3 |
</threat_model>

<verification>
Gate slice-1 green; no unclassified PT comments in packages; tag present.
</verification>

<success_criteria>
Slice 1 (packages) complete: identifiers and comments English, no regression.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-15-SUMMARY.md` when done
</output>
