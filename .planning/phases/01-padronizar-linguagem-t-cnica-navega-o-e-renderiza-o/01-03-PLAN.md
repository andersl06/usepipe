---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tools/std/route-match.ts
  - tools/std/route-match.test.ts
  - tools/std/ddl-snapshot.sh
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/ddl-before.sql
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/smoke-checklist.md
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/route-drift-allow.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-routes.json
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-route-consumers.csv
autonomous: true
requirements: [STD-03, STD-06, STD-11]

must_haves:
  truths:
    - "Every /v1 path used by a front, test, package or API bootstrap string is matched to a live API route, or listed as drift"
    - "Each API route's guard decorators are captured so a rename can be proven not to drop a guard"
    - "The SQL DDL of the schema is snapshotted before any rename so later diffs prove SQL names are untouched (D-08)"
    - "A manual STD-11 smoke checklist exists"
  artifacts:
    - path: "tools/std/route-match.ts"
      provides: "API route set + guards, consumer matching, internal path strings check, compare-with-map"
    - path: "tools/std/ddl-snapshot.sh"
      provides: "save|check of sorted drizzle-kit export"
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/ddl-before.sql"
      provides: "baseline DDL"
      contains: "CREATE TABLE"
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/smoke-checklist.md"
      provides: "STD-11 manual checklist"
  key_links:
    - from: "tools/std/route-match.ts"
      to: "apps/api/src/**/*.ts @Controller decorators"
      via: "TS AST scan of all api files (not a fixed folder)"
      pattern: "Controller"
    - from: "tools/std/ddl-snapshot.sh"
      to: "packages/db drizzle-kit export"
      via: "pnpm exec drizzle-kit export | sort"
      pattern: "drizzle-kit export"
---

<objective>
Build the route-consumer matcher (STD-03 proof, guard-preservation proof) and the DDL snapshot (STD-06/D-08 proof), run both on the baseline, and write the STD-11 manual smoke checklist.

Purpose: Paths, guards and SQL names are string or schema contracts the compiler cannot link. These two tools make "nothing silently broke" checkable per slice.
Output: `tools/std/route-match.ts`, `tools/std/ddl-snapshot.sh`, baseline reports, `std/ddl-before.sql`, `std/smoke-checklist.md`, `std/route-drift-allow.csv`.

Model routing: Claude (Sonnet). No Codex.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-RESEARCH.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Tool conventions (same as plan 01-02): plain `node tools/std/x.ts`, erasable TS only, `.ts` relative imports, `typescript` compiler API, `node --test` tests, file lists from `git ls-files`, global exclusions `referencias-blip/**`, `**/dist/**`, `node_modules`, `.planning/**`, `tools/std/fixtures/**`.

Facts:
- `apps/api/src/servidor.ts` scopes parsers by path strings at lines 75, 82, 88, 94, 101, 111: `/v1/anexos`, `/v1/contatos/importacoes`, `/v1/canais/whatsapp/:id/perfil`, `/v1/canais/whatsapp/:id/modelos`, `/v1/gestao/fluxos/:id/builder`, `/v1/gestao/contrato/certificados`.
- `apps/api/src/autenticacao.ts:123` `fluxoDaRota` uses regex `/\/fluxos\/:(\w+)(?=\/|$)/` and param `fluxoId`; error message at :157 embeds `/v1/gestao/fluxos/:id/…`.
- Guard-bearing decorators in API: `@ComSessao`, `@Escopos(...)`, `@ChaveOuSessao`, `@UseGuards(...)`, plus any custom decorator applied to controller class or method — capture ALL decorators except the HTTP-method/Controller ones.
- Drizzle config lives in `packages/db` (`drizzle.config.ts`, `casing: 'snake_case'`). Never run `drizzle-kit generate` or `push` in this phase.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: route-match tool with tests, baseline run, drift allowlist</name>
  <files>tools/std/route-match.ts, tools/std/route-match.test.ts, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-routes.json, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-route-consumers.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/route-drift-allow.csv</files>
  <read_first>
    - apps/api/src/servidor.ts
    - apps/api/src/main.ts (global prefix / versioning, if any)
    - apps/api/src/autenticacao.ts (lines 110-210)
    - apps/desk-vite/src/lib/consulta.ts (useLeitura signature and query key ['api', caminho])
    - one controller file in apps/api/src/controladores/ (decorator style)
  </read_first>
  <behavior>
    - normalizePath('/v1/conversas/${id}/mensagens?x=1') -> '/v1/conversas/:*/mensagens'; normalizePath('v1/conversas/:id') -> '/v1/conversas/:*'
    - A fixture controller `@Controller('v1/x') class A { @Get(':id') @ComSessao() f(){} }` yields route {method:'GET', path:'/v1/x/:*', guards:[{name:'ComSessao',args:[]}]} merged with class-level decorators
    - Consumer '/v1/x/${id}' matches; '/v1/y' reported ORPHAN
    - compare(): baseline route renamed through a map row (endpoint /v1/x/:id -> /v1/items/:id, symbol ComSessao -> RequireSession) equals the new route set; dropping a guard produces a diff
  </behavior>
  <action>
    Create `tools/std/route-match.ts` exporting `normalizePath`, `collectRoutes(files)`, `collectConsumers(files)`, `compareRoutes(baseline, current, renames)` and a CLI:
    `node tools/std/route-match.ts --emit <routes.json> [--consumers <csv>] [--compare <baseline-routes.json> --map <STD/map>] [--allow <STD/route-drift-allow.csv>] [--check]`.
    Routes: scan every tracked `apps/api/src/**/*.ts` for classes decorated with `Controller(prefix)`; join prefix + method decorator path (`Get|Post|Put|Patch|Delete|All|Head|Options`); apply any `setGlobalPrefix` found in `main.ts`/`servidor.ts`; normalize `:param` and `${...}` to `:*`, strip query string, ensure leading `/`. Emit JSON array sorted by `method + path`: `{method, path, rawPath, controller, handler, guards:[{name, args}], file, line}` where guards = class decorators + method decorators excluding Controller/HTTP-method/`Body|Param|Query|Req|Res|Headers` param decorators, args as literal text.
    Consumers: every string/template literal containing `/v1/` in tracked files under `apps/{desk-vite,gestao-vite,crm,ponte,workers}/src`, `apps/*/tests`, `packages/*/src`, `packages/*/tests`; also literal args of `invalidateQueries`/`queryKey` arrays. Write CSV `file,line,raw,normalized,match` where match is the matched route path or `ORPHAN` (a consumer matches when normalized path equals a route path or is a prefix-at-segment-boundary of one — prefix matching is needed for query-cache invalidation keys).
    Internal path strings: in `apps/api/src/**` outside decorators — `app.use('<path>')` first args, RegExp literals containing `/v1/` or `fluxos`, and string literals containing `/v1/` — each must match a live route (prefix match allowed); report as kind `internal`.
    Compare: load baseline JSON, translate each baseline route through approved map rows (`status` in approved|applied|verified) of kind `endpoint` (old path pattern -> new) and kind `symbol` (controller class names and decorator names), then require exact equality of the sorted lists of `{method, path, guards}`; print every difference.
    `--check`: exit 1 if any ORPHAN consumer or unmatched internal string is not listed in `--allow` (CSV `file,raw,reason,resolve_by`) or if compare shows a difference.
    Tests in `tools/std/route-match.test.ts` for the behavior cases using in-memory fixture strings (use `ts.createSourceFile`, no disk fixtures).
    Baseline run: `node tools/std/route-match.ts --emit STD/reports/baseline-routes.json --consumers STD/reports/baseline-route-consumers.csv`. For every ORPHAN or unmatched internal string found now (existing drift, Research Open Question 6), add a row to `STD/route-drift-allow.csv` with `resolve_by` = `01-18` (the API endpoint plan must fix or justify each). Record in the SUMMARY the route count and orphan count.
  </action>
  <verify>
    <automated>node --test tools/std/route-match.test.ts && node tools/std/route-match.ts --emit .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-routes.json --consumers .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-route-consumers.csv --allow .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/route-drift-allow.csv --check</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tools/std/route-match.test.ts` exits 0
    - `node -e "console.log(require('./.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-routes.json').length)"` prints a number >= 200 (research counted 207 method decorators)
    - baseline-routes.json contains an entry whose `guards` includes a name `Escopos`
    - The `--check` command above exits 0 (all baseline drift is in the allowlist)
    - `route-drift-allow.csv` header is `file,raw,reason,resolve_by`
  </acceptance_criteria>
  <done>Route set, guards and consumers captured; baseline drift listed.</done>
</task>

<task type="auto">
  <name>Task 2: DDL snapshot script, ddl-before.sql, STD-11 smoke checklist</name>
  <files>tools/std/ddl-snapshot.sh, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/ddl-before.sql, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/smoke-checklist.md</files>
  <read_first>
    - packages/db/drizzle.config.ts
    - packages/db/package.json
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-VALIDATION.md (Manual-Only Verifications)
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-RESEARCH.md (Validation Architecture STD-11 row)
  </read_first>
  <action>
    Create `tools/std/ddl-snapshot.sh` (bash, `set -euo pipefail`, resolves repo root with `git rev-parse --show-toplevel`). `save`: `(cd packages/db && pnpm exec drizzle-kit export) | grep -v '^\s*$' | sort > STD/ddl-before.sql`. `check`: produce the same sorted output into a temp file (scratch dir via `mktemp`), `diff -u STD/ddl-before.sql <tmp>`; also `git diff --quiet limpeza -- packages/db/drizzle`; exit 1 with message `DDL CHANGED` or `MIGRATIONS CHANGED` on any difference, print `SQL unchanged` on success. Refuse any other argument. The script must never call `drizzle-kit generate` or `push`.
    Run `bash tools/std/ddl-snapshot.sh save`, then `bash tools/std/ddl-snapshot.sh check` (must print `SQL unchanged`).
    Create `STD/smoke-checklist.md`: a table `id | env (local/vps) | area | steps | expected | result | date` with rows at least for: Google login via `/v1/auth/google/callback` (local, vps) after Console re-registration (D-12); SSO callback `/v1/auth/sso/callback` (D-13); invite link on the renamed invite route (D-14); session cookie rename forces logout then new login works, incl. WebSocket `/v1/eventos` upgrade with new cookie (D-38); Desk: open conversation -> F5 -> list shown (D-27); Desk: open conversation -> browser back -> conversation closes, still in Desk (D-32, also mark NEEDS VALIDATION vs live Blip); Desk: contacts selection + ticket not in URL, F5 -> contacts list (D-29); Desk rail navigation /chat /contacts /analytics history normal; Gestão: each deep-linkable screen class from nav-contract.md opens by URL and survives F5 (D-34); Gestão: filters on monitoring/log/analysis/novidades remembered after reload, and NOT applied after switching tenant or user (D-30); Gestão wizard step behavior per nav-contract (D-31); CRM: sign-in and invite routes, middleware redirect with destination param; post-login `destino` redirect only to internal paths (open-redirect guard unchanged); upload of attachment/import/certificate works (body-limit paths, Pitfall 2); flow-scoped API key accepted on its flow route and 403 on another flow (Pitfall 3); Prometheus shows renamed metrics and `alertas.yml` rules load (D-38); queue drain log counts reach 0 for all old queues (D-10); visual sanity of Desk, Gestão, CRM, site after CSS rename (D-35). Leave `result`/`date` empty.
    Commit: `feat(01-03): route matcher, DDL snapshot, smoke checklist`.
  </action>
  <verify>
    <automated>bash tools/std/ddl-snapshot.sh check</automated>
  </verify>
  <acceptance_criteria>
    - `bash tools/std/ddl-snapshot.sh check` prints `SQL unchanged` and exits 0
    - `grep -c "CREATE TABLE" STD/ddl-before.sql` >= 100 (research: 103 tables)
    - `grep -c "generate\|push" tools/std/ddl-snapshot.sh` prints 0
    - `grep -c "^|" STD/smoke-checklist.md` >= 20
    - smoke-checklist.md contains `callback`, `F5`, `tenant`, `drain`, `NEEDS VALIDATION`
  </acceptance_criteria>
  <done>DDL baseline saved and self-check passes; smoke checklist written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → API guards | Guards on each route enforce session/scope/flow-key access |
| code → database schema | SQL names are a persisted contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Elevation of privilege | guard decorators on renamed controllers | mitigate | route-match captures guards per route; `--compare` through the map fails on any dropped/changed guard |
| T-01-03-02 | Denial of service | servidor.ts body-limit path strings | mitigate | internal path strings must match a live route or `--check` fails |
| T-01-03-03 | Tampering | SQL schema drift | mitigate | ddl-snapshot check + `git diff limpeza -- packages/db/drizzle` empty; script cannot run generate/push |
</threat_model>

<verification>
route-match tests pass and baseline `--check` is green with drift allowlisted; DDL check prints `SQL unchanged`.
</verification>

<success_criteria>
Baseline routes/guards/consumers and DDL are captured as machine-checkable references.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-03-SUMMARY.md` when done
</output>
