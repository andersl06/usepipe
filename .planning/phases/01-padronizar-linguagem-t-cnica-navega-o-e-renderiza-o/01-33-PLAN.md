---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 33
type: execute
wave: 28
depends_on: ["01-32"]
files_modified:
  - apps/api/src/drain-legacy-queues.ts
  - apps/api/tests/drain-legacy-queues.test.ts
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/CUTOVER-RUNBOOK.md
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/exceptions.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/smoke-checklist.md
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/cutover-log.md
autonomous: false
requirements: [STD-05, STD-06, STD-11]

must_haves:
  truths:
    - "A drain tool removes the 7 legacy job schedulers, waits until all 10 legacy queues have zero waiting/active/delayed/prioritized/waiting-children jobs, exports failed jobs of the unrecoverable queues, and only then obliterates legacy queues (D-10: no job lost)"
    - "A cutover runbook lists every step in order: window outside ~03:10, build/push images, maintenance, drain with old api/workers still running, stop old, VPS env GOOGLE_URL_RETORNO value, Google Console redirect, deploy with --remove-orphans, smoke, invite resend"
    - "The owner executed the cutover and the VPS smoke rows pass"
    - "History has one commit per slice on std/english-rename, merged back into limpeza (D-22)"
  artifacts:
    - path: "apps/api/src/drain-legacy-queues.ts"
      provides: "one-off drain CLI shipped in the api image"
      contains: "removeJobScheduler"
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/CUTOVER-RUNBOOK.md"
      provides: "ordered cutover steps"
      contains: "pipe-entrada"
  key_links:
    - from: "apps/api/src/drain-legacy-queues.ts LEGACY_QUEUES"
      to: "std/queue-rename.md old names"
      via: "same literal list"
      pattern: "LEGACY_QUEUES"
---

<objective>
Single coordinated cutover (D-10, D-12, D-20 "deploy é um único corte no final"): drain tool + runbook, owner-executed production cutover, then per-slice history and merge back to `limpeza` (D-22).

Purpose: Queue names, endpoints, cookie and callbacks all changed; production must switch in one controlled window without losing jobs.
Output: drain CLI with test, `std/CUTOVER-RUNBOOK.md`, cutover log, VPS smoke results, squashed per-slice branch merged into limpeza.

Model routing: Claude (Sonnet) writes the drain tool/runbook and does the git history work; the owner operates the VPS and Google Cloud Console (no Claude access to either).
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/queue-rename.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-RESEARCH.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

File paths `apps/api/src/drain-legacy-queues.ts` / `apps/api/tests/drain-legacy-queues.test.ts` use the renamed api folders (resolve the `src`/`tests` roots; they are not PT). Reuse the API's existing Redis connection helper (the module that builds the BullMQ connection from `PIPE_*` env; env names unchanged). BullMQ ^5.60: `Queue#removeJobScheduler(id)`, `Queue#getJobCounts(...types)`, `Queue#getJobs(['failed'])`, `Queue#obliterate({ force })`.
RESEARCH drain procedure (steps 1-6) and recoverability table are the source for the runbook. `pipe-entrada`, `pipe-process-http`, `pipe-importacao` have no sweep and must reach zero. Avoid the ~03:10 aggregation window. Meta webhook retry during the maintenance window is an assumption (A5): keep the window short.
Slice tags available: `std-slice-0-end` .. `std-slice-5-end`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drain CLI with integration test, cutover runbook</name>
  <files>apps/api/src/drain-legacy-queues.ts, apps/api/tests/drain-legacy-queues.test.ts, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/CUTOVER-RUNBOOK.md, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/exceptions.csv</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/queue-rename.md
    - the API queue module (renamed from apps/api/src/filas.ts) for the Redis connection helper
    - infra/compose/docker-compose.prod.yml and the renamed deploy script (from infra/compose/deploy.sh)
  </read_first>
  <behavior>
    - drainLegacyQueues({ queues, schedulers, connection, timeoutMs }) removes each scheduler id from each queue, then polls counts until all zero and returns per-queue final counts
    - with a delayed job still pending after timeoutMs it returns ok=false and never obliterates
    - exportFailed(queues, dir) writes one JSON file per queue with failed job data
    - obliterateLegacy refuses unless the last drain result was ok=true
  </behavior>
  <action>
    Implement `drain-legacy-queues.ts` exporting `LEGACY_QUEUES` (the 10 old names), `LEGACY_SCHEDULERS` (the 7 old ids with their queue), `drainLegacyQueues`, `exportFailed`, `obliterateLegacy`, and a CLI main guarded by `import.meta.url === pathToFileURL(process.argv[1]).href` with flags `--wait [--timeout-seconds 900]`, `--export-failed <dir>`, `--obliterate`; prints counts every 5 s. Old names are data here: add a C row to exceptions.csv for this file (pattern = the old names, justification "legacy queue names needed to drain Redis at cutover", ref D-10). Test (vitest, real local Redis as other API tests): uses unique test queue names injected via params, a scheduler + a delayed job; covers the behavior list. `pnpm --filter <api package> test -- drain-legacy-queues` green; `pnpm typecheck` green.
    Write `STD/CUTOVER-RUNBOOK.md`: preconditions (window not 02:50-03:30 VPS time; owner has Google Console + VPS root access; images version tag chosen), numbered steps: 1 build + push images `PUBLICAR=1 MSYS_NO_PATHCONV=1 bash infra/<build script> <version>`; 2 on VPS: stop Traefik routing (maintenance) — exact compose command; 3 keep OLD api/workers running; 4 run drain with the NEW api image against the same Redis: `docker compose -f <prod compose> run --rm --no-deps <new api service> node dist/drain-legacy-queues.js --wait` then `--export-failed /opt/pipe-dados/drain-<date>`; wait for "all zero"; 5 stop old api/workers; 6 `--obliterate`; 7 set VPS env `GOOGLE_URL_RETORNO=https://<vps host>/v1/auth/google/callback` (name unchanged); 8 Google Cloud Console: add the VPS and local `/v1/auth/google/callback` redirect URIs, remove `/retorno` ones after login works; 9 deploy `<renamed deploy script> <version>` ensuring `docker compose up -d --remove-orphans`; 10 start Traefik routing; 11 smoke `env=vps` rows; 12 resend pending invites (D-14); 13 note: all users are logged out (D-38), signed attachment URLs older than 15 min were invalidated, metrics series restart under new names. Rollback section: previous image tag + old compose file, and that legacy queues were obliterated only after drain (so rollback starts with empty queues; outbox sweep recovers deliveries).
    Commit `feat(01-33): legacy queue drain CLI and cutover runbook`.
  </action>
  <verify>
    <automated>pnpm typecheck && grep -c "removeJobScheduler" apps/api/src/drain-legacy-queues.ts</automated>
  </verify>
  <acceptance_criteria>
    - drain test file has 4 tests for the behavior list and passes
    - `LEGACY_QUEUES` has 10 entries and `LEGACY_SCHEDULERS` 7, equal to queue-rename.md old columns
    - CUTOVER-RUNBOOK.md has numbered steps 1-13 and a `## Rollback` section; mentions `pipe-entrada`, `pipe-process-http`, `pipe-importacao` as must-reach-zero
  </acceptance_criteria>
  <done>Drain tooling and runbook ready.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Owner executes the production cutover (VPS + Google Cloud Console)</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/CUTOVER-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/CUTOVER-RUNBOOK.md
  </read_first>
  <action>Build and push the images (step 1) if registry credentials work locally; otherwise hand step 1 to the owner. Then the owner performs steps 2-13 on the VPS and in Google Cloud Console (Claude has no access to either) and reports drain counts, deploy result and each `env=vps` smoke result.</action>
  <what-built>Drain CLI in the api image, runbook, and (if credentials allowed) pushed images tagged with the chosen version.</what-built>
  <how-to-verify>
    Follow `STD/CUTOVER-RUNBOOK.md` steps in order. Report: (1) drain output showing all legacy queues at zero before stop/obliterate, (2) failed-job export file count, (3) deploy output, (4) result of each `env=vps` row in `STD/smoke-checklist.md` (Google login via /callback, invite link, Desk F5/back, Gestão filters, CRM redirect, uploads, Prometheus metrics + alert rules loaded, WS connects).
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/CUTOVER-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - owner reported drain counts reaching zero for all 10 legacy queues and a passing result for every env=vps smoke row
  </acceptance_criteria>
  <resume-signal>Paste the drain/deploy output and smoke results, or describe what failed</resume-signal>
  <done>Production cut over.</done>
</task>

<task type="auto">
  <name>Task 3: Record cutover, build one-commit-per-slice history, merge into limpeza</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/cutover-log.md, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/smoke-checklist.md</files>
  <read_first>
    - owner report from Task 2
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/smoke-checklist.md
  </read_first>
  <action>
    1. Write `STD/reports/cutover-log.md` (date, version, drain counts per queue, failed export count, deploy output summary) and fill `result`/`date` of every `env=vps` smoke row. Commit `docs(01-33): cutover log and production smoke results`; `git tag std-cutover-end`.
    2. D-22 history: `git branch -m std/english-rename std/english-rename-work`; `git switch -c std/english-rename limpeza`; for each tag in order `std-slice-0-end`, `std-slice-1-end`, `std-slice-2-end`, `std-slice-3-end`, `std-slice-4-end`, `std-slice-5-end`, `std-cutover-end`: `git read-tree -u --reset <tag>` then `git commit -m "<message>"` with messages `chore(std): slice 0 — baseline, tooling, inventory, approved glossary and map`, `refactor(std): slice 1 — packages`, `refactor(std): slice 2 — API and workers`, `refactor(std): slice 3 — fronts, CSS and CRM`, `refactor(std): slice 4 — infra, app and package names`, `refactor(std): slice 5 — residual references and final scan`, `chore(std): cutover tooling and log` (each ending with the attribution trailer lines from the environment). Verify `git diff std/english-rename-work std/english-rename --stat` is empty.
    3. `git switch limpeza && git merge --ff-only std/english-rename`. If fast-forward is impossible (limpeza moved despite the D-22 freeze), stop and report; do not rebase or force.
    Do not push (not requested).
  </action>
  <verify>
    <automated>test -z "$(git diff std/english-rename-work std/english-rename --stat)" && git merge-base --is-ancestor std/english-rename limpeza</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline limpeza -8` shows the 7 slice commits on top of the pre-phase limpeza head
    - `git diff std/english-rename-work limpeza --stat` is empty
    - cutover-log.md exists; all env=vps smoke rows have results
  </acceptance_criteria>
  <done>Phase delivered and merged back into limpeza.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Meta webhooks → API during maintenance | inbound messages may be retried or lost |
| Redis legacy queues → new workers | in-flight jobs under old names |
| Google OAuth → new callback | redirect URI registration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-33-01 | Denial of service | lost inbound jobs in pipe-entrada/process-http/importacao | mitigate | drain with old consumers running; zero counts required; failed jobs exported; obliterate refused unless drain ok |
| T-01-33-02 | Denial of service | Meta webhook deliveries during maintenance | accept | short window; Meta retry assumed (A5), documented in runbook |
| T-01-33-03 | Spoofing | OAuth redirect mismatch | mitigate | env value + Console registration steps in order; old /retorno removed only after login works |
| T-01-33-04 | Tampering | history rewrite losing work | mitigate | read-tree from tags + empty diff check vs work branch; ff-only merge; work branch kept |
</threat_model>

<verification>
Drain test green; owner-reported production smoke passes; per-slice history merged ff-only into limpeza.
</verification>

<success_criteria>
Coordinated cutover completed without job loss; D-22 history delivered.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-33-SUMMARY.md` when done
</output>
