---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 25
type: execute
wave: 21
depends_on: ["01-24"]
files_modified:
  - tools/std/rename-css.ts
  - tools/std/rename-css.test.ts
  - packages/ui/**
  - apps/desk-vite/src/**
  - apps/gestao-vite/src/**
  - apps/crm/src/**
  - apps/site/**
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/css.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-3e-*
autonomous: true
requirements: [STD-07, STD-10]

must_haves:
  truths:
    - "Every approved CSS class, custom property and data-* attribute row (css.csv: css-class, css-var, data-attr) is renamed in CSS files and in every TS/TSX/HTML usage together (D-35, W6)"
    - "No old class token or custom property from an applied row remains anywhere; dynamically built class names are listed and fixed by hand"
    - "The number of distinct class selectors and custom properties is unchanged (no accidental merges)"
    - "packages/ui token check (verificar-tokens) and all front builds pass"
  artifacts:
    - path: "tools/std/rename-css.ts"
      provides: "map-driven CSS class/var rename across css, tsx/ts, html"
  key_links:
    - from: "css.csv css-class rows"
      to: "className literals, clsx args, classList, querySelector, html class attributes"
      via: "whole-token replacement"
      pattern: "className"
---

<objective>
Slice 3e: apply the approved CSS map (D-35) across packages/ui, desk-vite, gestao-vite, crm and site.

Purpose: CSS names are technical identifiers in STD-10 scope per D-35 but invisible to the compiler; a dedicated token-aware tool plus before/after counts is the proof.
Output: `tools/std/rename-css.ts` with tests, renamed CSS and usages, gate report.

Model routing: Claude (Sonnet) writes the tool; application is Haiku-eligible (approved rows); Sonnet fixes dynamic class constructions.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-04-PLAN.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Tool conventions as in plan 01-02; map library `tools/std/lib/map.ts`. CSS facts: 44 CSS files, ~36k lines, ~3,025 class selectors, 482 custom properties (RESEARCH). `packages/ui` exports `./estilos.css` (path may be renamed) and its test `node verificar-tokens.mjs` checks tokens. `apps/site/index.html` holds inline CSS and class attributes.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rename-css tool with tests</name>
  <files>tools/std/rename-css.ts, tools/std/rename-css.test.ts</files>
  <read_first>
    - tools/std/lib/map.ts
    - one CSS file from packages/ui/src and one TSX using className with template literals (grep `className={\``)
  </read_first>
  <behavior>
    - `.dk-conversa-vazia, .dk-conversa-vazia:hover > .x {}` with row dk-conversa-vazia -> dk-empty-conversation renames both selectors, not `.dk-conversa-vazia-2`
    - `--cor-fundo: #fff; color: var(--cor-fundo)` renames declaration and var() use; `style={{ '--cor-fundo': c }}` and `el.style.setProperty('--cor-fundo', c)` too
    - `className="a dk-conversa-vazia b"`, `className={\`dk-conversa-vazia ${x}\`}`, `clsx('dk-conversa-vazia', {...})`, `classList.add('dk-conversa-vazia')`, `querySelector('.dk-conversa-vazia')` all renamed
    - `className={\`dk-${estado}\`}` is reported as dynamic (file:line) and not modified
    - `<div class="hero dk-conversa-vazia">` and `<style>.dk-conversa-vazia{}</style>` in .html renamed
    - JSX text `dk-conversa-vazia` (user-visible text) is not touched
    - data-attr rows `data-painel` -> `data-panel` and `data-painel=aberto` -> `data-panel=open` rename `<div data-painel="aberto">`, `data-painel={aberto ? 'aberto' : 'fechado'}` (only the mapped value), `el.dataset.painel` -> `el.dataset.panel`, `getAttribute('data-painel')`, `querySelector('[data-painel="aberto"]')` and CSS `[data-painel="aberto"]` / `[data-painel]`
  </behavior>
  <action>
    Implement `tools/std/rename-css.ts` CLI `node tools/std/rename-css.ts --map STD/map --scopes css [--dry-run]` exporting `renameCss(opts)`: CSS files parsed by a small tokenizer (skip comments and strings; class selector tokens `.name` bounded by `[^a-zA-Z0-9_-]`; custom properties `--name` in declarations and `var(--name`); TS/TSX via the TypeScript AST: string literals and template-literal static parts in `className`/`class` JSX attributes, args of `clsx|cn|classNames|classList.add|remove|toggle|contains`, `querySelector(All)` selector strings, object keys inside `clsx` objects, `style` object keys starting with `--`, `setProperty/getPropertyValue` first args; `.html` files: `class="..."` attributes and `<style>` blocks. data-attr rows (W6): JSX/HTML attribute names `data-<old>`, their string-literal values (including literal branches of conditional expressions), `dataset.<camelCase old>` property accesses, `get|set|has|removeAttribute('data-<old>')` args, attribute selectors inside `querySelector(All)`/`closest`/`matches` strings and CSS attribute selectors `[data-<old>]`, `[data-<old>="<value>"]`. Whole-token matching only. Dynamic constructions (template literal whose static part ends with a mapped prefix followed by `${`) are written to `STD/reports/css-dynamic-<timestamp>.csv`. Tests in `rename-css.test.ts` for the behavior list. Commit `feat(01-25): CSS rename tool`.
  </action>
  <verify>
    <automated>node --test tools/std/rename-css.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tools/std/rename-css.test.ts` exits 0 with 7 behavior tests
  </acceptance_criteria>
  <done>Tool tested.</done>
</task>

<task type="auto">
  <name>Task 2: Apply css.csv, fix dynamic classes, verify counts; gate</name>
  <files>packages/ui/**, apps/desk-vite/src/**, apps/gestao-vite/src/**, apps/crm/src/**, apps/site/**, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/css.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-3e-*</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/css.csv
    - packages/ui/verificar-tokens.mjs (or its renamed path)
  </read_first>
  <action>
    1. Before: count distinct class selectors and custom properties across all tracked CSS + site HTML (use the tool's `--count` output or a one-off node script in the scratch dir); save to `STD/reports/slice-3e-css-counts-before.txt`.
    2. `node tools/std/rename-css.ts --map STD/map --scopes css --dry-run`, then apply. Fix every entry of the dynamic report by hand: the dynamic suffix values must map to renamed classes (e.g. `dk-${state}` where state values are English after slice renames, or an explicit lookup object) — Sonnet decides per case, listed in SUMMARY.
    3. After-count must equal before-count (distinct selectors and vars). For every applied row, `rg -n -w -F "<old>" packages/ui apps/desk-vite/src apps/gestao-vite/src apps/crm/src apps/site` returns nothing (run as a loop; print offenders).
    4. `pnpm --filter @pipe/ui test` (token check) exit 0; `bash tools/std/gate.sh slice-3e` all PASS (builds all fronts). Visual sanity is a smoke-checklist row in plan 01-32.
    Commit `refactor(01-25): rename CSS classes and custom properties (D-35); gate slice-3e green`.
  </action>
  <verify>
    <automated>bash tools/std/gate.sh slice-3e</automated>
  </verify>
  <acceptance_criteria>
    - no css.csv row (css-class, css-var, data-attr) remains at status approved
    - before/after distinct class and var counts equal (both files committed under STD/reports)
    - old-token loop prints no offenders
    - `bash tools/std/gate.sh slice-3e` exits 0
  </acceptance_criteria>
  <done>CSS English per map; builds green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS → UI behavior | some classes drive visibility (e.g. hidden panels, overlays) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-25-01 | Information disclosure | a class that hides sensitive UI (masked data, closed panel) losing its style | mitigate | whole-token rename in CSS and usages together; old-token loop must be empty; count equality; visual smoke check |
| T-01-25-02 | Tampering | third-party/Blip CSS introduced | accept | tool only renames existing project CSS; no Blip material enters the repo (Out of Scope rule) |
</threat_model>

<verification>
Tool tests pass; counts equal; no old tokens; gate slice-3e green.
</verification>

<success_criteria>
D-35 applied: CSS classes and custom properties in English everywhere.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-25-SUMMARY.md` when done
</output>
