---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 17
type: execute
wave: 13
depends_on: ["01-16"]
files_modified:
  - apps/api/**
  - apps/ponte/src/**
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/api.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-2b-*
autonomous: true
requirements: [STD-07, STD-10]

must_haves:
  truths:
    - "API classes use D-05 suffixes (e.g. ControladorAnexos -> AttachmentsController, GuardaSessao -> SessionGuard, ErroPipe -> PipeError, FiltroDeErro -> ErrorFilter) per the approved map"
    - "API folders (controladores, dominio, ...) and files are renamed; all 186 dynamic import() and vi.mock paths in API tests keep working with .js specifiers"
    - "@pipe/api subpath exports (./dominio/fluxo, ./dominio/gestao/builder-do-fluxo, ./dominio/conversa, ./dominio/envio, ./dominio/assumir) are renamed together with ponte's specifiers"
    - "Every route keeps exactly the same guard decorators (route-match compare through the map = no difference)"
    - "Endpoint URL strings are NOT changed in this plan (plan 01-18)"
  artifacts:
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-2b-gate.md"
      provides: "gate result incl. guard compare"
  key_links:
    - from: "apps/api/package.json exports"
      to: "apps/ponte/src imports of @pipe/api/<subpath>"
      via: "subpath-export rows"
      pattern: "@pipe/api/"
---

<objective>
Slice 2b: apply the approved map for API identifiers, files, directories, subpath exports and test titles (not endpoint strings), and prove guards are preserved.

Purpose: The API is the largest backend workspace (192 files, 53k LOC). Separating code identifiers from URL contracts keeps each step verifiable: here route-match must show identical routes (translated names only) and identical guards.
Output: renamed API sources/tests, gate report.

Model routing (D-25): Haiku-eligible tool application; Sonnet reviews the route-match guard comparison output and any tool report. Escalation rules as plan 01-13.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-13-PLAN.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Slice rules: plan 01-13 context. Scope: api. Kinds here: symbol, ts-prop, ts-local, file, dir, subpath-export, test-title. Excluded here: endpoint, query-param, error-code, ws-event, cookie, wire-key of non-db origin (plans 01-18/01-19).

Facts:
- API uses relative `.js` specifiers (581 in src); API tests have 186 dynamic `import()` and 4 `vi.mock` (RESEARCH). Typecheck uses `tsconfig.test.json`.
- `apps/api/vitest.config.ts` has `globalSetup` pointing into `packages/db/tests` (path may have changed in slice 1; move-files rewrites it).
- Security-relevant files (renamed here, logic untouched): `autenticacao.ts` (guards, `fluxoDaRota`), `sessao.ts`, `eventos-ws.ts`, `controladores/entrar.ts` (`destinoAbsoluto` open-redirect guard at ~:103), `controladores/sso.ts`, `controladores/anexos.ts`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: API symbols, props, locals</name>
  <files>apps/api/src/**, apps/api/tests/**, apps/ponte/src/**, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/api.csv</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/api.csv (kinds symbol/ts-prop/ts-local)
    - apps/api/src/autenticacao.ts
    - apps/api/src/controladores/entrar.ts (lines 90-120, destinoAbsoluto)
  </read_first>
  <action>
    Save a pre-change copy of the destinoAbsoluto function body text to the scratch dir. `node tools/std/rename-symbols.ts --map STD/map --scopes api --kinds symbol,ts-prop,ts-local --dry-run` (missing=0), apply. `pnpm typecheck` exit 0. Compare the destinoAbsoluto body with the saved copy: only identifier tokens may differ (normalize identifiers through the approved rows and diff = empty). Commit `refactor(01-17): rename API symbols per approved map`.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - rename-symbols reports missing=0
    - `pnpm typecheck` exits 0
    - SUMMARY records the destinoAbsoluto body comparison result as "identifier-only changes"
    - `grep -rn "class ControladorAnexos\|class GuardaSessao\|class ErroPipe\|class FiltroDeErro" apps/api/src` returns nothing (unless the row is KEEP)
  </acceptance_criteria>
  <done>API identifiers renamed.</done>
</task>

<task type="auto">
  <name>Task 2: API files/dirs, subpath exports, test titles; guard-preservation gate</name>
  <files>apps/api/**, apps/ponte/src/**, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-2b-*</files>
  <read_first>
    - apps/api/package.json (exports), apps/api/vitest.config.ts, apps/api/tsconfig.json, apps/api/tsconfig.test.json
    - apps/api/Dockerfile (COPY paths, entry point)
  </read_first>
  <action>
    1. `node tools/std/move-files.ts --map STD/map --scopes api --kinds file,dir` (dry-run first). Confirm no stale dynamic import: `rg -n "import\('\.{1,2}/" apps/api/tests | rg -v "\.js'\)"` returns nothing, and every path in `import('...')`/`vi.mock('...')` of apps/api/tests resolves to an existing file (small node one-liner or the gate's typecheck + tests).
    2. `node tools/std/rewrite-literals.ts --map STD/map --scopes api --kinds subpath-export,test-title,literal-value` (dry-run first). ponte specifiers `@pipe/api/<old subpath>` must be gone.
    3. Fix `apps/api/Dockerfile` / `Dockerfile.dockerignore` technical paths from the move leftovers report.
    4. `bash tools/std/gate.sh slice-2b` all PASS. Step 8 (route-match `--compare` through the map) must show zero differences: same routes (endpoint strings unchanged here) with guard decorator names translated by the map. Sonnet reads the compare output and records "no guard dropped or changed" in the SUMMARY.
    Commit `refactor(01-17): move API files/dirs, subpath exports, test titles; gate slice-2b green`.
  </action>
  <verify>
    <automated>bash tools/std/gate.sh slice-2b</automated>
  </verify>
  <acceptance_criteria>
    - `bash tools/std/gate.sh slice-2b` exits 0 (includes route-match compare = no difference and test counts >= baseline)
    - `git ls-files apps/api/src/controladores | wc -l` prints 0 if the `controladores` dir row is not KEEP
    - `rg -n "@pipe/api/dominio" apps packages` returns nothing if the `dominio` rows are not KEEP
    - backend `.js` specifier check (gate step 10) PASS
  </acceptance_criteria>
  <done>API code layer English; routes and guards identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP → Nest guards | access control on every controller |
| login → redirect target | `destino` open-redirect check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-17-01 | Elevation of privilege | guard decorators lost in class/file moves | mitigate | route-match `--compare` through the map in gate must show no difference; Sonnet signs off in SUMMARY |
| T-01-17-02 | Spoofing | destinoAbsoluto open-redirect logic altered | mitigate | body compared before/after; only identifier tokens may differ |
| T-01-17-03 | Denial of service | stale dynamic import after move (Pitfall 1) | mitigate | .js specifier check + dynamic import resolution check + full API tests in gate |
</threat_model>

<verification>
Gate slice-2b green with zero route/guard differences.
</verification>

<success_criteria>
API identifiers/files English, security behavior provably unchanged.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-17-SUMMARY.md` when done
</output>
