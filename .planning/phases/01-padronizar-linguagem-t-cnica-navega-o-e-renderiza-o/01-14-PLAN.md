---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 14
type: execute
wave: 9
depends_on: ["01-13"]
files_modified:
  - packages/contracts/src/**
  - packages/ui/src/**
  - packages/ai/**
  - packages/autenticacao/src/**
  - packages/autenticacao/tests/**
  - packages/armazenamento/src/**
  - packages/armazenamento/tests/**
  - packages/tempo-real/src/**
  - packages/tempo-real/tests/**
  - packages/mcp/src/**
  - apps/*/src/**
  - apps/*/tests/**
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-*.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-1b-*
autonomous: true
requirements: [STD-06, STD-07, STD-10]

must_haves:
  truths:
    - "All approved TS-level rows of packages-contracts, packages-ui, packages-ai, packages-autenticacao, packages-armazenamento, packages-tempo-real, packages-mcp are applied with consumers"
    - "@pipe/ai and @pipe/ui subpath exports and every consumer specifier use the new names"
    - "Package NAMES (@pipe/autenticacao, @pipe/armazenamento, @pipe/tempo-real) are NOT renamed here (slice 4, D-19)"
    - "CSS classes/custom properties are NOT renamed here (plan 01-25)"
    - "No contracts/package property that reaches a jsonb column is renamed (jsonb-keys check + jsonb-compat test pass after the symbol rename; plan 01-13 Slice rules jsonb guard)"
    - "Slice gate passes"
  artifacts:
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-1b-gate.md"
      provides: "gate result"
  key_links:
    - from: "packages/ai/package.json exports"
      to: "'@pipe/ai/<subpath>' specifiers"
      via: "subpath-export rows"
      pattern: "@pipe/ai/"
---

<objective>
Slice 1b (D-19 step 1, rest of the packages): apply approved TS-level rows for contracts, ui, ai, autenticacao, armazenamento, tempo-real and mcp, with consumers, and pass the gate.

Purpose: Completes the shared-foundation layer before API/workers.
Output: renamed package internals, applied map rows, `std/reports/slice-1b-gate.md`.

Model routing (D-25): Haiku-eligible, same escalation rules as plan 01-13 (no invented names; Sonnet for missing rows/unexpected failures; owner for new domain terms).
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-13-PLAN.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-13-SUMMARY.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Slice rules: see plan 01-13 context ("Slice rules"). Scopes here: packages-contracts, packages-ui, packages-ai, packages-autenticacao, packages-armazenamento, packages-tempo-real, packages-mcp (list = SCOPES below).
Kinds excluded here: `package` (slice 4), `css-class`/`css-var` (plan 01-25), `cookie` (plan 01-19, D-38), `ws-event` (plan 01-19), `storage-key` (per-front plans).

Facts:
- `@pipe/ai` exports `.`, `./transcricao`, `./resumo`, `./classificacao`, `./avaliacao`, `./consumo`, `./bancada`; typecheck also runs `tsconfig.testes.json`.
- `@pipe/ui` exports `.`, `./estilos.css`, `./tema`; its test is `node verificar-tokens.mjs` (reads CSS tokens; keep passing). `apps/gestao-vite/vite.config.ts` aliases `packages/ui/src/estilos.css`.
- `@pipe/contracts` types are consumed by api, desk-vite, gestao-vite, crm, tempo-real; renaming exported types renames them everywhere via the multi-root Project.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply symbol/prop/local rows for SCOPES</name>
  <files>packages/contracts/src/**, packages/ui/src/**, packages/ai/**, packages/autenticacao/src/**, packages/armazenamento/src/**, packages/tempo-real/src/**, packages/mcp/src/**, apps/*/src/**, apps/*/tests/**</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-contracts.csv
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-autenticacao.csv
    - packages/autenticacao/src/sessao.ts (cookie constant stays until plan 01-19)
  </read_first>
  <action>
    `node tools/std/rename-symbols.ts --map STD/map --scopes packages-contracts,packages-ui,packages-ai,packages-autenticacao,packages-armazenamento,packages-tempo-real,packages-mcp --kinds symbol,ts-prop,ts-local,wire-key --dry-run` (missing=0), then apply. Then the same scopes with `rewrite-literals.ts --kinds wire-key` for untyped consumers. Run the jsonb guard from plan 01-13 Slice rules (`node tools/std/jsonb-keys.ts --check` + api jsonb-compat test); handle any failure exactly as that rule says. `pnpm typecheck` exit 0. Commit `refactor(01-14): rename remaining package symbols per approved map`.
  </action>
  <verify>
    <automated>node tools/std/jsonb-keys.ts --check && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - rename-symbols reports `missing=0`
    - `node tools/std/jsonb-keys.ts --check` exits 0 and the jsonb-compat test passes with goldens untouched
    - `pnpm typecheck` exits 0
    - no row of kinds symbol/ts-prop/ts-local/wire-key in the 7 scope CSVs remains at status approved
  </acceptance_criteria>
  <done>Package identifiers renamed.</done>
</task>

<task type="auto">
  <name>Task 2: Move files/dirs, subpath exports, test titles, other literals; slice gate</name>
  <files>packages/contracts/src/**, packages/ui/src/**, packages/ai/**, packages/autenticacao/**, packages/armazenamento/**, packages/tempo-real/**, packages/mcp/src/**, apps/*/src/**, apps/*/tests/**, apps/gestao-vite/vite.config.ts, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-1b-*</files>
  <read_first>
    - packages/ai/package.json, packages/ui/package.json (exports)
    - apps/gestao-vite/vite.config.ts, apps/desk-vite/vite.config.ts (aliases)
    - packages/ui/verificar-tokens.mjs (paths it reads)
  </read_first>
  <action>
    1. `node tools/std/move-files.ts --map STD/map --scopes <SCOPES> --kinds file,dir` (dry-run first). Check `packages/ui/verificar-tokens.mjs` and vite aliases still point to existing files (move-files rewrites config strings; `.mjs` path strings must be checked by hand from the leftovers report).
    2. `node tools/std/rewrite-literals.ts --map STD/map --scopes <SCOPES> --kinds subpath-export,test-title,error-code,query-param,job-name,queue,metric,literal-value` (dry-run first).
    3. Resolve leftovers reports; docs references are listed for slice 5.
    4. `bash tools/std/gate.sh slice-1b` -> all PASS.
    Commit `refactor(01-14): move package files, subpath exports, test titles; gate slice-1b green`.
  </action>
  <verify>
    <automated>bash tools/std/gate.sh slice-1b</automated>
  </verify>
  <acceptance_criteria>
    - `bash tools/std/gate.sh slice-1b` exits 0
    - `grep -c '"name": "@pipe/autenticacao"' packages/autenticacao/package.json` prints 1 (package name untouched in this slice)
    - for every subpath-export row of packages-ai.csv with new != KEEP, `rg -n "@pipe/ai/<old>['/]" apps packages` returns nothing (executor lists the command outputs in the SUMMARY)
    - `pnpm --filter @pipe/ui test` exits 0
  </acceptance_criteria>
  <done>Slice 1b green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auth package → API/CRM | session and password primitives |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14-01 | Spoofing | packages/autenticacao session/crypto helpers renamed | mitigate | identifier-only rename through language service; autenticacao tests in gate; cookie name untouched until plan 01-19 |
| T-01-14-02 | Tampering | signed-URL logic in armazenamento | mitigate | identifier-only rename; armazenamento tests in gate; algorithm untouched |
</threat_model>

<verification>
Gate slice-1b green.
</verification>

<success_criteria>
All package internals in English per map; package names pending slice 4.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-14-SUMMARY.md` when done
</output>
