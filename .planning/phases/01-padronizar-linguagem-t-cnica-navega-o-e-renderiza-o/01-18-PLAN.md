---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 18
type: execute
wave: 13
depends_on: ["01-17"]
files_modified:
  - apps/api/**
  - apps/desk-vite/src/**
  - apps/desk-vite/tests/**
  - apps/gestao-vite/src/**
  - apps/gestao-vite/tests/**
  - apps/crm/src/**
  - apps/ponte/src/**
  - packages/contracts/src/**
  - packages/tempo-real/src/**
  - .env.example
  - infra/compose/env.prod.exemplo
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/route-drift-allow.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-2c-*
autonomous: true
requirements: [STD-02, STD-03, STD-05, STD-07, STD-10]

must_haves:
  truths:
    - "Every approved endpoint row is applied to the API decorators AND every consumer (fronts, CRM, ponte, packages, API tests, TanStack query keys) in the same plan (D-19, STD-05 coordinated cut)"
    - "servidor.ts body-limit/raw-parser path strings and fluxoDaRota regex/param/error message match the renamed routes (Pitfalls 2, 3)"
    - "Google and SSO callbacks are /v1/auth/google/callback and /v1/auth/sso/callback; GOOGLE_URL_RETORNO value points to the new callback in .env, .env.example and env.prod.exemplo (name unchanged) (D-12, D-13, D-36)"
    - "The invite link built by the API and the invite route in desk, gestão and CRM use the same new path (D-14); no PT->EN redirects exist"
    - "route-match: zero orphan consumers, zero unmatched internal path strings, guards identical; drift allowlist empty"
  artifacts:
    - path: "apps/api/src (servidor bootstrap file)"
      provides: "body limits on renamed paths"
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/route-drift-allow.csv"
      provides: "empty (header only) after this plan"
  key_links:
    - from: "API @Controller/@Get paths"
      to: "front useLeitura/api.* paths and invalidateQueries keys"
      via: "same endpoint rows applied repo-wide"
      pattern: "/v1/"
    - from: "invite URL builder in API domain (convites)"
      to: "front invite routes (desk, gestão, crm folder)"
      via: "same new invite segment"
      pattern: "invite|/convite/"
---

<objective>
Slice 2c: rename API endpoints and every path-string dependent, including auth callbacks, invite links, body-limit scoping and flow-key route parsing.

Purpose: Endpoints are string contracts; STD-05 decided a coordinated v1 cut because all consumers are Pipe-controlled, so producer and consumers must change in one atomic step. This is also where security-relevant path strings (body limits, flow-scoped API keys, OAuth callbacks) are most likely to break silently.
Output: renamed endpoints and consumers, env value update, empty drift allowlist, gate report.

Model routing (D-25): endpoint/route/param rewriting is Haiku-eligible (approved rows). Sonnet owns: `fluxoDaRota` regex + param + message edit, `servidor.ts` path review, callback/env value changes, the drift allowlist resolution, and sign-off of the route-match report.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-13-PLAN.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/nav-contract.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Slice rules: plan 01-13 context. Rows applied here: every `endpoint` row (all scopes; they live in api.csv), front-route rows for the invite route (desk-vite.csv, gestao-vite.csv, crm.csv) and any query-param rows read by BOTH API and fronts (e.g. the post-login destination param used by API sign-in handler, front `exigir-sessao.tsx` and CRM `middleware.ts`).

Facts (original paths; resolve through map after slice 2b):
- `apps/api/src/servidor.ts:75-111`: `app.use('/v1/anexos'|'/v1/contatos/importacoes'|'/v1/canais/whatsapp/:id/perfil'|'/v1/canais/whatsapp/:id/modelos'|'/v1/gestao/fluxos/:id/builder'|'/v1/gestao/contrato/certificados', <raw/large parser>)`.
- `apps/api/src/autenticacao.ts:117-157`: `fluxoDaRota` reads param `fluxoId` or regex `/\/fluxos\/:(\w+)(?=\/|$)/` over the route pattern; fails closed with 403 code `chave_de_fluxo`; message embeds `/v1/gestao/fluxos/:id/…`. Covered by `apps/api/tests/chave-de-fluxo.test.ts` and `integracoes.test.ts` (names may be renamed now).
- Callbacks: `controladores/entrar.ts:267,289` (Google `v1/auth/google/retorno`), `controladores/sso.ts:136` (`v1/auth/sso/retorno`).
- Invite link: `apps/api/src/dominio/convites.ts:72` builds `${base}/convite/${token}`; front routes `/convite/:token` in desk and gestão App.tsx; CRM folder `convite` + public-route regex `/^\/(entrar|convite)/` in `apps/crm/src/middleware.ts:15` and `apps/crm/src/componentes/estrutura-crm.tsx:69`.
- `GOOGLE_URL_RETORNO` VALUE appears in `.env` (untracked, local), `.env.example`, `infra/compose/env.prod.exemplo`; the NAME stays (D-36). The VPS env file is updated at cutover (plan 01-33).
- TanStack query keys are `['api', caminho]`; 14 `invalidateQueries` call sites use literal path prefixes (Pitfall 9).
- Webhooks `webhooks/*` and `/l/:codigo` rows are KEEP (D-15).
- API-key scope strings in `@Escopos('...')` are persisted (D-40) and must not change: record `rg -c "@Escopos\(" apps/api/src` and the sorted list of scope literals before and after.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite endpoints and all consumers; invite route; shared query params</name>
  <files>apps/api/**, apps/desk-vite/src/**, apps/desk-vite/tests/**, apps/gestao-vite/src/**, apps/gestao-vite/tests/**, apps/crm/src/**, apps/ponte/src/**, packages/contracts/src/**, packages/tempo-real/src/**</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/api.csv (kind endpoint)
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-route-consumers.csv
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/route-drift-allow.csv
    - apps/crm/src/middleware.ts, apps/crm/src/componentes/estrutura-crm.tsx
    - apps/desk-vite/src/componentes/exigir-sessao.tsx, apps/gestao-vite/src/componentes/exigir-sessao.tsx (paths resolved via map)
  </read_first>
  <action>
    1. Record the sorted list of `@Escopos(...)` literal args into the scratch dir (before).
    2. `node tools/std/rewrite-literals.ts --map STD/map --scopes api --kinds endpoint --dry-run`; inspect unmatched; apply. This rewrites decorators, `app.use` paths, all `/v1/...` consumer literals/templates repo-wide, `invalidateQueries`/`queryKey` prefixes and RegExp literals in listed consumer files.
    3. Invite route: `rewrite-literals.ts --scopes desk-vite,gestao-vite,crm --kinds front-route --ids <ids of the rows whose old is the invite route>` and `move-files.ts --scopes crm --kinds dir --ids <id of the CRM invite folder row>`; update the API invite URL builder (convites) to the same new segment; update both CRM public-route regexes identically (same alternation with the new sign-in segment only if the sign-in folder row is also applied now — keep sign-in untouched here unless its row is applied; apply the invite part only).
    4. Shared query params (post-login destination and any other param read by both API and a front, per map rows): `rewrite-literals.ts --kinds query-param` restricted to those rows across api, desk-vite, gestao-vite, crm.
    5. `pnpm typecheck` exit 0. Compare the `@Escopos` literal list with step 1: must be identical (D-40).
    Commit `refactor(01-18): rename API endpoints and consumers, invite route (coordinated v1 cut)`.
  </action>
  <verify>
    <automated>pnpm typecheck && node tools/std/route-match.ts --emit .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-2c-pre-routes.json --consumers .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-2c-pre-consumers.csv --allow .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/route-drift-allow.csv --check</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` exits 0
    - route-match `--check` exits 0 (no ORPHAN consumer outside the allowlist)
    - `@Escopos` literal list identical before/after (recorded in SUMMARY)
    - `rg -n "/convite/" apps --glob '!**/dist/**'` returns nothing if the invite row is not KEEP
    - `grep -c "webhooks/whatsapp" STD/reports/*routes*.json` unchanged vs baseline (webhooks KEEP)
  </acceptance_criteria>
  <done>Endpoints and consumers renamed atomically.</done>
</task>

<task type="auto">
  <name>Task 2: Security path strings, callbacks, env value, drift allowlist; gate</name>
  <files>apps/api/src/**, apps/api/tests/**, .env.example, infra/compose/env.prod.exemplo, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/route-drift-allow.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-2c-*</files>
  <read_first>
    - apps/api/src (servidor bootstrap file, resolved via map from servidor.ts)
    - apps/api/src (auth guard file resolved from autenticacao.ts, lines around fluxoDaRota)
    - apps/api/src (sign-in controller resolved from controladores/entrar.ts; SSO controller from sso.ts)
    - .env.example, infra/compose/env.prod.exemplo
  </read_first>
  <action>
    1. Body limits: confirm every `app.use('<path>', ...)` path in the bootstrap file equals a renamed route prefix (route-match internal strings report must be empty of unmatched). If the tool left any, fix by hand from the endpoint rows (Sonnet).
    2. `fluxoDaRota` (Sonnet): update the regex segment to the new flows segment from the endpoint rows (e.g. `/\/flows\/:(\w+)(?=\/|$)/` if `fluxos`->`flows`), update the param name it reads to the renamed param (e.g. `flowId`) — every route whose path contains the flows segment must declare that param name (grep the renamed controllers), update the human-readable 403 message path to the new path (message text stays Portuguese; only the embedded path changes). The error CODE `chave_de_fluxo` follows its map row (plan 01-19) — do not change it here. Run the flow-key tests (renamed `chave-de-fluxo.test.ts` and `integracoes.test.ts`) and confirm a key for flow A is 403 on flow B.
    3. Callbacks: confirm decorators now read `v1/auth/google/callback` and `v1/auth/sso/callback`; any string that builds the Google redirect URI from `GOOGLE_URL_RETORNO` stays keyed on that env NAME. Set the VALUE of `GOOGLE_URL_RETORNO` in `.env.example` and `infra/compose/env.prod.exemplo` (and in local `.env`, untracked) to the same host/port with path `/v1/auth/google/callback`. Update SSO documentation strings that print the redirect path.
    4. Drift allowlist: every row of `STD/route-drift-allow.csv` (baseline drift, resolve_by 01-18) is fixed (consumer path corrected to a live route or dead consumer code deleted when it has no caller — Sonnet decides and lists each in SUMMARY). Leave only the header line.
    5. `bash tools/std/gate.sh slice-2c` all PASS (route-match compare through map shows identical guards; zero orphans; zero unmatched internal strings).
    Commit `refactor(01-18): path-bound security strings, auth callbacks, env value; gate slice-2c green`.
  </action>
  <verify>
    <automated>bash tools/std/gate.sh slice-2c && test "$(wc -l < .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/route-drift-allow.csv)" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `bash tools/std/gate.sh slice-2c` exits 0
    - route-drift-allow.csv has only the header line
    - `grep -n "GOOGLE_URL_RETORNO=.*/v1/auth/google/callback" .env.example infra/compose/env.prod.exemplo` returns 2 lines
    - `rg -n "google/retorno|sso/retorno" apps packages .env.example infra` returns nothing
    - `rg -n "fluxos\\\\/" apps/api/src` returns nothing if the flows segment row is not KEEP
    - flow-key tests pass (part of gate test step)
  </acceptance_criteria>
  <done>Path-bound security behavior preserved on renamed routes; slice 2c green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → API routes | every renamed endpoint keeps its guards |
| Google/SSO IdP → callback | redirect URI must match registration exactly |
| API key → flow routes | flow-scoped keys authorized by route pattern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-18-01 | Elevation of privilege | guards after endpoint rename | mitigate | route-match compare through map = no difference in gate |
| T-01-18-02 | Elevation of privilege / DoS | fluxoDaRota regex/param mismatch | mitigate | Sonnet edit; fails closed (403); flow-key tests in gate |
| T-01-18-03 | Denial of service | body-limit paths stale -> 413 or wrong parser (Pitfall 2) | mitigate | route-match internal-string check must be empty |
| T-01-18-04 | Spoofing | OAuth redirect URI mismatch | mitigate | env value updated in all tracked files now; VPS env + Google Console at cutover (01-33) |
| T-01-18-05 | Spoofing | open redirect via destination param rename | mitigate | only param name changes; destinoAbsoluto body verified identifier-only in 01-17 |
| T-01-18-06 | Elevation of privilege | API-key scope strings altered | mitigate | `@Escopos` literal list compared before/after; must be identical (D-40) |
</threat_model>

<verification>
Gate slice-2c green; drift allowlist empty; callback/env greps pass.
</verification>

<success_criteria>
Coordinated v1 cut implemented in code with all consumers and security path strings consistent.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-18-SUMMARY.md` when done
</output>
