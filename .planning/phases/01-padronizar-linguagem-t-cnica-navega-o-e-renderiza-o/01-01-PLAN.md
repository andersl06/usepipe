---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/fluxo/gerenciador.teste.ts
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/baseline.md
autonomous: true
requirements: [STD-07, STD-11]

must_haves:
  truths:
    - "Work happens on branch std/english-rename created from limpeza (D-22)"
    - "Root `pnpm typecheck` exits 0 (D-21)"
    - "A green baseline (typecheck, test, build) with per-package test counts is recorded before any rename"
  artifacts:
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/baseline.md"
      provides: "Baseline commands, exit codes, per-package test counts, known flaky tests"
      contains: "instagram.test.ts"
    - path: "packages/core/src/fluxo/gerenciador.teste.ts"
      provides: "Type fix at line 101"
      contains: "Record<string, string>"
  key_links:
    - from: "packages/core/src/fluxo/gerenciador.teste.ts"
      to: "packages/core/src/fluxo/contexto.ts (Contexto.variaveis: Record<string,string>)"
      via: "explicit annotation"
      pattern: "const variaveis: Record<string, string>"
---

<objective>
Create the phase branch, fix the only root typecheck failure, and record the green baseline that every later slice gate is compared against (D-21, D-22).

Purpose: No slice may start with a known regression (D-20). Without a green, measured baseline, later regressions cannot be told apart from pre-existing breakage.
Output: branch `std/english-rename`, fixed `gerenciador.teste.ts`, `std/baseline.md`.

Model routing: Claude (Sonnet) executes. No Codex, no Haiku.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-CONTEXT.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-RESEARCH.md
@PROJECT-HANDOFF.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Facts (verified during planning):
- `pnpm typecheck --continue` has exactly one error: `packages/core/src/fluxo/gerenciador.teste.ts(140,22): TS2339 Property 'status' does not exist on type '{ nome: string; }'`. Line 101 reads `const variaveis = { nome: 'Ana' };`. `Contexto.variaveis` is `Record<string, string>` (`packages/core/src/fluxo/contexto.ts:167`).
- Root scripts: `pnpm typecheck` / `pnpm test` / `pnpm build` all run through turbo (`typecheck` and `test` depend on `^build`).
- Docker containers `pipe-postgres` and `pipe-redis` are required by API/db tests.
- Known flaky under full-suite load (D-21): `apps/api/tests/instagram.test.ts`, `apps/api/tests/fluxo.test.ts`, `apps/api/tests/messenger.test.ts`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create branch std/english-rename and fix the root typecheck</name>
  <files>packages/core/src/fluxo/gerenciador.teste.ts</files>
  <read_first>
    - packages/core/src/fluxo/gerenciador.teste.ts (lines 90-145)
    - packages/core/src/fluxo/contexto.ts (line 160-170, type of variaveis)
  </read_first>
  <action>
    Per D-22: confirm `git status --porcelain` is empty and the current branch is `limpeza`, then run `git switch -c std/english-rename`. If the tree is dirty or the branch is not `limpeza`, stop and report (do not stash user work).
    Per D-21: change line 101 of `packages/core/src/fluxo/gerenciador.teste.ts` from `const variaveis = { nome: 'Ana' };` to `const variaveis: Record<string, string> = { nome: 'Ana' };`. Change nothing else in the file (no renames yet: the map is not approved).
    Run `pnpm typecheck` from the repo root and confirm exit 0.
    Commit: `fix(01-01): annotate variaveis in gerenciador.teste.ts so root typecheck passes`.
  </action>
  <verify>
    <automated>git rev-parse --abbrev-ref HEAD | grep -x std/english-rename && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse --abbrev-ref HEAD` prints `std/english-rename`
    - `git merge-base --is-ancestor limpeza HEAD` exits 0
    - `grep -n "const variaveis: Record<string, string> = { nome: 'Ana' };" packages/core/src/fluxo/gerenciador.teste.ts` returns line 101
    - `pnpm typecheck` exits 0
    - `git diff limpeza --stat` lists only `packages/core/src/fluxo/gerenciador.teste.ts`
  </acceptance_criteria>
  <done>Branch exists, root typecheck green, fix committed.</done>
</task>

<task type="auto">
  <name>Task 2: Run and record the green baseline</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/baseline.md</files>
  <read_first>
    - PROJECT-HANDOFF.md (known problems section: Docker, flaky tests, MSYS_NO_PATHCONV)
    - package.json (root scripts)
    - turbo.json
  </read_first>
  <action>
    1. Check `docker ps --format '{{.Names}} {{.Status}}'` shows `pipe-postgres` and `pipe-redis` healthy; if not, run `pnpm banco:subir` and wait until healthy.
    2. Run, from repo root, capturing full logs into the scratchpad (not the repo): `pnpm typecheck`, `pnpm build`, `pnpm turbo run test --continue`. Record exit code and wall time for each.
    3. From the turbo test log, extract per-package test counts: vitest packages print `Tests  N passed` (prefixed with `@pipe/<pkg>:test:`); node:test packages (desk-vite, gestao-vite, crm) print `# pass N` / `# fail N`; `@pipe/ui` test is `node verificar-tokens.mjs` (record exit code only).
    4. If the only failures are inside the 3 known flaky files, run each isolated: `pnpm --filter @pipe/api exec vitest run tests/instagram.test.ts`, same for `tests/fluxo.test.ts` and `tests/messenger.test.ts`; they must pass isolated. Any other failing test = stop and report (the baseline is not green; do not continue the phase).
    5. Write `STD/baseline.md` with sections: `## Commit` (hash of Task 1 commit), `## Environment` (node, pnpm, docker versions), `## Commands` (table: command | exit | duration), `## Test counts per package` (table: package | runner | passed | failed | skipped), `## Known flaky (D-21)` listing the 3 files with isolated-run results, `## Notes` (any warnings).
    Commit: `docs(01-01): record green baseline`.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/baseline.md && grep -c "@pipe/" .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/baseline.md</automated>
  </verify>
  <acceptance_criteria>
    - `STD/baseline.md` exists with headings `## Commands`, `## Test counts per package`, `## Known flaky (D-21)`
    - The test-count table has one row for each of: @pipe/api, @pipe/workers, @pipe/ponte, @pipe/core, @pipe/db, @pipe/ai, @pipe/autenticacao, @pipe/armazenamento, @pipe/tempo-real, @pipe/desk-vite, @pipe/gestao-vite, @pipe/crm, @pipe/ui
    - `pnpm typecheck` and `pnpm build` rows show exit 0
    - Each of `instagram.test.ts`, `fluxo.test.ts`, `messenger.test.ts` appears with an isolated-run result of pass
  </acceptance_criteria>
  <done>Baseline recorded and committed; phase may proceed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → git history | Baseline is the reference for all regression claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Repudiation | std/baseline.md | mitigate | Record commit hash, exact commands and exit codes so later gates are comparable |
| T-01-01-02 | Tampering | branch creation | mitigate | Refuse to branch from a dirty tree or from a branch other than limpeza |
</threat_model>

<verification>
`pnpm typecheck` exits 0 on `std/english-rename`; `std/baseline.md` contains per-package counts.
</verification>

<success_criteria>
Branch created from limpeza, typecheck green, baseline recorded with test counts and flaky-test isolation results.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-01-SUMMARY.md` when done
</output>
