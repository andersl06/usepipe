---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 08
type: execute
wave: 4
depends_on: ["01-05", "01-06"]
files_modified:
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/*.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/inventory-summary.md
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/persisted.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/wire-contracts.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/exceptions.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/persisted-*.json
autonomous: true
requirements: [STD-02, STD-03, STD-06]

must_haves:
  truths:
    - "The complete candidate inventory exists for every scope (STD-02 inventory; STD-03 dependents via consumers column + route consumers report)"
    - "Every persisted PT data/contract item is in persisted.csv with its own decision and is excluded from the rename map (STD-06, D-08, D-09, D-11, D-40)"
    - "Every API endpoint is classified for wire-key impact (raw Drizzle rows, mapped DTO, jsonb) per D-09"
    - "No row in the map has persisted=unknown after this plan"
  artifacts:
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/persisted.csv"
      provides: "STD-06 inventory"
      contains: "conversas:ler"
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/wire-contracts.csv"
      provides: "D-09 per-endpoint classification"
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/inventory-summary.md"
      provides: "counts per scope x kind"
  key_links:
    - from: "std/exceptions.csv"
      to: "std/persisted.csv"
      via: "B rows referencing persisted ids"
      pattern: ",B,"
---

<objective>
Run the deterministic inventory, then classify persisted items and wire contracts (STD-06, D-09, D-11, D-40) so the map never contains a persisted name.

Purpose: STD-02 requires a complete inventory before any rename; STD-06 requires persisted items to be listed with their own decision and kept out of the mechanical rename.
Output: `std/map/*.csv` (status=candidate), `std/comments/*.csv`, `std/persisted.csv`, `std/wire-contracts.csv`, updated `std/exceptions.csv`.

Model routing (D-23/D-24): inventory run = Claude (mechanical). Persisted classification: Codex account 1 for scopes packages-db, packages-contracts, packages-autenticacao, packages-armazenamento, packages-tempo-real, packages-mcp, workers, api, infra; Codex account 2 for packages-core, packages-ai, packages-ui, desk-vite, gestao-vite, crm, ponte, site, css. Sonnet reviews every persisted=yes row, every unknown left, and all D-11 decisions.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-CONTEXT.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-RESEARCH.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-05-SUMMARY.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-06-SUMMARY.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Known persisted items (RESEARCH Runtime State Inventory) that MUST end in persisted.csv: SQL table/column names (D-08, represented by the `sql=` notes, never map rows); values read/written to `erro_codigo` (conversas.ts:359), `ultimo_erro` (automacao.ts:705, conversas.ts:414) incl. delivery codes like `parametros_perdidos`; API-key scopes in `chave_api.escopos` used by `@Escopos(...)` (`conversas:ler`, `contatos:escrever`, ...) (D-40); `permissao.codigo` seeded rows; `importacao.estado`; ticket/conversation status literals and flow block types (D-11); jsonb keys (D-09); API key token format `pipe_<prefixo>_<segredo>`; localStorage `desk.pref.*`, CRM `CHAVE`/`CHAVE_TEMA`/`visoes` keys (browser-persisted: decide keep vs rename-with-reset).
NOT persisted by owner decision (they rename): cookie `pipe_sessao` and Prometheus metric names (D-38); BullMQ queue names, job payloads, WS events, non-stored error codes (D-10); `pipe_desafio` OAuth cookie (short-lived).

persisted.csv columns: `id,scope,kind,old,declared_at,where_persisted,decision,decision_ref,notes`; `decision` in `keep` (stays PT, category B), `keep-literal` (D-11: PT literal stays as data under an English TS name), `edge-translation` (D-11: English in code, translated at the DB boundary — only when Sonnet finds a single boundary function; otherwise keep-literal), `migrate-later` (deferred per CONTEXT Deferred Ideas).
wire-contracts.csv columns: `endpoint,method,response_source,keys_affected,decision,decision_ref,notes`; `response_source` in raw-rows|mapped|jsonb|none; `decision` in `rename-with-db-key` (wire key follows the Drizzle TS key rename in the same slice as its consumers), `keep` (API maps back to the old key), `persisted` (jsonb/stored -> persisted.csv).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the inventory and review totals</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/*.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/inventory-summary.md</files>
  <read_first>
    - tools/std/inventory.ts (CLI)
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-RESEARCH.md (Sizing table, to sanity-check totals)
  </read_first>
  <action>
    Run `node tools/std/inventory.ts --out STD`. Compare totals in `STD/inventory-summary.md` with RESEARCH Sizing: endpoints ~207 method routes (PT subset), front routes (Gestão ~75 unique segments, Desk 11, CRM 21 pages), queues 10, scheduler ids 7, error codes ~108, test titles (PT subset of 1,747), CSS ~3,025 selectors + 482 vars (PT subset). If any category is 0 or wildly off (>2x or <0.3x of the research figure where research counted PT items), fix the extractor position list in `tools/std/inventory.ts` (and its test) before continuing. Add a `## Sanity check` section to inventory-summary.md with research figure vs extracted figure per category.
    Confirm every endpoint in `STD/reports/baseline-routes.json` whose path has a PT segment has a map row (endpoint rows are the STD-02 route inventory; `STD/reports/baseline-route-consumers.csv` is the STD-03 consumer list).
    Commit: `docs(01-08): run technical inventory (candidate rows per scope)`.
  </action>
  <verify>
    <automated>ls .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv | wc -l && grep -c "Sanity check" .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/inventory-summary.md</automated>
  </verify>
  <acceptance_criteria>
    - `ls STD/map/*.csv | wc -l` prints 18
    - `ls STD/comments/*.csv | wc -l` >= 15
    - every map CSV header equals `id,scope,slice,kind,old,new,declared_at,consumers,persisted,category,decision_ref,status,owner,notes`
    - inventory-summary.md has a `## Sanity check` table with no category at 0 extracted
    - `grep -c ",endpoint," STD/map/api.csv` >= 100
  </acceptance_criteria>
  <done>Full candidate inventory committed.</done>
</task>

<task type="auto">
  <name>Task 2: Persisted and wire-contract classification (Codex 1 + Codex 2, Sonnet review)</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/persisted.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/wire-contracts.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/persisted-*.json, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/exceptions.csv</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/TEMPLATE-persisted.md
    - packages/db/src/schema/ (all files: pgEnum values, $type unions, jsonb columns)
    - apps/api/src/autenticacao.ts (Escopos, fluxoDaRota)
    - apps/api/src/controladores/ (one controller returning rows directly, found via grep for `return await db.select` / `.returning()`)
  </read_first>
  <action>
    1. Chunk rows with `persisted=unknown` plus all ts-prop rows whose notes contain `jsonb`: `node tools/std/codex-prompt.ts --template STD/prompts/TEMPLATE-persisted.md --rows STD/map --filter persisted=unknown --chunk 300 --out-dir STD/prompts/persisted-codex1` restricted to the account-1 scopes listed in the objective, and the same to `STD/prompts/persisted-codex2` for account-2 scopes. Run each chunk: `bash tools/std/codex-run.sh 1 tools/std/schemas/map-rows.schema.json <chunk> STD/out/persisted-codex1-NNN.json` (account 2 likewise). Merge: `node tools/std/merge-proposals.ts --in "STD/out/persisted-codex1-*.json" --target STD/map --owner codex1 --persisted-only` and the same for codex2 (rows stay `candidate`; names are proposed in plans 01-10/01-11; persisted=yes rows move to persisted.csv).
    2. Persisted values not visible as identifiers (D-11): Codex account 1 reads `packages/db/src/schema/**` (pgEnum values, `$type<...>` unions, check constraints) and code comparisons to status/type columns, and outputs rows kind `persisted-value` that are appended directly to `STD/persisted.csv` (Sonnet writes them from the JSON; they never enter the map).
    3. Wire contracts (D-09): Codex account 1 lists, for every endpoint row, whether the handler returns raw Drizzle rows (`select()` results, `.returning()`), a mapped object, or jsonb content; Sonnet writes `STD/wire-contracts.csv`. Default decision: `rename-with-db-key` for raw-rows endpoints whose keys come from Drizzle TS keys (all consumers are Pipe-controlled, STD-05 coordinated cut); `persisted` for any key read from/written to jsonb; `keep` only when a consumer outside the repo exists (none expected).
    4. Sonnet review (D-24): every persisted=yes row, every D-11 decision, every cookie/storage-key/metric/queue/job-name/ws-event/error-code row. Enforce the owner decisions listed in context (D-38 cookie+metrics rename -> persisted=no; D-40 scopes + stored codes -> persisted=yes, decision keep; D-10 queues/jobs/events/non-stored codes -> persisted=no). Choose `edge-translation` only when a single DB-boundary function exists; otherwise `keep-literal`.
    5. For every persisted.csv row add an exceptions.csv row: glob = file glob of its declaring/consuming files (or `**/*`), pattern = `^<old>$` escaped, kind = its kind, category B, justification = `persisted: <where_persisted>`, ref = `persisted.csv:<id>`.
    6. Run `node tools/std/check-map.ts --map STD/map --scopes all --require-status candidate` and fix any row still `persisted=unknown`.
    Commit: `docs(01-08): persisted inventory (STD-06) and wire-contract classification (D-09)`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p='.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/';let n=0;for(const f of fs.readdirSync(p))n+=(fs.readFileSync(p+f,'utf8').match(/,unknown,/g)||[]).length;if(n)process.exit(1);console.log('unknown=0')" && grep -c "conversas:ler" .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/persisted.csv</automated>
  </verify>
  <acceptance_criteria>
    - no map CSV row has `persisted` = `unknown`
    - persisted.csv header equals `id,scope,kind,old,declared_at,where_persisted,decision,decision_ref,notes`; every row has a non-empty `decision` in keep|keep-literal|edge-translation|migrate-later
    - persisted.csv contains `conversas:ler` with decision `keep` and ref `D-40`
    - persisted.csv contains rows for `erro_codigo`/`ultimo_erro` stored codes (where_persisted mentions the column)
    - `grep -c "pipe_sessao" STD/persisted.csv` prints 0 (D-38: it renames)
    - wire-contracts.csv has one row per endpoint row in STD/map/api.csv (counts equal) and no empty `decision`
    - exceptions.csv has one `,B,` row per persisted.csv data row
    - codex-log-1.csv and codex-log-2.csv show calls for this plan, all exit 0
  </acceptance_criteria>
  <done>Persisted inventory and wire classification complete; map has no unknown persistence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code → DB / Redis / browser / external registrations | persisted names cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08-01 | Elevation of privilege | API-key scope strings (`conversas:ler`) | mitigate | classified persisted (D-40), excluded from map; check-map errors on any persisted row in map; exceptions B row |
| T-01-08-02 | Tampering | stored error codes renamed in code but not in DB | mitigate | codes written to `erro_codigo`/`ultimo_erro` are persisted rows; only non-stored codes rename |
| T-01-08-03 | Repudiation | Codex misclassifies persistence | mitigate | Sonnet reviews every persisted/cookie/storage/metric/queue/code row (D-24) |
</threat_model>

<verification>
18 map CSVs, 0 unknown persistence, persisted.csv and wire-contracts.csv complete, exceptions B rows added.
</verification>

<success_criteria>
Inventory complete and persisted items isolated with decisions, ready for glossary and map.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-08-SUMMARY.md` when done
</output>
