---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 08
type: execute
wave: 5
depends_on: ["01-05", "01-38"]
files_modified:
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/*.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/inventory-summary.md
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/front-route-dependents.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/jsonb-reach.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/persisted.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/exceptions.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/persisted-*.json
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/jsonb-*.json
autonomous: true
requirements: [STD-02, STD-03, STD-06, STD-10]

must_haves:
  truths:
    - "The complete candidate inventory exists for every scope (STD-02); every front route has its file:line dependents listed (STD-03) and every endpoint its consumer list"
    - "Every persisted PT data/contract item is in persisted.csv with its own decision and is excluded from the rename map (STD-06, D-08, D-09, D-11, D-40)"
    - "Every property and literal member of a type that reaches a jsonb column is classified with evidence; by default it is persisted (keep) and never enters the rename map"
    - "Non-persisted string-literal union members (literal-value) and data-* attributes (data-attr) are classified like any other candidate (W6)"
    - "No map row has persisted=unknown after this plan"
  artifacts:
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/persisted.csv"
      provides: "STD-06 inventory"
      contains: "conversas:ler"
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/front-route-dependents.csv"
      provides: "STD-03 per-route dependents"
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/inventory-summary.md"
      provides: "counts per scope x kind"
  key_links:
    - from: "std/exceptions.csv"
      to: "std/persisted.csv"
      via: "B rows referencing persisted ids"
      pattern: ",B,"
    - from: "std/reports/jsonb-reach.csv"
      to: "std/persisted.csv"
      via: "jsonb-reached props classified keep"
      pattern: "jsonb:"
---

<objective>
Run the deterministic inventory, then classify persisted items — including every jsonb-reached property and literal — so the map never contains a persisted name (STD-06, D-09, D-11, D-40). Wire-contract classification (D-09 per endpoint) is plan 01-35.

Purpose: STD-02 requires a complete inventory before any rename; STD-06 requires persisted items to be listed with their own decision and kept out of the mechanical rename. jsonb keys are the largest hidden persisted surface.
Output: `std/map/*.csv` (status=candidate), `std/comments/*.csv`, `std/reports/front-route-dependents.csv`, `std/reports/jsonb-reach.csv`, `std/persisted.csv`, updated `std/exceptions.csv`.

Model routing (D-23/D-24): inventory run = Claude (mechanical). Persisted classification of non-jsonb rows: Codex account 1 for scopes packages-db, packages-contracts, packages-autenticacao, packages-armazenamento, packages-tempo-real, packages-mcp, workers, api, infra; Codex account 2 for packages-core, packages-ai, packages-ui, desk-vite, gestao-vite, crm, ponte, site, css. jsonb-reached rows: Codex account 1 (all scopes; one coherent persistence question). Sonnet reviews as listed per task.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-CONTEXT.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-05-SUMMARY.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-06-SUMMARY.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-38-SUMMARY.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Codex calls go only through `tools/std/codex-run.sh` (disposable worktree of HEAD): commit the map CSVs and reports produced by Task 1 before any Codex call.

Known persisted items (RESEARCH Runtime State Inventory) that MUST end in persisted.csv: SQL table/column names (D-08, represented by the `sql=` notes, never map rows); values read/written to `erro_codigo` (conversas.ts:359), `ultimo_erro` (automacao.ts:705, conversas.ts:414) incl. delivery codes like `parametros_perdidos`; API-key scopes in `chave_api.escopos` used by `@Escopos(...)` (`conversas:ler`, `contatos:escrever`, ...) (D-40); `permissao.codigo` seeded rows; `importacao.estado`; ticket/conversation status literals and flow block types (D-11); jsonb keys and jsonb-stored literal values (D-09; rows with notes `jsonb:`); API key token format `pipe_<prefixo>_<segredo>`; localStorage `desk.pref.*`, CRM `CHAVE`/`CHAVE_TEMA`/`visoes` keys (browser-persisted: decide keep vs rename-with-reset).
NOT persisted by owner decision (they rename): cookie `pipe_sessao` and Prometheus metric names (D-38); BullMQ queue names, job payloads, WS events, non-stored error codes (D-10); `pipe_desafio` OAuth cookie (short-lived). Note: BullMQ job payload keys are NOT jsonb, but the outbox row payload IS jsonb (persisted) — the D-10 sweep replays it.

persisted.csv columns: `id,scope,kind,old,declared_at,where_persisted,decision,decision_ref,notes`; `decision` in `keep` (stays PT, category B), `keep-literal` (D-11: PT literal stays as data under an English TS name), `edge-translation` (D-11: English in code, translated at the DB boundary — only when Sonnet finds a single boundary function; otherwise keep-literal), `migrate-later` (deferred per CONTEXT Deferred Ideas). For jsonb-reached props the only allowed decisions are `keep` or `migrate-later` (renaming a stored key needs a data migration, out of scope).
Classification rule for the W6 kinds: `literal-value` rows are persisted when the literal is written to a DB column, jsonb document, Redis, browser storage or an external system (then `keep-literal` or `keep`), otherwise persisted=no and they get a name in plans 01-10/01-11; `data-attr` rows are persisted=no unless read from storage/URL by name (rare; then `keep`).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the inventory, review totals and route dependents</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/*.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/inventory-summary.md, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/front-route-dependents.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/jsonb-reach.csv</files>
  <read_first>
    - tools/std/inventory.ts (CLI)
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-RESEARCH.md (Sizing table, to sanity-check totals)
  </read_first>
  <action>
    Run `node tools/std/inventory.ts --out STD`. Compare totals in `STD/inventory-summary.md` with RESEARCH Sizing: endpoints ~207 method routes (PT subset), front routes (Gestão ~75 unique segments, Desk 11, CRM 21 pages), queues 10, scheduler ids 7, error codes ~108, test titles (PT subset of 1,747), CSS ~3,025 selectors + 482 vars (PT subset). If any category is 0 or wildly off (>2x or <0.3x of the research figure where research counted PT items), fix the extractor position list in `tools/std/inventory.ts` (and its test) before continuing. Add a `## Sanity check` section to inventory-summary.md with research figure vs extracted figure per category, plus `literal-value`, `data-attr` and jsonb-reach counts per column.
    Confirm every endpoint in `STD/reports/baseline-routes.json` whose path has a PT segment has a map row (endpoint rows are the STD-02 route inventory; `STD/reports/baseline-route-consumers.csv` is the STD-03 endpoint consumer list). Confirm every front-route row has at least one row in `STD/reports/front-route-dependents.csv`; a route with zero dependents gets `notes` `dependents: declaration-only` after a manual `rg` confirms it (STD-03 per-route list, W5).
    Commit: `docs(01-08): run technical inventory (candidate rows per scope, route dependents, jsonb reach)`.
  </action>
  <verify>
    <automated>ls .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv | wc -l && grep -c "Sanity check" .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/inventory-summary.md && test -s .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/front-route-dependents.csv</automated>
  </verify>
  <acceptance_criteria>
    - `ls STD/map/*.csv | wc -l` prints 18
    - `ls STD/comments/*.csv | wc -l` >= 15
    - every map CSV header equals `id,scope,slice,kind,old,new,declared_at,consumers,persisted,category,decision_ref,status,owner,notes`
    - inventory-summary.md has a `## Sanity check` table with no category at 0 extracted
    - `grep -c ",endpoint," STD/map/api.csv` >= 100
    - front-route-dependents.csv header is `route_row_id,app,route,file,line,kind,snippet`; every front-route row id appears in it or has notes `dependents: declaration-only`
    - `STD/reports/jsonb-reach.csv` exists and every row of it corresponds to a map row with notes containing `jsonb:` and persisted=unknown
  </acceptance_criteria>
  <done>Full candidate inventory committed with per-route dependents and jsonb reach.</done>
</task>

<task type="auto">
  <name>Task 2: Persisted classification of non-jsonb rows (Codex 1 + Codex 2, Sonnet review)</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/persisted.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/persisted-*.json, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/TEMPLATE-persisted.md
    - packages/db/src/schema/ (pgEnum values, check constraints via listaCheck, status/estado columns)
    - apps/api/src/autenticacao.ts (Escopos, fluxoDaRota)
  </read_first>
  <action>
    1. Chunk rows with `persisted=unknown` whose notes do NOT contain `jsonb:` (kinds error-code, storage-key, cookie, ws-event, job-name, queue, wire-key, literal-value): `node tools/std/codex-prompt.ts --template STD/prompts/TEMPLATE-persisted.md --rows STD/map --filter persisted=unknown --chunk 300 --out-dir STD/prompts/persisted-codex1` restricted to the account-1 scopes listed in the objective and excluding `jsonb:` rows, and the same to `STD/prompts/persisted-codex2` for account-2 scopes. Run each chunk: `bash tools/std/codex-run.sh 1 tools/std/schemas/map-rows.schema.json <chunk> STD/out/persisted-codex1-NNN.json` (account 2 likewise). Merge: `node tools/std/merge-proposals.ts --in "STD/out/persisted-codex1-*.json" --target STD/map --owner codex1 --persisted-only` and the same for codex2 (rows stay `candidate`; names are proposed in plans 01-10/01-11/01-36; persisted=yes rows move to persisted.csv).
    2. Persisted values not visible as identifiers (D-11): Codex account 1 reads `packages/db/src/schema/**` (pgEnum values, `$type<...>` unions, `listaCheck` constants, check constraints) and code comparisons to status/type columns, and outputs rows kind `persisted-value` that Sonnet appends directly to `STD/persisted.csv` (they never enter the map).
    3. Sonnet review (D-24), bounded to: every persisted=yes row from step 1-2, every D-11 decision, every cookie/storage-key/metric/queue/job-name/ws-event/error-code row. Enforce the owner decisions listed in context (D-38 cookie+metrics rename -> persisted=no; D-40 scopes + stored codes -> persisted=yes, decision keep; D-10 queues/jobs/events/non-stored codes -> persisted=no). Choose `edge-translation` only when a single DB-boundary function exists; otherwise `keep-literal`. literal-value rows: 10% seeded sample (`check-map --sample 0.1 --seed 2`) plus every row Codex marked persisted=no whose literal also appears in `packages/db/src/schema/**`.
    Commit: `docs(01-08): persisted classification of non-jsonb rows (STD-06)`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p='.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/';let n=0;for(const f of fs.readdirSync(p))for(const l of fs.readFileSync(p+f,'utf8').split('\n'))if(/,unknown,/.test(l)&&!/jsonb:/.test(l))n++;if(n)process.exit(1);console.log('non-jsonb unknown=0')" && grep -c "conversas:ler" .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/persisted.csv</automated>
  </verify>
  <acceptance_criteria>
    - no map row without `jsonb:` in notes has `persisted` = `unknown`
    - persisted.csv header equals `id,scope,kind,old,declared_at,where_persisted,decision,decision_ref,notes`; every row has a non-empty `decision` in keep|keep-literal|edge-translation|migrate-later
    - persisted.csv contains `conversas:ler` with decision `keep` and ref `D-40`
    - persisted.csv contains rows for `erro_codigo`/`ultimo_erro` stored codes (where_persisted mentions the column)
    - `grep -c "pipe_sessao" STD/persisted.csv` prints 0 (D-38: it renames)
    - `STD/reports/codex-log-1-persisted-codex1.csv` and `STD/reports/codex-log-2-persisted-codex2.csv` show calls for this task, all exit 0
  </acceptance_criteria>
  <done>Non-jsonb persistence classified.</done>
</task>

<task type="auto">
  <name>Task 3: jsonb-reached rows classification, exceptions B rows, zero unknown</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/persisted.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/jsonb-*.json, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/exceptions.csv</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/jsonb-reach.csv
    - apps/api/tests/fixtures/jsonb/manifest.json (real stored documents from plan 01-34)
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/baseline-jsonb-keys.json
  </read_first>
  <action>
    1. Pre-classify mechanically (no LLM): every `jsonb:` row whose `old` occurs as a key or string value in any fixture document of `apps/api/tests/fixtures/jsonb/*.json` is persisted=yes, decision `keep`, decision_ref D-09 (or D-11 for literal members), `where_persisted` = the `jsonb:` columns, notes += `fixture-evidence:<file>`. Merge those with a small node one-liner through `readMap`/`writeMap` + append to persisted.csv (they leave the map).
    2. Remaining `jsonb:` rows: chunk with `--filter persisted=unknown` restricted to notes containing `jsonb:` into `STD/prompts/jsonb-codex1`, run on account 1 (`STD/out/jsonb-codex1-NNN.json`), merge with `--persisted-only --owner codex1`. TEMPLATE-persisted already makes persisted=yes the default for `jsonb:` rows.
    3. Sonnet reviews 100% of `jsonb:` rows that ended persisted=no (each must cite the mapping function that strips the property before the insert, or it is flipped back to yes) and a 10% seeded sample of the yes rows. Record counts (reached, fixture-evidence yes, Codex yes, Codex no, Sonnet flips) in the SUMMARY.
    4. For every persisted.csv row add exceptions.csv B rows restricted to the files where the persisted name legitimately lives: one row per file among (a) the declaring file (`declared_at` path) and (b) the files that write or read the persisted value — for jsonb rows the insert-values/update-set/read-cast sites recorded for that name in `STD/reports/jsonb-reach.csv`, for stored codes/scopes/storage keys/literals the files that write or read the persisted column or key (cite them in `where_persisted`). Row: glob = that exact repo-relative file path (inventory-time path; scan-pt matches inventory-time paths through applied file/dir rows, plan 01-02) — never `**/*`, `**` or a directory-wide glob; pattern = `^<old>$` escaped; kind = the scanner kind at that position (never `*`); category B; justification = `persisted: <where_persisted>`; ref = `persisted.csv:<id>`. Common PT tokens (`conteudo`, `tipo`, `entrada`, `estado`, ...) therefore stay unclassified everywhere else and must be renamed or classified on their own; plan 01-31 audits B rows whose pattern matches outside their files.
    5. Run `node tools/std/check-map.ts --map STD/map --scopes all --require-status candidate` and fix any row still `persisted=unknown`.
    Commit: `docs(01-08): jsonb persisted classification and B exceptions (STD-06)`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p='.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/';let n=0;for(const f of fs.readdirSync(p))n+=(fs.readFileSync(p+f,'utf8').match(/,unknown,/g)||[]).length;if(n)process.exit(1);console.log('unknown=0')" && node tools/std/check-map.ts --map .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map --scopes all --require-status candidate</automated>
  </verify>
  <acceptance_criteria>
    - no map CSV row has `persisted` = `unknown`
    - every map row with notes `jsonb:` and persisted=no has notes containing `strip-evidence:` and `reviewed:sonnet`
    - every key of the flow and execution fixtures that is in `baseline-jsonb-keys.json` `present` is either absent from the map or on a row with persisted=no + strip-evidence (i.e. no stored key can be renamed)
    - persisted.csv contains the members `entrada`, `conteudo`, `saida` of `ListaDeAcoesSuspensa` (or cites fixture/strip evidence for each) and props of `Contexto`
    - every persisted.csv data row has at least one `,B,` exceptions row with ref `persisted.csv:<id>`; no B row with a `persisted.csv:` ref has a glob containing `*` or kind `*`
    - `STD/reports/codex-log-1-jsonb-codex1.csv` shows the jsonb calls, all exit 0
  </acceptance_criteria>
  <done>Persisted inventory complete including jsonb shapes; map has no unknown persistence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code → DB / Redis / browser / external registrations | persisted names cross here |
| code → jsonb documents | property names are stored keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08-01 | Elevation of privilege | API-key scope strings (`conversas:ler`) | mitigate | classified persisted (D-40), excluded from map; check-map errors on any persisted row in map; exceptions B row |
| T-01-08-02 | Tampering | stored error codes renamed in code but not in DB | mitigate | codes written to `erro_codigo`/`ultimo_erro` are persisted rows; only non-stored codes rename |
| T-01-08-03 | Repudiation | Codex misclassifies persistence | mitigate | Sonnet reviews every persisted/cookie/storage/metric/queue/code row and 100% of jsonb rows marked not persisted (D-24) |
| T-01-08-04 | Tampering | jsonb keys renamed (stored flows, executions, outbox, audit, CRM) | mitigate | fixture evidence pre-classifies keys as keep; default yes for jsonb rows; strip evidence required for no; jsonb gate checks from plan 01-34 |
</threat_model>

<verification>
18 map CSVs, route dependents listed, 0 unknown persistence, persisted.csv complete incl. jsonb shapes, exceptions B rows added.
</verification>

<success_criteria>
Inventory complete and persisted items (incl. jsonb shapes) isolated with decisions, ready for glossary, wire contracts and map.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-08-SUMMARY.md` when done
</output>
