---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 13
type: execute
wave: 8
depends_on: ["01-12"]
files_modified:
  - packages/core/**
  - packages/db/src/**
  - packages/db/tests/**
  - apps/*/src/**
  - apps/*/tests/**
  - packages/*/src/**
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-core.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-db.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-1a-*
autonomous: true
requirements: [STD-06, STD-07, STD-10]

must_haves:
  truths:
    - "All approved packages-core and packages-db rows are applied (symbols, props, locals, files, dirs, subpath exports, test titles) with every consumer updated in the same plan (D-19)"
    - "Drizzle TS names are English while SQL table/column names are byte-identical (D-08): sorted drizzle-kit export diff is empty"
    - "Wire keys that follow renamed Drizzle keys are renamed in every consumer in the same plan (D-09, wire-contracts.csv rename-with-db-key)"
    - "Core co-located tests still run: *.teste.ts -> *.test.ts with vitest include and tsconfig exclude updated; test counts >= baseline"
    - "No property name or literal member that reaches a jsonb column is renamed: jsonb-keys check and the jsonb-compat test (plan 01-34, goldens recorded pre-rename) pass after the symbol rename and in the gate (STD-06)"
    - "Non-persisted literal-value rows of these scopes are applied; persisted ones never appear in the map"
    - "Slice gate passes"
  artifacts:
    - path: "packages/core/vitest.config.ts"
      provides: "include pattern for renamed tests"
      contains: ".test.ts"
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-1a-gate.md"
      provides: "gate result"
  key_links:
    - from: "packages/core/package.json exports"
      to: "'@pipe/core/<subpath>' specifiers in all apps"
      via: "subpath-export rows applied together"
      pattern: "@pipe/core/"
---

<objective>
Slice 1a (D-19 step 1): apply the approved map for `@pipe/core` and `@pipe/db`, including every consumer, and pass the slice gate.

Purpose: Bottom-up order keeps each step atomic: foundation packages first, their consumers updated in the same change.
Output: renamed core/db sources and consumers, map rows at status applied, `std/reports/slice-1a-gate.md`.

Model routing (D-25): Haiku-eligible — every edit comes from approved rows via the tools. The executor must NOT invent names. Escalate to Sonnet when a tool reports missing/unmatched rows, when a test fails for a reason other than a missed map row, or when a PT identifier has no row. If a missing row needs a name derivable from the approved glossary, Sonnet adds it as `status=approved`, `owner=sonnet`, notes `post-gate2-derived`, and lists it in the SUMMARY; if it needs a new domain term, stop and ask the owner.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-12-SUMMARY.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Slice rules (apply to every slice plan 01-13..01-31):
- Order inside a plan: symbols (`rename-symbols.ts`) -> files/dirs (`move-files.ts`) -> string contracts (`rewrite-literals.ts`) -> manual leftovers from tool reports -> gate. Symbols first because `declared_at` paths are pre-move.
- Never use ts-morph `move()`, sed, or global text replace on PT words.
- After any tool run: `git status --short | head` sanity check, then `pnpm typecheck` before continuing.
- `files_modified` uses globs because consumers are found by the language service; the authoritative list of edits is the diff.
- jsonb guard (STD-06, applies to every slice plan): props/literals traced to jsonb columns are persisted (plan 01-08) and absent from the map; after every `rename-symbols` run execute `node tools/std/jsonb-keys.ts --check` and `pnpm --filter @pipe/api exec vitest run tests/jsonb-compat.test.ts` (resolve the test path via the map once apps/api tests are renamed); a failure means a stored key was renamed: revert that row's edits, set the row `new=KEEP` with notes `jsonb-guard`, add it to persisted.csv + B exception rows in the plan 01-08 step 4 format (exact-file globs; Sonnet), list it in the SUMMARY. Never re-record goldens to make this pass.
- `literal-value` rows (W6) of the plan's scopes are applied in the rewrite-literals step of every slice plan.
- Wire keys (D-09): `wire-key` rows are applied only by `rewrite-literals.ts --kinds wire-key` (untyped accesses in the row's `consumers`); `rename-symbols.ts` never takes `wire-key` (typed consumers follow the Drizzle/contract ts-prop rows). Owners: packages-db wire-key rows -> plan 01-13; api and packages-contracts wire-key rows -> plan 01-19 only (plan 01-14 does not apply them).
- Commit normally at the end of each task (`refactor(01-NN): ...`). Slice boundaries are git tags `std-slice-N-end`; plan 01-33 squashes each tag range into one commit per slice before merging into `limpeza` (D-22 "um commit por fatia"; per-plan history is kept only if the owner explicitly amends D-22 at the 01-33 checkpoint).

Facts:
- `@pipe/core` exports: `.`, `./metricas`, `./esforco`, `./score`, `./distribuicao`, `./sla`, `./conversa`, `./janela`, `./analise`. Consumers: db, api, workers, ponte, desk-vite, gestao-vite, crm.
- `packages/core/vitest.config.ts` include `src/**/*.teste.ts`; `packages/core/tsconfig.json` excludes `src/**/*.teste.ts`; core has `tsconfig.testes.json` used by its typecheck script (`tsc -p tsconfig.json --noEmit && tsc -p tsconfig.testes.json`).
- `packages/db` scripts `gerar`/`gerar:custom` (drizzle-kit generate) must NOT be run. Renaming these script keys happens in slice 4 (plan 01-30).
- Every column in `packages/db/src/schema` has an explicit SQL name (RESEARCH), so TS key renames do not change SQL.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply packages-core and packages-db symbol, prop, local and wire-key rows</name>
  <files>packages/core/src/**, packages/db/src/**, packages/db/tests/**, apps/*/src/**, apps/*/tests/**, packages/*/src/**, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-core.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-db.csv</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-core.csv
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/packages-db.csv
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/wire-contracts.csv
    - packages/db/src/schema/identidade.ts (TS key vs SQL name pattern)
  </read_first>
  <action>
    1. `node tools/std/rename-symbols.ts --map STD/map --scopes packages-core,packages-db --kinds symbol,ts-prop,ts-local --dry-run` -> missing must be 0; then run without `--dry-run`.
    2. jsonb guard (see Slice rules): `node tools/std/jsonb-keys.ts --check` and `pnpm --filter @pipe/api exec vitest run tests/jsonb-compat.test.ts` must pass; packages-db and packages-core hold most jsonb-reached types (flow engine `Contexto`, blocks, `ListaDeAcoesSuspensa`), so any failure here is handled as the rule says before continuing. `bash tools/std/ddl-snapshot.sh check` -> must print `SQL unchanged`. If it fails, the diff shows which TS key leaked into SQL (unnamed constraint or implicit name): revert that row (`git checkout -- <file>`), set the row `new=KEEP` with notes `ddl-leak` (Sonnet decision) OR add an explicit SQL `name:` equal to the old derived name — Sonnet chooses, recorded in SUMMARY — and rerun.
    3. Wire keys (D-09): typed consumers were already renamed in step 1 through the Drizzle ts-prop rows; run `node tools/std/rewrite-literals.ts --map STD/map --scopes packages-db --kinds wire-key --dry-run`, then without `--dry-run`, for untyped (`any`/`unknown`) accesses in the listed consumer files (API tests `res.body.<key>`, front code reading JSON); explain unmatched rows in the SUMMARY (key only read through typed code). SUMMARY note (required): wire keys renamed here reach the fronts' own typed interfaces only in slices 3a/3c (plans 01-21/01-23, whose ts-prop rows carry the same new names by check-map rule 10); until then front code still compiles against its old interface names while the API emits the new key — a runtime-only mismatch that never reaches users because D-20 deploys once, after every slice (plan 01-33). List the front files concerned (from the rows' `consumers`).
    4. `pnpm typecheck` must exit 0. Commit `refactor(01-13): rename core/db symbols and wire keys per approved map`.
  </action>
  <verify>
    <automated>bash tools/std/ddl-snapshot.sh check && node tools/std/jsonb-keys.ts --check && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - rename-symbols output shows `missing=0`
    - `bash tools/std/ddl-snapshot.sh check` prints `SQL unchanged`
    - `node tools/std/jsonb-keys.ts --check` exits 0 and the jsonb-compat test passes with `git diff std-slice-0-end -- apps/api/tests/fixtures/jsonb` empty (goldens untouched)
    - `pnpm typecheck` exits 0
    - no approved row of kinds symbol/ts-prop/ts-local/wire-key in packages-core.csv or packages-db.csv remains at status approved (all applied, or KEEP/REMOVE/STATE)
  </acceptance_criteria>
  <done>Core/db identifiers renamed; SQL untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Move core/db files and dirs, subpath exports, test files and titles; run slice gate</name>
  <files>packages/core/**, packages/db/src/**, packages/db/tests/**, apps/*/src/**, apps/*/tests/**, packages/*/src/**, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-1a-*</files>
  <read_first>
    - packages/core/package.json (exports), packages/core/vitest.config.ts, packages/core/tsconfig.json, packages/core/tsconfig.testes.json
    - packages/db/package.json (exports `.` and `./schema`), packages/db/vitest.config.ts, apps/api/vitest.config.ts (globalSetup into packages/db/tests)
  </read_first>
  <action>
    1. `node tools/std/move-files.ts --map STD/map --scopes packages-core,packages-db --kinds file,dir --dry-run`, then without `--dry-run`. This includes core `*.teste.ts` -> `*.test.ts`. Then set `packages/core/vitest.config.ts` include to the renamed pattern (`src/**/*.test.ts`), `packages/core/tsconfig.json` exclude to `src/**/*.test.ts`, and `tsconfig.testes.json` include accordingly (if move-files did not already rewrite them — check the diff).
    2. `node tools/std/rewrite-literals.ts --map STD/map --scopes packages-core,packages-db --kinds subpath-export,test-title,error-code,query-param,literal-value --dry-run`, then apply. Confirm every `'@pipe/core/<old subpath>'` specifier is gone: `rg -n "@pipe/core/(metricas|esforco|distribuicao|conversa|janela|analise)['/]" apps packages` returns nothing (for subpaths whose rows are not KEEP).
    3. Handle `STD/reports/move-leftovers-*.txt` and `rewrite-unmatched-*.csv` entries (Dockerfiles/docs referencing moved paths: fix technical references in Dockerfiles and config now; docs are handled in slice 5 — list them in the SUMMARY).
    4. Run `bash tools/std/gate.sh slice-1a`. All gate steps PASS, including step 11 (jsonb-keys) and the jsonb-compat test inside step 5 (test counts: core count must equal baseline — a drop means the test include pattern missed renamed files).
    Commit `refactor(01-13): move core/db files, subpath exports, test titles; gate slice-1a green`.
  </action>
  <verify>
    <automated>bash tools/std/gate.sh slice-1a</automated>
  </verify>
  <acceptance_criteria>
    - `bash tools/std/gate.sh slice-1a` exits 0; `STD/reports/slice-1a-gate.md` has no `FAIL`
    - `git ls-files 'packages/core/src/**/*.teste.ts' | wc -l` prints 0
    - `STD/reports/slice-1a-test-counts.json` core passed count equals baseline core passed count
    - `rg -n --pcre2 "from '\.{1,2}/[^']*(?<!\.js)'" packages/core/src packages/db/src` count <= baseline (gate step 10)
    - `git diff limpeza -- packages/db/drizzle` is empty
  </acceptance_criteria>
  <done>Slice 1a complete and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code → database | Drizzle TS layer maps to unchanged SQL |
| API → fronts (wire) | JSON keys produced from Drizzle rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13-01 | Tampering | SQL names drifting via derived constraint names | mitigate | ddl-snapshot check after symbol rename and in gate; `git diff limpeza -- packages/db/drizzle` empty |
| T-01-13-02 | Denial of service | fronts reading old JSON keys after Drizzle rename (Pitfall 10) | mitigate | typed consumers follow the Drizzle ts-prop rows and untyped consumers get wire-key rows (rewrite-literals) in the same plan; front interfaces follow in 01-21/01-23 with the same names (check-map rule 10); single deploy at the end (D-20); API tests in gate |
| T-01-13-03 | Repudiation | tests silently not running after *.teste.ts rename (Pitfall 8) | mitigate | gate compares per-package test counts to baseline |
</threat_model>

<verification>
Gate slice-1a green; DDL unchanged; core test count unchanged.
</verification>

<success_criteria>
Core and db are English per the approved map with all consumers updated and no regression.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-13-SUMMARY.md` when done
</output>
