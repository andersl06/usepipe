---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 37
type: execute
wave: 17
depends_on: ["01-20"]
files_modified:
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/api.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/workers.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/ponte.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/comments-api-ctrl-*.json
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/comments-api-rest-*.json
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/comments-workers-rest-*.json
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/comments-ponte-rest-*.json
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/comments-api-ctrl/**
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/comments-*-rest/**
autonomous: true
requirements: [STD-10, STD-11]

must_haves:
  truths:
    - "All backend comments are reviewed."
    - "Sensitive comments receive complete Sonnet review (D-24)."
  artifacts: []
  key_links: []
---

<objective>
Triage API controller and remaining backend comments.

Purpose: D-16/D-17/D-24 require reviewed decisions before edits.
Output: reviewed CSVs and proposals.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-15-PLAN.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-20-PLAN.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-20-SUMMARY.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Comment workflow: plan 01-15 context (steps 1-6). Batch naming and the sensitive-first rule: plan 01-20 context. Batches here: `ctrl` (Task 1, api controllers), `rest` (Task 2, everything still unreviewed in api, workers, ponte). The comments CSV `file` column holds inventory-time paths. `STD/reports/slice-2-sensitive-files.txt` (plan 01-20) lists the mandatory Sonnet set; those rows are already reviewed and must not be re-proposed.
</context>

<tasks>

<task type="auto">
  <name>Task 1: API controller comments — sensitive-first, then seeded sample</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/api.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/comments-api-ctrl-*.json</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/api.csv (unreviewed rows under apps/api/src/controladores/**)
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/TEMPLATE-comments.md
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-2-sensitive-files.txt
  </read_first>
  <action>
    1. Select unreviewed `api.csv` rows whose `file` starts with `apps/api/src/controladores/` (the mandatory webhook/auth/attachment/SSO controllers were reviewed in 01-20 and are not unreviewed); write them to a scratch CSV; `node tools/std/codex-prompt.ts --template STD/prompts/TEMPLATE-comments.md --rows <scratch csv> --chunk 150 --out-dir STD/prompts/comments-api-ctrl`; run each chunk with `bash tools/std/codex-run.sh 1 tools/std/schemas/comment-triage.schema.json <chunk> STD/out/comments-api-ctrl-NNN.json`; merge with `node tools/std/merge-proposals.ts --in "STD/out/comments-api-ctrl-*.json" --target STD/comments --owner codex1`.
    2. Sensitive-first: Sonnet reviews 100% of the batch rows Codex labelled security|architecture|integration|meta-blip (`reviewed_by=sonnet`).
    3. Seeded 10% sample (seed 3) of the remaining non-sensitive rows; if the error rate exceeds 10%, review the whole batch; mark the rest `reviewed_by=sample`, `status=reviewed`; every `update` needs code or test evidence.
    Record batch count, sensitive-labelled count, sample size and error rate in SUMMARY. Commit `docs(01-37): review API controller comments`.
  </action>
  <verify>
    <automated>node --input-type=module -e "import fs from 'node:fs';import {parseCsv} from './tools/std/lib/csv.ts';const [h,...v]=parseCsv(fs.readFileSync('.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/api.csv','utf8'));const rows=v.map(a=>Object.fromEntries(h.map((k,i)=>[k,a[i]])));const sens=['security','architecture','integration','meta-blip'];const b=rows.filter(r=>/^apps\/api\/src\/controladores\//.test(r.file));if(b.some(r=>r.status!=='reviewed'||(r.action==='update'&&!r.evidence)||(sens.includes(r.sensitivity)&&r.reviewed_by!=='sonnet')))process.exit(1);console.log('controllers ok',b.length)"</automated>
  </verify>
  <acceptance_criteria>
    - every api.csv row under `apps/api/src/controladores/` has status reviewed
    - every sensitive-labelled row of the batch and every 01-20 mandatory controller row has reviewed_by sonnet
    - every `update` row of the batch has non-empty evidence
    - `STD/reports/codex-log-1-comments-api-ctrl.csv` has one line per chunk, all exit 0
    - SUMMARY records batch count, sensitive-labelled count, seeded sample size and error rate
  </acceptance_criteria>
  <done>API controller comments reviewed.</done>
</task>

<task type="auto">
  <name>Task 2: Remaining api, workers and ponte comments — sensitive-first, then seeded sample</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/api.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/workers.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/ponte.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/comments-api-rest-*.json, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/comments-workers-rest-*.json, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/comments-ponte-rest-*.json</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/api.csv, workers.csv, ponte.csv (remaining unreviewed rows)
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/TEMPLATE-comments.md
  </read_first>
  <action>
    1. For each scope (api, workers, ponte): write every still-unreviewed row to a scratch CSV; `node tools/std/codex-prompt.ts --template STD/prompts/TEMPLATE-comments.md --rows <scratch csv> --chunk 150 --out-dir STD/prompts/comments-<scope>-rest`; run each chunk with `bash tools/std/codex-run.sh <1 for api/workers, 2 for ponte> tools/std/schemas/comment-triage.schema.json <chunk> STD/out/comments-<scope>-rest-NNN.json`; merge with `node tools/std/merge-proposals.ts --in "STD/out/comments-<scope>-rest-*.json" --target STD/comments --owner codex<1|2>`.
    2. Sensitive-first per scope: Sonnet reviews 100% of the batch rows Codex labelled security|architecture|integration|meta-blip.
    3. Seeded 10% sample (seed 3) of each scope's remaining non-sensitive rows; if a scope's error rate exceeds 10%, review that whole scope batch; mark the rest `reviewed_by=sample`, `status=reviewed`; every `update` needs evidence.
    Record per-app counts, sensitive-labelled counts, sample sizes and error rates in SUMMARY. Commit `docs(01-37): review remaining backend comments`.
  </action>
  <verify>
    <automated>node --input-type=module -e "import fs from 'node:fs';import {parseCsv} from './tools/std/lib/csv.ts';const d='.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/';const set=new Set(fs.readFileSync(d+'reports/slice-2-sensitive-files.txt','utf8').split('\n').filter(Boolean));const rows=['api.csv','workers.csv','ponte.csv'].flatMap(f=>{const [h,...v]=parseCsv(fs.readFileSync(d+'comments/'+f,'utf8'));return v.map(a=>Object.fromEntries(h.map((k,i)=>[k,a[i]])))});const sens=['security','architecture','integration','meta-blip'];if(rows.some(r=>r.status!=='reviewed'||(r.action==='update'&&!r.evidence)||((sens.includes(r.sensitivity)||set.has(r.file))&&r.reviewed_by!=='sonnet')))process.exit(1);console.log('backend comments ok',rows.length)"</automated>
  </verify>
  <acceptance_criteria>
    - every api.csv, workers.csv and ponte.csv row has status reviewed
    - every security|architecture|integration|meta-blip row and every row of a file in `slice-2-sensitive-files.txt` has reviewed_by sonnet; every `update` row has non-empty evidence
    - codex logs `codex-log-1-comments-api-rest.csv`, `codex-log-1-comments-workers-rest.csv`, `codex-log-2-comments-ponte-rest.csv` have exit 0 for every call
    - SUMMARY records each app's row count, sensitive-labelled count, seeded sample size and error rate
  </acceptance_criteria>
  <done>All backend app comments triaged and reviewed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| comments → maintainers of security code | rationale for guards, signatures, webhooks |
| reviewed CSV → source tree | apply-comments mutates code comments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-37-01 | Tampering/Repudiation | sensitive comment labelled by Codex outside the mandatory set | mitigate | sensitive-first rule: 100% Sonnet review before sampling; apply-comments refuses unreviewed sensitive rows |
| T-01-37-02 | Tampering | Meta/Blip literal evidence translated | mitigate | keep-original with C exception row (D-17) |
| T-01-37-03 | Repudiation | residual PT hidden by a lexicon re-baseline | mitigate | backend scan uses the full map lexicon; gate step 9 fingerprint frozen at gate 2 (plan 01-05) |
| T-01-37-04 | Information disclosure | Codex reading secrets | mitigate | codex-run worktree without .env*/keys; template rule |
</threat_model>

<verification>
Review checks pass for API, workers and ponte.
</verification>

<success_criteria>
Backend comments ready to apply.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-37-SUMMARY.md` when done
</output>
