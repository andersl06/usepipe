---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 20
type: execute
wave: 15
depends_on: ["01-19"]
files_modified:
  - apps/api/**
  - apps/workers/**
  - apps/ponte/**
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/api.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/workers.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/ponte.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/comments-*.json
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/exceptions.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-2-*
autonomous: true
requirements: [STD-10, STD-11]

must_haves:
  truths:
    - "Every PT comment in apps/api, apps/workers and apps/ponte is translated, removed or evidence-updated (D-16)"
    - "All comments in auth, session, WS, attachments/signed URLs, webhooks (Meta), sign-in/SSO, API keys and queue/outbox files were reviewed individually by Sonnet (D-24)"
    - "Meta/Blip literal evidence stays in original language with a C exception (D-17)"
    - "Slice 2 gate passes; tag std-slice-2-end exists"
  artifacts:
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-2-gate.md"
      provides: "slice 2 closing gate"
  key_links:
    - from: "std/comments/api.csv"
      to: "tools/std/apply-comments.ts"
      via: "reviewed rows applied"
      pattern: "applied"
---

<objective>
Close slice 2: comment triage for API, workers and ponte, slice gate, tag `std-slice-2-end`.

Purpose: The API holds most security/integration rationale (~938 PT comment blocks); D-17/D-24 require semantic care there.
Output: updated comments, `slice-2-gate.md`, tag.

Model routing: Codex account 1 proposes API and workers comment rows; account 2 proposes ponte rows, only through `bash tools/std/codex-run.sh` in read-only mode (D-37). Sonnet reviews 100% of security/auth/crypto/webhook/integration and architecture/meta-blip rows, plus a seeded 10% sample of the rest (D-16/D-17/D-24). Apply is mechanical.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-15-PLAN.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Comment workflow: plan 01-15 context (steps 1-6).

Mandatory Sonnet 100% review set (original names; resolve via map): `apps/api/src/autenticacao.ts`, `sessao.ts`, `eventos-ws.ts`, `servidor.ts`, `controladores/entrar.ts`, `controladores/sso.ts`, `controladores/anexos.ts`, every webhook controller/handler (`webhooks/*`, WhatsApp/Instagram/Messenger), API-key/chave files, mTLS/certificate files, `filas.ts` and outbox/sweep files in api and workers, `apps/workers/src/whatsapp/**`. Force sensitivity=security (auth/session/crypto/keys/certificates) or integration (Meta/queues/outbox) for these files regardless of the Codex label.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage security, auth, crypto, webhook and integration comments</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/api.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/workers.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/ponte.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/comments-*.json</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/TEMPLATE-comments.md
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/api.csv, workers.csv, ponte.csv
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-15-PLAN.md (comment workflow steps 1-4)
  </read_first>
  <action>
    Select rows by file path in the mandatory Sonnet review set in context, plus every row anywhere in api/workers/ponte labelled security|architecture|integration|meta-blip; reserve these rows from Tasks 2-4. Include security/auth/crypto/keys/certificates, all webhook handlers (Meta/WhatsApp/Instagram/Messenger), sign-in/SSO, queue/outbox and other integrations. Run 01-15 workflow steps 1-4 in chunks of at most 150 using `tools/std/codex-run.sh` (account 1 for api/workers, account 2 for ponte; read-only Codex proposal). Force sensitivity=security or integration for mandatory files regardless of Codex label. Sonnet opens and reviews 100% of selected comments in code context; `update` needs a code line or test name in `evidence`; preserve literal Meta/Blip evidence under D-17. Record selected row count and decisions by app in SUMMARY. Commit `docs(01-20): review backend security and integration comments`.
  </action>
  <verify>
    <automated>node --input-type=module -e "import fs from 'node:fs';import {parseCsv} from './tools/std/lib/csv.ts';const d='.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/';const rows=['api.csv','workers.csv','ponte.csv'].flatMap(f=&gt;{const [h,...v]=parseCsv(fs.readFileSync(d+f,'utf8'));return v.map(a=&gt;Object.fromEntries(h.map((k,i)=&gt;[k,a[i]])))});if(rows.filter(r=&gt;['security','architecture','integration','meta-blip'].includes(r.sensitivity)).some(r=&gt;r.status!=='reviewed'||r.reviewed_by!=='sonnet'))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - every mandatory-set row has sensitivity security|integration, status reviewed and reviewed_by sonnet; every other security|architecture|integration|meta-blip row has status reviewed and reviewed_by sonnet
    - every selected `update` row has non-empty `evidence` citing a code line or test name
    - SUMMARY records selected row counts for api, workers and ponte
  </acceptance_criteria>
  <done>Security and integration comments reviewed.</done>
</task>

<task type="auto">
  <name>Task 2: Triage API flow engine and domain comments</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/api.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/comments-api-*.json</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/api.csv (unreviewed rows under flow-engine and domain paths after Task 1)
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/TEMPLATE-comments.md
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-15-PLAN.md (comment workflow)
  </read_first>
  <action>
    For unreviewed `api.csv` rows whose source file is in the renamed equivalents of `apps/api/src/fluxo/**` or `apps/api/src/dominio/**`, run comment workflow steps 1-4 in chunks of at most 150 through read-only `tools/std/codex-run.sh` account 1. Exclude Task 1 rows. Sonnet reviews a seeded 10% sample (seed 3) of non-sensitive rows; if sample error rate exceeds 10%, review the whole batch. Set remaining rows reviewed_by=sample and status=reviewed. For every `update`, cite code or test evidence. Record row counts and sample error rate in SUMMARY. Commit `docs(01-20): review API flow and domain comments`.
  </action>
  <verify>
    <automated>node --input-type=module -e "import fs from 'node:fs';import {parseCsv} from './tools/std/lib/csv.ts';const [h,...v]=parseCsv(fs.readFileSync('.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/api.csv','utf8'));const rows=v.map(a=&gt;Object.fromEntries(h.map((k,i)=&gt;[k,a[i]])));if(rows.filter(r=&gt;/\/(flow|domain|fluxo|dominio)\//.test(r.file)).some(r=&gt;r.status!=='reviewed'))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - every flow-engine/domain row in api.csv has status reviewed; Task 1 sensitive rows retain reviewed_by sonnet
    - every `update` row in this batch has non-empty evidence citing a code line or test name
    - SUMMARY records flow/domain row count, seeded sample count and error rate
  </acceptance_criteria>
  <done>API flow and domain comments reviewed.</done>
</task>

<task type="auto">
  <name>Task 3: Triage API controller comments</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/api.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/comments-api-*.json</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/api.csv (unreviewed controller rows after Tasks 1-2)
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/TEMPLATE-comments.md
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-15-PLAN.md (comment workflow)
  </read_first>
  <action>
    For unreviewed `api.csv` rows under the renamed `apps/api/src/controladores/**` path, run workflow steps 1-4 in chunks of at most 150 through read-only `tools/std/codex-run.sh` account 1. Task 1 already reviewed webhook/auth/attachment/integration controllers and stays untouched. Sonnet reviews a seeded 10% sample (seed 3) of remaining rows; if error rate exceeds 10%, review the whole batch. Set remaining rows reviewed_by=sample and status=reviewed; require evidence for `update`. Record counts and error rate in SUMMARY. Commit `docs(01-20): review API controller comments`.
  </action>
  <verify>
    <automated>node --input-type=module -e "import fs from 'node:fs';import {parseCsv} from './tools/std/lib/csv.ts';const [h,...v]=parseCsv(fs.readFileSync('.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/api.csv','utf8'));const rows=v.map(a=&gt;Object.fromEntries(h.map((k,i)=&gt;[k,a[i]])));if(rows.filter(r=&gt;/\/(controllers|controladores)\//.test(r.file)).some(r=&gt;r.status!=='reviewed'))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - every API controller row has status reviewed; Task 1 controller rows retain reviewed_by sonnet
    - every `update` row in this batch has non-empty evidence citing a code line or test name
    - SUMMARY records controller row count, seeded sample count and error rate
  </acceptance_criteria>
  <done>API controller comments reviewed.</done>
</task>

<task type="auto">
  <name>Task 4: Triage remaining API, workers and ponte comments</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/api.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/workers.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/ponte.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/comments-*.json</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/api.csv, workers.csv, ponte.csv (remaining unreviewed rows)
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/TEMPLATE-comments.md
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-15-PLAN.md (comment workflow)
  </read_first>
  <action>
    Run workflow steps 1-4 for all rows still unreviewed after Tasks 1-3, in batches of at most 150 per app; use read-only `tools/std/codex-run.sh` account 1 for api/workers and account 2 for ponte. Sonnet reviews a seeded 10% sample (seed 3) of non-sensitive rows; if error rate exceeds 10%, review the whole app batch. All security|architecture|integration|meta-blip rows must already have reviewed_by=sonnet from Task 1. Mark the remaining rows reviewed_by=sample, status=reviewed; require evidence for every `update`. Record per-app counts and sample error rate in SUMMARY. Commit `docs(01-20): review remaining backend comments`.
  </action>
  <verify>
    <automated>node --input-type=module -e "import fs from 'node:fs';import {parseCsv} from './tools/std/lib/csv.ts';const d='.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/';const rows=['api.csv','workers.csv','ponte.csv'].flatMap(f=&gt;{const [h,...v]=parseCsv(fs.readFileSync(d+f,'utf8'));return v.map(a=&gt;Object.fromEntries(h.map((k,i)=&gt;[k,a[i]])))});if(rows.some(r=&gt;r.status!=='reviewed'||(r.action==='update'&amp;&amp;!r.evidence)||(['security','architecture','integration','meta-blip'].includes(r.sensitivity)&amp;&amp;r.reviewed_by!=='sonnet')))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - every api.csv, workers.csv and ponte.csv row has status reviewed
    - every security|architecture|integration|meta-blip row has reviewed_by sonnet; every `update` row has non-empty evidence
    - SUMMARY records each app's row count, seeded sample count and error rate
  </acceptance_criteria>
  <done>All backend app comments triaged and reviewed.</done>
</task>

<task type="auto">
  <name>Task 5: Apply, slice 2 gate, tag std-slice-2-end</name>
  <files>apps/api/**, apps/workers/**, apps/ponte/**, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/exceptions.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-2-*</files>
  <read_first>
    - tools/std/apply-comments.ts
  </read_first>
  <action>
    Workflow steps 5-6 for scopes api, workers, ponte. Run `node tools/std/scan-pt.ts --out STD/reports/slice-2-backend-scan.csv --summary STD/reports/slice-2-backend-scan-summary.md --map STD/map`; no unclassified finding (any kind) may remain under `apps/api/`, `apps/workers/`, `apps/ponte/` except kinds `package`/`app`/`script` (slice 4) — fix residual identifiers by adding approved-derived rows (Sonnet, notes `post-gate2-derived`) and applying them, residual comments by adding comment rows. Run `bash tools/std/gate.sh slice-2`. Commit `docs(01-20): apply backend comment triage; gate slice-2 green`; `git tag std-slice-2-end`.
  </action>
  <verify>
    <automated>bash tools/std/gate.sh slice-2 && git rev-parse -q --verify refs/tags/std-slice-2-end</automated>
  </verify>
  <acceptance_criteria>
    - apply-comments not-found=0, refused=0
    - in slice-2-backend-scan.csv, rows with file under apps/api|apps/workers|apps/ponte, empty category and kind not in package|app|script: 0
    - `bash tools/std/gate.sh slice-2` exits 0
    - tag std-slice-2-end at HEAD
  </acceptance_criteria>
  <done>Slice 2 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| comments → maintainers of security code | rationale for guards, signatures, webhooks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-20-01 | Tampering/Repudiation | security/integration comments mistranslated | mitigate | forced sensitivity on the mandatory set; 100% Sonnet review; apply-comments refuses unreviewed sensitive rows |
| T-01-20-02 | Tampering | Meta/Blip literal evidence translated | mitigate | keep-original with C exception row (D-17) |
</threat_model>

<verification>
Gate slice-2 green; no unclassified PT in backend apps except slice-4 kinds; tag present.
</verification>

<success_criteria>
Slice 2 (API + workers + ponte) complete.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-20-SUMMARY.md` when done
</output>
