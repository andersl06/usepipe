---
phase: 01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o
plan: 20
type: execute
wave: 16
depends_on: ["01-19"]
files_modified:
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/api.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/workers.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/ponte.csv
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/comments-api-sec-*.json
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/comments-workers-sec-*.json
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/comments-ponte-sec-*.json
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/comments-api-flow-*.json
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/comments-*-sec/**
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/comments-api-flow/**
  - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-2-sensitive-files.txt
autonomous: true
requirements: [STD-10, STD-11]

must_haves:
  truths:
    - "All comments in auth, session, WS, attachments/signed URLs, webhooks (Meta), sign-in/SSO, API keys, mTLS/certificates and queue/outbox files of api/workers/ponte were reviewed individually by Sonnet with forced sensitivity security|integration (D-24)"
    - "Every comment row Codex labels security|architecture|integration|meta-blip in these batches is reviewed 100% by Sonnet BEFORE the sampling step; only the non-sensitive rest is sampled (D-16/D-17/D-24)"
    - "Every API flow-engine and domain comment row is reviewed (Sonnet 100% for sensitive, seeded 10% sample otherwise, whole batch if the sample error rate > 10%)"
    - "Meta/Blip literal evidence stays in original language (keep-original, D-17)"
    - "Every `update` row cites a code line or test name in `evidence` (D-16)"
  artifacts:
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-2-sensitive-files.txt"
      provides: "inventory-time paths of the mandatory Sonnet review set (read by the Task 1 verify and by plan 01-37)"
    - path: ".planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/api.csv"
      provides: "reviewed security/integration and flow/domain comment rows"
  key_links:
    - from: "std/out/comments-<scope>-sec-*.json"
      to: "std/comments/<scope>.csv"
      via: "merge-proposals comments mode (batch-specific glob)"
      pattern: "comments-.*-sec"
---

<objective>
Slice 2 comment triage, part 1 of 2 (split from the original single plan to keep one executor context bounded): the mandatory security/integration set of api, workers and ponte, and the API flow-engine/domain comments. Part 2 (plan 01-37) triages controllers and the rest; plan 01-40 applies everything, runs the slice-2 gate and tags `std-slice-2-end`.

Purpose: The API holds most security/integration rationale (~938 PT comment blocks); D-17/D-24 require semantic care there.
Output: reviewed rows in `std/comments/{api,workers,ponte}.csv` for these batches, `std/reports/slice-2-sensitive-files.txt`. No source file is edited in this plan (apply happens in 01-40).

Model routing: Codex account 1 proposes api and workers rows; account 2 proposes ponte rows, only through `bash tools/std/codex-run.sh` in read-only mode (D-37). Sonnet reviews 100% of the mandatory set and 100% of any row Codex labels security|architecture|integration|meta-blip, then a seeded 10% sample of the remaining rows (D-16/D-17/D-24).
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-15-PLAN.md

STD = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std`

Comment workflow: plan 01-15 context (steps 1-4). Batch naming (01-15 context): each batch uses its own chunk dir `STD/prompts/comments-<scope>-<batch>` and outputs `STD/out/comments-<scope>-<batch>-NNN.json`, and merges only `--in "STD/out/comments-<scope>-<batch>-*.json"`. Batches in this plan: `sec` (Task 1), `flow` (Task 2). The comments CSV `file` column holds inventory-time (pre-rename) paths; apply-comments resolves them to current paths.

Mandatory Sonnet 100% review set (inventory-time paths, as in the `file` column): `apps/api/src/autenticacao.ts`, `apps/api/src/sessao.ts`, `apps/api/src/eventos-ws.ts`, `apps/api/src/servidor.ts`, `apps/api/src/controladores/entrar.ts`, `apps/api/src/controladores/sso.ts`, `apps/api/src/controladores/anexos.ts`, every webhook controller/handler (`apps/api/src/webhooks/**`, WhatsApp/Instagram/Messenger handlers in api), API-key/chave files, mTLS/certificate files, `apps/api/src/filas.ts`, `apps/workers/src/filas.ts` and outbox/sweep files in api and workers, `apps/workers/src/whatsapp/**`, and ponte files that sign, authenticate or forward webhook payloads. Force sensitivity=security (auth/session/crypto/keys/certificates) or integration (Meta/queues/outbox) for these files regardless of the Codex label.

Sensitive-first rule (applies to every triage batch here and in plan 01-37): after merging a batch's Codex proposals, every row of that batch whose sensitivity is security|architecture|integration|meta-blip is reviewed by Sonnet (100%, `reviewed_by=sonnet`) BEFORE the seeded sample is drawn; the sample is drawn only from the batch's remaining non-sensitive rows.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mandatory security and integration set (api, workers, ponte) — 100% Sonnet review</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/api.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/workers.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/ponte.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/comments-api-sec-*.json, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/comments-workers-sec-*.json, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/comments-ponte-sec-*.json, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/reports/slice-2-sensitive-files.txt</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/TEMPLATE-comments.md
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/api.csv, workers.csv, ponte.csv (header and `file` column values)
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-15-PLAN.md (comment workflow steps 1-4)
  </read_first>
  <action>
    1. Build the mandatory set: expand the list in context against the distinct `file` values of api.csv, workers.csv and ponte.csv (node one-liner with `parseCsv`; globs via `path.matchesGlob`); add ponte files matched by `rg -l -i "assinatura|signature|hmac|token|webhook" apps/ponte/src` mapped back to their inventory-time path through the applied file rows. Write the sorted inventory-time paths, one per line, to `STD/reports/slice-2-sensitive-files.txt` and commit it.
    2. For each scope (api, workers, ponte) write the rows whose `file` is in that list to a scratch CSV, then `node tools/std/codex-prompt.ts --template STD/prompts/TEMPLATE-comments.md --rows <scratch csv> --chunk 150 --out-dir STD/prompts/comments-<scope>-sec`; run every chunk with `bash tools/std/codex-run.sh <1 for api/workers, 2 for ponte> tools/std/schemas/comment-triage.schema.json <chunk> STD/out/comments-<scope>-sec-NNN.json`; merge with `node tools/std/merge-proposals.ts --in "STD/out/comments-<scope>-sec-*.json" --target STD/comments --owner codex<1|2>`.
    3. Force sensitivity=security (auth/session/crypto/keys/certificates) or integration (Meta/queues/outbox) on every row of the mandatory set regardless of the Codex label. Sonnet opens each comment in code context and reviews 100% of them: exact technical meaning kept (D-17), `update` only with a code line or test name in `evidence` (D-16), literal Meta/Blip quotes/payloads/official names as `keep-original` with evidence (D-17); set `reviewed_by=sonnet`, `status=reviewed`.
    Record the mandatory-set file count and row count per app and the decision counts (translate/remove/update/keep-original) in SUMMARY. Commit `docs(01-20): review backend security and integration comments`.
  </action>
  <verify>
    <automated>node --input-type=module -e "import fs from 'node:fs';import {parseCsv} from './tools/std/lib/csv.ts';const d='.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/';const set=new Set(fs.readFileSync(d+'reports/slice-2-sensitive-files.txt','utf8').split('\n').filter(Boolean));if(set.size===0)process.exit(1);const rows=['api.csv','workers.csv','ponte.csv'].flatMap(f=>{const [h,...v]=parseCsv(fs.readFileSync(d+'comments/'+f,'utf8'));return v.map(a=>Object.fromEntries(h.map((k,i)=>[k,a[i]])))});const bad=rows.filter(r=>set.has(r.file)&&(!['security','integration'].includes(r.sensitivity)||r.status!=='reviewed'||r.reviewed_by!=='sonnet'||(r.action==='update'&&!r.evidence)));if(bad.length){console.error(bad.length);process.exit(1)}console.log('mandatory rows ok',rows.filter(r=>set.has(r.file)).length)"</automated>
  </verify>
  <acceptance_criteria>
    - `STD/reports/slice-2-sensitive-files.txt` exists, is non-empty and contains `apps/api/src/autenticacao.ts` and `apps/api/src/sessao.ts`
    - every row whose `file` is in that list has sensitivity security|integration, status reviewed and reviewed_by sonnet
    - every `update` row of the list has non-empty `evidence` citing a code line or test name
    - codex logs `STD/reports/codex-log-1-comments-api-sec.csv`, `codex-log-1-comments-workers-sec.csv` and `codex-log-2-comments-ponte-sec.csv` exist with exit 0 for every call
    - SUMMARY records file and row counts per app and decision counts
  </acceptance_criteria>
  <done>Mandatory security and integration comments reviewed 100% by Sonnet.</done>
</task>

<task type="auto">
  <name>Task 2: API flow-engine and domain comments — sensitive-first, then seeded sample</name>
  <files>.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/api.csv, .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/out/comments-api-flow-*.json</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/api.csv (unreviewed rows under apps/api/src/fluxo/** and apps/api/src/dominio/** after Task 1)
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/prompts/TEMPLATE-comments.md
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-15-PLAN.md (comment workflow)
  </read_first>
  <action>
    1. Select unreviewed `api.csv` rows whose `file` starts with `apps/api/src/fluxo/` or `apps/api/src/dominio/` (inventory-time paths), excluding every Task 1 row; write them to a scratch CSV; `node tools/std/codex-prompt.ts --template STD/prompts/TEMPLATE-comments.md --rows <scratch csv> --chunk 150 --out-dir STD/prompts/comments-api-flow`; run each chunk with `bash tools/std/codex-run.sh 1 tools/std/schemas/comment-triage.schema.json <chunk> STD/out/comments-api-flow-NNN.json`; merge with `node tools/std/merge-proposals.ts --in "STD/out/comments-api-flow-*.json" --target STD/comments --owner codex1`.
    2. Sensitive-first (context rule): every row of this batch that Codex labelled security|architecture|integration|meta-blip is reviewed by Sonnet in code context (100%), `reviewed_by=sonnet`, `status=reviewed`, before any sampling.
    3. From the remaining non-sensitive rows of the batch, Sonnet reviews a seeded 10% sample (seed 3; `check-map`-style seeded PRNG, stratified by file); if the sample error rate exceeds 10%, review the whole batch. Mark the remaining rows `reviewed_by=sample`, `status=reviewed`. Every `update` needs code or test evidence.
    Record batch row count, sensitive-labelled count (all Sonnet), sample size and error rate in SUMMARY. Commit `docs(01-20): review API flow and domain comments`.
  </action>
  <verify>
    <automated>node --input-type=module -e "import fs from 'node:fs';import {parseCsv} from './tools/std/lib/csv.ts';const [h,...v]=parseCsv(fs.readFileSync('.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/comments/api.csv','utf8'));const rows=v.map(a=>Object.fromEntries(h.map((k,i)=>[k,a[i]])));const sens=['security','architecture','integration','meta-blip'];const b=rows.filter(r=>/^apps\/api\/src\/(fluxo|dominio)\//.test(r.file));if(b.some(r=>r.status!=='reviewed'||(r.action==='update'&&!r.evidence)||(sens.includes(r.sensitivity)&&r.reviewed_by!=='sonnet')))process.exit(1);console.log('flow/domain ok',b.length)"</automated>
  </verify>
  <acceptance_criteria>
    - every api.csv row under `apps/api/src/fluxo/` or `apps/api/src/dominio/` has status reviewed
    - every row of the batch with sensitivity security|architecture|integration|meta-blip has reviewed_by sonnet; Task 1 rows keep reviewed_by sonnet
    - every `update` row of the batch has non-empty evidence citing a code line or test name
    - SUMMARY records batch row count, sensitive-labelled count, seeded sample size and error rate
  </acceptance_criteria>
  <done>API flow and domain comments reviewed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| comments → maintainers of security code | rationale for guards, signatures, webhooks |
| Codex proposal → comment CSV | model output becomes comment text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-20-01 | Tampering/Repudiation | security/integration comments mistranslated | mitigate | mandatory set selected by path before Codex runs, forced sensitivity, 100% Sonnet review; sensitive-first rule for every Codex-labelled sensitive row; apply-comments (01-40) refuses unreviewed sensitive rows |
| T-01-20-02 | Tampering | Meta/Blip literal evidence translated | mitigate | keep-original with evidence (D-17); C exception row written by apply-comments in 01-40 |
| T-01-20-03 | Tampering | a later batch merge resetting reviewed rows | mitigate | batch-specific output globs; merge-proposals refuses to touch reviewed|applied comment rows (plan 01-06) |
| T-01-20-04 | Information disclosure | Codex reading secrets | mitigate | codex-run worktree without .env*/keys; template rule |
</threat_model>

<verification>
Mandatory set and flow/domain rows reviewed with the sensitive-first rule; both verify commands exit 0.
</verification>

<success_criteria>
Security/integration and flow/domain comment decisions for slice 2 are reviewed and ready for plan 01-40 to apply.
</success_criteria>

<output>
Create `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/01-20-SUMMARY.md` when done
</output>
