---
phase: 01.1-subdominio-por-tenant
plan: 03
type: execute
wave: 3
depends_on: ["01.1-02"]
files_modified:
  # caminhos de hoje; o nome pós-Phase 1 está em names.env
  - apps/api/src/controladores/entrar.ts           # $API_LOGIN_FILE
  - apps/api/src/controladores/sso.ts              # $API_SSO_FILE
  - apps/api/src/dominio/convites.ts               # $API_INVITATIONS_FILE
  - apps/api/src/provisionar.ts                    # $API_PROVISION_FILE
  - apps/api/src/main.ts                           # validação de configuração no boot (renomeado pela Phase 1)
  - apps/crm/src/lib/configuracoes-dados.ts        # link de convite do CRM (renomeado pela Phase 1)
  - apps/api/tests/entrada.test.ts                 # renomeado pela Phase 1
  - apps/api/tests/convites.test.ts                # renomeado pela Phase 1
  - apps/api/tests/contas.test.ts                  # renomeado pela Phase 1 (provisionamento)
  - apps/api/tests/email.test.ts                   # renomeado pela Phase 1 (link no e-mail)
autonomous: true
requirements: [SUB-02, SUB-06, SUB-07, SUB-08]

must_haves:
  truths:
    - "Cookie de sessão sai com Domain=.<D>, HttpOnly, Secure, SameSite=Lax; logout apaga com o mesmo Domain; a API não sobe em modo tenant se PIPE_COOKIE_DOMINIO for diferente de .${PIPE_DOMINIO_CONTAS} (D-07)"
    - "O cookie do desafio OAuth é host-only em login.<D> (sem Domain), porque início e callback do Google/SSO acontecem nesse mesmo host (D-18)"
    - "Login (Google e SSO) só começa e termina em login.<D>; depois do login a pessoa vai para <slug>.<D>/application do tenant da própria identidade, ou para o returnTo se ele for válido e do mesmo tenant (D-17, D-20)"
    - "returnTo inválido, de outro tenant ou de outro domínio é descartado sem erro; nunca há redirect para fora de *.<D> (D-20)"
    - "Link de convite (API e CRM) aponta para login.<D>; ao aceitar, a pessoa vai para o /application do tenant do convite; saída do provisionamento mostra o endereço do tenant e o do login central (D-21)"
    - "Provisionar tenant com slug reservado (inclui login, portal, api, www, crm) falha com reserved_slug, usando a regra do módulo do plano 01"
  artifacts:
    - path: "apps/api/src/controladores/entrar.ts (renomeado pela Phase 1)"
      provides: "login central: desafio host-only, destino por tenant da identidade, returnTo validado"
      contains: "parseReturnTo"
    - path: "apps/api/src/dominio/convites.ts (renomeado pela Phase 1)"
      provides: "link de convite em login.<D>"
  key_links:
    - from: "callback Google/SSO em login.<D>"
      to: "tenant resolvido da identidade ou do convite"
      via: "destino = returnTo do mesmo tenant, senão buildTenantOrigin(slug,'application')/application"
      pattern: "buildTenantOrigin"
    - from: "urlDoConvite (API e CRM, renomeados pela Phase 1)"
      to: "packages/contracts/src/tenant-host.ts"
      via: "buildLoginUrl / origem do login central"
      pattern: "buildLoginUrl|isLoginHost"
---

<objective>
Levar cookie, login, OAuth, SSO, convite e provisionamento para o desenho de login central em `login.<D>` (padrão Blip, respostas do dono D-17 a D-21).

Purpose: hoje cada front tem sua tela de login, e a volta cai numa base única (`PIPE_URL_APP`) ou numa origem de lista fechada. O dono decidiu uma porta única de login: `login.<D>` autentica e manda a pessoa para o tenant dela. É aqui que um erro vira redirecionamento aberto, sessão criada no tenant errado ou cookie de desafio espalhado por todo `*.<D>`.
Output: cookie validado no boot, desafio host-only, destino por tenant da identidade, `returnTo` validado, convite e provisionamento pelo novo desenho, reservados no provisionamento.

Model routing: Sonnet (autenticação e redirects; D-25 da Phase 1).
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-subdominio-por-tenant/01.1-CONTEXT.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/CONVENTIONS-EN.md
@.planning/phases/01.1-subdominio-por-tenant/01.1-02-SUMMARY.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/nav-contract.md

Carregar os caminhos com `set -a; . .planning/phases/01.1-subdominio-por-tenant/names.env; set +a`.

Fatos (linhas de hoje; os arquivos são renomeados pela Phase 1):
- `opcoesDeCookie()` em `entrar.ts:53-62` lê `PIPE_COOKIE_DOMINIO`; `cookieDoDesafio` (`entrar.ts:125-140`) usa o mesmo `Domain`, com `Path=/v1/auth`.
- `baseDoApp(origem)` (`entrar.ts:81-84`) valida `?origem=` com `origemPermitida` e cai em `PIPE_URL_APP`. `urlDeErro` (`entrar.ts:86-95`) usa `PIPE_URL_ENTRADA` quando não há origem. `destinoAbsoluto` (`entrar.ts:103-106`) aceita só caminho interno.
- Início do Google: `entrar.ts:267-283` guarda `origem`, `convite` e `destino` no desafio. Callback: `entrar.ts:285+` (`$GOOGLE_CALLBACK_PATH` após a Phase 1); a URL de callback enviada ao Google vem do VALOR de `GOOGLE_URL_RETORNO`. SSO: `sso.ts:136-199`, desafio com `origem` e `tenantId` do fluxo; a URL de retorno do SSO é montada sobre `PIPE_URL_API`.
- A regra "1 identidade = 1 tenant" está em `packages/autenticacao/src/entrada.ts:131` (`outro_tenant`).
- Convite: `urlDoConvite(token)` em `apps/api/src/dominio/convites.ts:70-73` (base `PIPE_URL_APP`), chamado em `convites.ts:181`. O CRM tem cópia própria em `apps/crm/src/lib/configuracoes-dados.ts:314-320`, chamada em `:380`.
- Provisionamento: `SLUG_ACEITAVEL` em `provisionar.ts:66`; `comoEntrar()` em `provisionar.ts:221-222` imprime `PIPE_URL_APP`.
- Plano 01: `buildTenantOrigin`, `parseTenantHost`, `isLoginHost`, `buildLoginUrl`, `parseReturnTo`, `TENANT_SLUG_PATTERN`. Plano 02: `readTenantHostConfig`, `resolveRequestTenantSlug` (login.<D> não gera sinal).
- `returnTo` é o nome do query param novo (camelCase, CONVENTIONS-EN.md). Query params e códigos de recusa existentes (`?origem=`, `?destino=`, `?erro=`, `falha_no_provedor`, `outro_tenant`...) aparecem com o nome de hoje e seguem o mapa da Phase 1. Em modo tenant, `?origem=` e `?destino=` deixam de ser usados pelo login central (substituídos por `returnTo`); continuam valendo no modo desligado (dev localhost).
- Domínio de teste: `pipe.test`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cookie de sessão com Domain do domínio base, desafio host-only, validação no boot</name>
  <files>apps/api/src/controladores/entrar.ts, apps/api/src/main.ts, apps/api/tests/entrada.test.ts</files>
  <read_first>
    - $API_LOGIN_FILE (opcoesDeCookie, cookieDoDesafio, rota de logout)
    - $AUTH_SESSION_FILE (montarCookie; nome do cookie definido pela Phase 1, D-38)
    - arquivo de bootstrap da API (hoje `apps/api/src/main.ts`, renomeado pela Phase 1)
  </read_first>
  <action>
    1. Testes em `entrada.test.ts` com `PIPE_DOMINIO_CONTAS=pipe.test`, `PIPE_COOKIE_DOMINIO=.pipe.test`, `PIPE_COOKIE_SEGURO=true`, `GOOGLE_URL_RETORNO=https://login.pipe.test$GOOGLE_CALLBACK_PATH`, requisições com `host: login.pipe.test`:
       - cookie de sessão: `Set-Cookie` com `Domain=.pipe.test`, `HttpOnly`, `Secure`, `SameSite=Lax`, `Path=/`;
       - cookie do desafio (início do Google): SEM `Domain`, com `Path=/v1/auth`, `HttpOnly`, `Secure`, `SameSite=Lax`;
       - logout (em qualquer host): `Max-Age=0` com `Domain=.pipe.test`.
       Modo desligado: desafio continua como hoje.
    2. `cookieDoDesafio`: em modo tenant, sem `Domain` (host-only). Comentário curto em inglês explicando que início e callback ficam em `login.<D>` (D-18).
    3. `assertTenantDomainConfig`, chamada no bootstrap antes de `listen`: com `PIPE_DOMINIO_CONTAS` não vazio, exigir `PIPE_COOKIE_DOMINIO === '.' + PIPE_DOMINIO_CONTAS`, domínio só com `[a-z0-9.-]` sem ponto inicial nem final e sem começar com `desk.`, e `GOOGLE_URL_RETORNO` começando com `buildLoginUrl(config)` sem barra final. Falha → erro que diz qual variável corrigir, e o processo não sobe. A checagem de `GOOGLE_URL_RETORNO` só derruba o boot em produção (`NODE_ENV=production`) ou quando as credenciais do Google estiverem configuradas (`GOOGLE_CLIENTE_ID` não vazio); fora disso, só um aviso no log (ajuste do dono, comentário do PR #1). Teste de unidade: par correto passa; cookie `.outro.test`, cookie vazio e cookie `pipe.test` sem ponto falham sempre; callback em `api.pipe.test` falha com `NODE_ENV=production` e com `GOOGLE_CLIENTE_ID` preenchido, e só registra aviso (boot segue) sem os dois.
    4. Comentário de `opcoesDeCookie`, que hoje cita `api.`, `app.`, `gestao.`, `crm.`, reescrito para `<slug>.<D>`, `<slug>.desk.<D>` e `login.<D>`.
    Commit: `feat(01.1-03): session cookie on base domain, host-only login challenge, boot-time config check`.
  </action>
  <verify>
    <automated>set -a && . .planning/phases/01.1-subdominio-por-tenant/names.env && set +a && pnpm --filter "$API_PKG" exec vitest run tests/entrada.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Os três `Set-Cookie` se comportam como no passo 1
    - `assertTenantDomainConfig` recusa os três casos de cookie sempre; recusa o callback errado só em produção ou com credencial do Google, e nos outros casos só avisa no log
    - `rg -n "SameSite=None" apps packages --glob '!**/dist/**'` não devolve nada
  </acceptance_criteria>
  <done>Sessão compartilhada entre os apps do tenant; desafio preso ao login central; configuração errada impede o boot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Login central em login.&lt;D&gt;: returnTo, destino pelo tenant da identidade, SSO</name>
  <files>apps/api/src/controladores/entrar.ts, apps/api/src/controladores/sso.ts, apps/api/tests/entrada.test.ts</files>
  <read_first>
    - $API_LOGIN_FILE (baseDoApp, urlDeErro, destinoAbsoluto, início e callback do Google, login de dev)
    - $API_SSO_FILE (início e callback do SSO, montagem da URL de retorno)
    - $AUTH_LOGIN_FILE (resolução da identidade → tenant)
    - packages/contracts/src/tenant-host.ts (plano 01) e apps/api/src/request-tenant.ts (plano 02)
  </read_first>
  <action>
    Tudo abaixo vale em modo tenant (`PIPE_DOMINIO_CONTAS` não vazio). No modo desligado, o fluxo de hoje (`?origem=`/`?destino=`) fica igual.
    1. Início (Google, SSO e login de dev): só aceito com `isLoginHost(host)`. Em outro host, redirecionar 302 para `buildLoginUrl(config, returnTo)`, repassando `returnTo` se vier. Guardar no desafio o `returnTo` cru (validado no callback) e o convite, se houver.
    2. SSO: a URL de retorno enviada ao IdP passa a ser `buildLoginUrl(config)` sem barra + `$SSO_CALLBACK_PATH` (antes, `PIPE_URL_API`); a do Google continua vindo do valor de `GOOGLE_URL_RETORNO`, que agora aponta para `login.<D>` (validado no boot, Task 1).
    3. Callback (Google e SSO), depois de resolver o tenant da identidade (ou do convite, ou `desafio.tenantId` no SSO) e criar a sessão como hoje:
       - `alvo = parseReturnTo(desafio.returnTo, config)`;
       - se `alvo` existir e `alvo.slug` for o slug do tenant resolvido → redirecionar para `alvo.url`;
       - se `alvo` existir e for de outro slug (inclusive slug que não existe) → redirecionar para `buildTenantOrigin(slug, 'application', config) + '/application?deniedTenant=' + alvo.slug`: a Gestão do tenant da pessoa mostra o aviso curto "Você não tem acesso a <slug>. Levamos você para a sua conta." (D-20);
       - senão (sem `returnTo` ou `returnTo` inválido) → `buildTenantOrigin(slug, 'application', config) + '/application'`, sem aviso. Nunca tela de erro.
    4. Erros de login (recusas existentes: domínio desconhecido, sem convite, falha no provedor...): `urlDeErro` em modo tenant aponta para `buildLoginUrl(config)` + rota de login com `?erro=` (o login central mostra a recusa). Nunca para um host de tenant.
    5. Login central com sessão já válida: nada muda na API. O front de `login.<D>` chama `GET $ME_ENDPOINT` (sem sinal de tenant, plano 02) e aplica a mesma regra do passo 3 no navegador (plano 04).
    6. Testes em `entrada.test.ts` (Google mockado como hoje; tenants `alfa` e `beta`; base `pipe.test`; `host: login.pipe.test` no início e no callback):
       | returnTo do desafio | identidade | esperado |
       | https://alfa.pipe.test/application/x | usuário de alfa | 302 para https://alfa.pipe.test/application/x, sessão criada |
       | https://alfa.desk.pipe.test/ | usuário de alfa | 302 para https://alfa.desk.pipe.test/ |
       | https://alfa.pipe.test/application | usuário de beta | 302 para https://beta.pipe.test/application?deniedTenant=alfa, sessão de beta |
       | https://naoexiste.pipe.test/ | usuário de alfa | 302 para https://alfa.pipe.test/application?deniedTenant=naoexiste |
       | https://alfa.pipe.test.evil.example/ | usuário de alfa | 302 para https://alfa.pipe.test/application |
       | //evil.example/ ou javascript:... | usuário de alfa | 302 para https://alfa.pipe.test/application |
       | sem returnTo | usuário de alfa | 302 para https://alfa.pipe.test/application |
       | — (início em `host: alfa.pipe.test`) | — | 302 para https://login.pipe.test/?returnTo=... |
       | recusa (domínio desconhecido) | — | 302 para https://login.pipe.test/<LOGIN_ROUTE>?erro=... |
       E o equivalente mínimo no SSO: retorno enviado ao IdP em `https://login.pipe.test$SSO_CALLBACK_PATH`; destino segue a mesma tabela (linhas 1 e 3).
    Commit: `feat(01.1-03): central login on login host with returnTo and tenant-of-identity redirect`.
  </action>
  <verify>
    <automated>set -a && . .planning/phases/01.1-subdominio-por-tenant/names.env && set +a && pnpm --filter "$API_PKG" exec vitest run tests/entrada.test.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Tabela do passo 6 e casos de SSO passam
    - Nenhuma resposta do login redireciona para host fora de `*.pipe.test` (asserção em todos os casos)
    - `rg -n "PIPE_URL_APP|PIPE_URL_API" "$API_LOGIN_FILE" "$API_SSO_FILE"` só aparece no ramo de modo desligado
    - `pnpm typecheck` na raiz sai 0
  </acceptance_criteria>
  <done>Login só no login central, sempre terminando no tenant da própria identidade, sem redirect aberto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Convite em login.&lt;D&gt;, provisionamento pelo slug, reservados</name>
  <files>apps/api/src/dominio/convites.ts, apps/api/src/provisionar.ts, apps/crm/src/lib/configuracoes-dados.ts, apps/api/tests/convites.test.ts, apps/api/tests/contas.test.ts, apps/api/tests/email.test.ts</files>
  <read_first>
    - $API_INVITATIONS_FILE (urlDoConvite e quem chama)
    - $API_PROVISION_FILE (SLUG_ACEITAVEL, validação do pedido, comoEntrar)
    - apps/crm/src/lib/configuracoes-dados.ts:300-385 (cópia do CRM, renomeado pela Phase 1)
    - apps/api/tests/convites.test.ts, contas.test.ts, email.test.ts
  </read_first>
  <action>
    1. `urlDoConvite` (API): em modo tenant, `buildLoginUrl(config)` sem barra + `$INVITATION_ROUTE/<token>`; no modo desligado, a base de hoje (`PIPE_URL_APP`). O front de `login.<D>` serve a rota de convite (plano 04), e o login com convite segue a Task 2: o tenant do convite decide o destino `/application`.
    2. CRM: `urlDoConvite` do CRM usa a mesma regra (importa de `@pipe/contracts`, lê a config do mesmo jeito). Não manter uma terceira regra.
    3. Provisionamento: trocar `SLUG_ACEITAVEL` local por `TENANT_SLUG_PATTERN` e `isValidTenantSlug` do plano 01 (inclui reservados). Slug reservado → erro `reserved_slug`, com mensagem que diz o motivo. `comoEntrar()` em modo tenant imprime `buildTenantOrigin(slug,'application')/application`, `buildTenantOrigin(slug,'desk')/` e o login central `buildLoginUrl(config)`; no modo desligado, o comportamento de hoje.
    4. Testes: convite criado em `alfa` com base `pipe.test` → link `https://login.pipe.test<INVITATION_ROUTE>/<token>` (API e e-mail); modo desligado → link de hoje; aceitar o convite pelo login central → destino `https://alfa.pipe.test/application`; provisionar `api`, `login`, `portal`, `www`, `crm`, `desk` → `reserved_slug`; provisionar `acme` → ok, e `comoEntrar` contém `https://acme.pipe.test/application`, `https://acme.desk.pipe.test/` e `https://login.pipe.test/`.
    Commit: `feat(01.1-03): invitation links on login host, provisioning output by slug, reserved slugs`.
  </action>
  <verify>
    <automated>set -a && . .planning/phases/01.1-subdominio-por-tenant/names.env && set +a && pnpm --filter "$API_PKG" exec vitest run tests/convites.test.ts tests/contas.test.ts tests/email.test.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Casos do passo 4 passam
    - `rg -n "\^\[a-z0-9\]\+\(-\[a-z0-9\]\+\)\*\$" apps packages --glob '!**/dist/**'` só encontra a definição em `packages/contracts/src/tenant-host.ts`
    - As duas definições de link de convite (API e CRM) usam o módulo do plano 01
    - Suíte completa da API sem regressão
  </acceptance_criteria>
  <done>Convites entram pelo login central e terminam no tenant certo; slugs de sistema não podem ser provisionados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Navegador → `login.<D>` (`returnTo`, convite) | parâmetros controlados pelo cliente |
| Google/IdP → callback em `login.<D>` | resposta do provedor + cookie do desafio |
| API → e-mail de convite | link que sai do sistema e é clicado fora dele |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-03-01 | Spoofing | redirecionamento aberto por `returnTo` (host parecido, `//host`, `javascript:`, userinfo) | mitigate | `parseReturnTo` do plano 01 (sufixo por labels, esquema, sem userinfo); teste afirma que nenhum redirect sai de `*.pipe.test` |
| T-01.1-03-02 | Elevation of privilege | identidade de B levada ao host de A por `returnTo` | mitigate | `returnTo` só seguido se o slug for o do tenant da identidade; senão vai para o próprio tenant; o guard do plano 02 recusaria de qualquer forma |
| T-01.1-03-03 | Tampering | cookie do desafio lido ou sobrescrito por outro subdomínio | mitigate | Desafio host-only em `login.<D>` (D-18); teste afirma ausência de `Domain` |
| T-01.1-03-04 | Spoofing | redirect URI de OAuth apontando para host errado | mitigate | `assertTenantDomainConfig` exige `GOOGLE_URL_RETORNO` em `login.<D>`; deploy valida o mesmo (plano 05) |
| T-01.1-03-05 | Spoofing | cookie com Domain errado (sessão não compartilhada ou vazando para domínio pai) | mitigate | Validação no boot + validação no deploy (plano 05) |
| T-01.1-03-06 | Elevation of privilege | slug `login`/`portal`/`api`/`www`/`crm` provisionado e sequestrando host de sistema | mitigate | Validação única do plano 01 no provisionamento; teste |
| T-01.1-03-07 | Information disclosure | token de convite passando por host de tenant | mitigate | Link de convite em `login.<D>` (D-21) |
| T-01.1-03-08 | Information disclosure | aviso de acesso negado revelando dados do outro tenant | mitigate | `deniedTenant` carrega só o slug que a pessoa já digitou (validado com `TENANT_SLUG_PATTERN`); nenhum nome, existência ou dado do outro tenant |
</threat_model>

<verification>
Suítes `entrada`, `convites`, `contas` e `email` verdes; `pnpm typecheck` na raiz sai 0.
</verification>

<success_criteria>
Login central em `login.<D>` com callback no mesmo host, destino sempre no tenant da identidade, `returnTo` seguro, convites pelo login central e slugs de sistema protegidos.
</success_criteria>

<output>
Create `.planning/phases/01.1-subdominio-por-tenant/01.1-03-SUMMARY.md` when done
</output>
