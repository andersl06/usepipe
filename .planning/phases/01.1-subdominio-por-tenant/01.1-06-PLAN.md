---
phase: 01.1-subdominio-por-tenant
plan: 06
type: execute
wave: 4
depends_on: ["01.1-03", "01.1-04"]
files_modified:
  - .env.example
  - docs/tenant-subdomains-dev.md                  # novo
  - tools/check-domain-literals.mjs                # novo
  - tools/domain-literals-allow.txt                # novo
  - apps/api/tests/tenant-isolation.test.ts        # novo
  - scripts/dev-tenant-smoke.sh                    # novo
autonomous: true
requirements: [SUB-01, SUB-12, SUB-13]

must_haves:
  truths:
    - ".env.example documenta os três modos de dev: desligado (localhost, como hoje), lvh.me (cookie compartilhado entre Gestão e Desk) e *.localhost (sem cookie compartilhado), com o par PIPE_DOMINIO_CONTAS/PIPE_COOKIE_DOMINIO coerente (D-14)"
    - "Uma varredura automatizada falha se um domínio de produção (usepipe.app, usepipe.ai, usepipe.com.br) aparecer como host em código, compose, scripts ou Terraform fora da allowlist de exemplos (SUB-01)"
    - "Uma suíte de isolamento cobre, num só arquivo, o ciclo login → uso → troca de host → WebSocket → convite → logout entre dois tenants (SUB-13)"
    - "Um smoke de dev prova que o proxy do Vite entrega o Host do tenant à API (alfa.lvh.me passa, alfa com host de beta recebe 403)"
  artifacts:
    - path: "tools/check-domain-literals.mjs"
      provides: "varredura SUB-01 com allowlist"
    - path: "apps/api/tests/tenant-isolation.test.ts"
      provides: "suíte de aceite de isolamento entre tenants"
    - path: "docs/tenant-subdomains-dev.md"
      provides: "como rodar Gestão e Desk em <slug>.lvh.me"
  key_links:
    - from: "scripts/dev-tenant-smoke.sh"
      to: "vite.config.ts proxy /v1 (changeOrigin: false) → guard do plano 02"
      via: "curl com --resolve nos hosts lvh.me"
      pattern: "--resolve"
---

<objective>
Fechar o ciclo de desenvolvimento e de teste: dev local com subdomínio documentado, varredura que impede domínio literal no código e uma suíte de aceite de isolamento entre tenants.

Purpose: os planos 02 a 04 testam cada peça isolada. Falta provar o ciclo inteiro num lugar só e impedir que alguém reintroduza um host fixo (o erro que esta fase remove). Sem um dev com subdomínio, ninguém consegue reproduzir localmente um problema de cookie ou de Host.
Output: `.env.example` atualizado, guia de dev, varredura SUB-01, suíte de isolamento, smoke de dev.

Model routing: Sonnet.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-subdominio-por-tenant/01.1-CONTEXT.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/CONVENTIONS-EN.md
@.planning/phases/01.1-subdominio-por-tenant/01.1-02-SUMMARY.md
@.planning/phases/01.1-subdominio-por-tenant/01.1-03-SUMMARY.md
@.planning/phases/01.1-subdominio-por-tenant/01.1-04-SUMMARY.md

Carregar os caminhos com `set -a; . .planning/phases/01.1-subdominio-por-tenant/names.env; set +a`.

Fatos:
- `.env.example:245-260` (seção "Endereço por conta") cita `usepipe.ai` e o hub `app.usepipe.ai`, que estão fora desta fase. `:33-56` tem `GOOGLE_URL_RETORNO`, `PIPE_ORIGENS`, `PIPE_COOKIE_DOMINIO`, `PIPE_URL_APP`, `PIPE_URL_ENTRADA`, `PIPE_URL_API` de dev.
- Dev: API em 3010, Gestão em 3110, Desk em 3210 (planos 04 e `vite.config.ts`). `lvh.me` resolve qualquer subdomínio para 127.0.0.1 (DNS público; sem internet, usar entradas em `hosts`). `*.localhost` resolve local no navegador, mas nenhum navegador aceita `Domain=.localhost` no cookie.
- O Google não aceita `lvh.me` como redirect de OAuth em todos os casos. Em dev, o caminho de login é o de senha/dev já existente (`entrar.ts:240-257`, renomeado pela Phase 1), que o plano 02 ajustou.
- Domínio de teste nas suítes: `pipe.test`.
- Literais permitidos (D-01): `.env.example`, `infra/compose/env.prod.exemplo` (renomeado pela Phase 1), `infra/terraform/**/terraform.tfvars.exemplo`, o runbook do plano 08, `docs/` de spec datada, `.planning/`, `referencias-blip/` (fora do git) e endereços de e-mail (`nao-responda@...`) quando forem remetente e não host de rota. E-mail remetente é classificado na allowlist, não removido.
</context>

<tasks>

<task type="auto">
  <name>Task 1: .env.example e guia de dev com subdomínio</name>
  <files>.env.example, docs/tenant-subdomains-dev.md</files>
  <read_first>
    - .env.example (linhas 1-60 e 240-262 de hoje)
    - apps/gestao-vite/vite.config.ts, apps/desk-vite/vite.config.ts (plano 04)
    - PROJECT-HANDOFF.md (ambiente local: Docker, `[::1]`, `MSYS_NO_PATHCONV=1`)
  </read_first>
  <action>
    1. `.env.example`, seção "Endereço por conta": reescrever sem `usepipe.ai` e sem hub. Três blocos comentados:
       - desligado (padrão, vazio): comportamento de hoje, localhost e portas;
       - lvh.me (recomendado): `PIPE_DOMINIO_CONTAS=lvh.me`, `PIPE_COOKIE_DOMINIO=.lvh.me`, `PIPE_PORTA_PUBLICA=3110`, `PIPE_COOKIE_SEGURO=false`, `GOOGLE_URL_RETORNO=http://login.lvh.me:3110$GOOGLE_CALLBACK_PATH` (a API recusa subir com callback fora do login central, plano 03); entrar por `http://login.lvh.me:3110/` (login central, D-17) e usar `http://<slug>.lvh.me:3110/application` e `http://<slug>.desk.lvh.me:3210/`;
       - `*.localhost`: `PIPE_DOMINIO_CONTAS=localhost`, `PIPE_COOKIE_DOMINIO=` (vazio), com o aviso de que cada app pede login.
       Deixar claro que a API valida o par domínio/cookie no boot (plano 03) e que `PIPE_ORIGENS` agora é só para origens fixas extras (CRM de dev em 3300).
    2. `docs/tenant-subdomains-dev.md` (texto em português, identificadores no original): pré-requisitos, provisionar dois tenants locais (comando de provisionamento renomeado pela Phase 1, slugs `alfa` e `beta`), subir API e fronts, URLs, como entrar pelo login de dev, como entrar pelo login central `login.lvh.me:3110`, como ver o redirecionamento para o login ao trocar para o host de outro tenant, o que muda sem internet (arquivo `hosts`), e por que `*.localhost` não compartilha sessão. Aviso: `.app` está na lista de HSTS preload dos navegadores, e `http://*.usepipe.app` nunca funciona. Dev usa lvh.me, nunca o domínio real.
    Commit: `docs(01.1-06): document tenant subdomain dev modes in env example and dev guide`.
  </action>
  <verify>
    <automated>! rg -n "usepipe\.ai|app\.usepipe" .env.example && rg -q "PIPE_DOMINIO_CONTAS=lvh.me" .env.example && rg -q "PIPE_COOKIE_DOMINIO=.lvh.me" .env.example && test -f docs/tenant-subdomains-dev.md</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` sem `usepipe.ai` e sem hub; os três modos documentados
    - O guia existe e cita `tenant_mismatch`, `lvh.me`, `*.localhost` e HSTS
  </acceptance_criteria>
  <done>Qualquer pessoa sobe Gestão e Desk em subdomínio local seguindo um documento.</done>
</task>

<task type="auto">
  <name>Task 2: Varredura SUB-01 de domínio literal</name>
  <files>tools/check-domain-literals.mjs, tools/domain-literals-allow.txt</files>
  <read_first>
    - tools/ (convenção de scripts da Phase 1, ex.: `tools/std/*`)
    - .planning/phases/01.1-subdominio-por-tenant/01.1-CONTEXT.md (D-01)
  </read_first>
  <action>
    1. Script Node sem dependência nova: lista arquivos com `git ls-files`, procura `usepipe\.(app|ai|com\.br)` e reporta `arquivo:linha:trecho` de cada ocorrência fora da allowlist. Sai 1 se houver alguma.
    2. `tools/domain-literals-allow.txt`: um glob por linha, com comentário do motivo (os caminhos do bloco de contexto). Linhas específicas (ex.: remetente de e-mail em `.env.example`) entram como `arquivo:regex`, não liberando o arquivo inteiro de código.
    3. Rodar e corrigir o que sobrar dos planos 02 a 05. Comentários de código que citam o domínio antigo como exemplo trocam para `<slug>.<D>`. Se a correção exigir mudança de comportamento, parar e reportar (o plano dono do arquivo corrige).
    4. Registrar o comando no SUMMARY, para o plano 07 usar como porta de entrada do smoke.
    Commit: `test(01.1-06): add domain literal scan with allowlist (SUB-01)`.
  </action>
  <verify>
    <automated>node tools/check-domain-literals.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/check-domain-literals.mjs` sai 0
    - Um teste negativo (arquivo temporário com `https://acme.usepipe.app` em `apps/api/src`, criado e apagado na mesma execução) faz o script sair 1; resultado no SUMMARY
    - Allowlist sem entrada `apps/**` ou `packages/**` genérica
  </acceptance_criteria>
  <done>Domínio de produção não volta a ser literal no código sem que a varredura acuse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Suíte de isolamento entre tenants e smoke de dev</name>
  <files>apps/api/tests/tenant-isolation.test.ts, scripts/dev-tenant-smoke.sh</files>
  <read_first>
    - apps/api/tests/request-tenant.test.ts (plano 02), apps/api/tests/entrada.test.ts (plano 03), apps/api/tests/tempo-real.test.ts
    - scripts/ (convenção de scripts da raiz)
  </read_first>
  <action>
    1. `tenant-isolation.test.ts` (base `pipe.test`, tenants `alfa` e `beta`, um usuário em cada), um `describe` com passos em sequência:
       a. sem sessão, `GET $ME_ENDPOINT` em `alfa.pipe.test` → 401 (o front manda para o login central, plano 04); login de `alfa` em `login.pipe.test` com `returnTo=https://alfa.pipe.test/application` → cookie com `Domain=.pipe.test`, redirect para `https://alfa.pipe.test/application`;
       b. com esse cookie: `GET $ME_ENDPOINT` em `alfa.pipe.test` e em `alfa.desk.pipe.test` → 200, mesmo usuário (sessão compartilhada entre Gestão e Desk);
       c. mesmo cookie em `beta.pipe.test` e `beta.desk.pipe.test` → 403 `tenant_mismatch`; nenhum dado de beta nem de alfa no corpo;
       d. uma rota de escrita (ex.: criar etiqueta, renomeada pela Phase 1) em `beta.pipe.test` → 403, e nada gravado em beta;
       e. WebSocket em `beta.desk.pipe.test` → 403; em `alfa.desk.pipe.test` → conecta;
       f. convite criado em alfa → link em `https://login.pipe.test<INVITATION_ROUTE>/...`; aceitar → destino `https://alfa.pipe.test/application`;
       g. login de usuário de beta em `login.pipe.test` com `returnTo=https://alfa.pipe.test/application` → redirect para `https://beta.pipe.test/application` (returnTo de outro tenant descartado); com essa sessão, `GET $ME_ENDPOINT` em `alfa.pipe.test` → 403 `tenant_mismatch`, e em `login.pipe.test` → 200 com slug beta;
       h. logout em `alfa.desk.pipe.test` → cookie apagado com `Domain=.pipe.test`; depois, `GET $ME_ENDPOINT` em `alfa.pipe.test` → 401.
    2. `scripts/dev-tenant-smoke.sh`: pressupõe Postgres/Redis de dev e tenants `alfa`/`beta` provisionados (o script confere e explica como provisionar se faltar). Sobe API e os dois Vite em background com o `.env` no modo lvh.me, espera as portas, e com `curl --resolve login.lvh.me:3110:127.0.0.1 --resolve alfa.lvh.me:3110:127.0.0.1 --resolve beta.lvh.me:3110:127.0.0.1 ...` faz o login de dev de alfa via `login.lvh.me:3110/v1/...` (confere que o redirect final vai para `alfa.lvh.me:3110/application`), guarda o cookie e confere: 200 em `alfa.lvh.me:3110$ME_ENDPOINT`, 200 em `alfa.desk.lvh.me:3210$ME_ENDPOINT`, 403 em `beta.lvh.me:3110$ME_ENDPOINT`. Derruba os processos no `trap EXIT`. Não usa rede externa (`--resolve` dispensa DNS).
    Commit: `test(01.1-06): end-to-end tenant isolation suite and dev subdomain smoke`.
  </action>
  <verify>
    <automated>set -a && . .planning/phases/01.1-subdominio-por-tenant/names.env && set +a && pnpm --filter "$API_PKG" exec vitest run tests/tenant-isolation.test.ts && bash scripts/dev-tenant-smoke.sh</automated>
  </verify>
  <acceptance_criteria>
    - Os 8 passos da suíte passam
    - O smoke de dev imprime as 3 checagens como OK e sai 0
    - Suíte completa da API e dos fronts sem regressão (`pnpm test` na raiz; flaky conhecidos isolados)
  </acceptance_criteria>
  <done>O isolamento entre tenants está provado de ponta a ponta na API e no caminho real de dev (Vite → API).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Desenvolvedor → configuração local | `.env` de dev pode divergir do de produção |
| Repositório → produção | literal de domínio esquecido vira host fixo em produção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-06-01 | Elevation of privilege | regressão futura no guard ou no WS | mitigate | Suíte de isolamento de ponta a ponta no `pnpm test` |
| T-01.1-06-02 | Tampering | host fixo reintroduzido no código | mitigate | Varredura SUB-01 com allowlist estreita |
| T-01.1-06-03 | Spoofing | dev usando o domínio real (HSTS, cookie de produção) | mitigate | Guia e `.env.example` indicam só lvh.me/localhost; aviso de HSTS |
| T-01.1-06-04 | Information disclosure | smoke de dev expondo portas para a rede | accept | Script usa as mesmas portas de dev de hoje; `0.0.0.0` do Vite é dev local (plano 04); guia avisa para não rodar em rede pública |
</threat_model>

<verification>
Varredura SUB-01 sai 0; suíte de isolamento verde; smoke de dev verde; `pnpm test` na raiz sem regressão.
</verification>

<success_criteria>
Dev com subdomínio reproduzível, isolamento entre tenants provado de ponta a ponta e proteção contra domínio literal.
</success_criteria>

<output>
Create `.planning/phases/01.1-subdominio-por-tenant/01.1-06-SUMMARY.md` when done
</output>
