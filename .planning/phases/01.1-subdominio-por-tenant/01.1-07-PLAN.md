---
phase: 01.1-subdominio-por-tenant
plan: 07
type: execute
wave: 5
depends_on: ["01.1-05", "01.1-06"]
files_modified:
  - infra/compose/smoke-tenants.sh                                        # novo; nome candidato
  - .planning/phases/01.1-subdominio-por-tenant/01.1-SMOKE-CHECKLIST.md
  - .planning/phases/01.1-subdominio-por-tenant/reports/vps-smoke-log.md
autonomous: false
requirements: [SUB-10, SUB-13]

must_haves:
  truths:
    - "Existe um smoke por script, executável de qualquer máquina com curl e openssl, que recebe o domínio base e dois slugs e confere TLS, roteamento, CORS e isolamento; o mesmo script serve ao pré-domínio (sslip.io) e ao domínio real (runbook do plano 08)"
    - "Na VPS em modo pré-domínio (PIPE_DOMINIO_CONTAS=pipe.144-217-164-204.sslip.io, HTTP-01), os tenants demo-a e demo-b respondem nos seus hosts de Gestão e Desk com certificado válido"
    - "O dono validou no navegador: login em demo-a, abertura do Desk sem novo login, recusa ao trocar para demo-b, link de convite no host certo, logout valendo nos dois apps"
    - "Os resultados estão registrados em reports/vps-smoke-log.md, com cada linha do checklist marcada PASS/FAIL"
  artifacts:
    - path: "infra/compose/smoke-tenants.sh (nome em 01.1-NAMES.md)"
      provides: "smoke automatizado de borda e isolamento"
    - path: ".planning/phases/01.1-subdominio-por-tenant/01.1-SMOKE-CHECKLIST.md"
      provides: "linhas SMK-01..SMK-NN com modo (script|navegador) e resultado"
  key_links:
    - from: "infra/compose/smoke-tenants.sh"
      to: "infra/compose/docker-compose.tls-http.yml (plano 05)"
      via: "hosts demo-a/demo-b listados no override"
      pattern: "demo-a"
---

<objective>
Provar na VPS atual, antes de existir o domínio, que o modelo de subdomínio por tenant funciona de ponta a ponta: borda, TLS, cookie, login Google e isolamento.

Purpose: testes locais não cobrem Traefik real, Let's Encrypt, Google OAuth nem o comportamento do navegador com cookie de domínio. O modo pré-domínio (D-13) existe para isso. O mesmo roteiro depois valida o domínio real (plano 08).
Output: script de smoke, checklist, log de resultados na VPS.

Model routing: Sonnet escreve o script e o checklist; o dono executa na VPS e no Google Cloud Console (Claude não tem acesso a nenhum dos dois, como na virada 01-33 da Phase 1).
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-subdominio-por-tenant/01.1-CONTEXT.md
@.planning/phases/01.1-subdominio-por-tenant/01.1-NAMES.md
@.planning/phases/01.1-subdominio-por-tenant/01.1-05-SUMMARY.md
@.planning/phases/01.1-subdominio-por-tenant/01.1-06-SUMMARY.md
@.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/smoke-checklist.md

Carregar os caminhos com `set -a; . .planning/phases/01.1-subdominio-por-tenant/names.env; set +a`.

Fatos:
- VPS: `144.217.164.204`; hoje em `https://pipe.144-217-164-204.sslip.io` (PROJECT-HANDOFF.md:303-308). O sslip.io resolve qualquer nome que contenha o IP com hífens, então `demo-a.pipe.144-217-164-204.sslip.io` e `demo-a.desk.pipe.144-217-164-204.sslip.io` resolvem sem configurar DNS.
- A virada da Phase 1 (01-33) reconstruiu a VPS com `docker-compose.prod.yml` (renomeado pela Phase 1). D-43: pode reconstruir de novo; sem dados reais.
- Google OAuth: redirect URI tem que ser exato. No pré-domínio: `https://api.pipe.144-217-164-204.sslip.io$GOOGLE_CALLBACK_PATH`. O usuário de teste precisa estar na lista do app em modo Teste.
- Base de pré-domínio: `B=pipe.144-217-164-204.sslip.io`; slugs `demo-a`, `demo-b`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Script de smoke e checklist</name>
  <files>infra/compose/smoke-tenants.sh, .planning/phases/01.1-subdominio-por-tenant/01.1-SMOKE-CHECKLIST.md</files>
  <read_first>
    - infra/compose/docker-compose.prod.yml e os overrides de TLS (plano 05)
    - .planning/phases/01.1-subdominio-por-tenant/01.1-06-SUMMARY.md (comandos da varredura e do smoke de dev)
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/smoke-checklist.md (formato das linhas)
  </read_first>
  <action>
    1. `infra/compose/smoke-tenants.sh <dominio-base> <slug-a> <slug-b>` (bash, `set -euo pipefail`, só curl, openssl e jq). Cada checagem imprime `SMK-NN PASS|FAIL <descrição>`, e o script sai 1 se alguma falhar. Cookie de sessão opcional em `SMOKE_COOKIE_A` (valor copiado do navegador pelo dono; nunca gravado em arquivo nem impresso):
       - SMK-01 TLS: para `api.B`, `a.B`, `a.desk.B`, `b.B`, `b.desk.B`: `openssl s_client -servername <host> -connect <host>:443 -verify_return_error` conclui, e o certificado cobre o host;
       - SMK-02 roteamento: `https://a.B/` responde a SPA da Gestão (ou redireciona para `/application`); `https://a.desk.B/` responde a SPA do Desk (conferir um marcador de cada `index.html`);
       - SMK-03 API no host do tenant: `https://a.B/v1/<rota de saúde pública>` 200 (usar a rota pública de saúde da API; `/metrics` NÃO responde em nenhum host);
       - SMK-04 CORS: preflight `OPTIONS` em `https://api.B$ME_ENDPOINT` com `Origin: https://a.B` devolve `access-control-allow-origin: https://a.B`; com `Origin: https://a.B.evil.example` não devolve;
       - SMK-05 HTTP→HTTPS: `http://a.B/` → 301/308 para https;
       - SMK-06 (com `SMOKE_COOKIE_A`): `$ME_ENDPOINT` em `a.B` e `a.desk.B` → 200 com slug `a`; em `b.B` → 403 `tenant_mismatch`;
       - SMK-07 (com `SMOKE_COOKIE_A`): upgrade WS em `b.desk.B` → 403 (curl com cabeçalhos de upgrade);
       - SMK-08 host reservado: `https://desk.B/` e `https://admin.B/` não servem dados (tela "endereço não encontrado" da Gestão, ou erro de TLS no modo HTTP-01; ambos contam como PASS, registrar qual).
    2. `01.1-SMOKE-CHECKLIST.md`: tabela `| id | modo | passo | esperado | resultado |` com SMK-01..08 (modo `script`) e as linhas de navegador:
       - SMK-09 login Google em `https://demo-a.B/` volta para `demo-a.B/application` logado;
       - SMK-10 link "Desk" da Gestão abre `demo-a.desk.B/` sem pedir login;
       - SMK-11 com a sessão de demo-a, abrir `demo-b.B/application` → tela de recusa/403, nenhum dado de demo-b;
       - SMK-12 usuário de demo-b entrando por `demo-a.B` → recusa `tenant_mismatch`;
       - SMK-13 convite criado em demo-a → link em `https://demo-a.B/...`;
       - SMK-14 logout no Desk → Gestão também deslogada no F5;
       - SMK-15 DevTools → Application → Cookies: cookie de sessão com `Domain=.B`, `HttpOnly`, `Secure`, `SameSite=Lax`.
    3. `bash -n` e `shellcheck` (se instalado) no script.
    Commit: `test(01.1-07): tenant subdomain smoke script and checklist`.
  </action>
  <verify>
    <automated>bash -n infra/compose/smoke-tenants.sh && grep -c "^| SMK-" .planning/phases/01.1-subdominio-por-tenant/01.1-SMOKE-CHECKLIST.md | grep -qx 15</automated>
  </verify>
  <acceptance_criteria>
    - Script passa em `bash -n`; nenhum domínio literal nele (`node tools/check-domain-literals.mjs` sai 0)
    - Checklist com 15 linhas SMK
    - O script nunca imprime o valor de `SMOKE_COOKIE_A`
  </acceptance_criteria>
  <done>Roteiro de smoke pronto para a VPS e reaproveitável no domínio real.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Dono publica o modo pré-domínio na VPS e cadastra o redirect do Google</name>
  <files>.planning/phases/01.1-subdominio-por-tenant/01.1-SMOKE-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/01.1-subdominio-por-tenant/01.1-SMOKE-CHECKLIST.md
    - infra/compose/env.prod.exemplo (bloco pré-domínio, plano 05)
  </read_first>
  <action>Claude prepara e entrega ao dono a lista exata de valores do `.env` de pré-domínio (sem segredos) e os comandos. O dono, na VPS e no Google Cloud Console: (1) edita o arquivo SOPS de produção com `PIPE_DOMINIO_CONTAS=pipe.144-217-164-204.sslip.io`, `PIPE_COOKIE_DOMINIO=.pipe.144-217-164-204.sslip.io`, `PIPE_ACME_DESAFIO=http`, `PIPE_PORTA_PUBLICA=` vazio, `GOOGLE_URL_RETORNO=https://api.pipe.144-217-164-204.sslip.io$GOOGLE_CALLBACK_PATH`, `PIPE_ORIGENS=https://crm.pipe.144-217-164-204.sslip.io`, `PIPE_URL_ENTRADA=https://pipe.144-217-164-204.sslip.io/`; (2) cadastra o mesmo redirect URI no Google Cloud Console; (3) publica a versão com o script de deploy (renomeado pela Phase 1), ou reconstrói com o de primeira implantação (D-43); (4) provisiona `demo-a` e `demo-b` com um admin de teste cada.</action>
  <what-built>Código das Waves 1-4, overrides de TLS, deploy com validações, script de smoke.</what-built>
  <how-to-verify>
    O deploy termina sem parada de validação; `docker compose ps` mostra todos os serviços healthy; os logs do Traefik mostram certificados emitidos para api, demo-a, demo-a.desk, demo-b, demo-b.desk. Informar a versão publicada e a saída do provisionamento (sem segredos).
  </how-to-verify>
  <verify>
    <automated>test -f infra/compose/smoke-tenants.sh</automated>
  </verify>
  <acceptance_criteria>
    - Dono informou deploy concluído, 5 certificados emitidos e os dois tenants provisionados
  </acceptance_criteria>
  <resume-signal>Cole a saída do deploy e do provisionamento (sem segredos), ou descreva o que falhou</resume-signal>
  <done>VPS em modo pré-domínio com dois tenants.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Rodar o smoke por script e o roteiro de navegador</name>
  <files>.planning/phases/01.1-subdominio-por-tenant/reports/vps-smoke-log.md</files>
  <read_first>
    - .planning/phases/01.1-subdominio-por-tenant/01.1-SMOKE-CHECKLIST.md
  </read_first>
  <action>Claude roda `bash infra/compose/smoke-tenants.sh pipe.144-217-164-204.sslip.io demo-a demo-b` (SMK-01..05 e 08 não precisam de cookie). O dono faz o login em demo-a no navegador, roda SMK-09..15 e, se quiser SMK-06/07, reexecuta o script com `SMOKE_COOKIE_A` exportado no próprio terminal. Claude grava `reports/vps-smoke-log.md` com a saída do script (sem cookie) e o resultado de cada linha, e atualiza a coluna `resultado` do checklist.</action>
  <what-built>Modo pré-domínio publicado (Task 2).</what-built>
  <how-to-verify>
    Todas as 15 linhas SMK em PASS. Qualquer FAIL: registrar o sintoma e parar. A correção volta ao plano dono do arquivo (02, 03, 04 ou 05), e o smoke é repetido depois.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/01.1-subdominio-por-tenant/reports/vps-smoke-log.md && ! grep -q "FAIL" .planning/phases/01.1-subdominio-por-tenant/reports/vps-smoke-log.md</automated>
  </verify>
  <acceptance_criteria>
    - `reports/vps-smoke-log.md` com as 15 linhas em PASS
    - Nenhum valor de cookie ou token no log (`rg -n "pipe_|=ey|Bearer" reports/vps-smoke-log.md` só devolve nomes, nunca valores)
  </acceptance_criteria>
  <resume-signal>Informe o resultado de SMK-09..15 (e SMK-06/07 se rodados), ou descreva o que falhou</resume-signal>
  <done>Subdomínio por tenant validado na VPS real antes da compra do domínio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Máquina do smoke → VPS pública | requisições reais de fora |
| Dono → terminal/log | cookie de sessão real usado no smoke |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-07-01 | Information disclosure | cookie de sessão real vazando em log ou commit | mitigate | Só por variável de ambiente; script não imprime; checagem de log no aceite; sessão de teste encerrada por logout (SMK-14) |
| T-01.1-07-02 | Spoofing | redirect URI de OAuth amplo demais | mitigate | Um URI exato em `api.<B>`; nada de curinga (o Google nem aceita) |
| T-01.1-07-03 | Elevation of privilege | isolamento só testado localmente | mitigate | SMK-06/07/11/12 repetem a matriz na borda real |
| T-01.1-07-04 | Denial of service | rate limit do Let's Encrypt ao repetir deploys | accept | 5 hosts; `acme.json` persiste no volume entre deploys |
</threat_model>

<verification>
`reports/vps-smoke-log.md` com SMK-01..15 em PASS.
</verification>

<success_criteria>
Borda, TLS, cookie, login Google e isolamento funcionando na VPS em modo pré-domínio, com um roteiro reaproveitável para o domínio real.
</success_criteria>

<output>
Create `.planning/phases/01.1-subdominio-por-tenant/01.1-07-SUMMARY.md` when done
</output>
