---
phase: 01.1-subdominio-por-tenant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01.1-subdominio-por-tenant/names.env
  - packages/contracts/src/tenant-host.ts
  - packages/contracts/src/index.ts
  - packages/contracts/package.json
  - packages/contracts/tests/tenant-host.test.ts
autonomous: true
requirements: [SUB-01, SUB-02]

must_haves:
  truths:
    - "A fase só começa com a Phase 1 concluída: GLOSSARY.md, CONVENTIONS-EN.md e std/map/*.csv aprovados, e a Gestão já roteando sob /application. A pré-condição é verificada, não suposta"
    - "Os nomes novos da fase são os da tabela D-15 do CONTEXT (conferidos contra GLOSSARY.md/CONVENTIONS-EN.md aprovados) e não colidem com nenhum `new` do mapa aprovado; names.env dá os caminhos pós-Phase 1 dos arquivos existentes, usados pelos <verify>"
    - "Existe UM módulo puro, sem dependência de Node, que valida slug, conhece os reservados, faz o parse de um host em {slug, app} e monta a origem de um tenant a partir do domínio base configurado (D-01, D-03)"
    - "O parse é estrito: só aceita <slug>.<D> e <slug>.desk.<D> no limite de label, sem porta divergente, sem label extra, sem reservado"
  artifacts:
    - path: "packages/contracts/src/tenant-host.ts"
      provides: "RESERVED_SUBDOMAINS, validação de slug, parse de host, montagem de origem de tenant"
      contains: "desk"
    - path: "packages/contracts/tests/tenant-host.test.ts"
      provides: "casos de tabela do parse e da montagem, incluindo hosts maliciosos"
    - path: ".planning/phases/01.1-subdominio-por-tenant/names.env"
      provides: "variáveis de shell com pacotes, pastas e arquivos pós-Phase 1"
      contains: "AUTH_PKG="
  key_links:
    - from: "packages/contracts/src/tenant-host.ts"
      to: "apps/api/src/provisionar.ts:66 (SLUG_ACEITAVEL, renomeado pela Phase 1)"
      via: "mesma regra de slug; o provisionamento passa a importar daqui no plano 03"
      pattern: "a-z0-9"
---

<objective>
Verificar que a Phase 1 entregou o que esta fase pressupõe, fixar os nomes novos e criar o módulo puro de host de tenant que API, fronts e provisionamento vão compartilhar.

Purpose: toda regra desta fase (origem, guard, links, reservados) depende de responder "este host é de qual tenant, e de qual app?". Se cada camada implementar a própria resposta, elas divergem, e a divergência vira brecha (RESEARCH, Don't Hand-Roll). O módulo fica em `@pipe/contracts` porque os fronts já dependem dele e ele não pode importar `node:crypto`, que o `@pipe/autenticacao` importa.
Output: `names.env`, `tenant-host.ts` e testes.

Model routing: Sonnet executa as duas tarefas (a Task 1 lê o mapa e confere colisões; o Haiku não decide nome, D-25 da Phase 1).
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-subdominio-por-tenant/01.1-CONTEXT.md
@.planning/phases/01.1-subdominio-por-tenant/01.1-RESEARCH.md

P1 = `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o`
P = `.planning/phases/01.1-subdominio-por-tenant`

Fatos (código de hoje, antes da Phase 1):
- `SLUG_ACEITAVEL = /^[a-z0-9]+(-[a-z0-9]+)*$/` em `apps/api/src/provisionar.ts:66` (renomeado pela Phase 1). Só valida formato; não há lista de reservados.
- `@pipe/contracts` não tem script `test`. Os fronts usam `node --import tsx --test tests/*.test.ts` (`apps/desk-vite/package.json:13`); seguir o mesmo padrão.
- A Gestão hoje tem destino padrão `/portal` (`apps/gestao-vite/src/lib/entrada.ts:9`). D-42 da Phase 1 nomeia o módulo principal `application`; a URL-alvo desta fase é `<slug>.<D>/application/...`.
- Nomes novos (tabela D-15 do CONTEXT, já conferidos contra o glossário e a convenção aprovados): arquivo `tenant-host.ts`; `RESERVED_SUBDOMAINS`; `isValidTenantSlug(slug)`; `isReservedSubdomain(label)`; tipo `TenantApp = 'application' | 'desk'`; tipo `TenantHostConfig { baseDomain: string; publicPort: string; secure: boolean }`; `parseTenantHost(host, config): { slug: string; app: TenantApp } | null`; `buildTenantOrigin(slug, app, config): string`; `TENANT_SLUG_PATTERN`; códigos de erro `tenant_mismatch` (403) e `reserved_slug` (400).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar pré-condições da Phase 1 e resolver caminhos (names.env)</name>
  <files>.planning/phases/01.1-subdominio-por-tenant/names.env</files>
  <read_first>
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/GLOSSARY.md
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/CONVENTIONS-EN.md
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv (linhas dos arquivos citados em 01.1-CONTEXT.md)
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/nav-contract.md (rotas de login, convite, raiz do Desk)
  </read_first>
  <action>
    1. Pré-condições. Parar e reportar, sem editar nada, se qualquer item falhar:
       a. `P1/std/GLOSSARY.md` e `P1/std/CONVENTIONS-EN.md` continuam `Status: APPROVED` (já estavam em 2026-09-24, commit `8cfb254`) e os termos usados no D-15 (`application`, `desk`, `login`, `invitation`, `management`, `domain`, `session`) não mudaram; se algum mudou, parar e reportar.
       b. Portão 2 fechado: as linhas de `P1/std/map/*.csv` dos arquivos citados nos planos têm `status` aprovado e `new` preenchido (em 2026-09-24 ainda estavam `candidate`, com `new` vazio); e existe `P1/01-33-SUMMARY.md` (virada da Phase 1 feita).
       c. `rg -n "controladores/|pipe_sessao" apps packages --glob '!**/dist/**'` não devolve nada fora das exceções classificadas da Phase 1.
       d. O roteador da Gestão (App.tsx, renomeado pela Phase 1) declara as telas do Portal sob `/application`. Se não declarar, parar: a fase pressupõe as rotas no formato Blip (ROADMAP §Phase 01.1).
       e. O Desk tem o atendimento em `/` (D-42 da Phase 1).
    2. Para cada arquivo citado nos planos 01.1-01 a 01.1-08, resolver o caminho pós-Phase 1 pelo `std/map/*.csv` e escrever em `P/names.env`, uma variável por linha, sem aspas nem espaços:
       `AUTH_PKG` (nome npm de `@pipe/autenticacao`), `AUTH_DIR`, `CONTRACTS_PKG=@pipe/contracts`, `API_PKG=@pipe/api`, `MGMT_PKG`/`MGMT_DIR` (Gestão), `DESK_PKG`/`DESK_DIR`,
       `API_SERVER_FILE` (`servidor.ts`), `API_WS_FILE` (`eventos-ws.ts`), `API_SESSION_GUARD_FILE` (`apps/api/src/sessao.ts`), `API_AUTH_FILE` (`apps/api/src/autenticacao.ts`), `API_LOGIN_FILE` (`controladores/entrar.ts`), `API_SSO_FILE` (`controladores/sso.ts`), `API_INVITATIONS_FILE` (`dominio/convites.ts`), `API_PROVISION_FILE` (`provisionar.ts`),
       `AUTH_SESSION_FILE` (`packages/autenticacao/src/sessao.ts`), `AUTH_LOGIN_FILE` (`packages/autenticacao/src/entrada.ts`), `CONTRACTS_SESSION_FILE` (`packages/contracts/src/sessao.ts`),
       `MGMT_SHELL_FILE` (`componentes/estrutura-gestao.tsx`), `MGMT_ROLLOUT_PAGE_FILE` (`paginas/implantacao/page.tsx`), `MGMT_OPERATION_SHELL_FILE` (`paginas/operacao/casca.tsx`),
       `COMPOSE_PROD` (`infra/compose/docker-compose.prod.yml`), `ENV_PROD_EXAMPLE` (`infra/compose/env.prod.exemplo`), `FIRST_DEPLOY_SCRIPT` (`infra/compose/implantar.sh`), `DEPLOY_SCRIPT` (`infra/compose/deploy.sh`), `BUILD_IMAGES_SCRIPT` (`infra/construir-imagens.sh`), `DNS_MODULE_DIR` (`infra/terraform/modules/dns`), `PROD_TF_DIR` (`infra/terraform/ambientes/producao`),
       e os caminhos de URL `LOGIN_ROUTE` e `INVITATION_ROUTE` (novos nomes de `/entrar` e `/convite`), `ME_ENDPOINT` (novo nome de `/v1/eu`), `GOOGLE_CALLBACK_PATH` e `SSO_CALLBACK_PATH` (D-12/D-13 da Phase 1).
       Todo caminho de arquivo precisa existir (`test -e`).
    3. Colisão: para cada nome da tabela D-15 do CONTEXT, conferir que nenhuma coluna `new` do mapa aprovado já usa o mesmo nome com outro sentido (`rg -n "tenant-host|request-tenant|tenant-links|tenant_mismatch|reserved_slug|buildTenantOrigin|parseTenantHost|TENANT_SLUG_PATTERN" P1/std/map/`). Havendo colisão, parar e reportar; não renomear por conta própria.
    Commit: `docs(01.1-01): verify phase 1 preconditions and resolve post-rename paths`.
  </action>
  <verify>
    <automated>set -a && . .planning/phases/01.1-subdominio-por-tenant/names.env && set +a && for f in "$API_SERVER_FILE" "$API_WS_FILE" "$API_SESSION_GUARD_FILE" "$API_LOGIN_FILE" "$API_SSO_FILE" "$API_INVITATIONS_FILE" "$API_PROVISION_FILE" "$AUTH_SESSION_FILE" "$CONTRACTS_SESSION_FILE" "$MGMT_SHELL_FILE" "$COMPOSE_PROD" "$ENV_PROD_EXAMPLE" "$FIRST_DEPLOY_SCRIPT" "$BUILD_IMAGES_SCRIPT" "$DNS_MODULE_DIR"; do test -e "$f" || { echo "missing $f"; exit 1; }; done && grep -q "Status: APPROVED" .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/GLOSSARY.md && grep -q "Status: APPROVED" .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/CONVENTIONS-EN.md && ! awk -F, 'NR>1 && $12=="candidate"' .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - As 5 pré-condições foram checadas e o resultado de cada uma está no SUMMARY
    - `names.env` pode ser carregado com `set -a; . names.env` e todo caminho de arquivo existe
    - A checagem de colisão do passo 3 não encontrou nada (saída no SUMMARY)
    - Nenhum código alterado nesta tarefa (`git diff --stat HEAD~1 -- apps packages infra` vazio)
  </acceptance_criteria>
  <done>Pré-condições verificadas; caminhos pós-Phase 1 resolvidos para o resto da fase.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Módulo puro de host de tenant em @pipe/contracts, com testes</name>
  <files>packages/contracts/src/tenant-host.ts, packages/contracts/src/index.ts, packages/contracts/package.json, packages/contracts/tests/tenant-host.test.ts</files>
  <read_first>
    - .planning/phases/01.1-subdominio-por-tenant/01.1-CONTEXT.md (tabela D-15)
    - packages/contracts/src/index.ts, packages/contracts/package.json, packages/contracts/tsconfig.json
    - apps/api/src/provisionar.ts:55-75 (SLUG_ACEITAVEL; renomeado pela Phase 1, `$API_PROVISION_FILE`)
    - apps/desk-vite/package.json (padrão do script test com node:test)
  </read_first>
  <action>
    Teste primeiro (`tests/tenant-host.test.ts`, node:test + tsx, casos em tabela). Depois o módulo, com os nomes da tabela D-15:
    - Constante com a lista de reservados de D-03, `as const`, em minúsculas.
    - Validação de slug: mesma regex de `SLUG_ACEITAVEL`, comprimento 1 a 63, e fora dos reservados. Exportar também a regex como `TENANT_SLUG_PATTERN`, para o provisionamento importar (plano 03), em vez de manter duas cópias.
    - `TenantHostConfig`: `baseDomain` (valor de `PIPE_DOMINIO_CONTAS`, minúsculo, sem ponto inicial nem final), `publicPort` (valor de `PIPE_PORTA_PUBLICA`, vazio em produção), `secure` (true exceto dev http). O módulo NÃO lê `process.env` nem `window`: quem chama monta a config. Isso mantém o módulo puro e utilizável nos fronts.
    - Parse de host (entrada = valor cru de `Host` ou `hostname`, com ou sem porta): minúsculas; remover um ponto final; separar porta; se `publicPort` for não vazio, a porta tem que ser igual; se vazio, a porta tem que estar ausente ou ser 443/80 conforme `secure`. `baseDomain` vazio → sempre `null` (modo desligado, D-14). Aceitar só `<slug>.desk.<base>` → `{slug, app:'desk'}` e `<slug>.<base>` → `{slug, app:'application'}`, com o slug válido. Checar a forma desk antes da forma application. Qualquer outro formato → `null`.
    - Montagem de origem: `<http|https>://<slug>[.desk].<base>[:publicPort]`, sem barra final. Slug inválido ou reservado → lançar erro (é bug de quem chama, não entrada de usuário).
    - Exportar pelo `index.ts`. Adicionar `"test": "node --import tsx --test tests/*.test.ts"` ao `package.json` (e `tsx` em devDependencies se ainda não resolver pelo workspace).
    Casos obrigatórios da tabela (base `usepipe.app`, porta vazia, secure):
    `acme.usepipe.app`→acme/application; `acme.desk.usepipe.app`→acme/desk; `ACME.UsePipe.App.`→acme/application; `acme.usepipe.app:443`→acme/application; `acme.usepipe.app:8443`→null; `api.usepipe.app`→null; `desk.usepipe.app`→null; `www.usepipe.app`→null; `usepipe.app`→null; `a.b.usepipe.app`→null; `x.desk.desk.usepipe.app`→null; `api.desk.usepipe.app`→null; `evilusepipe.app`→null; `acme.usepipe.app.evil.com`→null; `-acme.usepipe.app`→null; `acme-.usepipe.app`→null; `xn--acme.usepipe.app`→null; label de 64 caracteres→null; `acme.usepipe.app` com base vazia→null; base `lvh.me` e porta `3110`: `acme.lvh.me:3110`→acme/application e `acme.lvh.me`→null. Origem: `acme`/desk → `https://acme.desk.usepipe.app`; `api` → lança erro.
    Commit: `feat(01.1-01): add pure tenant host module shared by api and fronts`.
  </action>
  <verify>
    <automated>pnpm --filter @pipe/contracts test && pnpm --filter @pipe/contracts typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @pipe/contracts test` passa com todos os casos da tabela acima
    - `rg -n "process\.env|window\.|node:" packages/contracts/src/tenant-host.ts` não devolve nada (módulo puro)
    - `rg -n "usepipe\.(app|ai|com\.br)" packages/contracts/src` não devolve nada (D-01; o domínio só aparece nos testes)
    - `pnpm typecheck` na raiz sai 0
  </acceptance_criteria>
  <done>Uma implementação só de "host → tenant/app" e "tenant/app → origem", testada contra hosts maliciosos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Header `Host`/`Origin` → parse | valor controlado pelo cliente, entra em toda decisão de tenant desta fase |
| Nome de slug → DNS/roteamento | slug vira label de subdomínio servido pelo Traefik |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-01-01 | Spoofing | parse de host aceitando sufixo sem limite de label (`evilusepipe.app`, `acme.usepipe.app.evil.com`) | mitigate | Casos negativos obrigatórios na tabela; comparação por labels, não `endsWith` cru |
| T-01.1-01-02 | Elevation of privilege | slug igual a host de sistema (`api`, `desk`, `admin`) | mitigate | Lista única de reservados, aplicada no parse e exportada para o provisionamento (plano 03) |
| T-01.1-01-03 | Tampering | duas regras de slug divergentes (API x fronts) | mitigate | Regex exportada de um só lugar; provisionamento passa a importar (plano 03) |
| T-01.1-01-04 | Information disclosure | domínio de produção fixo no código | mitigate | Módulo recebe config; varredura SUB-01 no plano 06 |
</threat_model>

<verification>
`pnpm --filter @pipe/contracts test` verde; `names.env` existe e resolve todos os caminhos.
</verification>

<success_criteria>
Phase 1 confirmada como concluída; caminhos resolvidos; módulo puro e testado de host de tenant disponível para API e fronts.
</success_criteria>

<output>
Create `.planning/phases/01.1-subdominio-por-tenant/01.1-01-SUMMARY.md` when done
</output>
