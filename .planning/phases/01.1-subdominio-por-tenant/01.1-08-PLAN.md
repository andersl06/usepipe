---
phase: 01.1-subdominio-por-tenant
plan: 08
type: execute
wave: 3
depends_on: ["01.1-05"]
files_modified:
  - infra/runbooks/domain-activation.md                     # novo; nome candidato
  - infra/README.md
  - docs/specs/2026-09-05-infraestrutura.md                 # só nota de adendo no topo (nome de arquivo conforme a Phase 1)
  - docs/specs/2026-09-07-implantacao.md                    # idem
  - docs/specs/<data>-subdominio-por-tenant.md              # novo; adendo datado (nome conforme CONVENTIONS-EN.md para docs)
  - infra/k8s/base/rotas.yaml                               # só comentário de desatualizado (renomeado pela Phase 1)
  - infra/k8s/valores/cert-manager-issuer.yaml              # idem
autonomous: true
requirements: [SUB-11, SUB-14]

must_haves:
  truths:
    - "Existe um runbook, em passos numerados e com dono de cada passo (dono ou sistema), que leva do domínio comprado ao Pipe servindo <slug>.usepipe.app e <slug>.desk.usepipe.app com certificado curinga, e que termina rodando o smoke do plano 07 com o domínio real"
    - "O caminho recomendado é Cloudflare Registrar + DNS, registros DNS only, token restrito à zona, DNS-01 cloudflare (D-11)"
    - "O fallback Hostinger está documentado e começa por um passo que verifica se a tag do Traefik tem o provider hostinger no binário; se não tiver, o passo diz qual ação tomar (D-12)"
    - "O runbook tem seção de rollback (voltar ao modo pré-domínio) e de proteção contra subdomain takeover (ordem de desmontagem, nada de CNAME de terceiro na zona do cookie)"
    - "As specs que descrevem os 4 hosts fixos ganham nota de adendo apontando para o adendo datado, sem edição silenciosa do texto antigo (RESEARCH, State of the Art)"
  artifacts:
    - path: "infra/runbooks/domain-activation.md (nome em 01.1-NAMES.md)"
      provides: "ativação do domínio, fallback Hostinger, rollback, takeover"
      contains: "CF_DNS_API_TOKEN"
    - path: "docs/specs/<data>-subdominio-por-tenant.md"
      provides: "adendo datado: topologia por tenant substitui os 4 hosts fixos"
  key_links:
    - from: "infra/runbooks/domain-activation.md"
      to: "infra/compose/smoke-tenants.sh (plano 07) e infra/compose/implantar.sh (plano 05, renomeado pela Phase 1)"
      via: "passos finais do runbook"
      pattern: "smoke-tenants"
---

<objective>
Escrever o runbook de ativação do domínio `usepipe.app` para quando ele for comprado, com o fallback Hostinger, e registrar nas specs que a topologia de 4 hosts fixos foi substituída.

Purpose: a compra do domínio acontece depois desta fase, fora de qualquer sessão de código, e mistura ações que só o dono faz (comprar, criar token, Google Console) com ações do sistema (apply, deploy, smoke). Sem um roteiro fechado, a ativação vira improviso no dia em que alguém for vender.
Output: runbook, adendo datado da spec, notas de desatualizado nas specs antigas e no caminho k8s.

Model routing: Sonnet.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-subdominio-por-tenant/01.1-CONTEXT.md
@.planning/phases/01.1-subdominio-por-tenant/01.1-NAMES.md
@.planning/phases/01.1-subdominio-por-tenant/01.1-05-SUMMARY.md
@.planning/phases/01.1-subdominio-por-tenant/01.1-RESEARCH.md

Carregar os caminhos com `set -a; . .planning/phases/01.1-subdominio-por-tenant/names.env; set +a`.

Fatos:
- Plano 05 entregou: overrides `tls-dns`/`tls-http`, variável de modo ACME, validações do deploy, módulo DNS com `@`, `www`, `api`, `crm`, `metricas`, `*`, `*.desk`, sem proxy e com CAA, e a tag do Traefik com o resultado da checagem dos providers (no SUMMARY).
- `.app` é TLD na lista de HSTS preload: todo `*.usepipe.app` só abre em HTTPS. Isso não atrapalha o ACME (DNS-01 não usa HTTP), mas proíbe qualquer teste em `http://`.
- E-mail: `PIPE_EMAIL_REMETENTE` usa domínio verificado no provedor (Resend). Registros de e-mail (MX, TXT, DKIM) não servem HTTP. Um subdomínio usado por terceiro (ex.: `send.` para bounce) recebe o cookie de sessão se servir HTTP, então precisa entrar na lista de reservados antes de ser criado.
- Google Cloud Console: redirect URI exato `https://api.usepipe.app$GOOGLE_CALLBACK_PATH`; domínio autorizado `usepipe.app` na tela de consentimento.
- SSO: a URL de retorno do SSO é `PIPE_URL_API` + `$SSO_CALLBACK_PATH` (`env.prod.exemplo`); não há cliente com SSO (D-13 da Phase 1).
- Hostinger: provider lego `hostinger`, variável `HOSTINGER_API_TOKEN`; token da Hostinger vale para a conta, não para uma zona. O provider Terraform `hostinger/hostinger ~> 0.1` já está nos ambientes, mas gerenciar registros DNS por ele precisa ser conferido na documentação do provider no dia; sem suporte, os registros entram pelo painel.
- Specs de hoje: `docs/specs/2026-09-05-infraestrutura.md`, `docs/specs/2026-09-07-implantacao.md` (4 hosts fixos, HTTP-01). Caminho k8s alternativo: `infra/k8s/base/rotas.yaml`, `infra/k8s/valores/cert-manager-issuer.yaml` (fora de uso).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Runbook de ativação do domínio (Cloudflare, com fallback Hostinger)</name>
  <files>infra/runbooks/domain-activation.md, infra/README.md</files>
  <read_first>
    - .planning/phases/01.1-subdominio-por-tenant/01.1-05-SUMMARY.md (tag do Traefik, providers no binário)
    - $FIRST_DEPLOY_SCRIPT, $DEPLOY_SCRIPT, $ENV_PROD_EXAMPLE, $DNS_MODULE_DIR/main.tf, $PROD_TF_DIR/main.tf
    - infra/compose/smoke-tenants.sh (plano 07; se ainda não existir, citar pelo nome de 01.1-NAMES.md)
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/cutover-runbook-draft.md (estilo de runbook do projeto)
  </read_first>
  <action>
    Escrever `infra/runbooks/domain-activation.md` (nome em 01.1-NAMES.md; texto em português), com cabeçalho "quando usar: depois de comprar usepipe.app" e estas seções, com passos numerados, marcados **[dono]** ou **[sistema]**, e comando exato quando houver:
    1. **Pré-requisitos:** fase 01.1 concluída e smoke pré-domínio (plano 07) em PASS; acesso à VPS; chave age do SOPS.
    2. **Comprar e criar a zona [dono]:** Cloudflare Registrar, `usepipe.app` (a zona nasce na Cloudflare). Anotar `zone_id`.
    3. **Token de DNS [dono]:** API Token personalizado com Zone → DNS → Edit e Zone → Zone → Read, recurso "Specific zone: usepipe.app", sem outras permissões, TTL de expiração anotado. Um token para o Traefik (DNS-01) e outro para o Terraform, para poder revogar um sem derrubar o outro.
    4. **Registros [sistema]:** `terraform.tfvars` com `dominio = "usepipe.app"` e `zone_id`; `terraform -chdir=$PROD_TF_DIR plan` e revisão (só registros `A` DNS only e o CAA); `apply`. Checar com `dig +short api.usepipe.app`, `dig +short qualquer-coisa.usepipe.app`, `dig +short qualquer-coisa.desk.usepipe.app`, `dig CAA usepipe.app`.
    5. **Google [dono]:** cadastrar o redirect URI exato de `api.usepipe.app`, adicionar `usepipe.app` aos domínios autorizados, e remover o redirect do sslip depois do passo 9.
    6. **E-mail [dono]:** verificar `usepipe.app` no provedor de e-mail. Se ele pedir subdomínio que sirva HTTP ou CNAME para terceiro, parar: o nome precisa entrar na lista de reservados (código, plano 01) e ser avaliado quanto ao cookie antes de ser criado.
    7. **Configuração [dono + sistema]:** no arquivo SOPS de produção: `PIPE_DOMINIO_CONTAS=usepipe.app`, `PIPE_COOKIE_DOMINIO=.usepipe.app`, `PIPE_ACME_DESAFIO=dns` (nome em 01.1-NAMES.md), `CF_DNS_API_TOKEN=<token do passo 3>`, `GOOGLE_URL_RETORNO`, `PIPE_URL_API`, `PIPE_URL_ENTRADA`, `PIPE_ORIGENS=https://crm.usepipe.app`, `ACME_EMAIL`. Lista completa copiada de `$ENV_PROD_EXAMPLE`.
    8. **Publicar [sistema]:** D-43: reconstruir com o script de primeira implantação (as validações do plano 05 param antes de qualquer `docker` se algo estiver errado), ou publicar com o script de deploy. Acompanhar `docker compose logs -f traefik` até os certificados `*.usepipe.app`, `*.desk.usepipe.app` e `usepipe.app`/`www` serem emitidos. Se o DNS-01 der timeout, conferir o token e o TTL do TXT (Pitfall 1) antes de tentar de novo (limite do Let's Encrypt).
    9. **Validar [sistema + dono]:** provisionar dois tenants de teste; rodar `bash infra/compose/smoke-tenants.sh usepipe.app <slug-a> <slug-b>` e as linhas de navegador do checklist do plano 07 com o domínio real; registrar o resultado.
    10. **Limpar o pré-domínio [dono]:** remover o redirect do sslip no Google; apagar `acme.json` do HTTP-01 se não for mais usado.
    11. **Rollback:** voltar o `.env` ao bloco pré-domínio (`PIPE_ACME_DESAFIO=http`, base sslip) e publicar de novo. Nenhum dado depende do domínio (D-43), então o rollback é só configuração.
    12. **Fallback Hostinger** (se o domínio for comprado ou mantido na Hostinger):
        a. verificar o provider na tag em uso: `docker run --rm --entrypoint grep traefik:<tag do compose> -c -a HOSTINGER_API_TOKEN /usr/local/bin/traefik`. Resultado 0 → trocar a tag por uma que o inclua (conferir as notas de versão do Traefik/lego), repetir a verificação e só então seguir. Nunca montar imagem própria do Traefik;
        b. no override `tls-dns`: `provider=hostinger` e `HOSTINGER_API_TOKEN` no lugar do token Cloudflare;
        c. registros: os mesmos da seção 4, pelo provider Terraform `hostinger` se ele gerenciar DNS (conferir a documentação no dia), senão pelo painel, conferindo que o painel aceita `*.desk` e CAA;
        d. risco registrado: o token da Hostinger vale para a conta inteira (VPS inclusive). Guardar só no SOPS, rotacionar depois da primeira emissão e preferir migrar a zona para a Cloudflare quando possível.
    13. **Subdomain takeover:** nunca criar CNAME para serviço de terceiro dentro de `usepipe.app` (o cookie `Domain=.usepipe.app` chegaria a ele); para desativar a VPS, apagar os registros DNS ANTES de liberar o IP; conferir periodicamente que `*.usepipe.app` resolve para o IP atual (a checagem de DNS do deploy faz isso a cada implantação); status page ou outro serviço externo fica em outro domínio.
    `infra/README.md`: uma linha apontando para o runbook, na seção de publicação.
    Commit: `docs(01.1-08): domain activation runbook with cloudflare path and hostinger fallback`.
  </action>
  <verify>
    <automated>f=infra/runbooks/domain-activation.md; test -f "$f" && for k in "CF_DNS_API_TOKEN" "HOSTINGER_API_TOKEN" "grep traefik" "smoke-tenants" "Rollback" "takeover" "CAA" "\*.desk" "DNS only"; do grep -qi -- "$k" "$f" || { echo "missing: $k"; exit 1; }; done && grep -q "domain-activation" infra/README.md</automated>
  </verify>
  <acceptance_criteria>
    - As 13 seções existem, com passos marcados [dono]/[sistema]
    - O fallback Hostinger começa pela verificação do provider no binário e diz o que fazer se o resultado for 0
    - Nenhum segredo ou valor de token no arquivo
  </acceptance_criteria>
  <done>A ativação do domínio é seguir uma lista, não improvisar.</done>
</task>

<task type="auto">
  <name>Task 2: Adendo datado nas specs e nota no caminho k8s</name>
  <files>docs/specs/2026-09-05-infraestrutura.md, docs/specs/2026-09-07-implantacao.md, docs/specs/&lt;data&gt;-subdominio-por-tenant.md, infra/k8s/base/rotas.yaml, infra/k8s/valores/cert-manager-issuer.yaml</files>
  <read_first>
    - docs/specs/2026-09-05-infraestrutura.md e docs/specs/2026-09-07-implantacao.md (trechos de hosts fixos, HTTP-01, "tenant pelo login")
    - .planning/phases/01.1-subdominio-por-tenant/01.1-CONTEXT.md
    - CONVENTIONS-EN.md da Phase 1 (nome de arquivo de doc)
  </read_first>
  <action>
    1. Novo adendo datado em `docs/specs/` (data da execução; nome conforme a convenção de docs da Phase 1): resumo de D-01 a D-14 em linguagem de spec, tabela de hosts (fixos e por tenant), regra de resolução de tenant pelo Host, cookie, TLS e DNS, e links para o runbook e para `01.1-CONTEXT.md`.
    2. Nas duas specs antigas, uma nota no topo: "Adendo <data>: a topologia de hosts fixos e o HTTP-01 descritos aqui foram substituídos por subdomínio por tenant; ver <adendo>". O corpo não muda (disciplina de spec versionada).
    3. Nos dois YAML do k8s, comentário no topo: caminho alternativo fora de uso, ainda no modelo de hosts fixos/HTTP-01; ao reativar, seguir o adendo (curinga via DNS-01 no cert-manager).
    Commit: `docs(01.1-08): dated spec addendum for per-tenant subdomains; mark fixed-host docs superseded`.
  </action>
  <verify>
    <automated>ls docs/specs | grep -qi "subdominio-por-tenant\|tenant-subdomain" && grep -qi "adendo" docs/specs/2026-09-05-infraestrutura.md && grep -qi "adendo" docs/specs/2026-09-07-implantacao.md && git diff HEAD~1 --numstat -- docs/specs/2026-09-05-infraestrutura.md docs/specs/2026-09-07-implantacao.md | awk '$2 > 0 { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - Adendo datado existe e cita o runbook
    - As specs antigas só ganharam linhas (nenhuma removida; o `numstat` do verify confere)
    - Os YAML do k8s só ganharam comentário (`kubectl`/yaml parse inalterado: `python3 -c "import yaml,sys;[list(yaml.safe_load_all(open(f))) for f in sys.argv[1:]]" infra/k8s/base/rotas.yaml infra/k8s/valores/cert-manager-issuer.yaml`)
  </acceptance_criteria>
  <done>Specs e caminho k8s não contradizem mais a topologia em produção.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dono → registrador/DNS | quem controla a zona controla o cookie e os certificados |
| Runbook → operador futuro | um passo fora de ordem abre takeover ou vaza token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-08-01 | Spoofing | subdomain takeover por CNAME de terceiro ou IP liberado | mitigate | Seção 13 do runbook; ordem de desmontagem; nenhum CNAME de terceiro na zona |
| T-01.1-08-02 | Elevation of privilege | token de DNS com escopo amplo (Hostinger vale para a conta) | mitigate | Cloudflare com token por zona como padrão; risco e rotação documentados no fallback |
| T-01.1-08-03 | Information disclosure | token colado no runbook ou em log | mitigate | Runbook só cita nomes de variável; valores no SOPS |
| T-01.1-08-04 | Denial of service | rate limit do Let's Encrypt por tentativas repetidas | mitigate | Passo 8 manda diagnosticar token/TTL antes de repetir |
| T-01.1-08-05 | Spoofing | serviço de e-mail/status em subdomínio recebendo o cookie | mitigate | Passo 6 e seção 13: subdomínio de terceiro só depois de reservado e avaliado; status em outro domínio |
</threat_model>

<verification>
Runbook com as 13 seções e as palavras-chave do verify; adendo datado; specs antigas só com linhas adicionadas.
</verification>

<success_criteria>
Quando `usepipe.app` for comprado, a ativação segue um roteiro testável (termina no smoke do plano 07), com fallback Hostinger verificável e proteção contra takeover documentada.
</success_criteria>

<output>
Create `.planning/phases/01.1-subdominio-por-tenant/01.1-08-SUMMARY.md` when done
</output>
