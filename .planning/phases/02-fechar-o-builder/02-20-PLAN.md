---
phase: 02-fechar-o-builder
plan: 20
type: execute
wave: 10
depends_on: ["02-18", "02-19"]
files_modified:
  - packages/db/src/schema/automacao.ts
  - packages/db/drizzle/
  - packages/core/src/fluxo/acoes.ts
  - packages/core/src/fluxo/acoes.teste.ts
  - packages/core/src/fluxo/contexto.ts
  - apps/api/src/dominio/fluxo.ts
  - apps/api/tests/flow-platform-actions.test.ts
  - apps/gestao-vite/src/paginas/builder/acoes-do-bloco.ts
  - apps/gestao-vite/src/paginas/builder/painel-acoes.tsx
  - apps/gestao-vite/tests/builder-actions.test.ts
autonomous: true
requirements: [BUILDER-01]

must_haves:
  truths:
    - "Cada ação de plataforma que o dono aprovou como 'equivalente nativo' (ex.: SetBucket, ManageList, SendCommand/ProcessCommand, TrackContactsJourney, ProcessContentAssistant) existe no Builder e executa no motor sobre um serviço do Pipe (D-20)"
    - "Cada ação aprovada como 'dependência externa' aparece só em fluxo importado, só leitura, com a marca 'Não executada no Pipe' e a mensagem do UI-SPEC; não é criável (D-20, D-24)"
    - "conferir-catalogo --all passa: todo item reproduzível está nos dois lados da whitelist e todo externo está em EXTERNAL_DEPENDENCY_ACTIONS (D-24)"
  artifacts:
    - path: "apps/gestao-vite/src/paginas/builder/acoes-do-bloco.ts"
      provides: "EXTERNAL_DEPENDENCY_ACTIONS + entradas das ações nativas"
      contains: "EXTERNAL_DEPENDENCY_ACTIONS"
  key_links:
    - from: "EXTERNAL_DEPENDENCY_ACTIONS"
      to: "marca 'Não executada no Pipe'"
      via: "leitura de fluxo importado"
      pattern: "Não executada no Pipe"
---

<objective>
Fechar o catálogo de ações (BUILDER-01): ações de plataforma com equivalente nativo aprovado e registro explícito das dependências externas (D-20, D-24).
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fechar-o-builder/02-UI-SPEC.md
@.planning/phases/02-fechar-o-builder/ref/inventario-acoes.md
@.planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md

Escopo exato: itens de `ref/catalogo-aprovado.json` com `"slot": "acoes-plataforma"` — `reproduzivel` recebe implementação nativa; `externa` entra em `EXTERNAL_DEPENDENCY_ACTIONS`. A opção aprovada por ação está na linha `APROVADO:` (D-20) de `CLASSIFICACAO-PORTAO.md`. Bloqueados ficam fora (D-03).

Pré-condição e nomes (D-05, STD-01): o gate da Phase 1 (tag `std-apply-all-end`) é imposto pela Task 0 (Pre-flight Gate) de 02-05/02-06/02-08, das quais este plano depende transitivamente via `depends_on` (02-18 → 02-13 → 02-08). Caminhos/símbolos EXISTENTES com nome em português de 2026-09-26; localizar o nome vigente pela linha `status=approved` de `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv` ou por `ref/REFERENCIA-CONGELADA.md` §Caminhos resolvidos. Identificadores NOVOS em inglês; SQL novo em snake_case português (D-08 da Phase 1); `type`/`settings` literais da Blip (STD-06). `@pipe/gestao-vite` vira `@pipe/management-vite` após D-46. Nenhum CSS novo.
Copy da marca externa (UI-SPEC): "Esta ação depende de um serviço da Blip que o Pipe ainda não reproduz. Marcada como não executada — revise antes de publicar."
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persistência das ações nativas que exigem estado (D-20)</name>
  <files>packages/db/src/schema/automacao.ts, packages/db/drizzle/</files>
  <read_first>
    - .planning/phases/02-fechar-o-builder/ref/inventario-acoes.md (Opções para o portão (D-20) de cada ação do slot)
    - .planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md (APROVADO: por ação)
    - packages/db/src/schema/automacao.ts, packages/db/drizzle/0001_rls.sql
  </read_first>
  <action>
    Para cada ação nativa aprovada que precise de estado (ex.: SetBucket → armazenamento chave-valor por tenant/escopo documentado; ManageList → listas de distribuição e membros), criar as tabelas descritas na opção (a) aprovada, com `tenant_id`, `carimbos()`, índices únicos pela chave documentada, limite de tamanho de valor (do inventário ou 64 KB) e migration custom com RLS `tenant_isolado`. Se nenhuma ação aprovada precisar de estado, não criar tabela e registrar `Sem persistência nova` no SUMMARY.
  </action>
  <verify>
    <automated>pnpm banco:subir && pnpm banco:migrar && pnpm --filter @pipe/db typecheck</automated>
  </verify>
  <acceptance_criteria>
    - toda tabela nova tem política `tenant_isolado` na migration custom
    - SUMMARY lista tabela por ação ou `Sem persistência nova`
    - verify sai 0
  </acceptance_criteria>
  <done>Estado das ações nativas persistido com isolamento.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Motor e serviços das ações nativas</name>
  <files>packages/core/src/fluxo/acoes.ts, packages/core/src/fluxo/acoes.teste.ts, packages/core/src/fluxo/contexto.ts, apps/api/src/dominio/fluxo.ts, apps/api/tests/flow-platform-actions.test.ts</files>
  <read_first>
    - packages/core/src/fluxo/acoes.ts (padrões de 02-14/02-16)
    - packages/core/src/fluxo/contexto.ts (ServicosDoMotor)
    - apps/api/src/dominio/fluxo.ts (servicos)
    - .planning/phases/02-fechar-o-builder/ref/inventario-acoes.md (settings, comportamento, erros, variáveis de saída de cada ação)
  </read_first>
  <behavior>
    - Por ação nativa: settings válidos → efeito documentado e variáveis de saída; obrigatório ausente → mensagem literal; serviço indisponível → comportamento documentado.
    - Escrita/leitura só no tenant da execução (teste com dado de outro tenant invisível).
    - SendCommand/ProcessCommand (se nativos): só URIs de comando da lista aprovada; URI fora da lista → erro, nunca repassada a serviço interno arbitrário.
  </behavior>
  <action>
    Um `AcaoDoMotor` por ação nativa registrado em `ACOES_DO_MOTOR`; serviços opcionais novos em `ServicosDoMotor` (inglês, ex.: `bucketSet`, `bucketGet`, `listManage`, `sendCommand`, `trackJourney`) implementados em `fluxo.ts` dentro da transação, com filtro por tenant. Para comandos, uma allowlist explícita `ALLOWED_COMMAND_URIS` derivada do inventário. Testes `describe('platform actions')` e `flow-platform-actions.test.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @pipe/core exec vitest run src/fluxo && pnpm banco:subir && pnpm --filter @pipe/api exec vitest run tests/flow-platform-actions.test.ts && pnpm --filter @pipe/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - cada ação nativa aprovada com `tipo: '<Tipo>'` no motor
    - teste de isolamento entre tenants presente e verde
    - se houver comando: `ALLOWED_COMMAND_URIS` existe e teste de URI fora da lista falha a ação
    - verify sai 0
  </acceptance_criteria>
  <done>Ações de plataforma nativas executam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Editor das ações nativas e lista de dependências externas (D-20, D-24)</name>
  <files>apps/gestao-vite/src/paginas/builder/acoes-do-bloco.ts, apps/gestao-vite/src/paginas/builder/painel-acoes.tsx, apps/gestao-vite/tests/builder-actions.test.ts</files>
  <read_first>
    - apps/gestao-vite/src/paginas/builder/acoes-do-bloco.ts (CATALOGO_DE_ACOES, comentário 21-27 sobre "Não executada no Pipe", ACOES_DO_SISTEMA)
    - apps/gestao-vite/src/paginas/builder/painel-acoes.tsx (renderização da marca)
    - .planning/phases/02-fechar-o-builder/02-UI-SPEC.md (copy de dependência externa)
  </read_first>
  <behavior>
    - Ação nativa: criável, com padrões e ida e volta.
    - Ação em `EXTERNAL_DEPENDENCY_ACTIONS` vinda de import: exibida só leitura com a marca e a mensagem do UI-SPEC; não aparece no menu de criação.
    - Ação desconhecida (nem catálogo nem externa) vinda de import: mantém o comportamento atual de só leitura.
  </behavior>
  <action>
    Entradas `TipoDeAcao` das ações nativas; exportar `EXTERNAL_DEPENDENCY_ACTIONS` (array readonly com os `type` aprovados como externos); `painel-acoes.tsx` mostra para esses a marca "Não executada no Pipe" e a mensagem literal do UI-SPEC; o menu de criação não lista externos. Testes `platform actions:`. Rodar `conferir-catalogo.mjs --slot acoes-plataforma` e `--all`.
  </action>
  <verify>
    <automated>pnpm --filter @pipe/gestao-vite test && pnpm --filter @pipe/gestao-vite typecheck && node .planning/phases/02-fechar-o-builder/ref/conferir-catalogo.mjs --all</automated>
  </verify>
  <acceptance_criteria>
    - `conferir-catalogo.mjs --all` imprime `OK` e sai 0
    - `acoes-do-bloco.ts` exporta `EXTERNAL_DEPENDENCY_ACTIONS`
    - `painel-acoes.tsx` contém `Esta ação depende de um serviço da Blip que o Pipe ainda não reproduz.`
  </acceptance_criteria>
  <done>Catálogo de ações fechado; externos registrados explicitamente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fluxo → armazenamento do tenant | chaves/valores de bucket e listas vindos do fluxo |
| fluxo → comandos internos | URIs de comando definidas no Builder |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-15 | Tampering / cross-tenant | bucket/listas | high | mitigate | Task 1 RLS `tenant_isolado`; Task 2 teste de isolamento |
| T-2-30 | Elevation of privilege | SendCommand/ProcessCommand | high | mitigate | Task 2 `ALLOWED_COMMAND_URIS` + teste de URI fora da lista |
| T-2-28 | Denial of service | valores grandes | medium | mitigate | limite de tamanho no banco (Task 1) |
| T-2-05 | Tampering | ação externa importada | low | mitigate | Task 3 só leitura, não executada |
</threat_model>

<verification>
`conferir-catalogo.mjs --all` OK; testes verdes.
</verification>

<success_criteria>
Critério 1 do roadmap completo do lado de ações: suporte ponta a ponta ou dependência externa registrada, item a item.
</success_criteria>

<output>
Create `.planning/phases/02-fechar-o-builder/02-20-SUMMARY.md` when done
</output>
