---
phase: 02-fechar-o-builder
plan: 22
type: execute
wave: 12
depends_on: ["02-05", "02-06", "02-08", "02-09", "02-10", "02-11", "02-12", "02-13", "02-14", "02-15", "02-16", "02-17", "02-18", "02-19", "02-20", "02-21"]
files_modified:
  - .planning/phases/02-fechar-o-builder/ref/medir-tela.js
  - .planning/phases/02-fechar-o-builder/ref/VERIFICACAO-VISUAL.md
  - .planning/phases/02-fechar-o-builder/ref/GATE-FINAL.md
autonomous: false
requirements: [BUILDER-01, BUILDER-02, BUILDER-03, BUILDER-04, BUILDER-05]

must_haves:
  truths:
    - "O repositório inteiro passa typecheck, testes e build depois da fase, e fluxos salvos antes da fase continuam abrindo e publicando (compatibilidade do DesenhoDoBuilder, STD-06) (D-24)"
    - "Todo item do catálogo aprovado está nos dois lados da whitelist ou em EXTERNAL_DEPENDENCY_ACTIONS; itens ja-suportada conferidos quanto à paridade (D-04, D-24)"
    - "Cada tela/estado do inventário congelado tem comparação lado a lado Blip × Pipe com medidas; diferenças só de azul→verde; estados sem captura ficam NEEDS VALIDATION com nota (D-30, D-31, D-32, Claude's Discretion)"
    - "O dono aprova o Builder no portão final da fase (OWNER APPROVED fica também para a Phase 3) (D-04)"
  artifacts:
    - path: ".planning/phases/02-fechar-o-builder/ref/VERIFICACAO-VISUAL.md"
      provides: "comparação por tela/estado com status VISUALLY VERIFIED | NEEDS VALIDATION"
      contains: "| Tela/estado | Medidas Blip | Medidas Pipe | Diferenças | Status |"
    - path: ".planning/phases/02-fechar-o-builder/ref/GATE-FINAL.md"
      provides: "saída dos gates automáticos e aprovação do dono"
  key_links:
    - from: "ref/medir-tela.js"
      to: "getBoundingClientRect/getComputedStyle"
      via: "mesmo snippet nos dois lados (Blip pelo dono, Pipe por CDP)"
      pattern: "getBoundingClientRect"
---

<objective>
Regressão final, verificação visual lado a lado e portão final do dono da Phase 2.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fechar-o-builder/02-CONTEXT.md
@.planning/phases/02-fechar-o-builder/02-UI-SPEC.md
@.planning/phases/02-fechar-o-builder/ref/REFERENCIA-CONGELADA.md
@.planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md

Critério de aceite visual (Claude's Discretion do CONTEXT): foto lado a lado por tela/estado do inventário congelado, captura da Blip fornecida pelo dono e do Pipe, medidas por `getBoundingClientRect`/`getComputedStyle`, diferenças restritas a azul→verde; Builder sai como VISUALLY VERIFIED; OWNER APPROVED fica no portão final e na Phase 3 (VALSURF-05). Não há Playwright no repositório: o Pipe é medido por Chrome/Edge headless via Chrome DevTools Protocol com o `WebSocket` nativo do Node 22 (script descartável no scratchpad, nenhuma dependência nova); o lado Blip é medido pelo dono colando `ref/medir-tela.js` no console do Builder da Blip logado.

Pré-condição e nomes (D-05): comandos com `@pipe/gestao-vite` viram `@pipe/management-vite` após D-46; conferir nomes vigentes pela `ref/REFERENCIA-CONGELADA.md` §Caminhos resolvidos.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gates automáticos de regressão</name>
  <files>.planning/phases/02-fechar-o-builder/ref/GATE-FINAL.md</files>
  <read_first>
    - .planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md (itens ja-suportada e bloqueados)
    - apps/api/tests/jsonb-compat.test.ts (compatibilidade de jsonb gravado, Phase 1)
    - SUMMARY de 02-05..02-21 (itens BLOQUEADO registrados)
  </read_first>
  <action>
    Rodar e registrar em `GATE-FINAL.md` (comando, código de saída, resumo): `pnpm typecheck`, `pnpm test` (com `pnpm banco:subir`), `pnpm build` (com a exclusão já aceita na Phase 1 para `@pipe/crm` se ainda valer), `node .planning/phases/02-fechar-o-builder/ref/conferir-catalogo.mjs --all`, `node .../conferir-catalogo.mjs --slot ja-suportada`, `pnpm --filter @pipe/api exec vitest run tests/jsonb-compat.test.ts`, `grep -rn "#4a5d23\|--bl-verde"` no diretório do Builder (deve ser vazio) e `grep -rn "dangerouslySetInnerHTML"` no diretório do Builder (deve ser vazio). Acrescentar a tabela `| Item | Status final | Plano | Nota |` com todo item do catálogo: IMPLEMENTADO, EXTERNO (registrado) ou BLOQUEADO (C-NN). Qualquer gate vermelho: corrigir na causa (arquivo do plano responsável) antes de seguir e registrar a correção.
  </action>
  <verify>
    <automated>pnpm typecheck && node .planning/phases/02-fechar-o-builder/ref/conferir-catalogo.mjs --all && test $(grep -rn "#4a5d23\|--bl-verde" apps/*/src/*/builder 2>/dev/null | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `GATE-FINAL.md` registra código de saída 0 para typecheck, test, build, conferir-catalogo (--all e ja-suportada) e jsonb-compat
    - a tabela de itens não tem linha sem status
  </acceptance_criteria>
  <done>Regressão completa verde e rastreada.</done>
</task>

<task type="auto">
  <name>Task 2: Verificação visual lado a lado</name>
  <files>.planning/phases/02-fechar-o-builder/ref/medir-tela.js, .planning/phases/02-fechar-o-builder/ref/VERIFICACAO-VISUAL.md</files>
  <read_first>
    - .planning/phases/02-fechar-o-builder/ref/inventario-visual.md e ref/inventario-paineis-e-setas.md (lista de telas/estados)
    - .planning/phases/02-fechar-o-builder/02-UI-SPEC.md (medidas fixas: nó 175px, padding 28px, raio 8px, anel 4px, painel 360px)
    - capturas do dono em referencias-blip/ (medições da Blip)
  </read_first>
  <action>
    Criar `ref/medir-tela.js`: snippet de console que, dado um mapa `{ nome: seletor }`, imprime JSON com `getBoundingClientRect()` (x, y, width, height) e `getComputedStyle` (color, background-color, border-color, box-shadow, font-size, font-weight, line-height, padding) de cada elemento; sem dependência. Pedir ao dono (listar no SUMMARY e em `VERIFICACAO-VISUAL.md`) as medições da Blip que ainda faltarem. Medir o Pipe com um script descartável no scratchpad que abre Edge/Chrome headless (`--remote-debugging-port`), conecta por `WebSocket` nativo, navega ao Builder local (`pnpm --filter @pipe/gestao-vite dev` + API com seed), injeta o mesmo snippet via `Runtime.evaluate` e salva `Page.captureScreenshot` em `referencias-blip/pipe-capturas/` (fora do git). Preencher `VERIFICACAO-VISUAL.md` com `| Tela/estado | Medidas Blip | Medidas Pipe | Diferenças | Status |`: diferença de geometria/tipografia > 1px ou de cor que não seja azul→token verde vira correção no CSS do plano dono da tela (registrada) ou NEEDS VALIDATION com nota; sem captura da Blip → NEEDS VALIDATION (captura C-NN).
  </action>
  <verify>
    <automated>grep -q "getBoundingClientRect" .planning/phases/02-fechar-o-builder/ref/medir-tela.js && grep -qF "| Tela/estado | Medidas Blip | Medidas Pipe | Diferenças | Status |" .planning/phases/02-fechar-o-builder/ref/VERIFICACAO-VISUAL.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - toda tela/estado dos inventários visual e de painéis tem uma linha com Status `VISUALLY VERIFIED` ou `NEEDS VALIDATION` + nota
    - nenhuma linha `VISUALLY VERIFIED` tem diferença além de azul→verde
    - nenhuma captura/print é versionada (só medidas em texto)
  </acceptance_criteria>
  <done>Builder verificado visualmente contra o snapshot.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Portão final do dono</name>
  <what-built>Phase 2 completa: catálogo de conteúdo/ação ponta a ponta, biblioteca de funções, seletor de destino com busca, pesquisa de satisfação + etiquetas, Versões/Filas/Teste, setas validadas, identidade escura azul→verde, fixes do ProcessHttp.</what-built>
  <how-to-verify>
    1. Ler `ref/GATE-FINAL.md` (todos os gates com saída 0; tabela de itens) e `ref/VERIFICACAO-VISUAL.md`.
    2. Subir `pnpm banco:subir`, API e gestão; abrir um fluxo no Builder.
    3. Criar um bloco de cada tipo de conteúdo e uma ação de cada slot; publicar; conversar no painel de Teste e conferir o Debug.
    4. Ligar blocos pelo seletor com busca; conferir as setas; criar a pesquisa de satisfação a partir do atendimento humano.
    5. Exportar uma versão antiga pela aba Versões e reimportar.
    6. Conferir que as linhas NEEDS VALIDATION estão corretas e anotar o que vai para a Phase 3.
  </how-to-verify>
  <resume-signal>Responder "aprovado" ou listar os problemas por item.</resume-signal>
  <files>.planning/phases/02-fechar-o-builder/ref/GATE-FINAL.md</files>
  <read_first>
    - .planning/phases/02-fechar-o-builder/ref/GATE-FINAL.md
    - .planning/phases/02-fechar-o-builder/ref/VERIFICACAO-VISUAL.md
  </read_first>
  <action>Apresentar GATE-FINAL.md e VERIFICACAO-VISUAL.md ao dono; após a resposta, acrescentar em GATE-FINAL.md a linha **Aprovação final do dono:** AAAA-MM-DD + resposta.</action>
  <verify>
    <automated>grep -q "Aprovação final do dono:" .planning/phases/02-fechar-o-builder/ref/GATE-FINAL.md</automated>
  </verify>
  <acceptance_criteria>
    - GATE-FINAL.md contém `**Aprovação final do dono:**` com data e resposta
  </acceptance_criteria>
  <done>Phase 2 aprovada ou lista de problemas registrada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capturas/medidas da Blip → repositório | evidência da conta do dono |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Information disclosure | VERIFICACAO-VISUAL.md | high | mitigate | só medidas numéricas e cores; prints ficam em `referencias-blip/` (gitignored); gate grep de cookie/Authorization/bearer = 0 |
| T-2-31 | Repudiation | aprovação final | low | mitigate | GATE-FINAL.md datado com a resposta do dono |
</threat_model>

<verification>
GATE-FINAL.md verde; VERIFICACAO-VISUAL.md completo; dono aprovou.
</verification>

<success_criteria>
Os 5 critérios de sucesso da Phase 2 verificados e aprovados pelo dono.
</success_criteria>

<output>
Create `.planning/phases/02-fechar-o-builder/02-22-SUMMARY.md` when done
</output>
