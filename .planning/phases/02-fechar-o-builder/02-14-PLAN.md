---
phase: 02-fechar-o-builder
plan: 14
type: execute
wave: 5
depends_on: ["02-07", "02-11"]
files_modified:
  - packages/core/src/fluxo/acoes.ts
  - packages/core/src/fluxo/acoes.teste.ts
  - packages/core/src/fluxo/contexto.ts
  - apps/api/src/dominio/fluxo.ts
  - apps/api/tests/flow-actions.test.ts
  - apps/gestao-vite/src/paginas/builder/acoes-do-bloco.ts
  - apps/gestao-vite/src/paginas/builder/painel-acoes.tsx
  - apps/gestao-vite/tests/builder-actions.test.ts
autonomous: true
requirements: [BUILDER-01]

must_haves:
  truths:
    - "O atendente cria no Builder cada ação do slot acoes-contexto aprovada no portão, com todos os campos, padrões e validações da referência (D-19, D-04)"
    - "O motor executa cada uma dessas ações com o comportamento, erros, timeouts e variáveis de saída documentados (D-19)"
    - "Motor e tela sobem juntos: conferir-catalogo --slot acoes-contexto passa (D-24)"
    - "Toda chamada HTTP originada por ação nova passa pela mesma guarda de URL do ProcessHttp (D-19)"
  artifacts:
    - path: "packages/core/src/fluxo/acoes.ts"
      provides: "um AcaoDoMotor por ação do slot, registrado em ACOES_DO_MOTOR"
    - path: "apps/gestao-vite/src/paginas/builder/acoes-do-bloco.ts"
      provides: "uma entrada TipoDeAcao por ação do slot em CATALOGO_DE_ACOES"
  key_links:
    - from: "ACOES_DO_MOTOR"
      to: "CATALOGO_DE_ACOES"
      via: "whitelist dupla verificada por conferir-catalogo.mjs"
      pattern: "--slot acoes-contexto"
---

<objective>
Fechar ponta a ponta as ações do slot `acoes-contexto` aprovadas no portão (ex.: MergeContact, ExecuteTemplate, SendMessageFromHttp, campos faltantes de SetVariable/TrackEvent — a lista exata é a do catálogo aprovado), com editor, contrato serializado, motor e serviços da API (D-19, D-24).
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fechar-o-builder/02-PATTERNS.md
@.planning/phases/02-fechar-o-builder/ref/inventario-acoes.md
@.planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md

Escopo exato: itens de `ref/catalogo-aprovado.json` com `"slot": "acoes-contexto"` e `"classificacao": "reproduzivel"`; também as ações `ja-suportada` cujo inventário aponta campo faltante (ex.: expiração de `SetVariable`, citada em `docs/builder-cards-pendencias.md`). Itens `bloqueado` ficam fora e listados no SUMMARY (D-03). Todo detalhe vem da seção `### <Tipo>` do inventário congelado.

Pré-condição e nomes (D-05, STD-01): só executa depois que a Phase 1 aplicou o mapa (tag `std-apply-all-end`). Caminhos e símbolos EXISTENTES aparecem com o nome em português de 2026-09-26 (ex.: `ACOES_DO_MOTOR` tem linha aprovada em `std/map/packages-core.csv`); localizar o nome vigente pela linha `status=approved` de `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv` ou por `ref/REFERENCIA-CONGELADA.md` §Caminhos resolvidos; nunca escrever nome proposto não aplicado. Identificadores/arquivos/títulos de teste NOVOS em inglês. O `type` serializado da ação (ex.: `MergeContact`) e as chaves de `settings` são literais da Blip e não mudam (STD-06). `@pipe/gestao-vite` vira `@pipe/management-vite` após D-46. Nenhum CSS novo neste plano (usar componentes de `@pipe/ui`).
Padrões: `setVariable` (acoes.ts 47-56) para ação pura; `processHttp` (acoes.ts ~188-230) para ação com serviço/efeito externo; `servicos` em `apps/api/src/dominio/fluxo.ts` (~324-360) com `confirmarUrlSegura` + `chamarComMtls`; entrada `SetVariable` de `CATALOGO_DE_ACOES` (acoes-do-bloco.ts 92-105) e `tipo: 'cabecalhos'` como campo estruturado.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Motor e serviços das ações do slot</name>
  <files>packages/core/src/fluxo/acoes.ts, packages/core/src/fluxo/acoes.teste.ts, packages/core/src/fluxo/contexto.ts, apps/api/src/dominio/fluxo.ts, apps/api/tests/flow-actions.test.ts</files>
  <read_first>
    - packages/core/src/fluxo/acoes.ts (AcaoDoMotor, setVariable, processHttp, ACOES_DO_MOTOR ~233-247)
    - packages/core/src/fluxo/acoes.teste.ts
    - packages/core/src/fluxo/contexto.ts (ServicosDoMotor, definirVariavel)
    - apps/api/src/dominio/fluxo.ts (servicos ~324-360, carregarContato ~915-935)
    - .planning/phases/02-fechar-o-builder/ref/inventario-acoes.md (seções das ações do slot: settings, motor, erros, variáveis)
  </read_first>
  <behavior>
    - Por ação: settings válidos → efeito documentado (variável de saída definida, contato mesclado, template renderizado, mensagem enviada a partir do HTTP etc.).
    - Campo obrigatório ausente → erro com a mensagem literal no padrão `O valor '<campo>' é obrigatório na ação '<Tipo>'.` (ou a da referência).
    - Erro/timeout do serviço → comportamento documentado (variável de status, falha da ação, seguir fluxo).
    - Ação com URL: URL insegura recusada pela guarda.
    - Interpolação de variáveis (`{{...}}`) só substitui texto; nunca avalia expressão.
  </behavior>
  <action>
    Para cada ação do slot, criar um `AcaoDoMotor` com `tipo` igual ao `type` serializado e registrá-lo em `ACOES_DO_MOTOR`; efeitos fora do motor viram serviços opcionais novos em `ServicosDoMotor` (nomes em inglês, ex.: `mergeContact(fields)`, `fetchForMessage(request)`), implementados no objeto `servicos` de `fluxo.ts` dentro da transação da entrada, com escrita só no tenant da execução. HTTP de ação nova sempre via `confirmarUrlSegura` + `chamarComMtls` com o mesmo timeout do ProcessHttp. Testes vitest em `acoes.teste.ts` (`describe('context actions')`) e teste de API `flow-actions.test.ts` para as ações com serviço (padrão de `tests/fluxo.test.ts`).
  </action>
  <verify>
    <automated>pnpm --filter @pipe/core exec vitest run src/fluxo && pnpm banco:subir && pnpm --filter @pipe/api exec vitest run tests/flow-actions.test.ts && pnpm --filter @pipe/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - cada `tipo` do slot aparece como `tipo: '<Tipo>'` no arquivo de ações do motor e na lista `ACOES_DO_MOTOR` (nome vigente)
    - `acoes.teste.ts` tem `describe('context actions'` com casos válido/obrigatório/erro por ação
    - `flow-actions.test.ts` cobre URL insegura rejeitada quando o slot tem ação com HTTP
    - verify sai 0
  </acceptance_criteria>
  <done>Motor executa as ações de contexto aprovadas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Editor das ações no Builder</name>
  <files>apps/gestao-vite/src/paginas/builder/acoes-do-bloco.ts, apps/gestao-vite/src/paginas/builder/painel-acoes.tsx, apps/gestao-vite/tests/builder-actions.test.ts</files>
  <read_first>
    - apps/gestao-vite/src/paginas/builder/acoes-do-bloco.ts (CATALOGO_DE_ACOES 57-126, novaAcao 164-173, valorDoCampo/comCampo 178-224, comentário 21-27)
    - apps/gestao-vite/src/paginas/builder/painel-acoes.tsx
    - .planning/phases/02-fechar-o-builder/ref/inventario-acoes.md (nome exibido, grupo, info, campos, padrões, validações)
    - packages/ui/src/icones.tsx (ícones de 02-08)
  </read_first>
  <behavior>
    - `novaAcao('<Tipo>')` devolve settings com os valores padrão da referência.
    - Validação da tela aponta os obrigatórios com a mensagem da referência.
    - Ida e volta: ação salva reabre com os mesmos campos (inclusive estruturados).
  </behavior>
  <action>
    Acrescentar a `CATALOGO_DE_ACOES` uma entrada `TipoDeAcao` por ação do slot (`tipo`, `rotulo`, `titulo`, `grupo`, `info` e `campos` com `chave`/`rotulo`/`obrigatorio`/`tipo` exatamente do inventário, textos em português da referência); se um campo exigir editor estruturado novo (ex.: mapa de campos do contato), acrescentar um novo valor de `tipo` de campo tratado em `painel-acoes.tsx` no mesmo padrão de `'cabecalhos'`. Remover dessas ações a marca "Não executada no Pipe". Ícones do Pipe (D-33). Testes `actions:` em `builder-actions.test.ts` (`node:test`). Rodar o gate de catálogo.
  </action>
  <verify>
    <automated>pnpm --filter @pipe/gestao-vite test && pnpm --filter @pipe/gestao-vite typecheck && node .planning/phases/02-fechar-o-builder/ref/conferir-catalogo.mjs --slot acoes-contexto</automated>
  </verify>
  <acceptance_criteria>
    - `conferir-catalogo.mjs --slot acoes-contexto` imprime `OK` e sai 0
    - `builder-actions.test.ts` tem teste de padrão e de ida e volta por ação
    - `painel-acoes.tsx` sem `dangerouslySetInnerHTML`
  </acceptance_criteria>
  <done>Atendente cria as ações de contexto no Builder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fluxo definido/importado → motor | settings de ação não confiáveis |
| motor → rede externa | ações com HTTP |
| motor → dados do contato | ações que escrevem no contato |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-06 | SSRF | ações com HTTP (ex.: SendMessageFromHttp) | high | mitigate | Task 1: `confirmarUrlSegura` + `chamarComMtls`, teste de URL insegura |
| T-2-08 | Tampering (injeção por interpolação) | variáveis em settings | high | mitigate | Task 1: interpolação só textual, sem `eval`/`Function`; teste com `{{constructor}}`/expressão retorna texto literal |
| T-2-15 | Tampering / cross-tenant | serviços que escrevem contato | high | mitigate | Task 1: escrita dentro de `noTenant` da execução, filtro por `contato_id` da entrada; teste com contato de outro tenant falha |
| T-2-05 | Tampering | fluxo importado | medium | mitigate | validação na publicação (obrigatórios) |
</threat_model>

<verification>
Testes de core, api e app verdes; gate `--slot acoes-contexto` OK.
</verification>

<success_criteria>
Slot acoes-contexto fechado ponta a ponta.
</success_criteria>

<output>
Create `.planning/phases/02-fechar-o-builder/02-14-SUMMARY.md` when done
</output>
