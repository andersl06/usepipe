---
phase: 02-fechar-o-builder
plan: 16
type: execute
wave: 7
depends_on: ["02-07", "02-15"]
files_modified:
  - apps/api/package.json
  - pnpm-lock.yaml
  - apps/api/src/dominio/script-sandbox.ts
  - apps/api/tests/script-sandbox.test.ts
  - apps/api/src/dominio/fluxo.ts
  - packages/core/src/fluxo/contexto.ts
  - packages/core/src/fluxo/acoes.ts
  - packages/core/src/fluxo/acoes.teste.ts
  - apps/gestao-vite/src/paginas/builder/acoes-do-bloco.ts
  - apps/gestao-vite/src/paginas/builder/painel-acoes.tsx
  - apps/gestao-vite/tests/builder-actions.test.ts
autonomous: false
requirements: [BUILDER-01]

must_haves:
  truths:
    - "ExecuteScript e ExecuteScriptV2 executam no motor com o comportamento documentado (ECMAScript, variáveis de entrada/saída, retorno, erros, timezone, HTTP se exposto) (D-19, D-21)"
    - "O script roda no sandbox aprovado no portão, sem acesso a require/process/fs/rede fora da API documentada, com limite de tempo e memória (D-21)"
    - "O atendente cria e edita as duas ações no Builder com os campos e validações da referência; conferir-catalogo --slot acoes-script passa (D-24)"
  artifacts:
    - path: "apps/api/src/dominio/script-sandbox.ts"
      provides: "runFlowScript(source, inputs, options) isolado"
      exports: ["runFlowScript"]
    - path: "apps/api/tests/script-sandbox.test.ts"
      provides: "testes de comportamento e de fuga do sandbox"
  key_links:
    - from: "packages/core/src/fluxo/acoes.ts ExecuteScript/ExecuteScriptV2"
      to: "ServicosDoMotor.runScript → runFlowScript"
      via: "serviço injetado em fluxo.ts"
      pattern: "runScript"
---

<objective>
Implementar ExecuteScript e ExecuteScriptV2 ponta a ponta com o mecanismo de sandbox aprovado no portão (D-21), incluindo o gate de legitimidade de pacote quando o mecanismo exigir dependência nova.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fechar-o-builder/ref/inventario-acoes.md
@.planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md

Escopo: itens do slot `acoes-script` (ExecuteScript, ExecuteScriptV2) com `reproduzivel`. Mecanismo: linha `APROVADO:` de D-21 em `CLASSIFICACAO-PORTAO.md` (§Decisões de mecanismo) e `## Scripts (D-21)` do inventário (limites, APIs expostas, timezone, retorno).

Pré-condição e nomes (D-05, STD-01): o gate da Phase 1 (tag `std-apply-all-end`) é imposto pela Task 0 (Pre-flight Gate) de 02-05/02-06/02-08, das quais este plano depende transitivamente via `depends_on` (02-15 → 02-10 → 02-05). Caminhos e símbolos EXISTENTES com nome em português de 2026-09-26; localizar o nome vigente pela linha `status=approved` de `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv` ou por `ref/REFERENCIA-CONGELADA.md` §Caminhos resolvidos. Identificadores/arquivos NOVOS em inglês. `type`/`settings` serializados (`ExecuteScript`, `inputVariables`, `outputVariable`, `source`, …) são literais da Blip (STD-06). `@pipe/gestao-vite` vira `@pipe/management-vite` após D-46.
Não há `## Package Legitimacy Audit` no RESEARCH.md desta fase: qualquer pacote npm do mecanismo aprovado é tratado como `[ASSUMED]` e exige o checkpoint da Task 1 antes de instalar.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Legitimidade do pacote do sandbox (supply chain)</name>
  <what-built>Nada instalado ainda. O mecanismo aprovado em D-21 está em `CLASSIFICACAO-PORTAO.md`.</what-built>
  <how-to-verify>
    1. Abrir `CLASSIFICACAO-PORTAO.md` e ler a linha `APROVADO:` de D-21. Se não houver `Exige pacote npm:`, responder `n/a` (a Task 2 usa só módulos `node:`).
    2. Se houver pacote, abrir `https://www.npmjs.com/package/<nome>` e conferir: repositório oficial ligado, mantenedores conhecidos, versão estável recente, downloads semanais altos, sem aviso de segurança aberto. Conferir também `https://github.com/<org>/<repo>/security/advisories`.
    3. Confirmar o nome EXATO e a versão a fixar (sem `^`).
  </how-to-verify>
  <resume-signal>Responder "n/a", ou "aprovado <nome>@<versão exata>", ou "rejeitado" (volta ao portão D-21).</resume-signal>
  <files>apps/api/package.json</files>
  <read_first>
    - .planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md (APROVADO: D-21)
  </read_first>
  <action>Apresentar ao dono a linha APROVADO de D-21 e, se houver pacote, os dados de npm/GitHub; não instalar nada antes da resposta.</action>
  <verify>
    <automated>grep -q "APROVADO:" .planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md</automated>
  </verify>
  <acceptance_criteria>
    - Resposta do dono é n/a, aprovado nome@versão exata, ou rejeitado; a Task 2 só instala com aprovado
  </acceptance_criteria>
  <done>Decisão de supply chain registrada no SUMMARY.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sandbox e ações ExecuteScript/ExecuteScriptV2 no motor (D-21)</name>
  <files>apps/api/package.json, pnpm-lock.yaml, apps/api/src/dominio/script-sandbox.ts, apps/api/tests/script-sandbox.test.ts, apps/api/src/dominio/fluxo.ts, packages/core/src/fluxo/contexto.ts, packages/core/src/fluxo/acoes.ts, packages/core/src/fluxo/acoes.teste.ts</files>
  <read_first>
    - .planning/phases/02-fechar-o-builder/ref/inventario-acoes.md (## Scripts (D-21) e seções ExecuteScript/ExecuteScriptV2)
    - .planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md (APROVADO: D-21)
    - packages/core/src/fluxo/acoes.ts (processHttp como analog de ação com serviço; ACOES_DO_MOTOR)
    - packages/core/src/fluxo/contexto.ts (ServicosDoMotor, obterVariavelDeContexto, definirVariavel)
    - apps/api/src/dominio/fluxo.ts (servicos, confirmarUrlSegura, chamarComMtls)
  </read_first>
  <behavior>
    - Script `function run(a, b) { return a + b; }` com inputVariables [x, y] = ["1","2"] grava outputVariable conforme a coerção documentada.
    - V2 com função assíncrona e API HTTP documentada (se exposta): HTTP passa por `confirmarUrlSegura`; URL privada recusada.
    - Laço infinito termina no limite de tempo documentado com o erro/comportamento da referência.
    - Alocação excessiva termina no limite de memória.
    - `require`, `process`, `globalThis.process`, `import()`, `Function('return this')().process`, acesso a `fs`/`child_process` e ao `constructor` de objetos passados falham sem vazar nada do host.
    - Erro lançado pelo script: comportamento documentado (ação falha / variável de erro).
    - `new Date()` respeita o timezone documentado.
  </behavior>
  <action>
    Se a Task 1 aprovou pacote, instalar com versão exata em `apps/api` (`pnpm --filter @pipe/api add <nome>@<versão> --save-exact`). Criar `script-sandbox.ts` exportando `runFlowScript(source, inputs, { timeoutMs, memoryMb, timezone, http })` usando o mecanismo aprovado: contexto isolado sem globais do Node, só as APIs documentadas injetadas, limites de tempo/memória vindos do inventário, serialização de entrada/saída por JSON (nada de objeto do host atravessa a fronteira). Em `contexto.ts`, serviço opcional `runScript`; em `acoes.ts`, `AcaoDoMotor` para `ExecuteScript` e `ExecuteScriptV2` registrados em `ACOES_DO_MOTOR`; em `fluxo.ts`, `servicos.runScript` chama `runFlowScript` com HTTP roteado por `confirmarUrlSegura` + `chamarComMtls`. Testes de comportamento e de fuga primeiro em `script-sandbox.test.ts` (sem banco) e de ação em `acoes.teste.ts` (`describe('script actions')`).
  </action>
  <verify>
    <automated>pnpm --filter @pipe/api exec vitest run tests/script-sandbox.test.ts && pnpm --filter @pipe/core exec vitest run src/fluxo && pnpm --filter @pipe/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `script-sandbox.test.ts` tem ≥6 casos de fuga (require, process, import(), Function-constructor, fs, constructor de objeto de entrada) e todos passam
    - teste de laço infinito termina em menos de 2× o limite configurado
    - `script-sandbox.ts` não contém `eval(` nem `new Function(` executados no contexto do host
    - se houve instalação, `apps/api/package.json` fixa a versão sem `^`/`~`
    - T-2-06: `script-sandbox.test.ts` tem `it('blocks private URL from script HTTP (SSRF)'` — HTTP do script para `http://127.0.0.1`, `http://169.254.169.254` e `http://10.0.0.1` é recusado por `confirmarUrlSegura` (nome vigente) sem requisição de saída; se o V2 não expõe HTTP, o teste afirma que não há API HTTP no escopo do script
    - verify sai 0
  </acceptance_criteria>
  <done>Scripts executam isolados e com limites.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Editor de script no Builder</name>
  <files>apps/gestao-vite/src/paginas/builder/acoes-do-bloco.ts, apps/gestao-vite/src/paginas/builder/painel-acoes.tsx, apps/gestao-vite/tests/builder-actions.test.ts</files>
  <read_first>
    - apps/gestao-vite/src/paginas/builder/acoes-do-bloco.ts (CATALOGO_DE_ACOES; entradas de 02-14; CORES_DAS_ACOES em etiquetas-do-bloco.ts para ExecuteScript)
    - apps/gestao-vite/src/paginas/builder/painel-acoes.tsx
    - .planning/phases/02-fechar-o-builder/ref/inventario-acoes.md (campos do editor de script: fonte, variáveis de entrada, variável de saída, padrão do código)
  </read_first>
  <behavior>
    - `novaAcao('ExecuteScript')` e `novaAcao('ExecuteScriptV2')` trazem o código padrão e campos da referência.
    - Validação aponta variável de saída/código ausentes com as mensagens da referência.
    - Ida e volta preserva código e listas de variáveis.
  </behavior>
  <action>
    Entradas em `CATALOGO_DE_ACOES` para as duas ações com campos do inventário; novo tipo de campo `code` (editor de código em `<textarea>` monoespaçado com `--p-fonte-mono`, sem dependência nova de editor) e `variableList` para variáveis de entrada, tratados em `painel-acoes.tsx`; ícone Pipe. Testes `script actions:`. Gate de catálogo.
  </action>
  <verify>
    <automated>pnpm --filter @pipe/gestao-vite test && pnpm --filter @pipe/gestao-vite typecheck && node .planning/phases/02-fechar-o-builder/ref/conferir-catalogo.mjs --slot acoes-script</automated>
  </verify>
  <acceptance_criteria>
    - `conferir-catalogo.mjs --slot acoes-script` imprime `OK` e sai 0
    - testes `script actions:` passam
    - nenhuma dependência nova no `package.json` do app de gestão
  </acceptance_criteria>
  <done>Atendente cria scripts no Builder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| código de script escrito no Builder/importado → processo da API | código arbitrário não confiável |
| script → rede | HTTP a partir do script |
| registry npm → repositório | dependência nova |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-07 | Elevation of privilege (fuga do sandbox / RCE) | script-sandbox.ts | high | mitigate | Task 2: isolamento aprovado, sem globais do Node, fronteira por JSON, ≥6 testes de fuga |
| T-2-28 | Denial of service | laço/alocação no script | high | mitigate | Task 2: limite de tempo e memória, testes de laço infinito e alocação |
| T-2-06 | SSRF | HTTP do script | high | mitigate | Task 2: `confirmarUrlSegura` + `chamarComMtls`, teste de URL privada |
| T-2-SC | Tampering (supply chain) | pacote do sandbox | high | mitigate | Task 1 checkpoint bloqueante humano + versão exata fixada |
| T-2-24 | Information disclosure | logs de erro de script | medium | mitigate | erro registrado sem valores de variáveis do contato |
</threat_model>

<verification>
Testes de sandbox, core e app verdes; gate `--slot acoes-script` OK.
</verification>

<success_criteria>
Scripts suportados ponta a ponta com isolamento comprovado por teste.
</success_criteria>

<output>
Create `.planning/phases/02-fechar-o-builder/02-16-SUMMARY.md` when done
</output>
