---
phase: 02-fechar-o-builder
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/fluxo/gerenciador.ts
  - packages/core/src/fluxo/gerenciador.teste.ts
  - apps/api/src/filas.ts
  - apps/api/src/dominio/fluxo.ts
  - apps/api/tests/process-http-retomada.test.ts
  - .planning/todos/pending/process-http-entering-actions.md
  - .planning/todos/pending/process-http-bullmq-sweep.md
autonomous: true
requirements: [BUILDER-01]

must_haves:
  truths:
    - "Um ProcessHttp em $enteringCustomActions suspende e, na retomada, as ações seguintes da mesma lista executam uma única vez e as anteriores não repetem (D-25)"
    - "Em modo BullMQ existe varredura periódica que recupera process_http_execucao presa em 'chamando' além do timeout e registra alerta (D-26)"
    - "A retomada de ProcessHttp não reinsere id_provedor (opção A) e um teste de regressão falha se a guarda for removida (D-27)"
    - "Cada fix nasceu de um teste que reproduzia o bug e ficou como regressão (D-28)"
  artifacts:
    - path: "packages/core/src/fluxo/gerenciador.teste.ts"
      provides: "regressão da retomada em ações de entrada (D-25)"
      contains: "$enteringCustomActions"
    - path: "apps/api/src/filas.ts"
      provides: "agendarVarreduraProcessHttp com upsertJobScheduler em BullMQ (D-26)"
      contains: "process-http-sweep"
    - path: "apps/api/src/dominio/fluxo.ts"
      provides: "recoverStuckProcessHttp(limiteMs) (D-26)"
      contains: "recoverStuckProcessHttp"
  key_links:
    - from: "apps/api/src/filas.ts"
      to: "recoverStuckProcessHttp"
      via: "Worker da fila de varredura"
      pattern: "recoverStuckProcessHttp\\("
    - from: "apps/api/src/dominio/fluxo.ts"
      to: "execucao_passo_entrada_uk"
      via: "let idProvedorUsado = Boolean(retomada)"
      pattern: "idProvedorUsado = Boolean\\(retomada\\)"
---

<objective>
Wave técnica do motor (D-28), independente das waves de UI: retomada de ProcessHttp em ações de entrada (D-25), varredura BullMQ de execuções presas (D-26) e regressão do duplicate-key (D-27), cada uma com teste que reproduz o bug.

Purpose: o princípio da fase é que o Builder não proíbe combinações para contornar o motor; ProcessHttp em entering actions tem de funcionar.
Output: fixes + testes de regressão; os dois todos dobrados movidos para concluídos.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fechar-o-builder/02-CONTEXT.md
@.planning/debug/process-http-auto-resume.md
@.planning/todos/pending/process-http-entering-actions.md
@.planning/todos/pending/process-http-bullmq-sweep.md

Pré-condição e nomes (D-05, STD-01): a Task 0 (Pre-flight Gate) é a imposição: o plano para se `git tag -l std-apply-all-end` não imprimir a tag (a Phase 1 precisa ter aplicado o mapa; o D-22 da Phase 1 congela código paralelo). Caminhos e símbolos EXISTENTES aparecem aqui com o nome em português de 2026-09-26 (confirmado no disco no planejamento): antes de cada task, localizar o nome vigente no disco pela linha `status=approved` de `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv` (colunas `old`/`new`/`declared_at`) ou por `ref/REFERENCIA-CONGELADA.md` §Caminhos resolvidos, e usar esse nome; símbolo sem linha no mapa mantém o nome atual; nunca escrever nome proposto não aplicado. Identificadores, arquivos, endpoints e títulos de teste NOVOS já vêm em inglês neste plano (`std/CONVENTIONS-EN.md`). Texto visível ao usuário continua em português; nomes SQL novos seguem o snake_case português do schema (D-08 da Phase 1); chaves `$…`/`settings` do desenho `{flow, globalActions}` são contrato persistido (STD-06) e não mudam; variáveis `PIPE_*` não mudam. Depois do D-46, `@pipe/gestao-vite` é `@pipe/management-vite` e `apps/gestao-vite` é `apps/management-vite` nos comandos.

Fatos já verificados no planejamento:
- D-27 já está no código: `let idProvedorUsado = Boolean(retomada);` em `apps/api/src/dominio/fluxo.ts` (~linha 487, commit 8dac98b) e `apps/api/tests/process-http-retomada.test.ts` existe (`describe('retomada de ProcessHttp')`, helper `contagemDeMensagensComId`). Falta provar que o teste pega a regressão.
- Estados de `process_http_execucao`: `ESTADOS_PROCESS_HTTP = ['pendente','chamando','respondida','retomada']` (`packages/db/src/schema/automacao.ts:381`). Sem migration nesta wave.
- Varreduras BullMQ existentes a copiar: `agendarVarreduraEspelhoCrm`/`Midia`/`Sla`/`DicionarioCrm` em `apps/api/src/filas.ts` (`if (modo() === 'memoria') return;` + `Queue.upsertJobScheduler(nome, { every }, { name, data })`).
- Testes de API precisam do Postgres local: `pnpm banco:subir` antes (porta 5433).
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight Gate — Phase 1 aplicou o mapa (tag std-apply-all-end)</name>
  <files>nenhum (verificação somente leitura)</files>
  <action>Antes de qualquer outra task, rodar `git tag -l std-apply-all-end`. Se a saída não for exatamente `std-apply-all-end`, PARAR o plano: não tocar em nenhum arquivo, não commitar, não seguir para a Task 1 e devolver ao orquestrador/usuário "Pre-flight Gate: tag std-apply-all-end ausente — a Phase 1 ainda não aplicou o mapa de nomes; concluir a Phase 1 antes (D-22 da Phase 1 congela `apps/gestao-vite/src/paginas/builder/*`, `packages/core/src/fluxo/*`, `apps/api/src/dominio/fluxo.ts`)". Não criar a tag nem contornar o gate (D-05, STD-01). Com a tag presente, seguir para a Task 1.</action>
  <verify>
    <automated>test "$(git tag -l std-apply-all-end)" = "std-apply-all-end"</automated>
  </verify>
  <acceptance_criteria>
    - `git tag -l std-apply-all-end` imprime `std-apply-all-end`
    - se o gate falhar, `git status --porcelain` não mostra arquivo alterado por este plano e o plano termina sem SUMMARY de conclusão
  </acceptance_criteria>
  <done>Tag `std-apply-all-end` confirmada; plano liberado. Sem a tag, plano parado e escalado ao usuário.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Retomada de ProcessHttp em $enteringCustomActions (D-25, D-28)</name>
  <files>packages/core/src/fluxo/gerenciador.ts, packages/core/src/fluxo/gerenciador.teste.ts</files>
  <read_first>
    - packages/core/src/fluxo/gerenciador.ts (processarEntrada linhas ~170-310, processarAcoes linhas ~357-420)
    - packages/core/src/fluxo/gerenciador.teste.ts (teste 'suspende antes do ProcessHttp e retoma...' linha ~86: montagem de suspenderHttp/retomarProcessHttp)
    - packages/core/src/fluxo/contexto.ts (ListaDeAcoesSuspensa, CursorDeProcessHttp linhas 123-135)
    - .planning/todos/pending/process-http-entering-actions.md
  </read_first>
  <behavior>
    - Teste A (fluxo-nível): `fluxo.inputActions` = [SetVariable a=1, ProcessHttp, SetVariable b=2]; primeira entrada suspende no ProcessHttp; retomada com resposta → `b` vale 2, `a` foi definida uma vez (contar chamadas), nenhuma nova suspensão.
    - Teste B (estado-nível): estado destino com `$enteringCustomActions`/`inputActions` = [SendMessage "antes", ProcessHttp, SendMessage "depois"]; transição até ele suspende; retomada envia "depois" exatamente uma vez e não reenvia "antes".
    - Teste C (regressão do caso que já funciona): ProcessHttp em `$leavingCustomActions`/saída continua retomando igual ao teste da linha ~86.
  </behavior>
  <action>
    Escrever primeiro os testes A e B em `gerenciador.teste.ts` reaproveitando os dublês `suspenderHttp`/`retomarProcessHttp` do teste existente (linha ~86); rodar e confirmar que falham (registrar a saída de falha no SUMMARY). Diagnosticar em `processarAcoes`/`processarEntrada` por que o cursor com `lista: 'entrada'` não casa na retomada (hipóteses a checar: a chamada de `fluxo.inputActions` com `estadoId = null` retorna cedo por `if (cursor && !cursor.consumido && !alvo) return;` e o fluxo segue sem reposicionar no estado; o `estadoId` salvo no cursor difere do estado corrente reconstruído na retomada). Corrigir no ponto comum `processarAcoes`/reposicionamento de estado da retomada, sem caminho especial por lista (um fix, todos os chamadores). Não proibir ProcessHttp em entering actions no Builder (D-25).
  </action>
  <verify>
    <automated>pnpm --filter @pipe/core exec vitest run src/fluxo/gerenciador.teste.ts</automated>
  </verify>
  <acceptance_criteria>
    - `gerenciador.teste.ts` contém dois `it(` novos cujo título contém `entering` e `ProcessHttp`
    - SUMMARY registra a saída de falha dos testes A e B antes do fix
    - `pnpm --filter @pipe/core exec vitest run src/fluxo` sai com código 0
    - `pnpm --filter @pipe/core typecheck` sai com código 0
  </acceptance_criteria>
  <done>ProcessHttp em ações de entrada retoma sem pular nem repetir ações; regressão coberta.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Varredura BullMQ de process_http_execucao presa (D-26) e prova de regressão do D-27</name>
  <files>apps/api/src/filas.ts, apps/api/src/dominio/fluxo.ts, apps/api/tests/process-http-retomada.test.ts</files>
  <read_first>
    - apps/api/src/filas.ts (agendarVarreduraEspelhoCrm e irmãs; agendarVarreduraProcessHttp linhas ~111-129; consumidorProcessHttp ~100-110)
    - apps/api/src/dominio/fluxo.ts (executarProcessHttp ~529-655; update para 'chamando' ~548; select por 'chamando' ~592; linha ~487 idProvedorUsado)
    - apps/api/tests/process-http-retomada.test.ts (cenário, stubarHttpDeSaida, contagemDeMensagensComId)
    - .planning/debug/process-http-auto-resume.md (seção de recomendação, linha ~149)
    - packages/db/src/schema/automacao.ts (process_http_execucao linhas ~381-406)
  </read_first>
  <behavior>
    - Teste D-26: linha em `chamando` com `atualizado_em` mais antiga que o limite → `recoverStuckProcessHttp(limiteMs)` devolve o id, a linha sai de `chamando`, a conversa volta a responder à próxima mensagem do contato e o fluxo segue pelo caminho de resposta com status de timeout.
    - Teste D-26 negativo: linha em `chamando` dentro do limite não é tocada.
    - Teste D-27: a retomada grava exatamente um `execucao_passo` por `id_provedor` (`contagemDeMensagensComId` = 1); se `idProvedorUsado` fosse inicializado com `false`, o teste falharia (verificado por mutação manual, revertida).
  </behavior>
  <action>
    Em `apps/api/src/dominio/fluxo.ts` criar e exportar `recoverStuckProcessHttp(limiteMs: number): Promise<string[]>`: seleciona (no máx. 100 por rodada, `for update skip locked`) linhas de `process_http_execucao` com `estado = 'chamando'` e `atualizado_em < now() - limiteMs`, grava para cada uma uma resposta sintética de timeout no mesmo formato que o caminho de resposta HTTP já grava (status 408, corpo vazio; mesmos campos que `executarProcessHttp` lê ao retomar), passa a linha para `respondida`, e devolve os ids para reenfileirar via `enfileirarProcessHttp`. Cada linha recuperada emite um alerta estruturado com `console.error('[alert] process_http_stuck', { tenantId, processoId, desde })` (mesmo canal de log das outras varreduras; erro da varredura é registrado e engolido, como em `agendarVarreduraEspelhoCrm`).
    Em `apps/api/src/filas.ts`, estender `agendarVarreduraProcessHttp`: mantém o ramo memória atual e acrescenta o ramo BullMQ com fila própria `PROCESS_HTTP_SWEEP_QUEUE`, `upsertJobScheduler('process-http-sweep', { every: Number(process.env['PIPE_PROCESS_HTTP_VARREDURA_MS'] ?? 60_000) }, { name: 'varredura', data: {} })` e um `Worker` de concorrência 1 que chama `recoverStuckProcessHttp(Number(process.env['PIPE_PROCESS_HTTP_TIMEOUT_MS'] ?? 120_000))` e reenfileira cada id com `enfileirarProcessHttp`. Conferir que `agendarVarreduraProcessHttp` é chamada no bootstrap também em modo BullMQ (grep do chamador).
    Testes: acrescentar em `process-http-retomada.test.ts` os casos D-26 (chamando `recoverStuckProcessHttp` direto, envelhecendo `atualizado_em` por SQL) e reforçar D-27 com asserção de `contagemDeMensagensComId(id) === 1` depois da retomada se ainda não existir. Fazer a mutação `Boolean(retomada)` → `false`, rodar, confirmar falha, reverter; registrar no SUMMARY.
  </action>
  <verify>
    <automated>pnpm banco:subir && pnpm --filter @pipe/api exec vitest run tests/process-http-retomada.test.ts && pnpm --filter @pipe/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/filas.ts` contém `'process-http-sweep'` e `recoverStuckProcessHttp(`
    - `apps/api/src/dominio/fluxo.ts` exporta `recoverStuckProcessHttp` e contém `[alert] process_http_stuck` e `skip locked`
    - `apps/api/src/dominio/fluxo.ts` ainda contém `let idProvedorUsado = Boolean(retomada);`
    - `process-http-retomada.test.ts` contém `recoverStuckProcessHttp` e a asserção de contagem 1 por id_provedor
    - SUMMARY registra a falha observada na mutação do D-27
    - o comando do verify sai com código 0
  </acceptance_criteria>
  <done>Execuções presas em produção se recuperam com alerta; duplicate-key protegido por regressão.</done>
</task>

<task type="auto">
  <name>Task 3: Fechar os todos dobrados</name>
  <files>.planning/todos/pending/process-http-entering-actions.md, .planning/todos/pending/process-http-bullmq-sweep.md</files>
  <read_first>
    - .planning/todos/pending/process-http-entering-actions.md
    - .planning/todos/pending/process-http-bullmq-sweep.md
  </read_first>
  <action>
    Mover os dois arquivos para `.planning/todos/completed/` (criar a pasta se não existir) com `git mv`, acrescentando no fim de cada um `Resolved: phase 02, plan 02-05 (D-25|D-26), commit <hash>`.
  </action>
  <verify>
    <automated>test ! -f .planning/todos/pending/process-http-entering-actions.md && test ! -f .planning/todos/pending/process-http-bullmq-sweep.md && grep -l "Resolved: phase 02" .planning/todos/completed/process-http-*.md</automated>
  </verify>
  <acceptance_criteria>
    - Os dois arquivos existem em `.planning/todos/completed/` com a linha `Resolved: phase 02, plan 02-05`
  </acceptance_criteria>
  <done>Todos dobrados fechados com rastreio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fluxo publicado (definido no Builder) → motor | define onde ProcessHttp roda |
| Redis/BullMQ → worker da API | job de varredura sem entrada de usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-03 | Denial of service | process_http_execucao presa em 'chamando' | high | mitigate | Task 2: varredura BullMQ com timeout e alerta; teste de recuperação |
| T-2-04 | Tampering (processamento duplo) | Worker da varredura | medium | mitigate | `for update skip locked`, concorrência 1, lote ≤100; a transição chamando→respondida só ocorre uma vez por linha |
| T-2-05 | Tampering | retomada em entering actions | medium | mitigate | Task 1: ações antes do ProcessHttp não reexecutam (teste conta chamadas) — evita efeito duplicado de SendMessage/SetVariable |
| T-2-18 | Repudiation/Integrity | execucao_passo duplicado | high | mitigate | Task 2: regressão D-27 prova 1 passo por id_provedor; mutação registrada |
</threat_model>

<verification>
`pnpm --filter @pipe/core exec vitest run src/fluxo` e `pnpm --filter @pipe/api exec vitest run tests/process-http-retomada.test.ts` verdes.
</verification>

<success_criteria>
Três bugs do ProcessHttp fechados com testes de regressão que falham sem o fix.
</success_criteria>

<output>
Create `.planning/phases/02-fechar-o-builder/02-05-SUMMARY.md` when done
</output>
