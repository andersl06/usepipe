---
phase: 02-fechar-o-builder
plan: 11
type: execute
wave: 4
depends_on: ["02-07", "02-10"]
files_modified:
  - packages/db/src/schema/conversas.ts
  - packages/db/drizzle/
  - packages/contracts/src/satisfaction-survey.ts
  - packages/contracts/src/index.ts
  - packages/core/src/fluxo/contexto.ts
  - packages/core/src/fluxo/satisfaction-survey.ts
  - packages/core/src/fluxo/satisfaction-survey.test.ts
  - apps/api/src/dominio/fluxo.ts
  - apps/api/src/dominio/gestao/satisfaction-surveys.ts
  - apps/api/src/controladores/satisfaction-surveys.ts
  - apps/api/src/app.modulo.ts
  - apps/api/tests/satisfaction-survey.test.ts
autonomous: true
requirements: [BUILDER-03]

must_haves:
  truths:
    - "Uma resposta à pesquisa de satisfação nativa (1-5) em fluxo publicado fica gravada associada a ticket, contato, atendente e fila, cobrindo resposta completa, só nota, sem resposta e abandono como a referência (D-06, D-07, D-08)"
    - "O fluxo ramifica depois da pesquisa exatamente pela forma documentada em inventario-satisfacao-e-tags.md §1 (Ramificação:), não pelas categorias do relatório (D-09)"
    - "O schema não tem retenção de 3 meses nem restrição de idioma (D-10) e responde às consultas da Análise de Satisfação da referência por um endpoint autenticado (D-08.3, D-08.4, D-08.5)"
    - "Depois do encerramento pelo atendente ou por inatividade, o bloco seguinte lê input.content@tags e input.content@sequentialId; o encerramento pelo cliente segue o mecanismo documentado da referência (D-12)"
  artifacts:
    - path: "packages/db/src/schema/conversas.ts"
      provides: "tabela de respostas de satisfação com tenant_id + RLS tenant_isolado"
    - path: "packages/contracts/src/satisfaction-survey.ts"
      provides: "contratos da resposta e da consulta"
    - path: "apps/api/src/controladores/satisfaction-surveys.ts"
      provides: "GET v1/management/satisfaction-surveys/responses (rota final conforme padrão de rotas vigente)"
  key_links:
    - from: "apps/api/src/dominio/fluxo.ts ultimoAtendimento"
      to: "etiqueta / conversa_etiqueta"
      via: "tags e sequentialId no Ticket passado ao motor"
      pattern: "sequentialId"
    - from: "packages/core/src/fluxo/satisfaction-survey.ts"
      to: "ServicosDoMotor.recordSatisfactionAnswer"
      via: "serviço injetado implementado em fluxo.ts"
      pattern: "recordSatisfactionAnswer"
---

<objective>
Implementar o lado servidor da pesquisa de satisfação nativa e da exposição das etiquetas de encerramento ao fluxo, conforme as decisões APROVADAS no portão (D-04) sobre `ref/inventario-satisfacao-e-tags.md` §5 e §7-8.

Output: schema + migration com RLS, contratos, processamento no motor, persistência via serviço, `ultimoAtendimento` com tags/sequentialId, endpoint de consulta, testes.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fechar-o-builder/ref/inventario-satisfacao-e-tags.md
@.planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md

Pré-condição e nomes (D-05, STD-01): este plano só executa depois que a Phase 1 aplicou o mapa (tag `std-apply-all-end`). Caminhos e símbolos EXISTENTES aparecem com o nome em português de 2026-09-26: antes de cada task, localizar o nome vigente pela linha `status=approved` de `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv` ou por `ref/REFERENCIA-CONGELADA.md` §Caminhos resolvidos; símbolo sem linha no mapa mantém o nome; nunca escrever nome proposto não aplicado. Identificadores/arquivos/endpoints/títulos de teste NOVOS vêm em inglês (`std/CONVENTIONS-EN.md`; rota nova no padrão `v1/<plural-resource>` kebab-case). Nomes SQL novos seguem o snake_case português do schema (D-08 da Phase 1). Texto visível em português.

Fonte da verdade: `CLASSIFICACAO-PORTAO.md` §Decisões de mecanismo, linha `APROVADO:` de D-08.5 (tabela, colunas, índices, ramificação, endpoint). Se o portão aprovou algo diferente do descrito abaixo, vale o portão; o executor registra a diferença no SUMMARY. Itens `Bloqueado por C-NN` não são implementados (D-03).
Padrões a copiar: tabela `etiqueta` (`packages/db/src/schema/conversas.ts:486-502`: `id()`, `refTenant()`, `carimbos()`, `listaCheck`); RLS `tenant_isolado` (`packages/db/drizzle/0001_rls.sql:93-95`, migration custom via `pnpm --filter @pipe/db gerar:custom`); controlador com `@ComSessao()` + `sessaoDe` + `noTenant` (`apps/api/src/controladores/gestao-builder.ts`); permissão `relatorio.ver` e `relatorio.exportar` do catálogo (`packages/db/src/semente.ts:21-23`) com `exigirPermissao` (`apps/api/src/sessao.ts`); registro de controlador em `apps/api/src/app.modulo.ts`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema, migration com RLS e contratos (D-08.5, D-10)</name>
  <files>packages/db/src/schema/conversas.ts, packages/db/drizzle/, packages/contracts/src/satisfaction-survey.ts, packages/contracts/src/index.ts</files>
  <read_first>
    - .planning/phases/02-fechar-o-builder/ref/inventario-satisfacao-e-tags.md (## 5. Proposta de persistência)
    - .planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md (APROVADO: D-08.5)
    - packages/db/src/schema/conversas.ts (etiqueta 486-502, conversa, evento_atendimento)
    - packages/db/drizzle/0001_rls.sql (linhas 90-110)
    - packages/contracts/src/gestao-fluxo.ts (estilo: interface pura com comentário de proveniência, linhas 320-385)
  </read_first>
  <action>
    Criar a tabela aprovada (proposta base: SQL `resposta_pesquisa_satisfacao`, export Drizzle `satisfactionSurveyResponse`) com, no mínimo, as colunas que a Análise de Satisfação da referência consulta: `tenant_id` (refTenant), `conversa_id` (ticket), `contato_id`, `atendente_id` (nullable), `fila_id` (nullable), `fluxo_id`, `nota` (smallint com check 1..5 ou nulo), `comentario` (text nullable), `situacao` (listaCheck com os estados documentados: completa, so_nota, sem_resposta, abandonada ou os aprovados), `respondida_em`, `carimbos()`; índices por `(tenant_id, respondida_em)`, `(tenant_id, fila_id)`, `(tenant_id, atendente_id)` e único por `(tenant_id, conversa_id)` se a referência só aceita uma resposta por ticket. Sem coluna/job de expiração (D-10). Gerar migration com `pnpm --filter @pipe/db gerar` e migration custom com `ENABLE ROW LEVEL SECURITY` + `CREATE POLICY "tenant_isolado"` idêntica ao padrão de 0001_rls.sql. Contratos em `packages/contracts/src/satisfaction-survey.ts`: `SatisfactionSurveyResponse`, `SatisfactionSurveyQuery` (filtros/paginação da referência: período, fila, atendente, nota, busca em comentário, cursor/página), `SatisfactionSurveyPage`; exportar em `index.ts`.
  </action>
  <verify>
    <automated>pnpm banco:subir && pnpm banco:migrar && pnpm --filter @pipe/db typecheck && pnpm --filter @pipe/contracts typecheck && grep -l "tenant_isolado" packages/db/drizzle/*.sql | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - a migration nova contém `ENABLE ROW LEVEL SECURITY` e `CREATE POLICY "tenant_isolado"` para a tabela nova
    - `conversas.ts` contém a tabela com `refTenant()` e check de nota 1..5
    - nenhum arquivo novo contém `interval '3 months'` ou expiração de 90 dias
    - `packages/contracts/src/index.ts` exporta `satisfaction-survey`
    - migrar e typecheck saem 0
  </acceptance_criteria>
  <done>Persistência aprovada criada com isolamento por tenant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Motor processa a pesquisa e grava a resposta; ticket expõe tags (D-06, D-09, D-12)</name>
  <files>packages/core/src/fluxo/contexto.ts, packages/core/src/fluxo/satisfaction-survey.ts, packages/core/src/fluxo/satisfaction-survey.test.ts, apps/api/src/dominio/fluxo.ts</files>
  <read_first>
    - .planning/phases/02-fechar-o-builder/ref/inventario-satisfacao-e-tags.md (§1 estrutura serializada do bloco nativo e Ramificação; §2; §7 input.content por tipo de encerramento)
    - packages/core/src/fluxo/contexto.ts (ServicosDoMotor ~136-155, Atendimento ~105)
    - packages/core/src/fluxo/gerenciador.ts (como a entrada é avaliada contra $conditionOutputs)
    - apps/api/src/dominio/fluxo.ts (servicos ~324-360; ultimoAtendimento ~935-960; ramo `nova && estadoAntes?.startsWith('desk:')` ~488-500)
    - packages/db/src/schema/conversas.ts (conversa_etiqueta, etiqueta, sequencial do ticket se existir)
  </read_first>
  <behavior>
    - Fluxo com bloco de pesquisa nativo: entrada "5" grava resposta nota 5 e segue pela saída que a Ramificação documentada define para 5.
    - Nota + comentário (se a referência pede comentário): grava ambos; só nota e timeout/sem resposta gravam a `situacao` documentada e seguem o destino documentado.
    - Entrada fora de 1-5: comportamento documentado (reperguntar/erro) — nunca grava nota inválida.
    - Encerramento pelo atendente com etiquetas "Venda" e "Suporte": o bloco seguinte lê `input.content@tags` no formato documentado e `input.content@sequentialId`.
    - Encerramento pelo cliente: segue o mecanismo documentado em §7 (teste afirma o que a referência faz).
  </behavior>
  <action>
    Em `contexto.ts`, acrescentar a `ServicosDoMotor` o serviço opcional `recordSatisfactionAnswer(answer: { rating: number | null; comment: string | null; status: string })`. Criar `packages/core/src/fluxo/satisfaction-survey.ts` com a função pura que reconhece o bloco nativo (pela marca serializada documentada em §1), interpreta a entrada (nota/comentário/timeout) e chama o serviço; a ramificação usa as `$conditionOutputs` gravadas pelo Builder, sem regra especial por categoria (D-09). Testes vitest primeiro em `satisfaction-survey.test.ts`.
    Em `fluxo.ts`: implementar `recordSatisfactionAnswer` no objeto `servicos`, inserindo na tabela da Task 1 com `conversa_id` = último ticket (`ultimoAtendimento`), `atendente_id`/`fila_id` do ticket e `fluxo_id` atual, dentro da mesma transação; estender `ultimoAtendimento` para devolver `tags` (nomes das etiquetas de `conversa_etiqueta` → `etiqueta.nome`, no formato documentado) e `sequentialId` (identificador sequencial do ticket; se não houver coluna, usar o que o portão aprovou), só para encerramento por atendente/inatividade conforme §7; para encerramento pelo cliente, implementar o mecanismo aprovado (§7). Sem ramificação especial por etiqueta e sem UI nova (D-12).
  </action>
  <verify>
    <automated>pnpm --filter @pipe/core exec vitest run src/fluxo && pnpm --filter @pipe/core typecheck && pnpm --filter @pipe/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `satisfaction-survey.test.ts` cobre nota válida, nota+comentário, sem resposta/timeout, entrada inválida
    - `fluxo.ts` contém `recordSatisfactionAnswer` e `sequentialId`
    - `satisfaction-survey.ts` não contém os literais de categoria do relatório (`promotor`, `detrator`, `neutro`) como regra de ramificação
    - comandos do verify saem 0
  </acceptance_criteria>
  <done>Pesquisa executa e grava; etiquetas chegam ao fluxo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Endpoint de consulta das respostas (D-08.3, D-08.4) com autorização</name>
  <files>apps/api/src/dominio/gestao/satisfaction-surveys.ts, apps/api/src/controladores/satisfaction-surveys.ts, apps/api/src/app.modulo.ts, apps/api/tests/satisfaction-survey.test.ts</files>
  <read_first>
    - .planning/phases/02-fechar-o-builder/ref/inventario-satisfacao-e-tags.md (§3 e §4: filtros, colunas, paginação, busca de comentário)
    - apps/api/src/controladores/gestao-builder.ts (casca: @ComSessao, sessaoDe, noTenant, uuidOu404)
    - apps/api/src/controladores/gestao-analise.ts (endpoint de relatório com filtros)
    - apps/api/src/sessao.ts (exigirPermissao)
    - apps/api/tests/builder-por-fluxo.test.ts (montagem de API com sessão para teste)
  </read_first>
  <behavior>
    - Usuário com `relatorio.ver` recebe página de respostas do próprio tenant filtrada por período/fila/atendente/nota e busca em comentário.
    - Usuário sem `relatorio.ver` recebe 403.
    - Resposta de outro tenant nunca aparece (RLS), mesmo passando ids dele nos filtros.
    - Sem sessão → 401.
  </behavior>
  <action>
    Domínio `satisfaction-surveys.ts` com `listSatisfactionResponses(tx, userId, query)`: chama `exigirPermissao(tx, userId, 'relatorio.ver')`, valida filtros (datas ISO, uuids, nota 1..5, tamanho de página ≤ 100, busca ≤ 200 caracteres, parametrizada com `ilike` escapado), consulta dentro de `noTenant` e devolve `SatisfactionSurveyPage`. Controlador `SatisfactionSurveysController` com `@Get('responses')` sob o recurso `v1/<plural-resource>` equivalente ao padrão vigente de gestão (ex.: `v1/management/satisfaction-surveys`), `@ComSessao()`, tenant só da sessão; registrar em `app.modulo.ts`. Se o portão aprovou exportação, `GET .../responses/export` com `relatorio.exportar`, mesmo filtro, CSV. Teste de API cobrindo os quatro comportamentos.
  </action>
  <verify>
    <automated>pnpm banco:subir && pnpm --filter @pipe/api exec vitest run tests/satisfaction-survey.test.ts && pnpm --filter @pipe/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - o controlador contém `@ComSessao()` e o domínio contém `exigirPermissao(` com `'relatorio.ver'`
    - `satisfaction-survey.test.ts` tem casos 403, 401 e isolamento entre tenants
    - `app.modulo.ts` registra `SatisfactionSurveysController`
    - verify sai 0
  </acceptance_criteria>
  <done>Consultas da Análise de Satisfação reproduzíveis por endpoint seguro.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contato (canal) → motor | nota e comentário são texto livre do cliente |
| navegador do gestor → API de consulta | filtros não confiáveis |
| tenant A ↔ tenant B | isolamento de dados de pesquisa |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-10 | Information disclosure (PII) | tabela de respostas / endpoint | high | mitigate | Task 1 RLS `tenant_isolado`; Task 3 `relatorio.ver`, tenant da sessão, teste de isolamento e 403 |
| T-2-09 | Elevation of privilege | endpoint de consulta/exportação | high | mitigate | Task 3 `@ComSessao()` + `exigirPermissao` (`relatorio.ver`/`relatorio.exportar`); testes 401/403 |
| T-2-22 | Tampering (SQL injection) | busca em comentário | high | mitigate | Task 3 consulta parametrizada via `sql```, `ilike` com `%`/`_` escapados, limite de 200 caracteres |
| T-2-23 | Tampering | nota fora de 1..5 | medium | mitigate | Task 1 check no banco; Task 2 teste de entrada inválida |
| T-2-24 | Information disclosure | logs | medium | mitigate | comentário nunca é logado; alertas/erros só com ids |
</threat_model>

<verification>
Testes de core e api verdes; migração aplicada com RLS.
</verification>

<success_criteria>
Metade servidor do critério 3 do roadmap (pesquisa de satisfação) entregue com a persistência aprovada.
</success_criteria>

<output>
Create `.planning/phases/02-fechar-o-builder/02-11-SUMMARY.md` when done
</output>
