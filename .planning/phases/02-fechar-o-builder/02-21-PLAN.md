---
phase: 02-fechar-o-builder
plan: 21
type: execute
wave: 11
depends_on: ["02-13", "02-18", "02-20"]
files_modified:
  - apps/api/src/dominio/gestao/builder-test-run.ts
  - apps/api/src/controladores/gestao-builder.ts
  - apps/api/tests/builder-test-run.test.ts
  - apps/gestao-vite/src/paginas/builder-gravar.ts
  - apps/gestao-vite/src/paginas/builder/test-panel.tsx
  - apps/gestao-vite/src/paginas/builder/editor.tsx
  - apps/gestao-vite/src/paginas/builder/painel-bloco.css
  - apps/gestao-vite/tests/builder-painels.test.ts
autonomous: true
requirements: [BUILDER-04]

must_haves:
  truths:
    - "O atendente abre o painel de Teste no Builder, conversa com o rascunho atual e vê as respostas do bot com o mecanismo aprovado no portão (simulação local com o motor real ou canal de teste real) (D-14)"
    - "O Debug do painel mostra o que a referência mostra (bloco atual, variáveis, ações executadas, erros) e permite reset do contato de teste e variáveis de teste como a referência (D-14)"
    - "Todos os tipos de conteúdo e ações do catálogo aprovado executam no Teste como executam em produção (D-18, D-19)"
    - "O teste nunca envia mensagem a contato real nem mistura dados com conversas reais (D-14)"
  artifacts:
    - path: "apps/api/src/dominio/gestao/builder-test-run.ts"
      provides: "execução do rascunho para o contato de teste + trilha de debug"
    - path: "apps/gestao-vite/src/paginas/builder/test-panel.tsx"
      provides: "painel de Teste com conversa e Debug"
  key_links:
    - from: "test-panel.tsx"
      to: "POST :id/builder/<test-run>"
      via: "cliente em builder-gravar.ts"
      pattern: "runTest|testRun"
    - from: "builder-test-run.ts"
      to: "motor real (processarEntrada do core)"
      via: "mesmo ProvedorDeAcoes de produção, serviço de envio redirecionado ao painel"
      pattern: "PROVEDOR_PADRAO|processarEntrada"
---

<objective>
Entregar o painel de Teste do Builder (BUILDER-04, D-14) com o mecanismo aprovado no portão, rodando o motor real sobre o rascunho, com Debug e isolamento de dados reais.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fechar-o-builder/02-UI-SPEC.md
@.planning/phases/02-fechar-o-builder/ref/inventario-paineis-e-setas.md
@.planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md

Mecanismo: linha `APROVADO:` de D-14 em `CLASSIFICACAO-PORTAO.md`. Se estiver `BLOQUEADO — captura pendente` e a captura C-NN não tiver chegado, este plano NÃO executa (D-03): o executor para e registra o bloqueio no SUMMARY. Layout, textos, variáveis de teste, reset e Debug seguem `## Painel de Teste (D-14)` do inventário congelado.

Pré-condição e nomes (D-05, STD-01): o gate da Phase 1 (tag `std-apply-all-end`) é imposto pela Task 0 (Pre-flight Gate) de 02-05/02-06/02-08, das quais este plano depende transitivamente via `depends_on` (02-13 → 02-08). Caminhos/símbolos/rotas EXISTENTES com nome em português de 2026-09-26; localizar o nome vigente pela linha `status=approved` de `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv` ou por `ref/REFERENCIA-CONGELADA.md` §Caminhos resolvidos. Arquivos/identificadores/rotas NOVOS em inglês (sub-rota nova sob o prefixo vigente de `:id/builder`, ex.: `:id/builder/test-runs`). Texto visível em português. CSS só com tokens.
Motor: `packages/core/src/fluxo/gerenciador.ts` (`processarEntrada`) com `PROVEDOR_PADRAO` (`acoes.ts`) e `ServicosDoMotor` (`contexto.ts`); a execução de produção monta `servicos` em `apps/api/src/dominio/fluxo.ts`. Os runners de teste do core (`packages/core/src/fluxo/gerenciador.teste.ts`) já invocam o motor fora de HTTP — analog da simulação local.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execução de teste no servidor com trilha de debug (D-14)</name>
  <files>apps/api/src/dominio/gestao/builder-test-run.ts, apps/api/src/controladores/gestao-builder.ts, apps/api/tests/builder-test-run.test.ts</files>
  <read_first>
    - .planning/phases/02-fechar-o-builder/ref/inventario-paineis-e-setas.md (## Painel de Teste (D-14))
    - .planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md (APROVADO: D-14)
    - apps/api/src/dominio/fluxo.ts (montagem de `servicos`, contexto, variáveis, carregarContato)
    - apps/api/src/dominio/gestao/builder-do-fluxo.ts (carregarBuilder: rascunho; fluxoDoBuilder EDITAR_FLUXO)
    - packages/core/src/fluxo/gerenciador.ts (processarEntrada, rastro)
    - apps/api/src/controladores/gestao-builder.ts
  </read_first>
  <behavior>
    - POST test-run com texto "oi" sobre o rascunho devolve as mensagens do bot (no formato de `toChannelOutput`) e a trilha de debug (estados, variáveis após a entrada, ações executadas com resultado/erro).
    - Entradas seguintes continuam da posição salva do contato de teste; reset limpa estado e variáveis de teste.
    - Variáveis de teste definidas no painel são vistas pelo fluxo como a referência define.
    - Nenhuma linha é criada em `mensagem`, `outbox_mensagem`, `conversa` ou `execucao_fluxo` de produção; nada é enviado a canal.
    - Sem `builder.escrever` → 403; outro tenant → 404; mais de N execuções por minuto por usuário → 429 (N do inventário ou 30).
    - T-2-06: ação HTTP no teste para URL privada/metadata é recusada pela mesma guarda `confirmarUrlSegura` de produção, sem requisição de saída, e a recusa aparece na trilha de debug.
  </behavior>
  <action>
    Criar `builder-test-run.ts` com `runBuilderTest(tx, tid, userId, flowId, { input, testVariables, reset })`: carrega o rascunho via a mesma leitura de `carregarBuilder`, mantém o estado do contato de teste por `(tenant, usuário, fluxo)` em armazenamento isolado (tabela de estado de teste só se o portão aprovou; senão memória do processo com TTL de 30 min — marcar com comentário `ponytail:` o limite de instância única), monta `ServicosDoMotor` reaproveitando os mesmos serviços de produção exceto `enviar` (coleta a mensagem para a resposta), `encaminharParaAtendimento` (registra no debug sem abrir ticket real) e serviços que escrevem dados de contato real (usam o contato de teste). Chamadas HTTP e scripts rodam como em produção (mesmas guardas). Se o mecanismo aprovado for canal de teste real, implementar o adaptador de canal de teste documentado no inventário no lugar da coleta local, mantendo o mesmo contrato de resposta. Rotas `POST :id/builder/test-runs` e `DELETE :id/builder/test-runs` (reset) com `@ComSessao()` e `fluxoDoBuilder(EDITAR_FLUXO)`; limite por usuário. Testes cobrindo o behavior.
  </action>
  <verify>
    <automated>pnpm banco:subir && pnpm --filter @pipe/api exec vitest run tests/builder-test-run.test.ts && pnpm --filter @pipe/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - teste afirma contagem inalterada de `mensagem`, `outbox_mensagem`, `conversa`, `execucao_fluxo` após o teste
    - testes 403, 404 (outro tenant) e 429 presentes e verdes
    - `builder-test-run.ts` usa o provedor de ações de produção (grep `PROVEDOR_PADRAO` ou nome vigente)
    - T-2-06: `builder-test-run.test.ts` tem `it('blocks private URL in test-run HTTP (SSRF)'` — fluxo de teste com `ProcessHttp` para `http://169.254.169.254` não faz requisição de saída (stub de HTTP sem chamada) e a trilha de debug registra a recusa de `confirmarUrlSegura` (nome vigente)
    - verify sai 0
  </acceptance_criteria>
  <done>Servidor executa o rascunho em modo teste com debug e isolamento.</done>
</task>

<task type="auto">
  <name>Task 2: Painel de Teste e Debug no Builder (D-14)</name>
  <files>apps/gestao-vite/src/paginas/builder-gravar.ts, apps/gestao-vite/src/paginas/builder/test-panel.tsx, apps/gestao-vite/src/paginas/builder/editor.tsx, apps/gestao-vite/src/paginas/builder/painel-bloco.css, apps/gestao-vite/tests/builder-painels.test.ts</files>
  <read_first>
    - apps/gestao-vite/src/paginas/builder-gravar.ts
    - apps/gestao-vite/src/paginas/builder/editor.tsx (cabeçalho/ações do editor, painéis laterais)
    - .planning/phases/02-fechar-o-builder/ref/inventario-paineis-e-setas.md (## Painel de Teste: layout, textos, Debug)
    - .planning/phases/02-fechar-o-builder/02-UI-SPEC.md (empty state "Nenhuma execução ainda")
  </read_first>
  <action>
    Em `builder-gravar.ts`, `runTest(id, payload)` e `resetTest(id)` no padrão `Resultado<T>`. Criar `test-panel.tsx`: abertura pelo mesmo controle da referência no `editor.tsx`, conversa (mensagens do bot renderizadas por tipo como texto/mídia/botões clicáveis que enviam a resposta interativa), campo de envio, variáveis de teste, botão de reset, aba/área de Debug com bloco atual (clicar destaca o nó no canvas), variáveis, ações e erros; empty state do UI-SPEC ajustado ao texto final da referência; salvar o rascunho antes de testar se a referência faz isso. Sem `dangerouslySetInnerHTML`; links com `noopener`. CSS com prefixo vigente + `test`, só tokens. Teste `test panel:` de função pura de formatação do debug em `builder-painels.test.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @pipe/gestao-vite test && pnpm --filter @pipe/gestao-vite typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `editor.tsx` monta `TestPanel` (grep)
    - `builder-gravar.ts` contém `runTest` e `resetTest`
    - `test-panel.tsx` sem `dangerouslySetInnerHTML`; CSS novo sem hex
    - test e typecheck saem 0
  </acceptance_criteria>
  <done>Atendente testa o fluxo dentro do Builder com Debug.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → execução do motor | rascunho e entradas de teste não confiáveis |
| execução de teste → dados/canais reais | risco de efeito colateral real |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-13 | Tampering (efeito real) | builder-test-run.ts | high | mitigate | Task 1: `enviar`/`encaminharParaAtendimento`/escritas de contato redirecionados; teste de contagem inalterada nas tabelas de produção |
| T-2-09 | Elevation of privilege | rotas test-runs | high | mitigate | Task 1: `@ComSessao()` + `fluxoDoBuilder(EDITAR_FLUXO)`; testes 403/404 |
| T-2-28 | Denial of service | execução repetida | medium | mitigate | Task 1: limite por usuário (429) e mesmos limites de script/HTTP de produção |
| T-2-06 | SSRF | HTTP no teste | high | mitigate | mesmas guardas `confirmarUrlSegura` de produção (serviços reaproveitados) |
| T-2-16 | Tampering (XSS) | render das mensagens no painel | medium | mitigate | Task 2 sem HTML cru |
</threat_model>

<verification>
Testes de api e app verdes.
</verification>

<success_criteria>
Critério 4 do roadmap (painel de Teste com paridade funcional) completo.
</success_criteria>

<output>
Create `.planning/phases/02-fechar-o-builder/02-21-SUMMARY.md` when done
</output>
