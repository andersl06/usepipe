---
phase: 02-fechar-o-builder
plan: 09
type: execute
wave: 4
depends_on: ["02-06", "02-07", "02-08"]
files_modified:
  - apps/gestao-vite/src/paginas/builder/destination-picker.tsx
  - apps/gestao-vite/src/paginas/builder/painel-saidas.tsx
  - apps/gestao-vite/src/paginas/builder/variaveis.ts
  - apps/gestao-vite/src/paginas/builder/painel-bloco.css
  - apps/gestao-vite/src/paginas/builder/modelo.ts
  - apps/gestao-vite/tests/builder-editor.test.ts
  - .planning/phases/02-fechar-o-builder/ref/validacao-setas.md
autonomous: true
requirements: [BUILDER-02, BUILDER-05]

must_haves:
  truths:
    - "O atendente seleciona o destino de uma ligação digitando parte do título ou id do bloco, sem acento/caixa, nos três pontos do painel de saídas (condição normal, saída de disponibilidade, saída padrão) (D-23)"
    - "Forma e estados do seletor seguem ref/inventario-paineis-e-setas.md §Seletor de destino (D-23, D-02)"
    - "Setas do Pipe e da referência foram comparadas em exports reais (incluindo AUVP Capital) por quantidade, origem, destino e ausência/presença (D-29)"
    - "Se a comparação mostrou paridade, BUILDER-05 fecha como validado sem alteração artificial de arestasDe(); se mostrou divergência, o caso mínimo foi registrado antes do fix e virou teste de regressão (D-29)"
  artifacts:
    - path: "apps/gestao-vite/src/paginas/builder/destination-picker.tsx"
      provides: "combobox pesquisável de bloco de destino"
      exports: ["DestinationPicker"]
    - path: ".planning/phases/02-fechar-o-builder/ref/validacao-setas.md"
      provides: "comparação de setas Pipe vs referência por fluxo real (D-29.3)"
      contains: "| Fluxo | Setas referência | Setas Pipe |"
  key_links:
    - from: "apps/gestao-vite/src/paginas/builder/painel-saidas.tsx"
      to: "DestinationPicker"
      via: "substitui os 3 usos de seletorDeDestino/<Selecao>"
      pattern: "<DestinationPicker"
    - from: "apps/gestao-vite/src/paginas/builder/destination-picker.tsx"
      to: "variaveis.ts normalizar"
      via: "import do filtro sem acento/caixa"
      pattern: "normalizar|filtrar"
---

<objective>
Entregar o seletor de destino com busca (BUILDER-02, D-23) e fechar BUILDER-05 com a validação de setas em fluxos reais (D-29.3-5).

Output: `destination-picker.tsx`, `painel-saidas.tsx` usando-o, `ref/validacao-setas.md` e, só se houver divergência reproduzida, fix em `arestasDe()` com regressão.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fechar-o-builder/02-PATTERNS.md
@.planning/phases/02-fechar-o-builder/02-UI-SPEC.md
@.planning/phases/02-fechar-o-builder/ref/inventario-paineis-e-setas.md
@.planning/phases/02-fechar-o-builder/02-06-SUMMARY.md

Pré-condição e nomes (D-05, STD-01): o gate da Phase 1 (tag `std-apply-all-end`) é imposto pela Task 0 (Pre-flight Gate) de 02-05/02-06/02-08, das quais este plano depende transitivamente via `depends_on` (direto 02-06, 02-08). Caminhos e símbolos EXISTENTES aparecem aqui com o nome em português de 2026-09-26 (confirmado no disco no planejamento): antes de cada task, localizar o nome vigente no disco pela linha `status=approved` de `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv` (colunas `old`/`new`/`declared_at`) ou por `ref/REFERENCIA-CONGELADA.md` §Caminhos resolvidos, e usar esse nome; símbolo sem linha no mapa mantém o nome atual; nunca escrever nome proposto não aplicado. Identificadores, arquivos, endpoints e títulos de teste NOVOS já vêm em inglês neste plano (`std/CONVENTIONS-EN.md`). Texto visível ao usuário continua em português; nomes SQL novos seguem o snake_case português do schema (D-08 da Phase 1); chaves `$…`/`settings` do desenho `{flow, globalActions}` são contrato persistido (STD-06) e não mudam; variáveis `PIPE_*` não mudam. Depois do D-46, `@pipe/gestao-vite` é `@pipe/management-vite` e `apps/gestao-vite` é `apps/management-vite` nos comandos.
Item bloqueado por captura (D-03): se o export AUVP Capital ou o print do canvas da referência não estiver em `referencias-blip/`, a Task 2 registra `NEEDS VALIDATION` para esse fluxo e segue com os demais.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DestinationPicker com busca sem acento/caixa (D-23)</name>
  <files>apps/gestao-vite/src/paginas/builder/destination-picker.tsx, apps/gestao-vite/src/paginas/builder/painel-saidas.tsx, apps/gestao-vite/src/paginas/builder/variaveis.ts, apps/gestao-vite/src/paginas/builder/painel-bloco.css, apps/gestao-vite/tests/builder-editor.test.ts</files>
  <read_first>
    - apps/gestao-vite/src/paginas/builder/painel-saidas.tsx (seletorDeDestino 57-73 e os 3 usos ~135-139, ~227-231, ~252-256)
    - apps/gestao-vite/src/paginas/builder/variaveis.ts (normalizar/filtrarVariaveis 76-97)
    - apps/gestao-vite/src/paginas/builder/painel-variaveis.tsx (busca com useState, sem debounce)
    - .planning/phases/02-fechar-o-builder/ref/inventario-paineis-e-setas.md (## Seletor de destino (D-23))
    - apps/gestao-vite/src/paginas/builder/painel-bloco.css (tokens --p-builder-marca-* já migrados por 02-08)
  </read_first>
  <behavior>
    - `filterDestinations(blocos, 'atendi')` casa bloco de título "Atendimento" e "ATENDIMENTO humano"; `'acao'` casa "Ação"; busca vazia devolve todos; também casa por `id`.
    - O bloco de origem não aparece como destino se a referência não permitir auto-ligação (seguir inventário; registrar a regra no teste).
    - Valor apontando para id inexistente aparece como `<id> (não existe)` sem perder o valor.
  </behavior>
  <action>
    Em `variaveis.ts`, exportar `normalizar` (hoje privada) sem mudar comportamento. Criar `destination-picker.tsx` exportando a função pura `filterDestinations(blocos: Bloco[], busca: string): Bloco[]` (título `$title` e `id`, via `normalizar`) e o componente `DestinationPicker({ valor, blocos, rotulo, onEscolher })` com input de busca (placeholder "Buscar bloco de destino…"), lista filtrada, opção "Direcionar para…" (texto de `ROTULOS_DAS_SAIDAS.direcionar`), estado de destino inexistente, navegação por teclado (setas, Enter, Esc) e `role="combobox"`/`aria-expanded`/`aria-activedescendant`. Forma e textos seguem o inventário congelado; estado ativo usa `--p-builder-marca-selecionado` (UI-SPEC: accent reservado ao seletor de destino ativo). Em `painel-saidas.tsx`, substituir os três usos por `<DestinationPicker …/>` e remover `seletorDeDestino`. Classes novas em `painel-bloco.css` com prefixo `bl-destination-picker`, só tokens (`--p-e-*`, superfícies, `--p-builder-marca-*`), nenhum hex. Testes de `filterDestinations` em `builder-editor.test.ts` com títulos iniciando por `filterDestinations:`.
  </action>
  <verify>
    <automated>pnpm --filter @pipe/gestao-vite test && pnpm --filter @pipe/gestao-vite typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<DestinationPicker" apps/gestao-vite/src/paginas/builder/painel-saidas.tsx` = 3 e `grep -c "seletorDeDestino" painel-saidas.tsx` = 0
    - `destination-picker.tsx` contém `role="combobox"` e `Buscar bloco de destino…`
    - `grep -n "#[0-9a-fA-F]\{3,6\}" painel-bloco.css | grep bl-destination-picker` vazio
    - ≥3 testes `filterDestinations:` passam; test e typecheck saem 0
  </acceptance_criteria>
  <done>Destino de ligação selecionável por pesquisa nos três pontos.</done>
</task>

<task type="auto">
  <name>Task 2: Validar setas com fluxos reais e corrigir só divergência reproduzida (D-29.3-5)</name>
  <files>.planning/phases/02-fechar-o-builder/ref/validacao-setas.md, apps/gestao-vite/src/paginas/builder/modelo.ts, apps/gestao-vite/tests/builder-editor.test.ts</files>
  <read_first>
    - .planning/phases/02-fechar-o-builder/ref/inventario-paineis-e-setas.md (## Relações que geram seta (D-29.2))
    - .planning/phases/02-fechar-o-builder/02-06-SUMMARY.md (seção Divergências para 02-09)
    - apps/gestao-vite/src/paginas/builder/modelo.ts (arestasDe, lerDesenho)
    - referencias-blip/pesquisa/blip-portal-telas.md (§58-64, fluxo AUVP Capital)
    - exports reais disponíveis em referencias-blip/ (listar com `ls referencias-blip/**/*.json`)
  </read_first>
  <action>
    Escrever um script descartável no scratchpad (não versionado) que, para cada export real `{flow, globalActions}` disponível em `referencias-blip/` (incluindo AUVP Capital), roda `lerDesenho` + `arestasDe` e imprime `origem → destino`. Comparar com a contagem da referência (print/DOM do canvas da Blip do mesmo fluxo, ou a regra da tabela D-29.2 aplicada ao JSON quando não houver print) e registrar em `ref/validacao-setas.md`: tabela `| Fluxo | Setas referência | Setas Pipe | Faltando no Pipe | Sobrando no Pipe | Casos (atendimento, condições múltiplas, Redirect, ProcessHttp) |` + veredito por fluxo `PARIDADE`, `DIVERGÊNCIA` ou `NEEDS VALIDATION (captura C-NN pendente)`. Nenhum dado de cliente (nomes, telefones, textos) vai para o arquivo: só ids de bloco e contagens.
    Se todos os vereditos forem PARIDADE: não alterar `modelo.ts`; registrar `BUILDER-05: VALIDADO sem alteração de código (D-29.4)`.
    Se houver DIVERGÊNCIA: primeiro registrar o caso mínimo no arquivo (JSON sintético de 2-3 blocos), depois adicionar teste `edges: regression <caso>` que falha, então corrigir `arestasDe()` para o campo de destino faltante, mantendo `$defaultOutput` sem seta, e rodar os testes de caracterização de 02-06 (D-29.5).
  </action>
  <verify>
    <automated>grep -qE "PARIDADE|DIVERGÊNCIA|NEEDS VALIDATION" .planning/phases/02-fechar-o-builder/ref/validacao-setas.md && pnpm --filter @pipe/gestao-vite test</automated>
  </verify>
  <acceptance_criteria>
    - `validacao-setas.md` tem a tabela com ao menos uma linha por export disponível e uma linha para AUVP Capital (com veredito ou NEEDS VALIDATION)
    - Se `modelo.ts` mudou, existe teste `edges: regression` e o caso mínimo está no arquivo antes do fix (ordem dos commits)
    - Se `modelo.ts` não mudou, o arquivo contém `BUILDER-05: VALIDADO sem alteração de código`
    - Os 9 testes `edges:` de 02-06 continuam passando
  </acceptance_criteria>
  <done>BUILDER-05 fechado com evidência de fluxo real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| título de bloco (dado do fluxo, possivelmente importado) → DOM | renderização de texto não confiável |
| exports reais da Blip → repositório | podem conter dados de clientes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-16 | Tampering (XSS) | DestinationPicker | medium | mitigate | títulos renderizados como texto React; proibido `dangerouslySetInnerHTML` (grep no acceptance da Task 1) |
| T-2-01 | Information disclosure | ref/validacao-setas.md | high | mitigate | só ids de bloco e contagens; exports ficam em referencias-blip/ (gitignored); script no scratchpad |
</threat_model>

<verification>
Testes do app verdes; `validacao-setas.md` com veredito por fluxo.
</verification>

<success_criteria>
Critérios 2 (seleção de destino por pesquisa) e 5 (setas corretas com teste) do roadmap atendidos.
</success_criteria>

<output>
Create `.planning/phases/02-fechar-o-builder/02-09-SUMMARY.md` when done
</output>
