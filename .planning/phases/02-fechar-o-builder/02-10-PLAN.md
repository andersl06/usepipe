---
phase: 02-fechar-o-builder
plan: 10
type: execute
wave: 3
depends_on: ["02-05", "02-07"]
files_modified:
  - packages/core/src/fluxo/editor.ts
  - packages/core/src/fluxo/editor.teste.ts
  - apps/api/src/dominio/fluxo.ts
  - apps/api/tests/flow-content.test.ts
  - apps/workers/src/entrega.ts
  - apps/workers/src/whatsapp/midia.ts
  - apps/workers/src/instagram.ts
  - apps/workers/src/messenger.ts
  - apps/gestao-vite/src/paginas/builder/conteudo.ts
  - apps/gestao-vite/src/paginas/builder/painel-conteudo.tsx
  - apps/gestao-vite/tests/builder-content.test.ts
autonomous: true
requirements: [BUILDER-01]

must_haves:
  truths:
    - "O atendente cria no Builder cada tipo de conteúdo do slot conteudo-midia aprovado no portão, com editor, validações e mensagens da referência (D-18, D-04)"
    - "O motor publica e executa esses tipos, e o bot entrega a mídia em WhatsApp, Instagram e Messenger conforme o comportamento por canal do inventário congelado (D-18)"
    - "Quando o canal não suporta o tipo, o Pipe faz o que a referência faz (oculta/desabilita/avisa/bloqueia/fallback), nunca 'Não executada no Pipe' para item reproduzível (D-18, D-24)"
    - "Os dois lados da whitelist (motor e tela) sobem juntos para cada tipo aprovado (D-24)"
  artifacts:
    - path: "packages/core/src/fluxo/editor.ts"
      provides: "CONTEUDOS_SUPORTADOS com os MIME do slot"
    - path: "apps/api/src/dominio/fluxo.ts"
      provides: "toChannelOutput: MensagemDeSaida → { tipo: TipoEnvio, texto, dados }"
      contains: "toChannelOutput"
    - path: "apps/gestao-vite/src/paginas/builder/conteudo.ts"
      provides: "novoX por tipo + cartoesDe + errosDoConteudo"
  key_links:
    - from: "apps/api/src/dominio/fluxo.ts servicos.enviar"
      to: "toChannelOutput + gravarRespostaDoBot(tipo, dados.midia)"
      via: "outbox_mensagem"
      pattern: "toChannelOutput\\("
    - from: ".planning/phases/02-fechar-o-builder/ref/conferir-catalogo.mjs"
      to: "slot conteudo-midia"
      via: "gate da whitelist dupla"
      pattern: "--slot conteudo-midia"
---

<objective>
Fechar ponta a ponta os tipos de conteúdo do slot `conteudo-midia` aprovados no portão (imagem, áudio, vídeo, documento, figurinha e o que o portão colocou no slot): contrato serializado, motor, entrega por canal e editor (D-18, D-24).

Output: whitelist do motor estendida, tradução de saída do bot multi-tipo, entrega de mídia do bot pelos workers, editor de cada tipo, testes.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fechar-o-builder/02-PATTERNS.md
@.planning/phases/02-fechar-o-builder/ref/inventario-conteudo.md
@.planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md

Escopo exato: as linhas de `ref/catalogo-aprovado.json` com `"slot": "conteudo-midia"` e `"classificacao": "reproduzivel"`. Linhas `bloqueado` não são implementadas (D-03) e ficam listadas no SUMMARY. Todo detalhe (campos, padrões, limites, mensagens, canal) vem da seção `### <tipo>` do inventário congelado; nada é inventado.

Pré-condição e nomes (D-05, STD-01): este plano só executa depois que a Phase 1 aplicou o mapa (tag `std-apply-all-end`; o ROADMAP põe a Phase 2 depois da Phase 1 e o D-22 da Phase 1 congela código paralelo). Caminhos e símbolos EXISTENTES aparecem aqui com o nome em português de 2026-09-26 (confirmado no disco no planejamento): antes de cada task, localizar o nome vigente no disco pela linha `status=approved` de `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv` (colunas `old`/`new`/`declared_at`) ou por `ref/REFERENCIA-CONGELADA.md` §Caminhos resolvidos, e usar esse nome; símbolo sem linha no mapa mantém o nome atual; nunca escrever nome proposto não aplicado. Identificadores, arquivos, endpoints e títulos de teste NOVOS já vêm em inglês neste plano (`std/CONVENTIONS-EN.md`). Texto visível ao usuário continua em português; nomes SQL novos seguem o snake_case português do schema (D-08 da Phase 1); chaves `$…`/`settings` do desenho `{flow, globalActions}` são contrato persistido (STD-06) e não mudam; variáveis `PIPE_*` não mudam. Depois do D-46, `@pipe/gestao-vite` é `@pipe/management-vite` e `apps/gestao-vite` é `apps/management-vite` nos comandos.

Ponto de extensão real hoje: `servicos.enviar` em `apps/api/src/dominio/fluxo.ts` (~linha 325) chama `textoParaOCanal` (~897, lança erro para MIME sem tradução) e `gravarRespostaDoBot` (~827, grava `mensagem.tipo = 'texto'`). O Desk já envia mídia com `TipoEnvio = 'texto'|'imagem'|'audio'|'video'|'documento'|'template'` (`apps/api/src/dominio/envio.ts:28`) e os workers já entregam mídia do atendente — reaproveitar esse caminho para o bot em vez de criar outro.
Estilo: nenhum CSS novo neste plano (painel-bloco.css pertence a 02-08/02-09/02-12); usar `Campo`/`Etiqueta`/`Selecao`/`Icone` de `@pipe/ui` e ícones de `icones.tsx` (D-33).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Motor aceita e valida os tipos de mídia (editor.ts)</name>
  <files>packages/core/src/fluxo/editor.ts, packages/core/src/fluxo/editor.teste.ts</files>
  <read_first>
    - packages/core/src/fluxo/editor.ts (CONTEUDOS_SUPORTADOS ~151-153, recusa na publicação ~206)
    - packages/core/src/fluxo/editor.teste.ts (padrão vitest do arquivo)
    - .planning/phases/02-fechar-o-builder/ref/inventario-conteudo.md (seções dos tipos do slot: MIME, formato serializado, obrigatórios, limites)
  </read_first>
  <behavior>
    - Para cada tipo do slot: fluxo com `SendMessage` desse MIME e settings válidos publica sem erro.
    - Settings sem campo obrigatório (ex.: `uri` do media-link) → erro de publicação com a mensagem literal do inventário.
    - Limite de tamanho/quantidade do inventário excedido → erro com mensagem literal.
    - MIME fora do catálogo continua recusado como hoje.
  </behavior>
  <action>
    Acrescentar os MIME do slot a `CONTEUDOS_SUPORTADOS` e, no mesmo arquivo, a validação por MIME (função `engineContentErrors(tipo, settings)` chamada pelo ponto de recusa atual), com as regras do inventário. Testes primeiro em `editor.teste.ts` (`describe('media content')`), um `it` por tipo × (válido, obrigatório ausente, limite).
  </action>
  <verify>
    <automated>pnpm --filter @pipe/core exec vitest run src/fluxo/editor.teste.ts && pnpm --filter @pipe/core typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Cada `mime` do slot aprovado aparece em `CONTEUDOS_SUPORTADOS`
    - `editor.teste.ts` tem `describe('media content'` com ≥3 `it` por tipo implementado
    - comandos do verify saem 0
  </acceptance_criteria>
  <done>Motor publica os tipos de mídia aprovados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Saída do bot multi-tipo e entrega por canal</name>
  <files>apps/api/src/dominio/fluxo.ts, apps/api/tests/flow-content.test.ts, apps/workers/src/entrega.ts, apps/workers/src/whatsapp/midia.ts, apps/workers/src/instagram.ts, apps/workers/src/messenger.ts</files>
  <read_first>
    - apps/api/src/dominio/fluxo.ts (servicos.enviar ~325-331, gravarRespostaDoBot ~827-870, textoParaOCanal/perguntaDoSelect ~870-915, chamarHttp/confirmarUrlSegura ~340)
    - apps/api/src/dominio/envio.ts (TipoEnvio, PedidoDeEnvio: como o Desk grava mídia em mensagem/dados)
    - apps/workers/src/entrega.ts, apps/workers/src/whatsapp/midia.ts, apps/workers/src/instagram.ts, apps/workers/src/messenger.ts (como a mídia do atendente sai hoje)
    - apps/api/tests/fluxo.test.ts (montagem de cenário com Postgres para copiar)
    - .planning/phases/02-fechar-o-builder/ref/inventario-conteudo.md (linhas WhatsApp/Instagram/Messenger/Canal sem suporte de cada tipo)
  </read_first>
  <behavior>
    - Fluxo com bloco de imagem: a entrada do contato gera `mensagem` do bot com `tipo = 'imagem'`, `dados.midia.url` = uri do settings (após interpolação de variáveis) e linha em `outbox_mensagem`.
    - Texto e menu continuam idênticos ao comportamento atual (regressão).
    - Canal que não suporta o tipo segundo o inventário: aplica a regra da referência (fallback/aviso) e o teste afirma o resultado.
    - URL de mídia com esquema diferente de https, ou que `confirmarUrlSegura` recusa, faz a ação falhar com erro (sem mensagem pela metade).
  </behavior>
  <action>
    Em `fluxo.ts`, criar `toChannelOutput(m: MensagemDeSaida, canal)` que devolve `{ tipo: TipoEnvio, texto: string | null, dados: Record<string, unknown> | null } | null` (texto e menu delegam a `textoParaOCanal`/`perguntaDoSelect`, preservados; mídia → `tipo` da tabela do inventário e `dados.midia = { url, mime, titulo, nomeArquivo }`); `servicos.enviar` passa a usar `toChannelOutput`; `gravarRespostaDoBot` recebe `tipo` (default `'texto'`) e grava-o em `mensagem.tipo`. Toda URL de mídia passa por `confirmarUrlSegura` antes de gravar. Nos workers, o envio de mensagem de autor `bot` com `tipo` de mídia e `dados.midia.url` usa o mesmo caminho da mídia do atendente, enviando por link quando a API do canal aceita link e baixando com a mesma guarda de URL quando exige upload. Comportamento por canal exatamente como a coluna do inventário. Teste novo `apps/api/tests/flow-content.test.ts` (padrão de `fluxo.test.ts`, `PIPE_FILAS=memoria`, `PIPE_WHATSAPP_CLIENTE=duble`).
  </action>
  <verify>
    <automated>pnpm banco:subir && pnpm --filter @pipe/api exec vitest run tests/flow-content.test.ts tests/fluxo.test.ts && pnpm --filter @pipe/api typecheck && pnpm --filter @pipe/workers typecheck && pnpm --filter @pipe/workers test</automated>
  </verify>
  <acceptance_criteria>
    - `fluxo.ts` contém `toChannelOutput(` usado em `servicos.enviar` e `confirmarUrlSegura` aplicado à URL de mídia
    - `flow-content.test.ts` tem ao menos um teste por tipo do slot e um de URL insegura rejeitada
    - `tests/fluxo.test.ts` continua verde (texto/menu sem regressão)
    - typecheck de api e workers sai 0
  </acceptance_criteria>
  <done>Bot entrega mídia nos canais conforme a referência.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Editor de cada tipo de mídia no Builder</name>
  <files>apps/gestao-vite/src/paginas/builder/conteudo.ts, apps/gestao-vite/src/paginas/builder/painel-conteudo.tsx, apps/gestao-vite/tests/builder-content.test.ts</files>
  <read_first>
    - apps/gestao-vite/src/paginas/builder/conteudo.ts (fala, novoTexto/novoMenu/novoQuickReply 146-156, cartoesDe 105-129, errosDoConteudo 258-285, comentário 1-27)
    - apps/gestao-vite/src/paginas/builder/painel-conteudo.tsx (cartões + menu "+", 42-55)
    - .planning/phases/02-fechar-o-builder/ref/inventario-conteudo.md (nome/posição no menu, campos, padrões, preview, mensagens)
    - packages/ui/src/icones.tsx (ícones criados em 02-08)
  </read_first>
  <behavior>
    - `novo<Tipo>()` devolve `ItemDeConteudo` com `settings.type` = MIME do slot e `$typeOfContent` igual ao da referência.
    - `cartoesDe` reabre um bloco salvo com esse tipo como o mesmo cartão (ida e volta sem perda).
    - `errosDoConteudo` devolve as mensagens literais do inventário para obrigatório ausente e limite.
  </behavior>
  <action>
    Em `conteudo.ts`, uma função `novo<Tipo>` por tipo do slot sobre `fala(id, mime, conteudo, tipoDoCartao)`, ramo correspondente em `cartoesDe` e em `errosDoConteudo`; remover o tipo do comentário de "tipos que o Pipe recusa". Em `painel-conteudo.tsx`, entrada no menu "+" na posição exata da referência, com ícone Pipe de `icones.tsx`, editor com os campos/padrões/preview do inventário e suporte a variáveis onde a referência suporta. Sem CSS novo, sem hex. Teste novo `apps/gestao-vite/tests/builder-content.test.ts` (`node:test`), ida e volta por tipo. Rodar o gate `node .planning/phases/02-fechar-o-builder/ref/conferir-catalogo.mjs --slot conteudo-midia`.
  </action>
  <verify>
    <automated>pnpm --filter @pipe/gestao-vite test && pnpm --filter @pipe/gestao-vite typecheck && node .planning/phases/02-fechar-o-builder/ref/conferir-catalogo.mjs --slot conteudo-midia</automated>
  </verify>
  <acceptance_criteria>
    - `conferir-catalogo.mjs --slot conteudo-midia` imprime `OK` e sai 0
    - `builder-content.test.ts` tem teste de ida e volta para cada tipo implementado
    - `painel-conteudo.tsx` não contém `dangerouslySetInnerHTML` nem hex de cor
  </acceptance_criteria>
  <done>Atendente cria blocos de mídia no Builder, publicados e executados de verdade.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fluxo definido/importado no Builder → motor → worker | URL e metadados de mídia são não confiáveis |
| worker → rede externa | download de mídia por URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-14 | Information disclosure / SSRF | URL de mídia do bot (fluxo.ts, workers) | high | mitigate | Task 2: `confirmarUrlSegura` antes de gravar e antes de baixar; só https; teste de URL insegura rejeitada |
| T-2-05 | Tampering | settings de conteúdo de fluxo importado | medium | mitigate | Task 1: validação por MIME na publicação (obrigatórios/limites); publicação inválida já é 409 |
| T-2-16 | Tampering (XSS) | preview no painel | medium | mitigate | Task 3: preview renderiza texto/`<img src>` sem HTML cru; grep `dangerouslySetInnerHTML` = 0 |
</threat_model>

<verification>
Testes de core, api e gestao-vite verdes; `conferir-catalogo.mjs --slot conteudo-midia` OK.
</verification>

<success_criteria>
Slot conteudo-midia fechado ponta a ponta (editor + contrato + motor + canal).
</success_criteria>

<output>
Create `.planning/phases/02-fechar-o-builder/02-10-SUMMARY.md` when done
</output>
