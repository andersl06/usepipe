---
phase: 02-fechar-o-builder
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gestao-vite/tests/builder-editor.test.ts
autonomous: true
requirements: [BUILDER-05, BUILDER-04]

must_haves:
  truths:
    - "arestasDe() tem teste de caracterização cobrindo $conditionOutputs válidos, destino inexistente, múltiplas saídas, saídas de atendimento humano, $defaultOutput sem seta, $isDeskDefaultOutput e $isDeskCustomOutput (D-29)"
    - "$defaultOutput preenchido continua sem seta, preservando a referência (D-29)"
    - "Copiar, duplicar, copiar id e colar (IDs distintos, dados independentes, limite de 15) estão cobertos por teste (D-17)"
  artifacts:
    - path: "apps/gestao-vite/tests/builder-editor.test.ts"
      provides: "testes de caracterização de arestasDe e cobertura de copiar/colar"
      contains: "arestasDe"
  key_links:
    - from: "apps/gestao-vite/tests/builder-editor.test.ts"
      to: "apps/gestao-vite/src/paginas/builder/modelo.ts#arestasDe"
      via: "import nomeado"
      pattern: "arestasDe,"
---

<objective>
Caracterizar o comportamento atual de `arestasDe()` (D-29.1) e confirmar a cobertura de copiar/colar (D-17) sem alterar código de produção. Divergência encontrada aqui NÃO é corrigida neste plano: vira caso mínimo registrado para 02-09 (D-29.5).

Output: testes novos em `apps/gestao-vite/tests/builder-editor.test.ts`.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fechar-o-builder/02-PATTERNS.md

Pré-condição e nomes (D-05, STD-01): a Task 0 (Pre-flight Gate) é a imposição: o plano para se `git tag -l std-apply-all-end` não imprimir a tag (a Phase 1 precisa ter aplicado o mapa; o D-22 da Phase 1 congela código paralelo). Caminhos e símbolos EXISTENTES aparecem aqui com o nome em português de 2026-09-26 (confirmado no disco no planejamento): antes de cada task, localizar o nome vigente no disco pela linha `status=approved` de `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv` (colunas `old`/`new`/`declared_at`) ou por `ref/REFERENCIA-CONGELADA.md` §Caminhos resolvidos, e usar esse nome; símbolo sem linha no mapa mantém o nome atual; nunca escrever nome proposto não aplicado. Identificadores, arquivos, endpoints e títulos de teste NOVOS já vêm em inglês neste plano (`std/CONVENTIONS-EN.md`). Texto visível ao usuário continua em português; nomes SQL novos seguem o snake_case português do schema (D-08 da Phase 1); chaves `$…`/`settings` do desenho `{flow, globalActions}` são contrato persistido (STD-06) e não mudam; variáveis `PIPE_*` não mudam. Depois do D-46, `@pipe/gestao-vite` é `@pipe/management-vite` e `apps/gestao-vite` é `apps/management-vite` nos comandos.

Padrão de teste: `node:test` + `node:assert/strict`, sem fixture de framework (ver teste 'liga e desliga uma aresta...' na linha ~60).
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight Gate — Phase 1 aplicou o mapa (tag std-apply-all-end)</name>
  <files>nenhum (verificação somente leitura)</files>
  <action>Antes de qualquer outra task, rodar `git tag -l std-apply-all-end`. Se a saída não for exatamente `std-apply-all-end`, PARAR o plano: não tocar em nenhum arquivo, não commitar, não seguir para a Task 1 e devolver ao orquestrador/usuário "Pre-flight Gate: tag std-apply-all-end ausente — a Phase 1 ainda não aplicou o mapa de nomes; concluir a Phase 1 antes (D-22 da Phase 1 congela `apps/gestao-vite/src/paginas/builder/*`, `packages/core/src/fluxo/*`, `apps/api/src/dominio/fluxo.ts`)". Não criar a tag nem contornar o gate (D-05, STD-01). Com a tag presente, seguir para a Task 1.</action>
  <verify>
    <automated>test "$(git tag -l std-apply-all-end)" = "std-apply-all-end"</automated>
  </verify>
  <acceptance_criteria>
    - `git tag -l std-apply-all-end` imprime `std-apply-all-end`
    - se o gate falhar, `git status --porcelain` não mostra arquivo alterado por este plano e o plano termina sem SUMMARY de conclusão
  </acceptance_criteria>
  <done>Tag `std-apply-all-end` confirmada; plano liberado. Sem a tag, plano parado e escalado ao usuário.</done>
</task>

<task type="auto">
  <name>Task 1: Testes de caracterização de arestasDe() (D-29.1)</name>
  <files>apps/gestao-vite/tests/builder-editor.test.ts</files>
  <read_first>
    - apps/gestao-vite/tests/builder-editor.test.ts (imports linhas 1-19 e teste da linha ~60)
    - apps/gestao-vite/src/paginas/builder/modelo.ts (arestasDe ~421-434; SAIDAS_DE_ATENDIMENTO 129-133; novoBlocoDeAtendimento 283-319; tipo Bloco/saída)
    - apps/gestao-vite/src/paginas/builder/painel-saidas.tsx (linhas 83-153: saídas customizáveis OutOfAttendanceHour/NoAgentAvailable com $isDeskCustomOutput)
    - referencias-blip/builder/builder/PAINEL-Saidas.md (linha 53)
  </read_first>
  <action>
    Adicionar `arestasDe` e `novoBlocoDeAtendimento` ao import de `../src/paginas/builder/modelo.ts` e criar um bloco de testes com títulos começando por `edges:`:
    (1) `$conditionOutputs` com `stateId` existente → exatamente `{ de, para }`;
    (2) `stateId` inexistente no mapa → nenhuma aresta;
    (3) duas saídas do mesmo bloco para o mesmo destino → uma aresta (dedupe);
    (4) saídas para destinos diferentes → uma aresta por destino, na ordem das saídas;
    (5) bloco de atendimento de `novoBlocoDeAtendimento` com destinos em `ClosedAttendant`, `ClosedClient`, `ClosedClientInactivity` → três arestas;
    (6) saída com `$isDeskDefaultOutput: true` e destino existente → nenhuma aresta;
    (7) saída `$isDeskCustomOutput` (OutOfAttendanceHour/NoAgentAvailable) com destino existente → registra o comportamento atual (aresta ou não) com asserção explícita e comentário `// caracterização: conferir com ref/inventario-paineis-e-setas.md (D-29.2)`;
    (8) `$defaultOutput.stateId` preenchido e sem `$conditionOutputs` → nenhuma aresta (intencional, PAINEL-Saidas.md:53);
    (9) mapa vazio e bloco sem `$conditionOutputs` → `[]`.
    Não alterar `modelo.ts`. Se algum caso revelar comportamento suspeito, manter a asserção do comportamento atual e registrar o caso mínimo no SUMMARY sob `Divergências para 02-09`.
  </action>
  <verify>
    <automated>pnpm --filter @pipe/gestao-vite test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^test('edges:" apps/gestao-vite/tests/builder-editor.test.ts` ≥ 9
    - o arquivo contém `$isDeskDefaultOutput`, `$isDeskCustomOutput`, `$defaultOutput` e `ClosedClientInactivity`
    - `git diff --stat` não mostra alteração em `modelo.ts`
    - `pnpm --filter @pipe/gestao-vite test` sai com código 0
  </acceptance_criteria>
  <done>arestasDe() coberta por caracterização.</done>
</task>

<task type="auto">
  <name>Task 2: Cobertura de copiar/colar/duplicar (D-17)</name>
  <files>apps/gestao-vite/tests/builder-editor.test.ts</files>
  <read_first>
    - apps/gestao-vite/src/paginas/builder/modelo.ts (linhas 342-388: copiaDoBloco, duplicarBloco, textoDoBlocoCopiado, blocoDoTextoCopiado, colarBloco, limite de 15)
    - apps/gestao-vite/tests/builder-editor.test.ts (teste 'copia um bloco...' linha ~48)
    - apps/gestao-vite/tests/builder-painels.test.ts (procurar teste existente de limite 15)
  </read_first>
  <action>
    Rodar `grep -n "15" apps/gestao-vite/tests/builder-*.test.ts` e listar no SUMMARY quais comportamentos já têm teste. Acrescentar em `builder-editor.test.ts` apenas os que faltarem entre: duplicar gera id novo e dados independentes (mutar a cópia não altera o original); colar o texto copiado gera id distinto a cada colagem; colar texto inválido (JSON quebrado ou sem bloco) devolve erro/nulo sem alterar o mapa; limite de 15 aplicado à seleção inteira (16ª cópia rejeitada com a mensagem existente, sem reescrever o texto); a saída copiada não leva `stateId` para bloco inexistente. Títulos começando por `copy-paste:`.
  </action>
  <verify>
    <automated>pnpm --filter @pipe/gestao-vite test</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY contém a lista de comportamentos já cobertos vs acrescentados
    - Existe ao menos um teste cujo título começa por `copy-paste:` ou o SUMMARY prova, por grep, que todos os cinco comportamentos já estavam cobertos
    - `pnpm --filter @pipe/gestao-vite test` sai com código 0
  </acceptance_criteria>
  <done>Copiar/colar com cobertura confirmada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| área de transferência → editor | texto colado pode ser arbitrário |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-19 | Tampering | blocoDoTextoCopiado | low | mitigate | Task 2 testa texto inválido sem alterar o mapa |
| T-2-17 | Information disclosure | testes | low | accept | só dados sintéticos, nenhum export real no repositório |
</threat_model>

<verification>
`pnpm --filter @pipe/gestao-vite test` verde com ≥9 testes `edges:`.
</verification>

<success_criteria>
Critério de sucesso 5 do roadmap tem o teste que cobre `arestasDe()`.
</success_criteria>

<output>
Create `.planning/phases/02-fechar-o-builder/02-06-SUMMARY.md` when done
</output>
