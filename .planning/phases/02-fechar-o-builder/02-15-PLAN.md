---
phase: 02-fechar-o-builder
plan: 15
type: execute
wave: 6
depends_on: ["02-10", "02-14"]
files_modified:
  - packages/core/src/fluxo/editor.ts
  - packages/core/src/fluxo/editor.teste.ts
  - apps/api/src/dominio/fluxo.ts
  - apps/api/tests/flow-content.test.ts
  - apps/workers/src/entrega.ts
  - apps/workers/src/whatsapp/interativo.ts
  - apps/workers/src/instagram.ts
  - apps/workers/src/messenger.ts
  - apps/gestao-vite/src/paginas/builder/conteudo.ts
  - apps/gestao-vite/src/paginas/builder/painel-conteudo.tsx
  - apps/gestao-vite/tests/builder-content.test.ts
autonomous: true
requirements: [BUILDER-01]

must_haves:
  truths:
    - "O atendente cria cada tipo do slot conteudo-interativo aprovado (carrossel, variações de menu/quick reply, web link, enviar/solicitar localização, digitando, solicitar ligação ou o que o portão definiu) com editor, validações e preview da referência (D-18)"
    - "O motor publica/executa esses tipos e o bot os entrega em cada canal com o comportamento de canal documentado, incluindo o que a referência faz quando o canal não suporta (D-18)"
    - "Resposta do contato a conteúdo interativo (clique de botão, localização enviada) chega ao fluxo no formato que as condições de saída da referência avaliam (D-18)"
    - "conferir-catalogo --slot conteudo-interativo passa (D-24)"
  artifacts:
    - path: "packages/core/src/fluxo/editor.ts"
      provides: "MIME do slot em CONTEUDOS_SUPORTADOS + validação"
    - path: "apps/api/src/dominio/fluxo.ts"
      provides: "toChannelOutput cobre os tipos interativos"
      contains: "toChannelOutput"
  key_links:
    - from: "toChannelOutput"
      to: "apps/workers/src/whatsapp/interativo.ts"
      via: "dados estruturados no outbox"
      pattern: "toChannelOutput"
---

<objective>
Fechar ponta a ponta o slot `conteudo-interativo` aprovado no portão, reaproveitando `toChannelOutput` criado em 02-10 e o envio interativo já existente em `apps/workers/src/whatsapp/interativo.ts`.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fechar-o-builder/ref/inventario-conteudo.md
@.planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md
@.planning/phases/02-fechar-o-builder/02-10-SUMMARY.md

Escopo exato: itens de `ref/catalogo-aprovado.json` com `"slot": "conteudo-interativo"` e `"classificacao": "reproduzivel"`; `bloqueado` fica fora e listado no SUMMARY (D-03). Detalhes só do inventário congelado.

Pré-condição e nomes (D-05, STD-01): o gate da Phase 1 (tag `std-apply-all-end`) é imposto pela Task 0 (Pre-flight Gate) de 02-05/02-06/02-08, das quais este plano depende transitivamente via `depends_on` (02-10 → 02-05). Caminhos e símbolos EXISTENTES aparecem com o nome em português de 2026-09-26; localizar o nome vigente pela linha `status=approved` de `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv` ou por `ref/REFERENCIA-CONGELADA.md` §Caminhos resolvidos; nunca escrever nome proposto não aplicado. Identificadores/títulos de teste NOVOS em inglês. MIME, `settings` e `$typeOfContent` são literais persistidos (STD-06). `@pipe/gestao-vite` vira `@pipe/management-vite` após D-46. Nenhum CSS novo (usar `@pipe/ui`).
Hoje: menu `select` já vira pergunta estruturada via `perguntaDoSelect` → `dados.pergunta` → worker `interativo.ts` (botões ou lista); texto numerado é o plano B (`textoParaOCanal`).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Motor e entrega por canal dos tipos interativos</name>
  <files>packages/core/src/fluxo/editor.ts, packages/core/src/fluxo/editor.teste.ts, apps/api/src/dominio/fluxo.ts, apps/api/tests/flow-content.test.ts, apps/workers/src/entrega.ts, apps/workers/src/whatsapp/interativo.ts, apps/workers/src/instagram.ts, apps/workers/src/messenger.ts</files>
  <read_first>
    - packages/core/src/fluxo/editor.ts (CONTEUDOS_SUPORTADOS e validação por MIME de 02-10)
    - apps/api/src/dominio/fluxo.ts (toChannelOutput de 02-10, perguntaDoSelect, recepção da entrada do contato → MensagemDeEntrada)
    - packages/core/src/fluxo/contexto.ts (MensagemDeEntrada, criarEntrada)
    - apps/workers/src/whatsapp/interativo.ts, apps/workers/src/instagram.ts, apps/workers/src/messenger.ts, apps/workers/src/entrega.ts
    - apps/workers/tests/interativo.test.ts
    - .planning/phases/02-fechar-o-builder/ref/inventario-conteudo.md (seções do slot: MIME, formato, canais, canal sem suporte, formato da resposta do contato)
  </read_first>
  <behavior>
    - Por tipo: publica com settings válidos; recusa obrigatório ausente/limite excedido com mensagem literal.
    - Por tipo × canal: `toChannelOutput` produz o `tipo`/`dados` que o worker envia; canal sem suporte aplica a regra documentada.
    - Resposta do contato (botão, item de lista, localização) vira a entrada no formato que a condição de saída da referência lê (ex.: texto da opção, `input.content@latitude`).
    - Web link: URL só https e validada; carrossel respeita limite de cartões da referência.
  </behavior>
  <action>
    Acrescentar os MIME do slot a `CONTEUDOS_SUPORTADOS` com validação por MIME (mesma função criada em 02-10); estender `toChannelOutput` para cada tipo (dados estruturados em `mensagem.dados`, texto de plano B quando a referência faz fallback); nos workers, enviar o formato nativo de cada canal quando existe (botões/lista/localização do WhatsApp em `interativo.ts`, quick replies/templates genéricos do Messenger/Instagram) e a regra documentada quando não existe; mapear a resposta interativa do contato para a `MensagemDeEntrada` com o tipo/conteúdo que a referência entrega ao bot. Testes: `describe('interactive content')` em `editor.teste.ts`, casos por tipo em `flow-content.test.ts` e em `apps/workers/tests/interativo.test.ts` quando o worker mudar.
  </action>
  <verify>
    <automated>pnpm --filter @pipe/core exec vitest run src/fluxo && pnpm banco:subir && pnpm --filter @pipe/api exec vitest run tests/flow-content.test.ts tests/fluxo.test.ts && pnpm --filter @pipe/workers test && pnpm --filter @pipe/api typecheck && pnpm --filter @pipe/workers typecheck</automated>
  </verify>
  <acceptance_criteria>
    - cada MIME do slot em `CONTEUDOS_SUPORTADOS`
    - `flow-content.test.ts` tem ao menos um teste de envio e um de resposta do contato por tipo do slot
    - URL de web link não-https rejeitada (teste)
    - verify sai 0
  </acceptance_criteria>
  <done>Conteúdo interativo executa e volta ao fluxo corretamente.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Editor dos tipos interativos no Builder</name>
  <files>apps/gestao-vite/src/paginas/builder/conteudo.ts, apps/gestao-vite/src/paginas/builder/painel-conteudo.tsx, apps/gestao-vite/tests/builder-content.test.ts</files>
  <read_first>
    - apps/gestao-vite/src/paginas/builder/conteudo.ts (fala, novoMenu/novoQuickReply, cartoesDe, errosDoConteudo; funções de 02-10)
    - apps/gestao-vite/src/paginas/builder/painel-conteudo.tsx
    - apps/gestao-vite/tests/builder-content.test.ts (padrão de 02-10)
    - .planning/phases/02-fechar-o-builder/ref/inventario-conteudo.md (menu, campos, padrões, preview, mensagens)
  </read_first>
  <behavior>
    - Cada fábrica nova devolve settings com o MIME e os padrões da referência.
    - Ida e volta por tipo pelo `cartoesDe`.
    - Erros de campo com mensagens literais; carrossel além do limite recusado.
  </behavior>
  <action>
    Mesmo padrão de 02-10: fábrica por tipo sobre `fala`, ramo em `cartoesDe` e `errosDoConteudo`, entrada no menu "+" na posição da referência com ícone Pipe, editor de campos (cartões do carrossel com botões, itens de menu, URL/título/preview do web link, texto de solicitar localização), preview sem HTML cru (links renderizados com `rel="noopener noreferrer"`, só https). Testes `content:` por tipo. Gate de catálogo.
  </action>
  <verify>
    <automated>pnpm --filter @pipe/gestao-vite test && pnpm --filter @pipe/gestao-vite typecheck && node .planning/phases/02-fechar-o-builder/ref/conferir-catalogo.mjs --slot conteudo-interativo</automated>
  </verify>
  <acceptance_criteria>
    - `conferir-catalogo.mjs --slot conteudo-interativo` imprime `OK` e sai 0
    - ida e volta testada para cada tipo do slot
    - `painel-conteudo.tsx` sem `dangerouslySetInnerHTML`; links de preview com `noopener`
  </acceptance_criteria>
  <done>Atendente cria conteúdo interativo no Builder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fluxo → canal | URLs e textos de cartões não confiáveis |
| contato → motor | resposta interativa (payload de botão, localização) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-16 | Tampering (XSS) | preview de web link/carrossel | medium | mitigate | Task 2: sem HTML cru, só https, `noopener noreferrer` |
| T-2-27 | Spoofing | payload de botão do contato | medium | mitigate | Task 1: payload aceito só se casar com opção enviada; senão tratado como texto livre |
| T-2-14 | SSRF | imagens de carrossel baixadas pelo worker | high | mitigate | Task 1: mesma guarda `confirmarUrlSegura` de 02-10 |
</threat_model>

<verification>
Testes core/api/workers/app verdes; gate `--slot conteudo-interativo` OK.
</verification>

<success_criteria>
Slot conteudo-interativo fechado ponta a ponta.
</success_criteria>

<output>
Create `.planning/phases/02-fechar-o-builder/02-15-SUMMARY.md` when done
</output>
