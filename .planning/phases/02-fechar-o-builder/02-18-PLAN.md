---
phase: 02-fechar-o-builder
plan: 18
type: execute
wave: 9
depends_on: ["02-13", "02-17"]
files_modified:
  - apps/gestao-vite/src/paginas/builder/flow-functions.ts
  - apps/gestao-vite/src/paginas/builder/flow-functions-panel.tsx
  - apps/gestao-vite/src/paginas/builder/painel-configuracao.tsx
  - apps/gestao-vite/src/paginas/builder/painel-variaveis.tsx
  - apps/gestao-vite/src/paginas/builder/painel-acoes.tsx
  - apps/gestao-vite/src/paginas/builder/painel-bloco.css
  - apps/gestao-vite/tests/builder-functions.test.ts
autonomous: true
requirements: [BUILDER-02]

must_haves:
  truths:
    - "O atendente busca função da biblioteca por nome/descrição sem acento/caixa, no mesmo lugar e forma que a referência mostra, e a insere no ponto de chamada documentado (D-22, D-23)"
    - "O atendente cria, edita (e versiona, se aprovado) funções da biblioteca sem sair do Builder, com os campos e validações da referência (D-22)"
    - "A busca de variável continua funcionando e, se a referência une variáveis e funções no mesmo painel, o Pipe reproduz essa forma (D-22)"
  artifacts:
    - path: "apps/gestao-vite/src/paginas/builder/flow-functions.ts"
      provides: "cliente da API (Resultado<T>) + filterFlowFunctions"
      exports: ["filterFlowFunctions"]
    - path: "apps/gestao-vite/src/paginas/builder/flow-functions-panel.tsx"
      provides: "painel da biblioteca de funções"
  key_links:
    - from: "flow-functions.ts"
      to: "API FlowFunctionsController (02-17)"
      via: "api.get/post/put/delete"
      pattern: "api\\.(get|post|put|delete)"
    - from: "filterFlowFunctions"
      to: "variaveis.ts normalizar"
      via: "mesmo filtro sem acento/caixa"
      pattern: "normalizar|normalize"
---

<objective>
Entregar a metade de tela da biblioteca de funções (BUILDER-02): painel de gestão das funções, busca de função e inserção no ponto de chamada, na forma do inventário congelado.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fechar-o-builder/02-UI-SPEC.md
@.planning/phases/02-fechar-o-builder/ref/inventario-acoes.md
@.planning/phases/02-fechar-o-builder/02-17-SUMMARY.md

Pré-condição e nomes (D-05, STD-01): só executa depois que a Phase 1 aplicou o mapa (tag `std-apply-all-end`). Caminhos/símbolos EXISTENTES com nome em português de 2026-09-26; localizar o nome vigente pela linha `status=approved` de `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/gestao-vite.csv` ou por `ref/REFERENCIA-CONGELADA.md` §Caminhos resolvidos. Arquivos/identificadores NOVOS em inglês kebab-case/camelCase. Texto visível em português da referência. `@pipe/gestao-vite` vira `@pipe/management-vite` após D-46. CSS só com tokens (`--p-e-*`, superfícies, `--p-builder-marca-*`).
Padrões: busca de `painel-variaveis.tsx` (useState, filtro derivado sem debounce) e `normalizar` de `variaveis.ts` (exportado em 02-09); `Resultado<T>`/`motivoDe` de `builder-gravar.ts`; abas de `painel-configuracao.tsx`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cliente da API e filtro de funções</name>
  <files>apps/gestao-vite/src/paginas/builder/flow-functions.ts, apps/gestao-vite/tests/builder-functions.test.ts</files>
  <read_first>
    - apps/gestao-vite/src/paginas/builder-gravar.ts (Resultado<T>, motivoDe, api)
    - apps/gestao-vite/src/paginas/builder/variaveis.ts (normalizar)
    - packages/contracts/src/flow-functions.ts (02-17)
    - .planning/phases/02-fechar-o-builder/02-17-SUMMARY.md (rotas finais)
  </read_first>
  <behavior>
    - `filterFlowFunctions(list, 'calcul')` casa "Cálculo de frete" e descrição "calcula o total".
    - Busca vazia devolve todas na ordem da referência.
    - Funções de erro da API viram `{ ok: false, erro }` com mensagem em português.
  </behavior>
  <action>
    Criar `flow-functions.ts` com `listFlowFunctions`, `createFlowFunction`, `updateFlowFunction`, `deleteFlowFunction` (padrão `Resultado<T>`, nunca lançando para o componente) e a função pura `filterFlowFunctions(list, query)` sobre `normalizar`. Testes `functions:` em `builder-functions.test.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @pipe/gestao-vite test</automated>
  </verify>
  <acceptance_criteria>
    - `flow-functions.ts` exporta `filterFlowFunctions` e as quatro funções de API
    - ≥3 testes `functions:` passam
  </acceptance_criteria>
  <done>Camada de dados da biblioteca no front.</done>
</task>

<task type="auto">
  <name>Task 2: Painel da biblioteca, busca e inserção no ponto de chamada</name>
  <files>apps/gestao-vite/src/paginas/builder/flow-functions-panel.tsx, apps/gestao-vite/src/paginas/builder/painel-configuracao.tsx, apps/gestao-vite/src/paginas/builder/painel-variaveis.tsx, apps/gestao-vite/src/paginas/builder/painel-acoes.tsx, apps/gestao-vite/src/paginas/builder/painel-bloco.css</files>
  <read_first>
    - apps/gestao-vite/src/paginas/builder/painel-configuracao.tsx (abas atuais e aba Versões de 02-13)
    - apps/gestao-vite/src/paginas/builder/painel-variaveis.tsx
    - apps/gestao-vite/src/paginas/builder/painel-acoes.tsx (campo `code` de 02-16)
    - .planning/phases/02-fechar-o-builder/ref/inventario-acoes.md (## Biblioteca de funções (D-22): onde a biblioteca aparece, campos, como é chamada)
  </read_first>
  <action>
    Criar `flow-functions-panel.tsx`: lista com busca (placeholder no padrão "Buscar função…" ou o texto literal da referência), estado vazio, formulário de criar/editar (nome, descrição, parâmetros, código no mesmo campo `code` de 02-16, versão se aprovada), exclusão com confirmação, mensagens da API. Montá-lo no local que a referência usa (aba em `painel-configuracao.tsx` ou seção de `painel-variaveis.tsx`, conforme inventário). No campo de código das ações de script (`painel-acoes.tsx`), um seletor de função com a mesma busca insere a chamada no formato documentado (ex.: `nomeDaFuncao(param1, param2)`). CSS com prefixo de classe vigente do Builder + `functions`, só tokens.
  </action>
  <verify>
    <automated>pnpm --filter @pipe/gestao-vite typecheck && pnpm --filter @pipe/gestao-vite test</automated>
  </verify>
  <acceptance_criteria>
    - `flow-functions-panel.tsx` importa `filterFlowFunctions` e `listFlowFunctions`
    - o painel é montado no arquivo indicado pelo inventário (grep do componente nesse arquivo)
    - `painel-acoes.tsx` referencia o seletor de função
    - CSS novo sem hex; typecheck e test saem 0
  </acceptance_criteria>
  <done>Atendente busca e usa funções da biblioteca no Builder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dados da API (nome/descrição/código) → DOM | texto do tenant renderizado |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-16 | Tampering (XSS) | painel da biblioteca | medium | mitigate | renderização como texto React; código em `<textarea>`; sem `dangerouslySetInnerHTML` |
| T-2-09 | Elevation of privilege | chamadas da UI | high | transfer | autorização aplicada no servidor em 02-17 (401/403 testados); a UI só reflete o erro |
</threat_model>

<verification>
Testes e typecheck do app verdes.
</verification>

<success_criteria>
Critério 2 do roadmap (buscar variável e função da biblioteca) completo com 02-09 e 02-17.
</success_criteria>

<output>
Create `.planning/phases/02-fechar-o-builder/02-18-SUMMARY.md` when done
</output>
