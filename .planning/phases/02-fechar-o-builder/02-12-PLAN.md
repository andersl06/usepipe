---
phase: 02-fechar-o-builder
plan: 12
type: execute
wave: 5
depends_on: ["02-09", "02-11"]
files_modified:
  - apps/gestao-vite/src/paginas/builder/modelo.ts
  - apps/gestao-vite/src/paginas/builder/painel-saidas.tsx
  - apps/gestao-vite/src/paginas/builder/menu-novo-bloco.tsx
  - apps/gestao-vite/src/paginas/builder/no.tsx
  - apps/gestao-vite/src/paginas/builder/variaveis.ts
  - apps/gestao-vite/src/paginas/builder/painel-bloco.css
  - apps/gestao-vite/tests/builder-editor.test.ts
autonomous: true
requirements: [BUILDER-03]

must_haves:
  truths:
    - "O atendente cria o bloco de pesquisa de satisfação a partir do bloco de atendimento humano do mesmo jeito que a referência, com todos os campos e condições de saída documentados (D-06, D-08)"
    - "A condição 'Exibir apenas blocos de pesquisa de satisfação' se comporta como na referência (filtro do seletor ou lógica da saída, conforme inventário) (D-08)"
    - "As saídas do bloco de pesquisa são as documentadas (não derivadas do relatório) e desenham seta no canvas quando a referência desenha (D-09, D-29)"
    - "As variáveis input.content@tags e input.content@sequentialId aparecem na biblioteca de variáveis do sistema para uso nas condições normais, sem UI de 'ramificar por etiqueta' (D-12)"
  artifacts:
    - path: "apps/gestao-vite/src/paginas/builder/modelo.ts"
      provides: "newSurveyBlock + saídas da pesquisa"
      contains: "newSurveyBlock"
  key_links:
    - from: "apps/gestao-vite/src/paginas/builder/menu-novo-bloco.tsx / painel-saidas.tsx"
      to: "newSurveyBlock"
      via: "ação de criar pesquisa a partir do bloco de atendimento"
      pattern: "newSurveyBlock\\("
    - from: "bloco de pesquisa serializado"
      to: "packages/core/src/fluxo/satisfaction-survey.ts"
      via: "marca serializada documentada em §1 do inventário"
      pattern: "satisfaction"
---

<objective>
Levar a pesquisa de satisfação nativa e a exposição das etiquetas de encerramento para o editor do Builder (BUILDER-03), sobre o motor entregue em 02-11.

Output: função pura `newSurveyBlock`, UI de criação/configuração, saídas, variáveis do sistema, testes.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fechar-o-builder/02-PATTERNS.md
@.planning/phases/02-fechar-o-builder/ref/inventario-satisfacao-e-tags.md
@.planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md
@.planning/phases/02-fechar-o-builder/02-11-SUMMARY.md

Pré-condição e nomes (D-05, STD-01): só executa depois que a Phase 1 aplicou o mapa (tag `std-apply-all-end`). Caminhos e símbolos EXISTENTES aparecem com o nome em português de 2026-09-26: localizar o nome vigente pela linha `status=approved` de `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/gestao-vite.csv` ou por `ref/REFERENCIA-CONGELADA.md` §Caminhos resolvidos; nunca escrever nome proposto não aplicado. Identificadores/títulos de teste NOVOS em inglês. Texto visível em português. Chaves `$…` do desenho não mudam (STD-06). `@pipe/gestao-vite` vira `@pipe/management-vite` após D-46.
Padrão: `SAIDAS_DE_ATENDIMENTO` (modelo.ts 129-133) + `novoBlocoDeAtendimento` (283-319) — bloco especial gerado por função pura com `$conditionOutputs` prontos. CSS só com tokens (`--p-e-*`, superfícies, `--p-builder-marca-*`). Itens `Bloqueado por C-NN` não são implementados (D-03).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: newSurveyBlock e saídas da pesquisa no modelo (D-06, D-09)</name>
  <files>apps/gestao-vite/src/paginas/builder/modelo.ts, apps/gestao-vite/tests/builder-editor.test.ts</files>
  <read_first>
    - apps/gestao-vite/src/paginas/builder/modelo.ts (SAIDAS_DE_ATENDIMENTO, novoBlocoDeAtendimento, arestasDe)
    - .planning/phases/02-fechar-o-builder/ref/inventario-satisfacao-e-tags.md (§1: estrutura serializada, campos, condições, Ramificação, timeout)
    - .planning/phases/02-fechar-o-builder/02-11-SUMMARY.md (marca serializada que o motor reconhece)
    - apps/gestao-vite/tests/builder-editor.test.ts (testes edges: de 02-06)
  </read_first>
  <behavior>
    - `newSurveyBlock(map, position, id)` devolve bloco com a estrutura serializada documentada (conteúdo da pergunta, input, `$conditionOutputs` das saídas documentadas, marca reconhecida por `satisfaction-survey.ts`).
    - Ligar a pesquisa ao bloco de atendimento grava o destino na saída de encerramento indicada pela referência.
    - `montarDesenho` → `lerDesenho` ida e volta preserva o bloco de pesquisa.
    - As arestas do bloco de pesquisa aparecem em `arestasDe` conforme a tabela D-29.2.
  </behavior>
  <action>
    Criar em `modelo.ts` a constante de saídas da pesquisa (nomes/condições exatamente do inventário §1) e `newSurveyBlock(map, position, id = gerarId())`, pura, análoga a `novoBlocoDeAtendimento`. Se o inventário mostrar que "Exibir apenas blocos de pesquisa de satisfação" altera a lógica da saída de encerramento, representar isso no campo serializado documentado; se é só filtro do seletor, exportar o predicado `isSurveyBlock(block)` para a Task 2. Testes com prefixo `survey:` em `builder-editor.test.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @pipe/gestao-vite test</automated>
  </verify>
  <acceptance_criteria>
    - `modelo.ts` (nome vigente) exporta `newSurveyBlock` e `isSurveyBlock`
    - ≥4 testes `survey:` passam, incluindo ida e volta e arestas
    - os testes `edges:` de 02-06 continuam passando
  </acceptance_criteria>
  <done>Bloco de pesquisa existe no modelo puro.</done>
</task>

<task type="auto">
  <name>Task 2: UI de criação/configuração da pesquisa e variáveis de ticket (D-08, D-12)</name>
  <files>apps/gestao-vite/src/paginas/builder/painel-saidas.tsx, apps/gestao-vite/src/paginas/builder/menu-novo-bloco.tsx, apps/gestao-vite/src/paginas/builder/no.tsx, apps/gestao-vite/src/paginas/builder/variaveis.ts, apps/gestao-vite/src/paginas/builder/painel-bloco.css</files>
  <read_first>
    - apps/gestao-vite/src/paginas/builder/painel-saidas.tsx (saídas de atendimento 83-153, DestinationPicker de 02-09)
    - apps/gestao-vite/src/paginas/builder/menu-novo-bloco.tsx
    - apps/gestao-vite/src/paginas/builder/no.tsx
    - apps/gestao-vite/src/paginas/builder/variaveis.ts (VARIAVEIS_DO_SISTEMA 27-36)
    - .planning/phases/02-fechar-o-builder/ref/inventario-satisfacao-e-tags.md (§1 UI e campos, §7 input.content)
    - .planning/phases/02-fechar-o-builder/ref/inventario-visual.md (ícones da pesquisa)
  </read_first>
  <action>
    Reproduzir o caminho de criação da referência: a ação no bloco de atendimento humano (ou no menu "Novo bloco", conforme §1) chama `newSurveyBlock` e liga a saída documentada. Em `painel-saidas.tsx`, a condição "Exibir apenas blocos de pesquisa de satisfação" (texto literal da referência) aplica `isSurveyBlock` como filtro do `DestinationPicker` ou o comportamento alternativo documentado; os campos de configuração do bloco (pergunta, comentário opcional, timeout e destino após timeout, se existirem) com rótulos e mensagens literais. Em `no.tsx`, visual do nó de pesquisa com ícone Pipe (D-33). Em `variaveis.ts`, acrescentar a `VARIAVEIS_DO_SISTEMA` as entradas `input.content@tags`, `input.content@sequentialId` e demais campos do ticket documentados em §7, com descrição em português indicando que valem depois do encerramento pelo atendente/inatividade (D-12). Nenhuma UI de "ramificar por etiqueta". CSS novo com prefixo de classe vigente do Builder + `survey`, só tokens.
  </action>
  <verify>
    <automated>pnpm --filter @pipe/gestao-vite test && pnpm --filter @pipe/gestao-vite typecheck && grep -q "input.content@tags" apps/gestao-vite/src/paginas/builder/variaveis.ts</automated>
  </verify>
  <acceptance_criteria>
    - `painel-saidas.tsx` contém o texto `Exibir apenas blocos de pesquisa de satisfação`
    - `variaveis.ts` contém `input.content@tags` e `input.content@sequentialId`
    - nenhuma string `ramificar por etiqueta` (case-insensitive) no diretório do Builder
    - CSS novo sem hex (`grep -n "#[0-9a-fA-F]\{3,6\}"` nas regras `survey` vazio)
    - test e typecheck saem 0
  </acceptance_criteria>
  <done>Atendente configura a pesquisa de satisfação e usa as etiquetas nas condições normais.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fluxo importado → editor | bloco de pesquisa vindo de JSON não confiável |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-16 | Tampering (XSS) | texto da pergunta/nó | medium | mitigate | renderização como texto React; sem `dangerouslySetInnerHTML` |
| T-2-05 | Tampering | configuração da pesquisa | medium | mitigate | publicação passa pela validação do motor (409 com erros), coberta em 02-11 |
</threat_model>

<verification>
Testes do app verdes com `survey:` e `edges:`.
</verification>

<success_criteria>
Critério 3 do roadmap (pesquisa de satisfação + paleta de tags nas saídas de atendimento) completo com 02-08 e 02-11.
</success_criteria>

<output>
Create `.planning/phases/02-fechar-o-builder/02-12-SUMMARY.md` when done
</output>
