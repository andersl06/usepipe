---
phase: 02-fechar-o-builder
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-fechar-o-builder/ref/inventario-satisfacao-e-tags.md
autonomous: true
requirements: [BUILDER-03]

must_haves:
  truths:
    - "A pesquisa de satisfação nativa (escala 1-5, bloco dedicado ligado ao BAH 3.0) está documentada de ponta a ponta: Builder, Atendimento, Relatórios/Analytics, API e só então a proposta de persistência (D-06, D-07, D-08)"
    - "As saídas do bloco de pesquisa vêm do comportamento documentado do Builder, não das categorias 1-2/3/4-5 do relatório (D-09)"
    - "A proposta de schema não copia as limitações da Blip (3 meses, só PT-BR) (D-10)"
    - "Os dois sistemas de tag estão documentados: $tags do bloco (cores e sugestões, azul→verde) e etiquetas de encerramento disponíveis via input.content por tipo de encerramento (D-11, D-12, D-13)"
  artifacts:
    - path: ".planning/phases/02-fechar-o-builder/ref/inventario-satisfacao-e-tags.md"
      provides: "Investigação D-08.1..5 e D-11/D-12"
      contains: "## 5. Proposta de persistência (D-08.5)"
  key_links:
    - from: ".planning/phases/02-fechar-o-builder/ref/inventario-satisfacao-e-tags.md"
      to: "02-11 (backend de satisfação) e 02-12 (UI)"
      via: "portão 02-07"
      pattern: "Proposta de persistência"
---

<objective>
Investigar a pesquisa de satisfação nativa e os dois sistemas de tags da referência, entregando a base para BUILDER-03 sem decidir persistência/ramificação antes da evidência (D-08, D-09).

Output: `.planning/phases/02-fechar-o-builder/ref/inventario-satisfacao-e-tags.md`.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fechar-o-builder/02-CONTEXT.md

Fontes: `referencias-blip/pesquisa/blip-gestao-funcoes.md` §10, `referencias-blip/pesquisa/blip-desk-funcoes.md` §4, `referencias-blip/pesquisa/blip-gestao-regras-tecnicas.md` (:219 `AttendanceSatisfactionSurveyEnabled`, :1427 `desk-surveyDashboard`), `referencias-blip/pesquisa/blip-portal-telas.md` §58-64 (fluxo AUVP Capital), `referencias-blip/builder/builder/PAINEL-Atendimento.md`, `PAINEL-Saidas.md`, `PROJECT-HANDOFF.md` ("Próximas frentes" item 6). Help Center: `https://help.blip.ai/hc/en-us/articles/21974005289623`, `.../5423911157911`, `.../5320362057751`, `.../4474433590679`. Captura ao vivo só pelo dono (D-03).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pesquisa de satisfação nativa ponta a ponta (D-06..D-10)</name>
  <files>.planning/phases/02-fechar-o-builder/ref/inventario-satisfacao-e-tags.md</files>
  <read_first>
    - .planning/phases/02-fechar-o-builder/02-CONTEXT.md (D-06..D-10)
    - referencias-blip/pesquisa/blip-gestao-funcoes.md (§10)
    - referencias-blip/pesquisa/blip-gestao-regras-tecnicas.md (linhas ~200-240 e ~1410-1440)
    - referencias-blip/builder/builder/PAINEL-Atendimento.md
    - apps/gestao-vite/src/paginas/builder/modelo.ts (linhas 129-133 SAIDAS_DE_ATENDIMENTO, 283-319 novoBlocoDeAtendimento)
    - packages/db/src/schema/conversas.ts (tabela conversa, evento_atendimento, etiqueta linhas 486-502)
  </read_first>
  <action>
    Criar o arquivo com `**Data da investigação:** AAAA-MM-DD` e as seções numeradas exatamente como D-08:
    `## 1. Builder (D-08.1)`: criação do bloco de pesquisa a partir do bloco de atendimento humano; todos os campos do bloco e das condições de saída; se nota e comentário viram variáveis (nomes exatos); comportamento para `ClosedAttendant`, `ClosedClient`, `ClosedClientInactivity` e se cada um tem destino próprio; se "Exibir apenas blocos de pesquisa de satisfação" é filtro do seletor de destino ou muda a lógica da saída; estrutura serializada do bloco nativo (`$contentActions`, `$conditionOutputs`, input); forma de ramificação (nota 1-5, classificação, conclusão/timeout, outra); só nota vs nota+comentário vs sem resposta; timeout e destino após timeout.
    `## 2. Atendimento (D-08.2)`: associação a ticket, contato, atendente, fila; resposta completa, só nota, sem resposta, abandono.
    `## 3. Relatórios/Analytics (D-08.3)`: métricas, filtros, agrupamentos, tabela detalhada (ID do ticket, data, fila, atendente, ID do cliente, nota, avaliação, comentário), busca de comentários, exportação. Só documentar: a tela é fase futura.
    `## 4. API (D-08.4)`: endpoints de consulta de respostas/relatórios da referência (request, response, campos, filtros, paginação, identificadores) com fonte; sem documentação pública, apontar a captura de Network necessária.
    `## 5. Proposta de persistência (D-08.5)`: SÓ depois de 1-4, propor o modelo do Pipe (tabela(s), colunas, índices, retenção sem limite de 3 meses, sem restrição de idioma — D-10) capaz de reproduzir o comportamento e as consultas de 3/4, e o endpoint de consulta do Pipe. Incluir a linha `Ramificação: <forma documentada em 1>` com a fonte; é proibido derivar a ramificação das categorias do relatório (D-09).
    Desconhecidos → `PENDENTE-CAPTURA #N` em `## Capturas pendentes (D-03)`.
  </action>
  <verify>
    <automated>F=.planning/phases/02-fechar-o-builder/ref/inventario-satisfacao-e-tags.md; for s in "## 1. Builder (D-08.1)" "## 2. Atendimento (D-08.2)" "## 3. Relatórios/Analytics (D-08.3)" "## 4. API (D-08.4)" "## 5. Proposta de persistência (D-08.5)"; do grep -qF "$s" $F || echo "FALTA $s"; done; grep -q "^Ramificação:" $F && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - As 5 seções existem na ordem 1→5 (o verify não imprime FALTA)
    - A seção 1 menciona `ClosedAttendant`, `ClosedClient` e `ClosedClientInactivity`
    - A seção 5 contém `Ramificação:` com fonte e não contém a palavra `3 meses` como regra de retenção do Pipe
  </acceptance_criteria>
  <done>Pesquisa nativa documentada e proposta de persistência pronta para o portão.</done>
</task>

<task type="auto">
  <name>Task 2: Tags do bloco e etiquetas de encerramento (D-11, D-12, D-13)</name>
  <files>.planning/phases/02-fechar-o-builder/ref/inventario-satisfacao-e-tags.md</files>
  <read_first>
    - .planning/phases/02-fechar-o-builder/ref/inventario-satisfacao-e-tags.md (saída da Task 1)
    - referencias-blip/pesquisa/blip-desk-funcoes.md (§4: encerramento, tags, input.content)
    - apps/gestao-vite/src/paginas/builder/etiquetas-do-bloco.ts (paleta atual e troca azul→#4a5d23)
    - apps/gestao-vite/src/paginas/builder/painel.tsx (linhas 30-51: edição de $tags)
    - apps/api/src/dominio/fluxo.ts (função ultimoAtendimento, ~linha 935: o Ticket que o motor recebe hoje, sem tags nem sequentialId)
    - packages/contracts/src/encerramento.ts (EncerrarConversaInput.etiqueta_ids)
  </read_first>
  <action>
    Acrescentar `## 6. $tags do bloco (D-11a, D-13)`: paleta de cores completa da referência (nome da cor e hex medidos, papel), sugestões de rótulo automáticas, UI de adição/remoção; para cada cor azul, a linha `azul → token --p-builder-marca-<papel>` conforme D-13/D-32.
    Acrescentar `## 7. Etiquetas de encerramento no fluxo (D-11b, D-12)`: campos do ticket expostos em `input.content` depois do encerramento (`input.content@tags`, `input.content@sequentialId` e demais, com formato: array de nomes? objetos?), diferenças por tipo de encerramento (atendente e inatividade expõem; cliente não gera o mesmo `input.content`), o mecanismo da referência para recuperar os dados no encerramento pelo cliente (command/API — nome do comando/URI, payload) e se versões recentes do Builder mudaram isso. Declarar explicitamente: sem ramificação especial por etiqueta e sem UI nova de "ramificar por etiqueta" (D-12).
    Acrescentar `## 8. Mapa para o Pipe`: o que `ultimoAtendimento` precisa passar a incluir (nomes de campos), de qual tabela (`etiqueta`, `conversa_etiqueta`, sequencial do ticket), e `## Resumo` com tabela `| Item | Classificação proposta | Plano alvo | Bloqueado por captura |` (Plano alvo ∈ {02-08, 02-11, 02-12}).
  </action>
  <verify>
    <automated>F=.planning/phases/02-fechar-o-builder/ref/inventario-satisfacao-e-tags.md; grep -q "## 6. \$tags do bloco (D-11a, D-13)" $F && grep -q "## 7. Etiquetas de encerramento no fluxo (D-11b, D-12)" $F && grep -q "input.content@tags" $F && grep -q "## Capturas pendentes (D-03)" $F && test $(grep -ciE "authorization:|cookie:|bearer " $F) -eq 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Seção 6 contém ao menos uma linha `azul → token --p-builder-marca-`
    - Seção 7 contém `input.content@tags`, `input.content@sequentialId` e trata os três encerramentos
    - Seção 8 cita `ultimoAtendimento` e a tabela `etiqueta`
    - `## Resumo` só usa planos alvo 02-08, 02-11, 02-12
  </acceptance_criteria>
  <done>Os dois sistemas de tag documentados com o mapa de implementação.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capturas da conta Blip do dono → repositório | pode conter dados de clientes reais (comentários de pesquisa) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Information disclosure | ref/inventario-satisfacao-e-tags.md | high | mitigate | gate grep de credenciais = 0; exemplos de resposta de pesquisa usam dados fictícios, nunca nome/telefone/comentário real do fluxo AUVP Capital |
| T-2-10 | Information disclosure (PII) | proposta D-08.5 | high | mitigate | a proposta de schema lista quais colunas são PII (comentário, ID do cliente) e exige tenant_id + RLS `tenant_isolado`; aplicado em 02-11 |
</threat_model>

<verification>
Seções 1-8, Resumo e Capturas pendentes presentes; gate de credenciais zero.
</verification>

<success_criteria>
BUILDER-03 tem evidência completa para decidir schema, ramificação e exposição de etiquetas no portão.
</success_criteria>

<output>
Create `.planning/phases/02-fechar-o-builder/02-03-SUMMARY.md` when done
</output>
