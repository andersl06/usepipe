---
phase: 02-fechar-o-builder
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-fechar-o-builder/ref/inventario-paineis-e-setas.md
  - .planning/phases/02-fechar-o-builder/ref/inventario-visual.md
autonomous: true
requirements: [BUILDER-02, BUILDER-04, BUILDER-05]

must_haves:
  truths:
    - "O mecanismo real do painel de Teste da referência está documentado (UI, usuário Tester, variáveis de teste, reset de contato, Debug) com recomendação simulação local vs canal real, ou marcado como captura pendente (D-14, D-03)"
    - "O painel de Filas da referência está documentado antes de escolher entre atalho e CRUD embutido (D-15)"
    - "A listagem e exportação de versões publicadas da referência está documentada; o formato do Pipe continua {flow, globalActions} (D-16)"
    - "O menu de contexto de copiar/colar da referência está documentado para conferência de paridade (D-17)"
    - "O seletor de destino de ligação da referência está documentado (forma, busca, estados) (D-23)"
    - "Todas as relações que geram seta no canvas da referência estão listadas (D-29)"
    - "Cada azul da referência no Builder está mapeado a um papel visual e a um token --p-builder-marca-* (D-30, D-31, D-32), e os ícones do Pipe necessários (incluindo o equivalente de user-engaged) estão listados por significado (D-33)"
  artifacts:
    - path: ".planning/phases/02-fechar-o-builder/ref/inventario-paineis-e-setas.md"
      provides: "D-14, D-15, D-16, D-17, D-23, D-29.2"
      contains: "## Relações que geram seta (D-29.2)"
    - path: ".planning/phases/02-fechar-o-builder/ref/inventario-visual.md"
      provides: "Mapa azul→papel→token (D-32) e lista de ícones próprios (D-33)"
      contains: "| Seletor/elemento | Azul da referência | Papel visual | Token Pipe |"
  key_links:
    - from: ".planning/phases/02-fechar-o-builder/ref/inventario-visual.md"
      to: "02-08 (tokens e migração de CSS)"
      via: "tabela azul→token"
      pattern: "--p-builder-marca-"
---

<objective>
Investigar os painéis (Teste, Filas, Versões), o copiar/colar, o seletor de destino, as relações que geram seta no canvas e o mapa visual azul→verde da referência.

Output: `ref/inventario-paineis-e-setas.md` e `ref/inventario-visual.md` no diretório da fase.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fechar-o-builder/02-CONTEXT.md
@.planning/phases/02-fechar-o-builder/02-UI-SPEC.md

Fontes: `referencias-blip/builder/builder/PAINEIS.md`, `PAINEL-TestePublicar.md`, `PAINEL-Configuracoes.md`, `PAINEL-Historico.md`, `PAINEL-ImportExport.md`, `PAINEL-Saidas.md` (linha 53: saída padrão sem seta), `PAINEL-Builder.md`, `referencias-blip/builder/builder-fluxo__pagina.html`, `referencias-blip/builder/zip19/supernova.blip.ai` (CSS para os azuis), `referencias-blip/pesquisa/blip-portal-telas.md` §58-64. Builder ao vivo só pelo dono (D-03). Nada de ícone/CSS da Blip no repositório (D-33): registrar hex medido e papel, não regras CSS copiadas.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Painéis, copiar/colar, seletor de destino e setas</name>
  <files>.planning/phases/02-fechar-o-builder/ref/inventario-paineis-e-setas.md</files>
  <read_first>
    - .planning/phases/02-fechar-o-builder/02-CONTEXT.md (D-14..D-17, D-23, D-29)
    - referencias-blip/builder/builder/PAINEL-TestePublicar.md
    - referencias-blip/builder/builder/PAINEL-Configuracoes.md
    - referencias-blip/builder/builder/PAINEL-Saidas.md
    - apps/gestao-vite/src/paginas/builder/painel-filas.tsx (atalho atual, comentário 6-10)
    - apps/gestao-vite/src/paginas/builder/painel-configuracao.tsx (AbaDeVersoes)
    - apps/gestao-vite/src/paginas/builder/painel-saidas.tsx (seletorDeDestino linhas 57-73)
    - apps/gestao-vite/src/paginas/builder/modelo.ts (arestasDe ~linha 421, copiar/colar 342-388)
  </read_first>
  <action>
    Criar `ref/inventario-paineis-e-setas.md` com `**Data da investigação:** AAAA-MM-DD` e as seções:
    `## Painel de Teste (D-14)`: como abre, layout, usuário "Tester", variáveis de teste, reset de contato, o que o Debug mostra (bloco atual, variáveis, ações executadas, erros), se usa bot real via canal BlipChat; terminar com `Recomendação: simulação local | canal de teste real` justificada pelo comportamento observado (o Debug precisa mostrar o que o motor real faz). Sem evidência suficiente, a recomendação é `BLOQUEADO — captura pendente` (D-03).
    `## Painel de Filas (D-15)`: o que a referência mostra (CRUD embutido, campos, validações) e `Recomendação: atalho | CRUD embutido reaproveitando PaginaFilas`.
    `## Versões (D-16)`: lista de versões publicadas (colunas, ordenação, ações por linha), exportação de uma versão antiga (nome do arquivo, formato), restauração; confirmar que o formato é o mesmo `{flow, globalActions}` do rascunho.
    `## Copiar/colar (D-17)`: itens e ordem do menu de contexto, atalhos de teclado, limite de colagem, mensagens.
    `## Seletor de destino (D-23)`: forma (select, combobox, modal), busca (por título, id?), estado vazio, destino inexistente, estado ativo.
    `## Relações que geram seta (D-29.2)`: tabela `| Relação | Campo serializado | Gera seta na referência | Fonte |` cobrindo condições de saída, saídas do atendimento humano, `$defaultOutput` (sem seta, PAINEL-Saidas.md:53), `$isDeskDefaultOutput`, `Redirect`, ações com destino, e qualquer outro mecanismo encontrado.
    Desconhecidos → `PENDENTE-CAPTURA #N` em `## Capturas pendentes (D-03)`. Incluir nas capturas pendentes o pedido ao dono de export real do fluxo AUVP Capital + print do canvas da referência para a validação D-29.3 (plano 02-09).
  </action>
  <verify>
    <automated>F=.planning/phases/02-fechar-o-builder/ref/inventario-paineis-e-setas.md; for s in "## Painel de Teste (D-14)" "## Painel de Filas (D-15)" "## Versões (D-16)" "## Copiar/colar (D-17)" "## Seletor de destino (D-23)" "## Relações que geram seta (D-29.2)" "## Capturas pendentes (D-03)"; do grep -qF "$s" $F || echo "FALTA $s"; done; grep -q "AUVP Capital" $F && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - As 7 seções existem (verify sem FALTA)
    - As seções D-14 e D-15 terminam cada uma com uma linha iniciada por `Recomendação:`
    - A tabela de setas tem linhas para `$conditionOutputs`, `$defaultOutput`, `$isDeskDefaultOutput` e `Redirect`
    - Capturas pendentes contém o pedido do export do fluxo AUVP Capital
  </acceptance_criteria>
  <done>Painéis, seletor e setas da referência documentados.</done>
</task>

<task type="auto">
  <name>Task 2: Mapa visual azul→verde por papel e ícones próprios</name>
  <files>.planning/phases/02-fechar-o-builder/ref/inventario-visual.md</files>
  <read_first>
    - .planning/phases/02-fechar-o-builder/02-UI-SPEC.md (seção Color: lista fechada de tokens --p-builder-marca-*)
    - packages/ui/src/estilos/tokens.css (tokens escuros existentes, --p-marca escuro #a3b76a)
    - apps/gestao-vite/src/paginas/builder/editor.css (cabeçalho com medidas do DOM, ocorrências de #4a5d23)
    - apps/gestao-vite/src/paginas/builder/painel-bloco.css (--bl-verde)
    - packages/ui/src/icones.tsx (ícones existentes do Pipe)
  </read_first>
  <action>
    Criar `ref/inventario-visual.md` com `**Data da investigação:** AAAA-MM-DD`.
    `## Azuis da referência (D-30, D-32)`: tabela `| Seletor/elemento | Azul da referência | Papel visual | Token Pipe |` obtida varrendo o CSS de zip19 por hex/rgb azuis (`#3f7de8`, `#0096fa`, `#1e6bf1`, `#498bff` e qualquer outro de matiz azul) e pelos estados hover/active/focus/selected/glow/shadow/overlay/gradient. Papel visual e Token Pipe só podem ser: `--p-builder-marca`, `--p-builder-marca-hover`, `--p-builder-marca-ativo`, `--p-builder-marca-anel`, `--p-builder-marca-borda-ativa`, `--p-builder-marca-selecionado`, `--p-builder-marca-destaque`, `--p-builder-marca-brilho`, `--p-builder-marca-sombra`, `--p-builder-marca-sobreposicao`, `--p-builder-marca-gradiente`. Registrar opacidade/blur/spread de cada efeito para preservar intensidade (D-32).
    `## Tema escuro (D-31)`: superfícies escuras medidas (fundo do canvas, nó, painel) e confirmação de que não há tema claro.
    `## Ícones próprios necessários (D-33)`: tabela `| Significado | Onde aparece | Existe em icones.tsx? | Nome proposto |`, incluindo o equivalente de `user-engaged` (BUILDER-04) e ícones de cada tipo de conteúdo/ação novos. Descrever o significado, nunca o desenho da Blip.
    `## Pendências de medida`: telas cujo azul só é confirmável ao vivo → `PENDENTE-CAPTURA #N`.
  </action>
  <verify>
    <automated>F=.planning/phases/02-fechar-o-builder/ref/inventario-visual.md; grep -qF "| Seletor/elemento | Azul da referência | Papel visual | Token Pipe |" $F && grep -q "user-engaged" $F && grep -q "## Tema escuro (D-31)" $F && test $(grep -ciE "<svg|<path" $F) -eq 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Toda linha da tabela de azuis tem na coluna Token Pipe um dos 11 tokens listados
    - A tabela de ícones tem linha com `user-engaged`
    - `grep -ciE "<svg|<path"` no arquivo retorna 0
  </acceptance_criteria>
  <done>Mapa visual pronto para 02-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundles/DOM da Blip → repositório | risco de colar código/ícone e credenciais |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Information disclosure | ref/inventario-paineis-e-setas.md | high | mitigate | proibido transcrever cookie/token; o export AUVP Capital fica fora do git (referencias-blip/) e só contagens de setas entram no repositório |
| T-2-02 | Tampering (licença) | ref/inventario-visual.md | medium | mitigate | gate grep `<svg|<path` = 0; só hex e papel |
</threat_model>

<verification>
Os dois arquivos existem com as seções exigidas; gates de grep passam.
</verification>

<success_criteria>
Painéis, seletor, setas e visual da referência documentados com recomendações para o portão.
</success_criteria>

<output>
Create `.planning/phases/02-fechar-o-builder/02-04-SUMMARY.md` when done
</output>
