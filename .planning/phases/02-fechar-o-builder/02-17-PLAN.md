---
phase: 02-fechar-o-builder
plan: 17
type: execute
wave: 8
depends_on: ["02-07", "02-16"]
files_modified:
  - packages/db/src/schema/automacao.ts
  - packages/db/drizzle/
  - packages/contracts/src/flow-functions.ts
  - packages/contracts/src/index.ts
  - apps/api/src/dominio/gestao/flow-functions.ts
  - apps/api/src/controladores/flow-functions.ts
  - apps/api/src/app.modulo.ts
  - apps/api/src/dominio/fluxo.ts
  - packages/core/src/fluxo/acoes.ts
  - packages/core/src/fluxo/acoes.teste.ts
  - apps/api/tests/flow-functions.test.ts
autonomous: true
requirements: [BUILDER-02, BUILDER-01]

must_haves:
  truths:
    - "A biblioteca de funções do motor de conversa existe com criação, persistência, edição/versionamento, parâmetros, retorno e escopo iguais aos documentados da referência (D-22)"
    - "Ela é nova e própria do motor de conversa; a tabela `funcao` do motor de workflow não é reaproveitada (D-22)"
    - "Um fluxo publicado chama uma função da biblioteca pela forma documentada e a função roda no mesmo sandbox aprovado dos scripts (D-21, D-22)"
    - "CRUD da biblioteca exige sessão e permissão e é isolado por tenant (D-22)"
  artifacts:
    - path: "packages/contracts/src/flow-functions.ts"
      provides: "FlowFunction, FlowFunctionInput, FlowFunctionVersion"
    - path: "apps/api/src/controladores/flow-functions.ts"
      provides: "CRUD REST da biblioteca"
  key_links:
    - from: "apps/api/src/dominio/fluxo.ts"
      to: "runFlowScript com funções da biblioteca"
      via: "carregamento das funções do escopo do fluxo"
      pattern: "flowFunction|FlowFunction"
---

<objective>
Construir o lado servidor da biblioteca de funções do contrato (BUILDER-02, D-22): persistência, API, e execução no motor sobre o sandbox de 02-16. A UI e a busca de função vêm em 02-18.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fechar-o-builder/ref/inventario-acoes.md
@.planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md
@.planning/phases/02-fechar-o-builder/02-16-SUMMARY.md

Fonte: `## Biblioteca de funções (D-22)` do inventário e linha `APROVADO:` de D-22 no portão (escopo: por fluxo, por bot ou por tenant; versionamento; forma de chamada). Itens bloqueados por captura ficam fora (D-03).

Pré-condição e nomes (D-05, STD-01): só executa depois que a Phase 1 aplicou o mapa (tag `std-apply-all-end`). Caminhos/símbolos EXISTENTES com nome em português de 2026-09-26; localizar o nome vigente pela linha `status=approved` de `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv` ou por `ref/REFERENCIA-CONGELADA.md` §Caminhos resolvidos. Identificadores, arquivos e rotas NOVOS em inglês (`v1/<plural-resource>` kebab-case, sob o mesmo prefixo de gestão vigente das rotas de fluxo). Nomes SQL novos em snake_case português (proposta: tabela `funcao_do_fluxo`, colunas `nome`, `parametros`, `codigo`, `versao`, `escopo`); a tabela `funcao` do motor de workflow (`packages/db/src/schema/automacao.ts:443-454`) não é tocada.
Padrões: tabela `etiqueta` (`refTenant`, `carimbos`, `uniqueIndex`), RLS `tenant_isolado` (`packages/db/drizzle/0001_rls.sql`), controlador com `@ComSessao()` (`gestao-builder.ts`), permissões `automacao.fluxo.editar` (catálogo em `packages/db/src/semente.ts`) ou `exigirPermissaoNoFluxo(..., 'builder.escrever')` quando o escopo aprovado for por fluxo.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema, migration com RLS e contratos (D-22)</name>
  <files>packages/db/src/schema/automacao.ts, packages/db/drizzle/, packages/contracts/src/flow-functions.ts, packages/contracts/src/index.ts</files>
  <read_first>
    - .planning/phases/02-fechar-o-builder/ref/inventario-acoes.md (## Biblioteca de funções (D-22))
    - .planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md (APROVADO: D-22)
    - packages/db/src/schema/automacao.ts (tabela fluxo/bloco do motor de conversa; `funcao` do workflow 443-454 para NÃO reaproveitar)
    - packages/db/drizzle/0001_rls.sql (política tenant_isolado)
  </read_first>
  <action>
    Criar a tabela de funções do motor de conversa (export Drizzle `flowFunction`, SQL `funcao_do_fluxo`) com `tenant_id`, chave de escopo aprovada (`fluxo_id` nullable se o escopo for tenant), `nome` (único por escopo), `descricao`, `parametros` (jsonb: lista de nomes na ordem), `codigo` (text), `versao` (int) e `carimbos()`; se o portão aprovou versionamento, tabela filha `funcao_do_fluxo_versao` imutável por versão. Limite de tamanho de `codigo` por check (valor do inventário ou 64 KB). Migration gerada + custom com RLS `tenant_isolado`. Contratos `FlowFunction`, `FlowFunctionInput`, `FlowFunctionVersion` exportados em `index.ts`.
  </action>
  <verify>
    <automated>pnpm banco:subir && pnpm banco:migrar && pnpm --filter @pipe/db typecheck && pnpm --filter @pipe/contracts typecheck</automated>
  </verify>
  <acceptance_criteria>
    - migration nova com `ENABLE ROW LEVEL SECURITY` e `tenant_isolado` para a(s) tabela(s) nova(s)
    - `git diff` não altera a definição da tabela `funcao` do workflow
    - contratos exportados; verify sai 0
  </acceptance_criteria>
  <done>Biblioteca persistida com isolamento.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: API CRUD e execução no motor (D-22)</name>
  <files>apps/api/src/dominio/gestao/flow-functions.ts, apps/api/src/controladores/flow-functions.ts, apps/api/src/app.modulo.ts, apps/api/src/dominio/fluxo.ts, packages/core/src/fluxo/acoes.ts, packages/core/src/fluxo/acoes.teste.ts, apps/api/tests/flow-functions.test.ts</files>
  <read_first>
    - apps/api/src/controladores/gestao-builder.ts (casca de controlador)
    - apps/api/src/dominio/gestao/builder-do-fluxo.ts (fluxoDoBuilder, padrões de erro ErroPipe)
    - apps/api/src/dominio/script-sandbox.ts (runFlowScript de 02-16)
    - apps/api/src/dominio/fluxo.ts (servicos.runScript)
    - packages/core/src/fluxo/acoes.ts (ExecuteScriptV2 de 02-16)
    - .planning/phases/02-fechar-o-builder/ref/inventario-acoes.md (forma de chamada da função pelo Builder)
  </read_first>
  <behavior>
    - POST cria função (nome válido, parâmetros, código ≤ limite); nome duplicado no escopo → 409.
    - GET lista com busca por nome/descrição (sem acento/caixa) e paginação; GET por id; PUT edita (nova versão se versionado); DELETE conforme referência.
    - Sem sessão 401; sem permissão 403; função de outro tenant 404.
    - Fluxo publicado que chama a função pela forma documentada executa o código no sandbox com os parâmetros e grava o retorno.
    - Função inexistente no momento da execução → comportamento documentado (erro da ação).
  </behavior>
  <action>
    Domínio `flow-functions.ts` com `listFlowFunctions`, `getFlowFunction`, `createFlowFunction`, `updateFlowFunction`, `deleteFlowFunction`, todos com a checagem de permissão do escopo aprovado dentro de `noTenant`, validação de nome (regex de identificador JS), de parâmetros e de tamanho. Controlador `FlowFunctionsController` registrado em `app.modulo.ts`. No motor: a forma de chamada documentada (ex.: nome da função disponível dentro de `ExecuteScriptV2`, ou ação própria) resolve as funções do escopo via serviço `loadFlowFunctions` implementado em `fluxo.ts` e injeta no `runFlowScript` como código adicional do mesmo contexto isolado. Testes de API cobrindo o behavior e testes de ação em `acoes.teste.ts`.
  </action>
  <verify>
    <automated>pnpm banco:subir && pnpm --filter @pipe/api exec vitest run tests/flow-functions.test.ts tests/script-sandbox.test.ts && pnpm --filter @pipe/core exec vitest run src/fluxo && pnpm --filter @pipe/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - controlador com `@ComSessao()`; domínio com `exigirPermissao(` ou `exigirPermissaoNoFluxo(`
    - `flow-functions.test.ts` cobre 401, 403, 409, outro tenant e execução em fluxo publicado
    - verify sai 0
  </acceptance_criteria>
  <done>Funções da biblioteca criadas por API e executadas pelo motor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → API da biblioteca | código de função não confiável |
| código armazenado → sandbox | execução de código do tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-12 | Elevation of privilege | código de função executado | high | mitigate | Task 2 executa só via `runFlowScript` (sandbox de 02-16, testes de fuga); nunca `eval` no host |
| T-2-09 | Elevation of privilege | CRUD da biblioteca | high | mitigate | Task 2 `@ComSessao()` + permissão do escopo; testes 401/403 |
| T-2-29 | Information disclosure | função de outro tenant | high | mitigate | Task 1 RLS `tenant_isolado`; Task 2 teste de outro tenant 404 |
| T-2-28 | Denial of service | código enorme | medium | mitigate | check de tamanho no banco + validação na API |
</threat_model>

<verification>
Testes de api e core verdes; migração aplicada com RLS.
</verification>

<success_criteria>
Metade servidor da "função da biblioteca de contrato" de BUILDER-02 entregue.
</success_criteria>

<output>
Create `.planning/phases/02-fechar-o-builder/02-17-SUMMARY.md` when done
</output>
