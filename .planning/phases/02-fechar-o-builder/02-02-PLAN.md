---
phase: 02-fechar-o-builder
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-fechar-o-builder/ref/inventario-acoes.md
autonomous: true
requirements: [BUILDER-01, BUILDER-02]

must_haves:
  truths:
    - "Existe um inventário datado de todas as ações do Builder de referência, não limitado às 19 do SDK, com as dimensões de D-19 por ação (D-19)"
    - "Cada ação que depende de serviço da plataforma Blip tem as duas opções descritas (equivalente nativo no Pipe vs dependência externa) para decisão no portão (D-20)"
    - "O comportamento de ExecuteScript e ExecuteScriptV2 está documentado (ECMAScript, limites, APIs expostas, contexto, HTTP, erros, timezone, retorno) com uma recomendação de mecanismo de sandbox para o portão (D-21)"
    - "A biblioteca de funções da referência está documentada como funcionalidade do motor de conversa, sem presumir reuso de `funcao` do motor de workflow (D-22)"
  artifacts:
    - path: ".planning/phases/02-fechar-o-builder/ref/inventario-acoes.md"
      provides: "Inventário de ações, scripts (D-21) e biblioteca de funções (D-22)"
      contains: "## Biblioteca de funções (D-22)"
  key_links:
    - from: ".planning/phases/02-fechar-o-builder/ref/inventario-acoes.md"
      to: "02-07 (portão do dono)"
      via: "tabela-resumo com 'Classificação proposta' e 'Slot proposto'"
      pattern: "Slot proposto"
---

<objective>
Investigar o catálogo de AÇÕES do Builder de referência, o comportamento de scripts e a biblioteca de funções, produzindo inventário datado para o congelamento (D-02) e para o portão do dono (D-04).

Purpose: BUILDER-01 (todas as ações) e BUILDER-02 (biblioteca de funções) dependem desse inventário; D-19/D-21/D-22 proíbem decidir antes.
Output: `.planning/phases/02-fechar-o-builder/ref/inventario-acoes.md`.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fechar-o-builder/02-CONTEXT.md
@.planning/phases/02-fechar-o-builder/02-RESEARCH.md

Fontes: `referencias-blip/pesquisa/catalogo-gatilhos-acoes.md` (§0 dois motores, §2.2 19 actions do SDK), `referencias-blip/builder/zip19/supernova.blip.ai`, `referencias-blip/builder/builder/PAINEL-*.md`, `docs/builder-cards-pendencias.md`, repositório público `https://github.com/takenet/blip-sdk-csharp` (pasta `src/Take.Blip.Builder/Actions`), Help Center/docs públicos. Nada de código da Blip copiado para o repositório (D-33): descrever comportamento, não colar fonte. Builder ao vivo exige login do dono → capturas pendentes (D-03).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventário ação por ação (D-19, D-20)</name>
  <files>.planning/phases/02-fechar-o-builder/ref/inventario-acoes.md</files>
  <read_first>
    - .planning/phases/02-fechar-o-builder/02-CONTEXT.md (D-19, D-20, D-24)
    - referencias-blip/pesquisa/catalogo-gatilhos-acoes.md (§0, §2.2)
    - packages/core/src/fluxo/acoes.ts (ACOES_DO_MOTOR, linha ~233)
    - apps/gestao-vite/src/paginas/builder/acoes-do-bloco.ts (CATALOGO_DE_ACOES linhas 57-126, ACOES_DO_SISTEMA linha ~151, comentário 21-27)
    - docs/builder-cards-pendencias.md
  </read_first>
  <action>
    Criar `.planning/phases/02-fechar-o-builder/ref/inventario-acoes.md` com `**Data da investigação:** AAAA-MM-DD`. Uma seção `### <TipoSerializado>` por ação, cobrindo no mínimo: ExecuteScript, ExecuteScriptV2, SendMessage, SendMessageFromHttp, SendRawMessage, SendCommand, ProcessCommand, TrackEvent, ProcessHttp, ManageList, MergeContact, SetVariable, SetBucket, Redirect, CreateTicket, DeleteVariable, ProcessContentAssistant, TrackContactsJourney, ExecuteTemplate, ExecuteBlipFunction e toda ação ligada a agentes/IA encontrada nos bundles (grep em zip19 por `"type":"` perto de `settings`, `ActionType`, `actions.`). Para ExecuteBlipFunction e IA, declarar se pertencem ao Builder copiado ou a outra superfície, com fonte.
    Por ação, os rótulos exatos: `Nome exibido`, `Ícone (significado)`, `Entrada/Saída/Global`, `Campos do editor`, `Valores padrão`, `Validações`, `Condições de execução`, `Variáveis de entrada/saída`, `Formato serializado` (JSON de `settings`), `Comportamento no motor`, `Dependência de serviço da plataforma`, `Erros/timeouts/retries`, `Teste/Debug`, `Import/export`, `Dependência ausente/não configurada`, `Estado no Pipe hoje` (motor sim/não em acoes.ts; tela sim/não em acoes-do-bloco.ts), `Fonte`.
    Para cada ação com `Dependência de serviço da plataforma` não vazia (SetBucket, ManageList, SendCommand, ProcessCommand, ProcessContentAssistant, TrackContactsJourney e outras), subseção `Opções para o portão (D-20)` com (a) equivalente nativo no Pipe: tabela/serviço que precisaria existir, e (b) dependência externa: o que o importador marca como "Não executada no Pipe".
    Desconhecidos viram `PENDENTE-CAPTURA #N` listados em `## Capturas pendentes (D-03)`. Proibido transcrever tokens/cookies (T-2-01).
  </action>
  <verify>
    <automated>F=.planning/phases/02-fechar-o-builder/ref/inventario-acoes.md; for a in ExecuteScript ExecuteScriptV2 SendMessageFromHttp SendCommand ProcessCommand ManageList MergeContact SetBucket ProcessContentAssistant TrackContactsJourney ExecuteTemplate ExecuteBlipFunction; do grep -q "^### $a" $F || echo "FALTA $a"; done; test $(grep -ciE "authorization:|cookie:|bearer " $F) -eq 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Existe `### <Tipo>` para cada uma das 20 ações nomeadas na action (o loop do verify não imprime `FALTA`)
    - Cada seção contém `Formato serializado`, `Dependência de serviço da plataforma` e `Estado no Pipe hoje`
    - Toda ação com dependência de plataforma contém `Opções para o portão (D-20)`
    - `grep -ciE "authorization:|cookie:|bearer "` retorna 0
  </acceptance_criteria>
  <done>Catálogo de ações da referência inventariado com opções para o portão.</done>
</task>

<task type="auto">
  <name>Task 2: Scripts (D-21), biblioteca de funções (D-22) e resumo para o portão</name>
  <files>.planning/phases/02-fechar-o-builder/ref/inventario-acoes.md</files>
  <read_first>
    - .planning/phases/02-fechar-o-builder/ref/inventario-acoes.md (saída da Task 1)
    - packages/db/src/schema/automacao.ts (linhas 443-454: `funcao` do motor de WORKFLOW, para registrar que é outra máquina)
    - apps/gestao-vite/src/paginas/builder/variaveis.ts (linhas 76-97: padrão de busca que a biblioteca reaproveitará)
    - apps/api/src/dominio/fluxo.ts (linhas 320-360: `servicos` do motor, `confirmarUrlSegura`, `chamarComMtls`)
  </read_first>
  <action>
    Acrescentar `## Scripts (D-21)` documentando para ExecuteScript e ExecuteScriptV2 separadamente: versão ECMAScript, limite de tempo, limite de operações/memória, APIs expostas (ex.: `request.fetchAsync`, `context`, `time`), acesso a variáveis de entrada (`inputVariables`) e saída (`outputVariable`), HTTP/fetch, tratamento de erro (o que o fluxo faz quando o script lança), timezone, tipo de retorno, com fonte (SDK público e Help Center). Fechar com `### Recomendação de sandbox para o portão`: comparar três mecanismos em tabela (isolamento, suporte ao ECMAScript exigido, limite de tempo/memória, pacote npm necessário ou não): QuickJS em WebAssembly, `isolated-vm`, processo filho Node com `--max-old-space-size` e `vm` sem globais. Recomendar um, justificando pelo comportamento documentado; se o recomendado exigir pacote npm novo, escrever `Exige pacote npm: <nome>` para disparar o checkpoint de legitimidade no plano 02-16.
    Acrescentar `## Biblioteca de funções (D-22)`: criação, persistência, versionamento/edição, parâmetros, retorno, escopo (por fluxo, por bot, por tenant), como o Builder chama (qual ação/campo), busca na UI, relação com ExecuteScriptV2; declarar explicitamente que `funcao` de `packages/db/src/schema/automacao.ts` é do motor de workflow e não é reaproveitada.
    Por fim `## Resumo` com tabela `| Ação | Classificação proposta | Slot proposto | Bloqueado por captura |`; Slot ∈ {`acoes-contexto`, `acoes-script`, `acoes-funcoes`, `acoes-plataforma`, `ja-suportada`} (`ja-suportada` para as que já estão nos dois lados da whitelist e só precisam de conferência de paridade).
  </action>
  <verify>
    <automated>F=.planning/phases/02-fechar-o-builder/ref/inventario-acoes.md; grep -q "## Scripts (D-21)" $F && grep -q "### Recomendação de sandbox para o portão" $F && grep -q "## Biblioteca de funções (D-22)" $F && grep -q "| Ação | Classificação proposta | Slot proposto" $F && grep -q "## Capturas pendentes (D-03)" $F && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `## Scripts (D-21)` contém as palavras `ECMAScript`, `timezone`, `retorno` e `limite` para cada uma das duas ações
    - A recomendação de sandbox tem tabela com os 4 mecanismos e uma linha `Recomendado:`
    - `## Biblioteca de funções (D-22)` contém a frase `motor de workflow` e o caminho `packages/db/src/schema/automacao.ts`
    - A tabela `## Resumo` só usa slots da lista fechada
  </acceptance_criteria>
  <done>Scripts e biblioteca de funções documentados; resumo pronto para o portão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| referencias-blip/ e SDK público → repositório | evidência externa vira documento versionado |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Information disclosure | ref/inventario-acoes.md | high | mitigate | Task 1: gate grep de cookie/Authorization/bearer = 0 |
| T-2-02 | Tampering (licença) | ref/inventario-acoes.md | medium | mitigate | descrever comportamento do SDK, nunca colar fonte C#/JS |
| T-2-07 | Elevation of privilege (sandbox de script) | recomendação D-21 | high | mitigate | Task 2 compara isolamento/limites; decisão final e testes de escape ficam no 02-16 |
</threat_model>

<verification>
Todas as seções obrigatórias presentes; loop de ações sem `FALTA`.
</verification>

<success_criteria>
Inventário de ações, scripts e biblioteca de funções completo e datado, com classificação/slot proposto e recomendação de sandbox.
</success_criteria>

<output>
Create `.planning/phases/02-fechar-o-builder/02-02-SUMMARY.md` when done
</output>
