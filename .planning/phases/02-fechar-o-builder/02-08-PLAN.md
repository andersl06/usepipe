---
phase: 02-fechar-o-builder
plan: 08
type: execute
wave: 3
depends_on: ["02-07"]
files_modified:
  - packages/ui/src/estilos/tokens.css
  - packages/ui/src/tema.ts
  - docs/marca/MARCA.md
  - apps/gestao-vite/src/paginas/builder/editor.css
  - apps/gestao-vite/src/paginas/builder/painel-bloco.css
  - apps/gestao-vite/src/paginas/builder/editor.tsx
  - apps/gestao-vite/src/paginas/builder/etiquetas-do-bloco.ts
  - apps/gestao-vite/src/paginas/builder/painel.tsx
  - packages/ui/src/icones.tsx
  - apps/gestao-vite/tests/builder-painels.test.ts
autonomous: true
requirements: [BUILDER-03, BUILDER-04]

must_haves:
  truths:
    - "O Builder renderiza sempre no tema escuro, independente da preferência de tema do resto do produto (D-31)"
    - "Todo azul de marca/ênfase da referência aparece no Builder como o token --p-builder-marca-* do papel correspondente, sem achatar estados na mesma cor (D-30, D-32)"
    - "Nenhum #4a5d23 nem --bl-verde resta no diretório do Builder (D-32)"
    - "A paleta de cores e as sugestões dos $tags do bloco seguem a referência, com azul→verde (D-11, D-13)"
    - "O ícone equivalente a user-engaged e os demais ícones listados existem em icones.tsx, desenhados pelo Pipe (D-33)"
  artifacts:
    - path: "packages/ui/src/estilos/tokens.css"
      provides: "11 tokens --p-builder-marca-* no escopo escuro"
      contains: "--p-builder-marca-anel"
    - path: "packages/ui/src/tema.ts"
      provides: "TEMA.builder.marca.*"
      contains: "builder"
    - path: "docs/marca/MARCA.md"
      provides: "documentação dos tokens novos (D-32)"
      contains: "--p-builder-marca"
  key_links:
    - from: "apps/gestao-vite/src/paginas/builder/editor.tsx"
      to: "tokens escuros"
      via: "data-tema='escuro' no contêiner raiz do Builder"
      pattern: "data-tema"
---

<objective>
Aplicar a identidade visual do Builder: tema escuro fixo (D-31), tokens semânticos `--p-builder-marca-*` por papel no lugar de todo azul/verde solto (D-30, D-32), paleta de `$tags` (D-11a, D-13) e ícones próprios incluindo o equivalente de `user-engaged` (D-33, BUILDER-04).

Output: tokens + espelho em `tema.ts` + documentação em MARCA.md + CSS do Builder migrado + ícones.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fechar-o-builder/02-UI-SPEC.md
@.planning/phases/02-fechar-o-builder/ref/inventario-visual.md
@.planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md

Pré-condição e nomes (D-05, STD-01): a Task 0 (Pre-flight Gate) é a imposição: o plano para se `git tag -l std-apply-all-end` não imprimir a tag (a Phase 1 precisa ter aplicado o mapa; o D-22 da Phase 1 congela código paralelo). Caminhos e símbolos EXISTENTES aparecem aqui com o nome em português de 2026-09-26 (confirmado no disco no planejamento): antes de cada task, localizar o nome vigente no disco pela linha `status=approved` de `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv` (colunas `old`/`new`/`declared_at`) ou por `ref/REFERENCIA-CONGELADA.md` §Caminhos resolvidos, e usar esse nome; símbolo sem linha no mapa mantém o nome atual; nunca escrever nome proposto não aplicado. Identificadores, arquivos, endpoints e títulos de teste NOVOS já vêm em inglês neste plano (`std/CONVENTIONS-EN.md`). Texto visível ao usuário continua em português; nomes SQL novos seguem o snake_case português do schema (D-08 da Phase 1); chaves `$…`/`settings` do desenho `{flow, globalActions}` são contrato persistido (STD-06) e não mudam; variáveis `PIPE_*` não mudam. Depois do D-46, `@pipe/gestao-vite` é `@pipe/management-vite` e `apps/gestao-vite` é `apps/management-vite` nos comandos.
Itens com `Bloqueado por` preenchido não são implementados (D-03): o token fica com o valor semente `#a3b76a`-derivado e o SUMMARY lista o bloqueio.
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight Gate — Phase 1 aplicou o mapa (tag std-apply-all-end)</name>
  <files>nenhum (verificação somente leitura)</files>
  <action>Antes de qualquer outra task, rodar `git tag -l std-apply-all-end`. Se a saída não for exatamente `std-apply-all-end`, PARAR o plano: não tocar em nenhum arquivo, não commitar, não seguir para a Task 1 e devolver ao orquestrador/usuário "Pre-flight Gate: tag std-apply-all-end ausente — a Phase 1 ainda não aplicou o mapa de nomes; concluir a Phase 1 antes (D-22 da Phase 1 congela `apps/gestao-vite/src/paginas/builder/*`, `packages/core/src/fluxo/*`, `apps/api/src/dominio/fluxo.ts`)". Não criar a tag nem contornar o gate (D-05, STD-01). Com a tag presente, seguir para a Task 1.</action>
  <verify>
    <automated>test "$(git tag -l std-apply-all-end)" = "std-apply-all-end"</automated>
  </verify>
  <acceptance_criteria>
    - `git tag -l std-apply-all-end` imprime `std-apply-all-end`
    - se o gate falhar, `git status --porcelain` não mostra arquivo alterado por este plano e o plano termina sem SUMMARY de conclusão
  </acceptance_criteria>
  <done>Tag `std-apply-all-end` confirmada; plano liberado. Sem a tag, plano parado e escalado ao usuário.</done>
</task>

<task type="auto">
  <name>Task 1: Tokens --p-builder-marca-* no escopo escuro, espelho em tema.ts e MARCA.md (D-32)</name>
  <files>packages/ui/src/estilos/tokens.css, packages/ui/src/tema.ts, docs/marca/MARCA.md</files>
  <read_first>
    - packages/ui/src/estilos/tokens.css (bloco `:root[data-tema='escuro']`, --p-marca escuro #a3b76a)
    - packages/ui/src/tema.ts (estrutura de TEMA.marca)
    - docs/marca/MARCA.md
    - .planning/phases/02-fechar-o-builder/ref/inventario-visual.md (tabela azul→papel→token, opacidade/blur/spread)
  </read_first>
  <action>
    No bloco escuro de `tokens.css` criar exatamente: `--p-builder-marca`, `--p-builder-marca-hover`, `--p-builder-marca-ativo`, `--p-builder-marca-anel`, `--p-builder-marca-borda-ativa`, `--p-builder-marca-selecionado`, `--p-builder-marca-destaque`, `--p-builder-marca-brilho`, `--p-builder-marca-sombra`, `--p-builder-marca-sobreposicao`, `--p-builder-marca-gradiente`. Valor de cada um: o verde do Pipe equivalente ao azul medido na tabela do inventário, preservando a opacidade/intensidade do efeito (ex.: se o anel da referência é azul a 40%, o token é o verde a 40% via `color-mix(in srgb, var(--p-marca) 40%, transparent)`); `--p-builder-marca-destaque` substitui o `color-mix(in srgb, var(--p-marca) 24%, #282828)` hoje hardcoded em `editor.css`. Não reutilizar `--p-foco` (violeta, foco de teclado). Em `tema.ts` acrescentar `TEMA.builder.marca` com chaves `base, hover, ativo, anel, bordaAtiva, selecionado, destaque, brilho, sombra, sobreposicao, gradiente` apontando para `var(--p-builder-marca-…)`. Em `docs/marca/MARCA.md` nova seção `## Builder: tokens de marca (tema escuro)` com tabela `| Token | Valor | Papel | Onde é permitido |`.
  </action>
  <verify>
    <automated>test $(grep -c "\-\-p-builder-marca" packages/ui/src/estilos/tokens.css) -ge 11 && grep -q "builder" packages/ui/src/tema.ts && test $(grep -c "\-\-p-builder-marca" docs/marca/MARCA.md) -ge 11 && pnpm --filter @pipe/ui typecheck && pnpm --filter @pipe/ui test</automated>
  </verify>
  <acceptance_criteria>
    - `tokens.css` define os 11 tokens listados dentro do seletor escuro
    - `tema.ts` contém `anel: 'var(--p-builder-marca-anel)'` (ou forma equivalente com a mesma custom property)
    - MARCA.md tem uma linha de tabela por token
    - `pnpm --filter @pipe/ui typecheck` e `pnpm --filter @pipe/ui test` (verificar-tokens.mjs: todo `var(--p-*)` usado tem definição e tokens de cor existem nos DOIS blocos escuros, preferência do sistema e escolha explícita) saem 0; os 11 tokens ficam nos dois blocos escuros
  </acceptance_criteria>
  <done>Tokens semânticos do Builder criados e documentados.</done>
</task>

<task type="auto">
  <name>Task 2: Tema escuro fixo e migração do CSS do Builder (D-30, D-31, D-32)</name>
  <files>apps/gestao-vite/src/paginas/builder/editor.tsx, apps/gestao-vite/src/paginas/builder/editor.css, apps/gestao-vite/src/paginas/builder/painel-bloco.css</files>
  <read_first>
    - apps/gestao-vite/src/paginas/builder/editor.tsx (elemento raiz do editor)
    - apps/gestao-vite/src/paginas/builder/editor.css (todas as ocorrências de #4a5d23, color-mix com --p-marca, box-shadow 0 0 0 4px)
    - apps/gestao-vite/src/paginas/builder/painel-bloco.css (--bl-verde na linha ~10 e seus 4 usos)
    - packages/ui/src/estilos/tokens.css (tokens da Task 1 e superfícies escuras)
    - .planning/phases/02-fechar-o-builder/ref/inventario-visual.md (## Tema escuro (D-31))
  </read_first>
  <action>
    No contêiner raiz do editor em `editor.tsx`, aplicar `data-tema="escuro"` para que os tokens escuros valham dentro do Builder sem depender do alternador global nem de `prefers-color-scheme` (D-31); se o seletor de `tokens.css` só casar em `:root`, acrescentar `[data-tema='escuro']` ao mesmo seletor na Task 1 (sem duplicar valores). Substituir cada `#4a5d23`, `var(--bl-verde)` e `color-mix(...--p-marca...)` em `editor.css`/`painel-bloco.css` pelo token do papel correspondente (anel de seleção/hover do nó → `--p-builder-marca-anel`; seta ativa → `--p-builder-marca-borda-ativa`; aba ativa → `--p-builder-marca-selecionado`; botão primário → `--p-builder-marca`/`-hover`/`-ativo`; destaque → `--p-builder-marca-destaque`), conforme a tabela do inventário. Remover a declaração `--bl-verde`. Preservar spread/blur/opacidade (D-32). Não mudar dimensões (175px, 28px, 8px, 360px — UI-SPEC).
  </action>
  <verify>
    <automated>test $(grep -rn "#4a5d23\|--bl-verde" apps/gestao-vite/src/paginas/builder | wc -l) -eq 0 && grep -q "data-tema" apps/gestao-vite/src/paginas/builder/editor.tsx && pnpm --filter @pipe/gestao-vite typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "#4a5d23\|--bl-verde" apps/gestao-vite/src/paginas/builder` não retorna nada
    - `editor.css` contém `var(--p-builder-marca-anel)` e ao menos 3 outros tokens `--p-builder-marca-` distintos
    - `editor.tsx` contém `data-tema="escuro"`
    - typecheck do app sai 0
  </acceptance_criteria>
  <done>Builder escuro fixo, sem hex de marca solto.</done>
</task>

<task type="auto">
  <name>Task 3: Paleta de $tags (D-11a, D-13) e ícones próprios (D-33)</name>
  <files>apps/gestao-vite/src/paginas/builder/etiquetas-do-bloco.ts, apps/gestao-vite/src/paginas/builder/painel.tsx, packages/ui/src/icones.tsx, apps/gestao-vite/tests/builder-painels.test.ts</files>
  <read_first>
    - apps/gestao-vite/src/paginas/builder/etiquetas-do-bloco.ts (CORES_DAS_ACOES, corDaEtiqueta com lista de azuis → #4a5d23)
    - apps/gestao-vite/src/paginas/builder/painel.tsx (linhas 30-51: edição de $tags)
    - .planning/phases/02-fechar-o-builder/ref/inventario-satisfacao-e-tags.md (## 6. $tags do bloco)
    - .planning/phases/02-fechar-o-builder/ref/inventario-visual.md (## Ícones próprios necessários)
    - packages/ui/src/icones.tsx (padrão de declaração de ícone)
  </read_first>
  <action>
    Em `etiquetas-do-bloco.ts`: exportar `TAG_PALETTE` com as cores da referência na ordem do inventário (nome + valor), onde cada azul vira `var(--p-builder-marca…)` do papel ou o verde correspondente (D-13); `corDaEtiqueta` troca o retorno `'#4a5d23'` por `'var(--p-builder-marca)'`; exportar `TAG_SUGGESTIONS` (rótulos sugeridos pela referência). A cor gravada em `$tags` continua sendo o valor serializado que a Blip lê (hex) quando o fluxo é exportado: se a cor escolhida é token, exportar o hex verde resolvido (documentar no código em uma linha). Em `painel.tsx`, o seletor de cor da tag usa `TAG_PALETTE` e a entrada oferece `TAG_SUGGESTIONS` filtradas com o mesmo `normalizar` de `variaveis.ts`.
    Em `packages/ui/src/icones.tsx`: acrescentar um ícone por linha `Existe em icones.tsx? = não` do inventário visual, incluindo `userEngaged` (equivalente de user-engaged), desenhado pelo Pipe no mesmo traço/viewBox dos ícones existentes. Proibido copiar SVG da Blip.
    Teste: acrescentar em `apps/gestao-vite/tests/builder-painels.test.ts` um caso `tag color: blue becomes brand token` e um caso `TAG_PALETTE has no blue` (nenhum valor com hex azul da lista do inventário).
  </action>
  <verify>
    <automated>pnpm --filter @pipe/gestao-vite test && pnpm --filter @pipe/ui typecheck && grep -q "userEngaged" packages/ui/src/icones.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `etiquetas-do-bloco.ts` exporta `TAG_PALETTE` e `TAG_SUGGESTIONS` e não contém `#4a5d23`
    - `icones.tsx` contém `userEngaged`
    - `builder-painels.test.ts` contém os dois testes nomeados e `pnpm --filter @pipe/gestao-vite test` sai 0
  </acceptance_criteria>
  <done>Paleta de tags e ícones próprios entregues.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| referência visual Blip → código do Pipe | risco de licença |
| cor de tag de fluxo importado → CSS inline | valor não confiável vindo de JSON importado |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-02 | Tampering (licença) | icones.tsx, CSS | medium | mitigate | ícones desenhados pelo Pipe; nenhum SVG/CSS copiado; revisão no SUMMARY |
| T-2-21 | Tampering (CSS injection) | corDaEtiqueta | low | mitigate | cor de origem aceita só se casar `^#[0-9a-f]{3,8}$` (i); qualquer outro valor cai no token padrão |
</threat_model>

<verification>
`grep -rn "#4a5d23\|--bl-verde" apps/gestao-vite/src/paginas/builder` vazio; testes e typecheck verdes.
</verification>

<success_criteria>
Builder escuro, tokens por papel documentados, paleta de tags e ícone user-engaged próprios.
</success_criteria>

<output>
Create `.planning/phases/02-fechar-o-builder/02-08-SUMMARY.md` when done
</output>
