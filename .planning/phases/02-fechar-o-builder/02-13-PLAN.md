---
phase: 02-fechar-o-builder
plan: 13
type: execute
wave: 6
depends_on: ["02-07", "02-08", "02-12"]
files_modified:
  - apps/api/src/controladores/gestao-builder.ts
  - apps/api/src/dominio/gestao/builder-do-fluxo.ts
  - apps/api/tests/builder-por-fluxo.test.ts
  - apps/gestao-vite/src/paginas/builder-gravar.ts
  - apps/gestao-vite/src/paginas/builder/painel-configuracao.tsx
  - apps/gestao-vite/src/paginas/builder/importar-exportar.ts
  - apps/gestao-vite/src/paginas/builder/painel-filas.tsx
  - apps/gestao-vite/src/paginas/cadastros/atendentes-filas.tsx
  - apps/gestao-vite/src/paginas/builder/painel-bloco.css
  - apps/gestao-vite/tests/builder-painels.test.ts
autonomous: true
requirements: [BUILDER-04]

must_haves:
  truths:
    - "O atendente vê a lista de versões publicadas do fluxo no painel de Configuração e exporta qualquer versão antiga no mesmo formato {flow, globalActions} do rascunho (D-16)"
    - "A exportação de versão exige a mesma permissão do Builder e nunca expõe versão de outro tenant (D-16)"
    - "O painel de Filas tem a forma aprovada no portão (atalho evoluído ou CRUD embutido reaproveitando PaginaFilas), com paridade funcional com a referência (D-15)"
  artifacts:
    - path: "apps/gestao-vite/src/paginas/builder-gravar.ts"
      provides: "listVersions e loadVersion (Resultado<T>)"
      contains: "listVersions"
    - path: "apps/api/src/controladores/gestao-builder.ts"
      provides: "GET :id/builder/<versões>/:version devolvendo o desenho da versão"
  key_links:
    - from: "painel-configuracao.tsx aba Versões"
      to: "GET :id/builder/<versões>"
      via: "listVersions"
      pattern: "listVersions\\("
    - from: "exportar versão"
      to: "importar-exportar.ts textoDeExportacao"
      via: "mesmo serializador do rascunho"
      pattern: "textoDeExportacao|exportText"
---

<objective>
Fechar a parte de painéis administrativos de BUILDER-04: listar e exportar versão antiga (D-16) e painel de Filas com paridade funcional (D-15). Copiar/colar já foi coberto em 02-06 (D-17).

Output: endpoint de desenho por versão, cliente do front, aba Versões completa, painel de Filas conforme portão, testes.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fechar-o-builder/02-PATTERNS.md
@.planning/phases/02-fechar-o-builder/02-UI-SPEC.md
@.planning/phases/02-fechar-o-builder/ref/inventario-paineis-e-setas.md
@.planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md

Pré-condição e nomes (D-05, STD-01): o gate da Phase 1 (tag `std-apply-all-end`) é imposto pela Task 0 (Pre-flight Gate) de 02-05/02-06/02-08, das quais este plano depende transitivamente via `depends_on` (direto 02-08). Caminhos, rotas e símbolos EXISTENTES aparecem com o nome em português de 2026-09-26; localizar o nome vigente pela linha `status=approved` de `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/{api,gestao-vite,packages-contracts}.csv` e `std/wire-contracts.csv`, ou por `ref/REFERENCIA-CONGELADA.md` §Caminhos resolvidos; nunca escrever nome proposto não aplicado. A sub-rota nova segue o segmento vigente da rota irmã `GET :id/builder/versoes` (ex.: se virou `versions`, a nova é `:id/builder/versions/:version`). Identificadores/títulos de teste NOVOS em inglês. Texto visível em português (copy do UI-SPEC). `@pipe/gestao-vite` vira `@pipe/management-vite` após D-46.

Fatos do planejamento: `GET :id/builder/versoes` (`gestao-builder.ts:79-90`) devolve `VersaoDoFluxo[]` só com metadados (`packages/contracts/src/gestao-fluxo.ts:338-348`), sem o desenho; por isso exportar uma versão precisa de um endpoint que devolva o `DesenhoDoBuilder` daquela versão. `listarVersoes` e `restaurarVersao` em `builder-do-fluxo.ts:568-600` usam `fluxoDoBuilder(tx, tid, usuarioId, fluxoId, EDITAR_FLUXO)` → `exigirPermissaoNoFluxo(..., 'builder.escrever')`. `PaginaFilas` fica em `apps/gestao-vite/src/paginas/cadastros/atendentes-filas.tsx`.
Decisão D-15 vem da linha `APROVADO:` de `CLASSIFICACAO-PORTAO.md`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Endpoint do desenho de uma versão publicada (D-16)</name>
  <files>apps/api/src/controladores/gestao-builder.ts, apps/api/src/dominio/gestao/builder-do-fluxo.ts, apps/api/tests/builder-por-fluxo.test.ts</files>
  <read_first>
    - apps/api/src/controladores/gestao-builder.ts (todas as rotas; uuidOu404)
    - apps/api/src/dominio/gestao/builder-do-fluxo.ts (carregarBuilder, listarVersoes 568-581, restaurarVersao 584+, fluxoDoBuilder ~95-105)
    - apps/api/tests/builder-por-fluxo.test.ts (montagem de sessão e fluxo)
  </read_first>
  <behavior>
    - Gestor com permissão no fluxo recebe o `DesenhoDoBuilder` da versão N, igual ao que `carregarBuilder` montaria para aquela versão.
    - Versão inexistente, número não inteiro/≤0 → 404; id fora do padrão uuid → 404.
    - Usuário sem `builder.escrever` no fluxo → 403; fluxo de outro tenant → 404.
  </behavior>
  <action>
    Domínio: `loadVersionDrawing(tx, tid, userId, flowId, version)` com `fluxoDoBuilder(..., EDITAR_FLUXO)`, validação de `version` igual a `restaurarVersao`, e montagem do desenho reaproveitando a mesma função que `carregarBuilder` usa para versão publicada (não criar segundo serializador). Controlador: `@Get(':id/builder/<segmento de versões vigente>/:version')` com `@ComSessao()`, `uuidOu404`, tenant da sessão. Testes no arquivo existente com prefixo `version drawing:`.
  </action>
  <verify>
    <automated>pnpm banco:subir && pnpm --filter @pipe/api exec vitest run tests/builder-por-fluxo.test.ts && pnpm --filter @pipe/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `builder-do-fluxo.ts` (nome vigente) exporta `loadVersionDrawing` e chama `fluxoDoBuilder` (nome vigente) com `EDITAR_FLUXO`
    - testes `version drawing:` cobrem 200, 404 (versão), 403 e outro tenant
    - verify sai 0
  </acceptance_criteria>
  <done>API entrega o desenho de qualquer versão com autorização.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Aba Versões com listar, exportar e restaurar (D-16)</name>
  <files>apps/gestao-vite/src/paginas/builder-gravar.ts, apps/gestao-vite/src/paginas/builder/painel-configuracao.tsx, apps/gestao-vite/src/paginas/builder/importar-exportar.ts, apps/gestao-vite/tests/builder-painels.test.ts</files>
  <read_first>
    - apps/gestao-vite/src/paginas/builder-gravar.ts (salvarRascunho/publicarFluxo/restaurarVersao, Resultado<T>, motivoDe)
    - apps/gestao-vite/src/paginas/builder/painel-configuracao.tsx (AbaDeVersoes)
    - apps/gestao-vite/src/paginas/builder/importar-exportar.ts (textoDeExportacao, validarImportacao)
    - .planning/phases/02-fechar-o-builder/ref/inventario-paineis-e-setas.md (## Versões (D-16): colunas, ordenação, nome do arquivo)
    - .planning/phases/02-fechar-o-builder/02-UI-SPEC.md (copy "Exportar versão" e confirmação de restaurar)
  </read_first>
  <behavior>
    - `exportFileName(flowName, version, date)` gera o nome de arquivo no padrão da referência.
    - Exportar a versão N produz exatamente `textoDeExportacao(desenhoDaVersaoN)` e esse texto passa em `validarImportacao`.
  </behavior>
  <action>
    Em `builder-gravar.ts`: `listVersions(id)` (`api.get` na rota de listagem vigente) e `loadVersion(id, version)` (rota da Task 1), ambos devolvendo `Resultado<T>` com `motivoDe`, nunca lançando para o componente. Na aba Versões: tabela com as colunas/ordem do inventário (versão, estado, blocos, publicada em, publicada por), ação "Exportar versão" por linha (baixa arquivo `.json` via `Blob` + link temporário, conteúdo de `textoDeExportacao`, nome de `exportFileName` exportada de `importar-exportar.ts`), ação "Restaurar" com a confirmação literal do UI-SPEC ("Restaurar versão: o rascunho atual é substituído pela versão selecionada. Publique de novo para valer no atendimento."), estados de carregando/erro/vazio. Nenhum formato novo de arquivo (D-16). Testes `versions:` em `builder-painels.test.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @pipe/gestao-vite test && pnpm --filter @pipe/gestao-vite typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `builder-gravar.ts` contém `listVersions` e `loadVersion`
    - `painel-configuracao.tsx` contém `Exportar versão` e `Restaurar versão: o rascunho atual é substituído`
    - teste de ida e volta exportação→`validarImportacao` passa
    - test e typecheck saem 0
  </acceptance_criteria>
  <done>Versão antiga exportável pelo Builder.</done>
</task>

<task type="auto">
  <name>Task 3: Painel de Filas conforme o portão (D-15)</name>
  <files>apps/gestao-vite/src/paginas/builder/painel-filas.tsx, apps/gestao-vite/src/paginas/cadastros/atendentes-filas.tsx, apps/gestao-vite/src/paginas/builder/painel-bloco.css</files>
  <read_first>
    - apps/gestao-vite/src/paginas/builder/painel-filas.tsx (atalho atual e justificativa 6-10)
    - apps/gestao-vite/src/paginas/cadastros/atendentes-filas.tsx (PaginaFilas: listagem/criação/edição/ativação/exclusão)
    - .planning/phases/02-fechar-o-builder/ref/inventario-paineis-e-setas.md (## Painel de Filas (D-15))
    - .planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md (APROVADO: D-15)
  </read_first>
  <action>
    Se o portão aprovou CRUD embutido: extrair de `atendentes-filas.tsx` o conteúdo de `PaginaFilas` para um componente exportado reutilizável (ex.: `QueuesManager`) sem duplicar regra de validação, e renderizá-lo dentro do painel do Builder com o layout/estados da referência; a página original passa a usar o mesmo componente. Se aprovou atalho evoluído: reproduzir no painel exatamente os elementos da referência (resumo das filas, contagem, ações) e o botão de navegação para a página de filas. Em ambos os casos, textos literais da referência em português, CSS só com tokens. Registrar no SUMMARY qual opção foi aplicada e por qual linha `APROVADO:`.
  </action>
  <verify>
    <automated>pnpm --filter @pipe/gestao-vite typecheck && pnpm --filter @pipe/gestao-vite test</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY cita a linha `APROVADO:` de D-15 usada
    - se CRUD embutido: `atendentes-filas.tsx` e `painel-filas.tsx` importam o mesmo componente extraído (grep do nome nos dois)
    - nenhuma regra de validação de fila duplicada (grep das mensagens de erro de fila aparece em um só arquivo)
    - typecheck e testes saem 0
  </acceptance_criteria>
  <done>Painel de Filas com a paridade aprovada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → API de versões | id de fluxo e número de versão não confiáveis |
| tenant A ↔ tenant B | desenhos de fluxo são dados do tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-11 | Information disclosure | exportar versão antiga | high | mitigate | Task 1: `@ComSessao()`, tenant da sessão, `exigirPermissaoNoFluxo('builder.escrever')`, testes 403/outro tenant |
| T-2-09 | Elevation of privilege | endpoint novo | high | mitigate | Task 1 reaproveita `fluxoDoBuilder(EDITAR_FLUXO)` |
| T-2-25 | Tampering | número de versão | low | mitigate | validação inteira ≥1, 404 |
| T-2-26 | Elevation of privilege | CRUD de filas embutido | medium | mitigate | Task 3 reaproveita o componente e os endpoints existentes de filas, com as mesmas permissões |
</threat_model>

<verification>
Testes de api e app verdes.
</verification>

<success_criteria>
BUILDER-04: exportar versão antiga e painel de Filas entregues; copiar/colar coberto em 02-06.
</success_criteria>

<output>
Create `.planning/phases/02-fechar-o-builder/02-13-SUMMARY.md` when done
</output>
