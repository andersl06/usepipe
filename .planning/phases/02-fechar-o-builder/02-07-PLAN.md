---
phase: 02-fechar-o-builder
plan: 07
type: execute
wave: 2
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified:
  - .planning/phases/02-fechar-o-builder/ref/REFERENCIA-CONGELADA.md
  - .planning/phases/02-fechar-o-builder/ref/CAPTURAS-PENDENTES.md
  - .planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md
  - .planning/phases/02-fechar-o-builder/ref/catalogo-aprovado.json
  - .planning/phases/02-fechar-o-builder/ref/conferir-catalogo.mjs
autonomous: false
requirements: [BUILDER-01, BUILDER-02, BUILDER-03, BUILDER-04, BUILDER-05]

must_haves:
  truths:
    - "Nenhuma wave de implementação começa antes deste portão: investigar → congelar → aprovar → implementar (D-01, D-04)"
    - "Existe um snapshot da referência datado e congelado, que é a source of truth de implementação e testes; lançamentos posteriores da Blip viram delta futuro (D-02)"
    - "A lista de capturas pendentes para o dono existe e cada item bloqueado aponta o plano que ele bloqueia; nada bloqueado é improvisado (D-03)"
    - "Cada conteúdo e ação tem classificação 'reproduzível' ou 'dependência externa' aprovada pelo dono no portão antes de qualquer wave de implementação (D-04, D-20)"
    - "Todos os caminhos citados nos artefatos do portão foram resolvidos contra std/map/*.csv no momento da consolidação (D-05)"
    - "Um verificador automático compara o catálogo aprovado com os dois lados da whitelist (motor + tela) (D-24)"
  artifacts:
    - path: ".planning/phases/02-fechar-o-builder/ref/REFERENCIA-CONGELADA.md"
      provides: "índice congelado e datado dos inventários (D-02)"
      contains: "**Congelado em:**"
    - path: ".planning/phases/02-fechar-o-builder/ref/CAPTURAS-PENDENTES.md"
      provides: "capturas que o dono precisa fazer (D-03)"
    - path: ".planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md"
      provides: "classificação item a item + slot/plano + decisões de mecanismo aprovadas (D-04, D-20)"
      contains: "**Aprovado pelo dono em:**"
    - path: ".planning/phases/02-fechar-o-builder/ref/catalogo-aprovado.json"
      provides: "catálogo aprovado legível por máquina"
    - path: ".planning/phases/02-fechar-o-builder/ref/conferir-catalogo.mjs"
      provides: "gate automático da whitelist dupla por slot (D-24)"
  key_links:
    - from: ".planning/phases/02-fechar-o-builder/ref/conferir-catalogo.mjs"
      to: "catalogo-aprovado.json + whitelists motor/tela"
      via: "node conferir-catalogo.mjs --slot <slot>"
      pattern: "--slot"
---

<objective>
Consolidar as quatro investigações num snapshot congelado e datado (D-02), gerar a lista de capturas pendentes (D-03) e a classificação item a item, e passar pelo PORTÃO DO DONO (D-04). Nenhuma wave de implementação (02-08 em diante) começa antes da aprovação registrada aqui.

Output: 5 arquivos em `ref/` + aprovação registrada.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fechar-o-builder/02-CONTEXT.md
@.planning/phases/02-fechar-o-builder/ref/inventario-conteudo.md
@.planning/phases/02-fechar-o-builder/ref/inventario-acoes.md
@.planning/phases/02-fechar-o-builder/ref/inventario-satisfacao-e-tags.md
@.planning/phases/02-fechar-o-builder/ref/inventario-paineis-e-setas.md
@.planning/phases/02-fechar-o-builder/ref/inventario-visual.md

Slots e planos que os implementam (fixados no planejamento):
- `conteudo-midia` → 02-10 · `conteudo-interativo` → 02-15 · `conteudo-dinamico` → 02-19
- `acoes-contexto` → 02-14 · `acoes-script` → 02-16 · `acoes-funcoes` → 02-17/02-18 · `acoes-plataforma` → 02-20 · `ja-suportada` → conferência em 02-22
- satisfação/etiquetas → 02-11/02-12 · visual/ícones/paleta de $tags → 02-08 · seletor de destino/setas → 02-09 · Versões/Filas → 02-13 · Teste → 02-21
Capacidade por slot (para caber no contexto de um executor): até 6 tipos de conteúdo por slot de conteúdo; até 5 ações em `acoes-contexto` e em `acoes-plataforma`. Excedente é registrado como `EXCEDE CAPACIDADE` e o orquestrador roda `/gsd:plan-phase 2 --gaps` para o excedente depois do portão (nenhum item é cortado).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot congelado, capturas pendentes e classificação</name>
  <files>.planning/phases/02-fechar-o-builder/ref/REFERENCIA-CONGELADA.md, .planning/phases/02-fechar-o-builder/ref/CAPTURAS-PENDENTES.md, .planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md, .planning/phases/02-fechar-o-builder/ref/catalogo-aprovado.json</files>
  <read_first>
    - os cinco inventários em .planning/phases/02-fechar-o-builder/ref/ (seções Resumo e Capturas pendentes)
    - .planning/phases/02-fechar-o-builder/02-CONTEXT.md (D-02, D-03, D-04, D-05, D-20, D-24)
    - .planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/gestao-vite.csv, packages-core.csv, api.csv (cabeçalho: colunas de status e caminho novo)
  </read_first>
  <action>
    `REFERENCIA-CONGELADA.md`: `**Congelado em:** AAAA-MM-DD`, lista dos 5 inventários com o hash do commit que os contém (`git log -1 --format=%h -- <arquivo>`), a frase "Mudanças da Blip depois desta data são delta para fase futura (D-02)" e `## Caminhos resolvidos (D-05)` com tabela `| Caminho citado | Status no std/map | Caminho vigente |` para cada arquivo de código citado nos planos 02-08..02-22 (resolver pela linha do CSV; se a aplicação D-49 ainda não entrou, caminho vigente = caminho atual).
    `CAPTURAS-PENDENTES.md`: juntar todos os `PENDENTE-CAPTURA` dos inventários em tabela `| # | Inventário | Tela/estado | Tipo (screenshot/DOM/Network) | Bloqueia item | Bloqueia plano |`, renumerada C-01..C-NN.
    `CLASSIFICACAO-PORTAO.md`: tabela única `| Item | Tipo (conteúdo/ação/painel/satisfação/visual) | Classificação proposta | Slot | Plano | Bloqueado por | Decisão do dono |` (coluna Decisão vazia) e `## Decisões de mecanismo` com as recomendações dos inventários: sandbox de script (D-21, incluindo `Exige pacote npm:` se houver), painel de Teste simulação vs canal real (D-14), Filas atalho vs CRUD embutido (D-15), schema e ramificação da satisfação (D-08.5, D-09), biblioteca de funções (D-22) e, por ação de plataforma, as duas opções (D-20). Contar itens por slot e marcar `EXCEDE CAPACIDADE` acima do limite.
    `catalogo-aprovado.json` (rascunho, `"aprovado": false`): chaves `congeladoEm`, `aprovado`, `conteudos` (itens com `nome`, `mime`, `slot`, `classificacao` ∈ `reproduzivel|externa|bloqueado`) e `acoes` (itens com `tipo`, `slot`, `classificacao`).
  </action>
  <verify>
    <automated>cd .planning/phases/02-fechar-o-builder/ref && grep -q "Congelado em:" REFERENCIA-CONGELADA.md && grep -q "## Caminhos resolvidos (D-05)" REFERENCIA-CONGELADA.md && grep -q "| Item | Tipo" CLASSIFICACAO-PORTAO.md && grep -q "## Decisões de mecanismo" CLASSIFICACAO-PORTAO.md && node -e "const c=JSON.parse(require('fs').readFileSync('catalogo-aprovado.json','utf8'));if(!Array.isArray(c.conteudos)||!Array.isArray(c.acoes))process.exit(1)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Todo item das tabelas `## Resumo` dos inventários aparece em `CLASSIFICACAO-PORTAO.md`
    - Todo `PENDENTE-CAPTURA` dos inventários virou uma linha C-NN em `CAPTURAS-PENDENTES.md` com `Bloqueia plano` preenchido
    - `catalogo-aprovado.json` é JSON válido com `"aprovado": false`
    - A coluna Slot só usa os slots listados no contexto deste plano
  </acceptance_criteria>
  <done>Pacote do portão pronto para o dono.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verificador da whitelist dupla (conferir-catalogo.mjs)</name>
  <files>.planning/phases/02-fechar-o-builder/ref/conferir-catalogo.mjs</files>
  <read_first>
    - .planning/phases/02-fechar-o-builder/ref/catalogo-aprovado.json
    - packages/core/src/fluxo/editor.ts (CONTEUDOS_SUPORTADOS ~linha 152)
    - packages/core/src/fluxo/acoes.ts (objetos `tipo: '...'` e ACOES_DO_MOTOR)
    - apps/gestao-vite/src/paginas/builder/conteudo.ts e acoes-do-bloco.ts (CATALOGO_DE_ACOES)
    - apps/api/src/dominio/fluxo.ts (textoParaOCanal: tradução MIME → canal)
  </read_first>
  <behavior>
    - `node conferir-catalogo.mjs --slot ja-suportada` sai 0 no código atual (SetVariable, DeleteVariable, Redirect, ProcessHttp, TrackEvent já estão nos dois lados).
    - Com um item fictício `{ "tipo": "NaoExiste", "slot": "teste", "classificacao": "reproduzivel" }` em um JSON temporário passado por `--catalogo <arquivo>`, `--slot teste` sai 1 e imprime `FALTA motor NaoExiste` e `FALTA tela NaoExiste`.
  </behavior>
  <action>
    Script Node ESM só com stdlib (`node:fs`, `node:child_process`), flags `--slot <slot>` (ou `--all`) e `--catalogo <arquivo>` (padrão `catalogo-aprovado.json` ao lado do script). Para cada item `reproduzivel` do slot: conteúdo → o literal do `mime` aparece no arquivo de whitelist do motor (o que define `CONTEUDOS_SUPORTADOS`), no catálogo de conteúdo da tela (o que exporta `novoTexto`) e no arquivo de tradução para canal (o que define `textoParaOCanal`); ação → `tipo: '<Tipo>'` aparece no arquivo que define `ACOES_DO_MOTOR` e no que define `CATALOGO_DE_ACOES`. Para item `externa`: o `tipo` aparece na lista `EXTERNAL_DEPENDENCY_ACTIONS` (criada em 02-20; ausente = falha só quando o slot a exige). Localizar cada arquivo com `git grep -l -e <símbolo PT> -e <símbolo EN>`, onde o símbolo EN vem da linha aprovada do `std/map/*.csv` correspondente, para funcionar antes e depois da aplicação do mapa (D-05). Imprime `FALTA <lado> <item>` por ausência e sai 1 se houver alguma; senão imprime `OK <n> itens` e sai 0.
  </action>
  <verify>
    <automated>node .planning/phases/02-fechar-o-builder/ref/conferir-catalogo.mjs --slot ja-suportada</automated>
  </verify>
  <acceptance_criteria>
    - `--slot ja-suportada` sai 0 no código atual
    - o caso fictício do behavior sai 1 e imprime `FALTA motor NaoExiste` (registrado no SUMMARY)
    - `grep -c "require(\|from '" conferir-catalogo.mjs` só referencia módulos `node:`
  </acceptance_criteria>
  <done>Gate automático da whitelist dupla pronto para os planos de catálogo.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: PORTÃO DO DONO (D-04)</name>
  <decision>Aprovar o snapshot congelado, a classificação item a item (reproduzível vs dependência externa, D-20) e as decisões de mecanismo (D-14, D-15, D-21, D-22, D-08.5).</decision>
  <context>
    Abrir `.planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md`, `REFERENCIA-CONGELADA.md` e `CAPTURAS-PENDENTES.md`. Nenhuma wave de implementação começa antes desta aprovação (D-04). Itens com captura pendente ficam bloqueados até o dono fornecer a captura em `referencias-blip/` (D-03); o plano correspondente não os implementa por improviso. Slots marcados `EXCEDE CAPACIDADE` serão replanejados com `/gsd:plan-phase 2 --gaps`.
  </context>
  <options>
    <option id="aprovar">
      <name>Aprovar como está</name>
      <pros>Implementação começa imediatamente.</pros>
      <cons>Nenhum.</cons>
    </option>
    <option id="aprovar-com-ajustes">
      <name>Aprovar com ajustes linha a linha</name>
      <pros>Dono corrige classificação/mecanismo antes de congelar.</pros>
      <cons>Exige nova rodada do verify da Task 4.</cons>
    </option>
    <option id="recapturar">
      <name>Fornecer capturas antes de aprovar</name>
      <pros>Menos itens bloqueados.</pros>
      <cons>Adia o início da implementação.</cons>
    </option>
  </options>
  <resume-signal>Responder "aprovar", ou a lista de ajustes por linha (Item → decisão), ou "recapturar C-NN,...".</resume-signal>
  <files>.planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md</files>
  <read_first>
    - .planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md
    - .planning/phases/02-fechar-o-builder/ref/REFERENCIA-CONGELADA.md
    - .planning/phases/02-fechar-o-builder/ref/CAPTURAS-PENDENTES.md
  </read_first>
  <action>Apresentar ao dono os três arquivos do portão e aguardar a resposta; não editar nada nesta task (a Task 4 registra a decisão).</action>
  <verify>
    <automated>test -f .planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md</automated>
  </verify>
  <acceptance_criteria>
    - O dono respondeu com uma das três opções do resume-signal
  </acceptance_criteria>
  <done>Resposta do dono registrada para a Task 4.</done>
</task>

<task type="auto">
  <name>Task 4: Registrar a aprovação e congelar</name>
  <files>.planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md, .planning/phases/02-fechar-o-builder/ref/catalogo-aprovado.json</files>
  <read_first>
    - .planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md
    - resposta do dono na Task 3
  </read_first>
  <action>
    Preencher a coluna `Decisão do dono` de cada linha com a resposta (aprovar = repetir a classificação proposta), acrescentar `**Aprovado pelo dono em:** AAAA-MM-DD` e as decisões de mecanismo aprovadas em `## Decisões de mecanismo` com a palavra `APROVADO:` à frente de cada uma. Atualizar `catalogo-aprovado.json` com as classificações finais e `"aprovado": true`. Se houver slot `EXCEDE CAPACIDADE`, registrar no SUMMARY a lista de itens excedentes para o orquestrador.
  </action>
  <verify>
    <automated>cd .planning/phases/02-fechar-o-builder/ref && grep -q "Aprovado pelo dono em:" CLASSIFICACAO-PORTAO.md && node -e "const c=JSON.parse(require('fs').readFileSync('catalogo-aprovado.json','utf8'));process.exit(c.aprovado===true?0:1)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Nenhuma linha da tabela de classificação tem a coluna `Decisão do dono` vazia
    - `catalogo-aprovado.json` tem `"aprovado": true`
    - `## Decisões de mecanismo` tem uma linha `APROVADO:` para D-14, D-15, D-21, D-22 e D-08.5
  </acceptance_criteria>
  <done>Portão fechado; waves de implementação liberadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inventários → decisões de implementação | classificação errada libera execução de algo inseguro ou esconde item |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Information disclosure | ref/*.md | high | mitigate | Task 1 repete o gate `grep -ciE "authorization:|cookie:|bearer "` = 0 sobre `ref/` |
| T-2-20 | Repudiation | CLASSIFICACAO-PORTAO.md | medium | mitigate | aprovação datada e linha a linha (Task 4) |
| T-2-07 | Elevation of privilege | decisão de sandbox D-21 | high | mitigate | decisão explícita no portão; implementação e testes de escape em 02-16 |
</threat_model>

<verification>
`node ref/conferir-catalogo.mjs --slot ja-suportada` sai 0; `catalogo-aprovado.json` com `aprovado: true`.
</verification>

<success_criteria>
Snapshot congelado, capturas pendentes listadas, classificação aprovada pelo dono e gate automático da whitelist pronto.
</success_criteria>

<output>
Create `.planning/phases/02-fechar-o-builder/02-07-SUMMARY.md` when done
</output>
