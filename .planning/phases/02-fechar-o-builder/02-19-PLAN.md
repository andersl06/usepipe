---
phase: 02-fechar-o-builder
plan: 19
type: execute
wave: 9
depends_on: ["02-15", "02-17"]
files_modified:
  - packages/core/src/fluxo/editor.ts
  - packages/core/src/fluxo/editor.teste.ts
  - apps/api/src/dominio/fluxo.ts
  - apps/api/tests/flow-content.test.ts
  - apps/workers/src/entrega.ts
  - apps/gestao-vite/src/paginas/builder/conteudo.ts
  - apps/gestao-vite/src/paginas/builder/painel-conteudo.tsx
  - apps/gestao-vite/tests/builder-content.test.ts
autonomous: true
requirements: [BUILDER-01]

must_haves:
  truths:
    - "O atendente cria Conteúdo HTTP, Conteúdo Dinâmico e os demais tipos do slot conteudo-dinamico aprovados, com editor e validações da referência (D-18)"
    - "O motor resolve o conteúdo no momento do envio (HTTP ou variável) e o entrega pelo mesmo caminho dos outros tipos, com o comportamento de erro documentado (D-18)"
    - "Nenhum tipo reproduzível do catálogo aprovado termina como 'Não executada no Pipe'; conferir-catalogo --slot conteudo-dinamico passa (D-18, D-24)"
  artifacts:
    - path: "apps/api/src/dominio/fluxo.ts"
      provides: "resolução de conteúdo HTTP/dinâmico antes de toChannelOutput"
      contains: "toChannelOutput"
  key_links:
    - from: "conteúdo HTTP"
      to: "confirmarUrlSegura + chamarComMtls"
      via: "serviço de busca no envio"
      pattern: "confirmarUrlSegura"
---

<objective>
Fechar o slot `conteudo-dinamico` (Conteúdo HTTP, Conteúdo Dinâmico, pesquisa-conteúdo e o que o portão colocou aqui), completando o catálogo de conteúdo de BUILDER-01.
</objective>

<execution_context>
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/anderson.linhares/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fechar-o-builder/ref/inventario-conteudo.md
@.planning/phases/02-fechar-o-builder/ref/CLASSIFICACAO-PORTAO.md
@.planning/phases/02-fechar-o-builder/02-15-SUMMARY.md

Escopo exato: itens de `ref/catalogo-aprovado.json` com `"slot": "conteudo-dinamico"` e `"classificacao": "reproduzivel"`; bloqueados fora e listados no SUMMARY (D-03).

Pré-condição e nomes (D-05, STD-01): só executa depois que a Phase 1 aplicou o mapa (tag `std-apply-all-end`). Caminhos/símbolos EXISTENTES com nome em português de 2026-09-26; localizar o nome vigente pela linha `status=approved` de `.planning/phases/01-padronizar-linguagem-t-cnica-navega-o-e-renderiza-o/std/map/*.csv` ou por `ref/REFERENCIA-CONGELADA.md` §Caminhos resolvidos. Identificadores/títulos de teste NOVOS em inglês. MIME/`settings` literais (STD-06). `@pipe/gestao-vite` vira `@pipe/management-vite` após D-46. Nenhum CSS novo.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Motor resolve conteúdo HTTP/dinâmico e entrega</name>
  <files>packages/core/src/fluxo/editor.ts, packages/core/src/fluxo/editor.teste.ts, apps/api/src/dominio/fluxo.ts, apps/api/tests/flow-content.test.ts, apps/workers/src/entrega.ts</files>
  <read_first>
    - .planning/phases/02-fechar-o-builder/ref/inventario-conteudo.md (seções do slot: formato serializado, resolução, timeout, erro, canal)
    - packages/core/src/fluxo/editor.ts (whitelist e validação por MIME)
    - apps/api/src/dominio/fluxo.ts (servicos.enviar, toChannelOutput, chamarHttp/confirmarUrlSegura/chamarComMtls)
    - apps/api/tests/flow-content.test.ts
  </read_first>
  <behavior>
    - Conteúdo HTTP: URL https válida → busca no envio, resposta vira o conteúdo documentado (tipo LIME da resposta) e sai pelo canal; timeout/erro → comportamento documentado.
    - URL privada/loopback/metadata (169.254.169.254) → recusada.
    - Conteúdo Dinâmico: variável com JSON LIME válido → enviado como aquele tipo; JSON inválido ou tipo não suportado → comportamento documentado.
    - Conteúdo resolvido de tipo cuja execução não existe → erro claro (nunca mensagem pela metade).
  </behavior>
  <action>
    MIME do slot em `CONTEUDOS_SUPORTADOS` com validação na publicação. Em `fluxo.ts`, antes de `toChannelOutput`, uma etapa `resolveDynamicContent(message)` que, para Conteúdo HTTP, chama `confirmarUrlSegura` + `chamarComMtls` com o timeout do inventário e limite de tamanho de corpo (valor do inventário ou 1 MB), e para Conteúdo Dinâmico lê a variável e faz `JSON.parse` defensivo; o resultado passa pelo mesmo `toChannelOutput` dos outros tipos. Worker só muda se o tipo resolvido exigir. Testes `describe('dynamic content')` e casos em `flow-content.test.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @pipe/core exec vitest run src/fluxo && pnpm banco:subir && pnpm --filter @pipe/api exec vitest run tests/flow-content.test.ts tests/fluxo.test.ts && pnpm --filter @pipe/api typecheck && pnpm --filter @pipe/workers typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `fluxo.ts` contém `resolveDynamicContent` chamando `confirmarUrlSegura` (nome vigente)
    - `flow-content.test.ts` cobre 169.254.169.254 e localhost recusados, timeout e JSON inválido
    - verify sai 0
  </acceptance_criteria>
  <done>Conteúdo HTTP e dinâmico executam de verdade.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Editor dos tipos dinâmicos e fechamento do catálogo de conteúdo</name>
  <files>apps/gestao-vite/src/paginas/builder/conteudo.ts, apps/gestao-vite/src/paginas/builder/painel-conteudo.tsx, apps/gestao-vite/tests/builder-content.test.ts</files>
  <read_first>
    - apps/gestao-vite/src/paginas/builder/conteudo.ts (fábricas de 02-10/02-15, comentário 1-27)
    - apps/gestao-vite/src/paginas/builder/painel-conteudo.tsx
    - .planning/phases/02-fechar-o-builder/ref/inventario-conteudo.md
  </read_first>
  <behavior>
    - Fábricas do slot com padrões da referência; ida e volta; erros literais.
  </behavior>
  <action>
    Mesmo padrão de 02-10/02-15 para os tipos do slot (URL/método/cabeçalhos do Conteúdo HTTP reaproveitando o campo `cabecalhos`; nome da variável do Conteúdo Dinâmico com a busca de variáveis). Remover do comentário do topo de `conteudo.ts` a lista de "tipos que o Pipe recusa" para todo tipo agora suportado; tipos `externa` aprovados continuam só leitura/importação com a marca. Rodar `conferir-catalogo.mjs --all` e registrar a saída no SUMMARY.
  </action>
  <verify>
    <automated>pnpm --filter @pipe/gestao-vite test && pnpm --filter @pipe/gestao-vite typecheck && node .planning/phases/02-fechar-o-builder/ref/conferir-catalogo.mjs --slot conteudo-dinamico</automated>
  </verify>
  <acceptance_criteria>
    - `conferir-catalogo.mjs --slot conteudo-dinamico` imprime `OK` e sai 0
    - `conferir-catalogo.mjs --slot conteudo-midia` e `--slot conteudo-interativo` continuam OK
    - SUMMARY contém a saída de `--all` (falhas restantes só podem ser de `acoes-plataforma`, tratadas em 02-20)
  </acceptance_criteria>
  <done>Catálogo de conteúdo completo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fluxo → rede externa | Conteúdo HTTP busca URL definida no Builder |
| variável do contexto → canal | Conteúdo Dinâmico vindo de dado não confiável |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-06 | SSRF | Conteúdo HTTP | high | mitigate | Task 1: `confirmarUrlSegura` + `chamarComMtls`, testes de 169.254.169.254/localhost |
| T-2-28 | Denial of service | resposta HTTP gigante/lenta | medium | mitigate | Task 1: timeout e limite de corpo |
| T-2-05 | Tampering | Conteúdo Dinâmico | medium | mitigate | Task 1: `JSON.parse` defensivo e o tipo resolvido passa pela whitelist do motor |
</threat_model>

<verification>
Testes verdes; gates dos três slots de conteúdo OK.
</verification>

<success_criteria>
Critério 1 do roadmap (qualquer tipo de conteúdo previsto) completo.
</success_criteria>

<output>
Create `.planning/phases/02-fechar-o-builder/02-19-SUMMARY.md` when done
</output>
