{
  "name": "@pipe/gestao-vite",
  "version": "0.1.0",
  "private": true,
  "description": "Pipe Gestão — a mesma tela, em Vite + React (SPA). Substitui `@pipe/gestao` (Next) quando chegar à paridade.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src tests",
    "test": "node --import tsx --test tests/*.test.ts"
  },
  "dependencies": {
    "@pipe/contracts": "workspace:*",
    "@pipe/core": "workspace:*",
    "@pipe/ui": "workspace:*",
    "@tanstack/react-query": "^5.90.2",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "react-router-dom": "^7.9.3"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "@types/react": "^19.1.13",
    "@types/react-dom": "^19.1.9",
    "@vitejs/plugin-react-swc": "^4.1.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vite": "^7.1.7"
  }
}
